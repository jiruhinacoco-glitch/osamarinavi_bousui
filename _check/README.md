# _check/ ＝ 検証・作業スクリプト置き場（2026-08-18 保存）

このフォルダは、開発セッションで使ってきた**検証スクリプト（Playwright）と作業スクリプト（Python）**の保存場所。
CLAUDE.md の各節（§）が参照している `scratchpad/◯◯.js` の実体はこれ。
フォルダ名が `_` で始まるので **GitHub Pages では公開されない**（Jekyll の既定で除外）。

## 使い方（新しいセッションで）
1. ローカルサーバー: `python3 -m http.server 8899 --directory /home/user/osamarinavi_bousui`
2. 実行: `node _check/card6.js` など（Playwright は `require('/opt/node22/lib/node_modules/playwright')`、
   ブラウザは `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`。版番号は `ls /opt/pw-browsers` で確認）
3. よこ向きスマホの検証は、`env(safe-area-inset-left/right)` を 59px に置換した `_land.html` を使う。
   **各スクリプトが先頭で `require('./mkland')()` を呼んで自分で用意する**ので、下準備は要らない
   （2026-08-24q より。それまでは「誰かが先に作っている」前提で、単独では走らなかった）。
   ★よこ向きの検証を新しく書くときも、先頭でこの1行を呼ぶこと。

## 主な検証スクリプト（現役）
- 現場記録帳: card6/card7/kbn1/go1/go2/namefit/selcolor/pwlist/edit1/exist1/kai1〜5chk/
  photo4/faces1/memo1/card4chk/card5chk/kdef/fil17/tag17c/strip17d/fast1/cvscroll/scrollcnt
- 図面・積算: adjchk/ang5/cross/cross2/align1/zdir/warichk/sec2chk/zfix2/f3chk/f4chk/
  lapchk/facegap/beadaudit/d3chk/partschk/photochk/pinch/split1/tb2/sweep3d/zumen3d/fix4/clickchk/
  uxtool1（▭長方形・⊕点・辺ドラッグ・⧉複製・⊡全体表示。`node _check/uxtool1.js` / `ph`・2026-08-26a）
- 現場マップ: maprow/mapsharp/dot3d/mapchk1/lnmap/faces2
- 共通: allpages2（全ページ×両向きでJSエラー・横ずれ）/ leftnav / pczoom /
  allbuttons（全ページ全ボタンでJSエラー）/ deadbtn（押しても何も起きないボタン探し）/
  inputs（入力欄に変な値を入れて壊れないか）/ noxscroll（画面ごと横にずらせないか）

## 2026-08-24 に増えた検査（重さ・落ちる・数字の正しさ）
- `ctxlost`  … 3Dの土台（WebGL）を取り上げられて戻ったとき、形が真っ黒にならないか
- `beadcache`… 60秒使い続けても継目の置き場が増え続けないか（＝重くなる・落ちるの原因）
- `qty1`     … 数量と見積の金額を、図形から**手で計算した値**と突き合わせる（商売の根幹）
- `perf3d` / `stress3d` / `inst3d` … 組み直し・描画の速さ、長時間の安定、まとめ描き
- `deadbtn`  … 押しても画面も保存も変わらないボタンを総当たりで探す
- `noxscroll`… よこ向きで画面ごと横にずらせてしまわないか（隠した引き出しが顔を出す不具合）
- `nostore`  … 端末の保存（localStorage）がまったく使えない状態でも全11ページが開くか
- `junkdata` … 保存が壊れている（8通りの形）ときでも全11ページが開くか
- `degen`    … 点が0〜2個・面積0・自分と交差・座標がとんでもない、でも落ちない・固まらないか
- `relist`   … 一覧の絞り込みを続けて押しても件数が狂わないか（2段組み立ての取りこぼし）
- `docnan`  … 元請に出す書類9種の中に、あり得ない値（NaN・undefined など）が刷られていないか
- `mix1`    … 屋根ごとに仕様がちがう図面で、見積の行と単価が正しく分かれるか（お金）
- `photoimg`… 大きな写真を入れても小さくなって保存されるか・記憶を食いつぶさないか（2つの読み方の両方）
- `dash1`   … ダッシュボードの数字がたがいに食い違っていないか（粗利率・重複なしの集計・ファネル）
- `moto1`   … 元請別 成績：件数・受注額・利益・平均利益率・予実差・手戻りが物件データと合うか（2026-08-27b）
- `toriai`  … 取り合い：仕様の違う屋根の境界を自動で見つけ、納まり3種が3D・図面・積算に出るか（2026-08-27c）
- `fillband`… ホーム画面起動で申告値が全部嘘でも screen の高さで直るか・目印だけ後から伸びても追従するか・縮小ページで誤作動しないか（2026-08-27a）
  ★2026-08-27h：④縮小ページ＋嘘の申告（pt→CSSpx換算で直る）・⑤fixedナビの貼り直し を追加＝20項目
- `tenba1`  … 図面(2D)の天端の帯：角が留め継ぎで、二重塗り・図形の外への飛び出しが無いか（αを画素で読む・2026-08-27a）
- `d3heal`  … 3Dの全体表示：画面の大きさが変わったら合わせ直す・カメラを触ったあとは動かさない・回転では合わせ直す（2026-08-27a）
- `sndfix`  … タップ音：起きる装置で鳴る・鳴る予約が先に入る・死んだ装置は次のタップで作り直して鳴るか（2026-08-26b）
- `trail1`  … なぞりの光：タップでは描かない・なぞると描く・等倍・ナビの移動が壊れていないか（2026-08-26b）
- `vwthree` … 現場マップの書き出し用3D図面ビューアが、同梱の three.js で動くか（電波が弱くても開けるか）
- `datamodal`… 材料登録・仕様登録の「データ管理」が画面から開けるか（CSV書き出し・価格表の取り込み・バックアップ）
- `gantt1`   … 工程表（ガント）の保存が壊れていても、施工中のタブが開けるか
- `esc1`     … 打ち込んだ文字（現場名・メモ・屋根の名前）を画面に生で入れていないか（13か所）
  ★「A&B工務店」や「<至急>」でカードの後ろが消える不具合。全ページで実際に起きていた
- `escdoc`   … 元請に渡る書類9種に、打ち込んだ文字が生で入っていないか（紙は渡ってから気づく）
- `bignum`   … 数値の欄に とんでもない数字を打っても、固まらない・数量が桁違いにならないか
- `junkop`   … 保存が壊れているとき、開けるだけでなく**ふだんの操作もできる**か（8ページ×7通り）
  ★junkdata は「開けるか」まで。junkop は開いたあとの操作まで動かす（この穴から不具合が4件出た）
- `mkland.js`… よこ向き用のコピー `_land.html` を作る小道具（検査本体ではない）
- ニセTHREE（CDN不通環境用）: stub3.js

## 主な作業スクリプト（Python）
- newdata.py / mapsync.py … 物件データの生成と現場マップへの同期（★物件を変えたら両方流す）
- dechecker.py … 添付画像の市松模様（透過の焼き込み）除去
- build_frame.py … パネル四隅の金具画像の生成
- apply_*.py … 過去の一括置換（適用済み・記録として保存）

★`node ◯◯.js ph` ＝スマホたて／`land` ＝よこ、が多くのスクリプトの共通引数。

| `corner1.js` | パラペットの高さが辺ごとに違う角（§226・§228）：むき出しの45度カット・輪郭の外への飛び出しが無い・`before` で直す前と比較 |
| `tex1.js` | コンクリートの写真の質感（§225）：3Dタブまで読まない・縦横比・sRGB・無くても壊れない |
| `kasagi.js` | アルミ笠木（§224）：天端幅からの品番選定・3Dの向き（外壁側が高い）・積算の本数とコーナー数・保存 |

## 一回きりの道具は `_tools/` へ移した（2026-08-24v）
検査スクリプトが302本まで増えていたが、そのうち**105本は「合否（○／★NG）を出さない」
一回きりの道具**だった（画面を撮るだけ・数値を出すだけ・当時の調べもの用）。
毎日の点検には使えないうえ、本物の検査に混ざっていて「どれを流せばよいか」が
分からなくなっていたので、`_check/_tools/` に移した（**消してはいない**）。

・`_check/*.js`（200本）＝合否を出す検査、または引き継ぎメモが名前で参照しているもの
・`_check/_tools/*.js`（104本）＝一回きりの道具。使い方は `_tools/README.md`

★新しく書くときは、必ず ○／★NG を出す形にすること。出さないものは道具であって検査ではない。
