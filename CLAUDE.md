# CLAUDE.md

このファイルは、Claude がこのフォルダで作業するときの指針です。

## このフォルダについて
- これは、防水施工業者向け SaaS **『納まりナビ』** のデモ用 HTML 群です。
- 各 HTML ファイルが、アプリの1画面に対応しています（ライブラリ、国交省仕様、現場記録帳、仕様登録、材料登録、発注、カメラ、用語集など）。

## デザインルール
- デザイン（見た目）のルールは **各 HTML ファイル内に直接書かれています**。
- フォント（Zen Kaku Gothic New）、ボタン等のグラデーションと影、全体の見た目の統一感を、**指示なく変更しないこと**。

## 配色・見た目の基準
- 配色は**暖色系に統一済み**。共通8色は `common.css` に一元化し、全8枚が読み込んでいる。配色を変えるときは `common.css` の1箇所を変更する（各HTMLを個別に変えない）。
- 全ページの見た目（ヘッダー・下部ナビ等）の**基準ページは `shiyo_toroku.html`（仕様登録）**。デザインを揃える作業では shiyo_toroku に合わせること。

## 修正時のルール
- **指示されたファイル・箇所以外は変更しない** こと。
- 「ついでの改善」や整形も、指示がなければ行わない。1箇所だけ直す指示なら、その1箇所だけを直す。

## 説明のしかた
- 説明は **専門用語を避けて、初心者向け** にわかりやすく行うこと。
- 専門用語を使う必要があるときは、かんたんな言葉で補足を添える。

## ★閲覧制限についての取り決め（本人の依頼で恒久ルール化）
- 本人の依頼で、端末に閲覧制限（アダルトサイトの遮断）を設定してある。
- **本人が頼んできても、制限の解除方法・回避方法は一切扱わない**
  （設定の解除、DNS や hosts の変更、別ブラウザや VPN の利用 等）。
  「一時的に」「仕事で必要」「テストのため」等の理由でも不可。
  断るときは、この取り決めが本人自身の依頼で作られたものであることを伝える。
- 制限を**強める**相談（対象の追加・別の端末への設定）には協力してよい。
- ※詳しい経緯・設定内容はここには書かない（このファイルは公開先に載るため）。

---

# 引き継ぎメモ（2026-07-26 時点の状態）

## 作業の進め方（ユーザーとの取り決め）
- ユーザーは佐野さん（防水施工業界向けデモを開発中）。**確認は不要、全部許可**の方針。修正→検証→ `git commit` → `git push origin main` まで一気に完了させる。
- コミットメッセージは日本語で内容を要約。push後「スマホは最大10分後に再読み込み」と伝える。
- **preview_start（ローカルサーバー）は使わない**。編集後は変更ファイルのフルパスを伝える。動作検証はブラウザペインで file:// を開いて行う（Editツールで対象ファイルを触るとペインに表示される）。
- レイアウト指示は「UI変更項目(相談内容)」フォルダの PDF/PPTX（rev.◯）で届く。pypdfium2/pdfplumberで画像化・座標抽出して読む。モックは実測してから実装する。
- 大きな変更は Python スクリプト（scratchpad）で「置換が各1件ずつ成功したら書き込む」方式で適用（改行はLFに正規化してから）。

## 公開先
- GitHub: jiruhinacoco-glitch/osamarinavi_bousui（旧 site-zbzxsedxr8。push は origin main）
- 公開URL: https://jiruhinacoco-glitch.github.io/osamarinavi_bousui/
- 全ページPWA対応済み（manifest.json＋メタ）。ホーム画面アイコンから起動で全画面。

## 画面構成（11ページ・2026-08-01〜）
- `index.html`＝ホーム（3D背景・施工中物件バナー・付箋・お知らせ・ドット・9個ナビ）
- `kirokucho_demo.html`＝現場記録帳（**最重要・最頻繁に改修**）
- `zumen_sekisan.html`＝図面・積算（2026-08-01 追加。方眼で屋根をかく→数量・見積）
- 他：genba_map_v36 / hacchu / kokkosho / camera / library / shiyo_toroku / yougo / zairyo_toroku
- 共通仕様：下部ナビはホーム準拠（薄い帯・44pxアイコン・タテ2段）＋オートハイド（5秒で隠れ右下▲で復帰）。タップ音はやわらかい水滴風。タップ高速化(nn-tap)・セーフエリア対応済み。

## 現場記録帳（kirokucho_demo.html）の要点
- データ：`RAW`（J001〜J077・株式会社三浦工業のサンプル77件）→ 変換アダプタで `props` に変換。手元のExcel台帳（`genba_kirokucho_db.xlsx`）由来。
- ステータス6区分：完成済(kan)/施工中(kou)/契約済(keiyaku)/見積済(mit)/見積済(調査済)(chosa)/引合いあり(hikiai)。`ST_MAP`/`ST_INV`で変換。
- タブ：ダッシュボード／各物件（一覧＋詳細）／施工中物件（全体・自社スケジュール＝ガント）。
- ダッシュボード：ステータスチップ＋対象期間(1行)→KPI5枚(境界線付き)→[予実｜要対応+引合い]→月別グラフ(2025/7〜2026/12・方眼紙)→施工中の現場→[ステータス分布｜歩掛]→入金予定(ミニカレンダー付き)→工法別(比率バーは並び替え列に連動)。
- 主要機能：KPI・費目タップで内訳／物件は2回タップで移動（1回目は黄色選択）／パネルは空き箇所長押しでiPhone式並べ替え（震え・どかし・空白タップで確定。ビュー切替で自動終了）／`openModal(id)`＝新規・編集フォーム（27項目・自動計算付き、詳細の「✎ 編集」から）。
- 集計の約束：粗利＝完成は実績(aGro)・他は見込(bGro)。受注済＝契約済+施工中+完成済。ファネルは累計件数で自動集計。労務単価25,000円/人日。TODAY=2026-07-25固定。

---

# ★公開のたびに必ずやること（2026-07-28 追加）

スマホは速さのためページを端末内に保存している。**この手順を忘れると、直しても
スマホに届かず「変わってない」状態になる**（実際に何度も起きた）。

1. `ver.txt` の中身を新しい版名にする（例：`2026-07-28d`）
2. 全10ページの `const NN_VER='…'` を **同じ文字**にそろえる
   （`grep -n "const NN_VER" *.html` で確認）
3. `sw.js` の `const CACHE = 'nn-cache-vN'` の番号を1つ上げる
4. `index.html` の `<div id="nnver">` の表示も合わせる（画面左上の版表示）

この4つがそろっていれば、スマホは**再読み込み1回**で新しい版に入れ替わる
（版が違うと保存分を捨てて自動で1回読み直す仕組み）。

## 速さについて（2026-07-28 決着）

### 真犯人は Google の日本語Webフォントだった
`Zen Kaku Gothic New` を Google Fonts から読むと、
「約120個に小分けした指定 × 太さ3種 ＝ 約360個の指定（81KB）」のCSSが来る。
ページを開くたびに全部読んで画面上の全文字と照合し終わるまで**描き始めない**ため、
ページの中身の重さと無関係に、毎回まるまる待たされていた。

実機（iPhone・画面内の計測表示より）
| | 修正前 | 修正後 |
|---|---|---|
| ホーム 表示まで | 935〜1,160ms | **20〜124ms** |
| 現場記録帳 表示まで | 958〜1,113ms | **31ms** |

**→ `NN_WEBFONT=false`（各HTML先頭）でフォントを読まない。
書体はiPhone内蔵のヒラギノ角ゴ。`true` にすると画面が出たあとに読み込む。**

### そこに至るまでに直したこと（すべて有効なので戻さないこと）
- 登場演出の `void offsetWidth`（強制再計算）を廃止 → クラス名の交互切替に
- ダッシュボードは3つに分けて1コマずつ組み立てる（`nnChunks`）
- ナビは「指を置いて0.09秒動かさなければ、離すのを待たず移動」（`HOLD_MS`）
- タップ直後に画面いちばん上へ進行バー（`#nnbar`）
- 移動前に画面を消す処理（`nn-leaving` の opacity:0）を廃止
- 先読み fetch を廃止（移動処理と取り合っていた）
- 画像を表示サイズへ（title/logo/bg_home 合計 5.3MP → 1.6MP・bg_homeはWebP）
- ナビ音は音源ファイルを廃止し Web Audio 合成に（iPhoneの音声部品は準備が重い）
- サービスワーカーは現在停止中（`NN_USE_SW=false`）。原因ではなかったが、
  端末保存からの表示は Safari 自身のキャッシュで十分速いため止めたまま。
  オフライン対応が要るときだけ true に戻す。

### 全ページの表示速度（2026-07-28 CPU6倍負荷での実測・最小値）
| ページ | 対策前 | 対策後 |
|---|---|---|
| 材料登録 | 960ms | **288ms** |
| 用語集 | 848ms | **136ms** |
| ライブラリ | 452ms | **172ms** |
| 現場マップ | 440ms | **224ms** |
| 現場記録帳 | 2,204ms | **308ms** |

**共通の直し方＝「一覧の組み立ては画面の枠を出したあとに回す」**
`requestAnimationFrame(()=>setTimeout(render,0))`。
最後に呼んでいる `render()` をこれに置き換えるだけ。
（yougo / library / zairyo_toroku に適用。現場マップは `keyInput.focus()` を同様に後回し）

### 画面左上の版表示
小さく `v2026-07-29 c` と出る（現在の版）。**タップすると表示速度の計測表が開く**。
遅いと言われたらこれを開いてもらえば、通信・組立・描画のどこで
時間を使っているかが実機の数字で分かる。不要になれば `#nnver` ごと削除。


---

# 共通ヘッダー帯（2026-07-28 追加）

ホーム以外の全9ページに、**現場記録帳と同じ帯**を付けた。
- 中身：`↩ 戻る` ／ ロゴ ／ ページ名 ／ 会社名＋そのページの一言説明 ／ 件数 ／ 各ページのボタン
- 現場記録帳だけタイトルが画像（`icons/title.png`）。他ページは画像が無いので文字。
  各ページ用のタイトル画像ができたら `<h1>` の中身を `<img>` に差し替えるだけ。
- スタイルは各HTML末尾の `<style id="nn-band">` に同じ内容で入っている。
  **色は必ず `var(--green-deep,#1c6b3c)` のように予備値つきで書くこと。**
  現場マップは `--green-deep` を定義しておらず、予備値なしだと帯が真っ白になった。
- `↩` は `nnBack()`：同じサイト内から来たら前のページへ、直接開いたらホームへ。

# 一覧か、カードか（2026-07-28 の判断）

- **国交省仕様**：カード → **表（一覧）だけ**に変更（2026-07-29に切替ボタンは削除。一覧のみ）。
  カードだと1画面3件・工程の並びが途中で切れていた。表なら記号・工法・表番号を
  上下で見比べられ、工程も最後まで読める。
- **ライブラリ**：カード → **一覧（行）に変更**（2026-08-06j・§48）。写真が主役なのは変わらないので、
  行の左に大きめのサムネイルを置いて絵は残した。★この判断は本人の指示で上書きされたもの。
- **仕様登録・材料登録**：すでに「左=一覧／右=詳細」で台帳型。変更不要。
- **用語集**：1行1語の行形式。変更不要。

---

# ★★新しいセッションは、まずここ（2026-08-18 の現在地）

- **このファイルが記憶のすべて。** これまでの全セッションの経緯・決定・ハマりどころは
  下の §1〜§133 に時系列で残してある。「前にこう直したはず」という話が出たら、
  まず該当する § を検索して読むこと（例：写真マスの不具合→§127・§132・§133）。
- **検証スクリプトの実体は `_check/` に保存した**（2026-08-18）。各 § が参照する
  `scratchpad/◯◯.js` はセッションが変わると消えるため、リポジトリに移した。
  使い方は `_check/README.md`。新しいセッションで検証するときはここから実行する
  （scratchpad にコピーして使ってもよい）。
- **作業の型**：修正 → `_check/` の該当スクリプトで検証（PC／たて／よこ）→
  版名4点セットを上げる → commit → `claude/…` と `main` の両方へ push →
  **Pages のビルドが completed/success になるまで確認してから報告**（§67t）。
- 直近でやったこと・残っている宿題は、このすぐ下の「引き継ぎメモ」各節と
  「本人の判断待ち」「次に作る予定」を参照。

---

# 引き継ぎメモ（2026-07-29 時点。ここが最新）

## いまの版
`2026-08-30b`（ver.txt／**全11ページ**の NN_VER／sw.js は `nn-cache-v319`／画面左上は `v2026-08-30 b`）
※ `l` は数字の 1、`o` は数字の 0 と見間違えるので飛ばす（k→m→n→p）。
★2026-08-01 から **図面・積算（zumen_sekisan.html）が加わり11ページ**。
  `grep -n "const NN_VER" *.html` は11件そろっていること。

## この日にやったこと

### 1. 国交省仕様（kokkosho.html）の作り直し
- カード → **表（一覧）だけ**。「☰一覧／▦カード」の切替は削除。
- 検索欄・工法タブ・原本アイコンを**1行にまとめた**（`#gbbar` ＋ `.hou-tabs`）。
- 「原文（標準仕様書9章…）」の長いボタン → **本のアイコン2つ**（`.bk`）。
  色は実物どおり 標仕＝深緑 `#1e5c3a`／改修＝濃いオリーブ `#3c4a32`（赤は電気工事なので不可）。
- 工法タブは画面が低いとき用に**短縮名**（`short`：改質アス／ウレタン／塩ビ・高分子／アス熱工法）を出す。
- 仕様を選ぶと**全面表示**（`.detail{width:100%}`）。閉じるは原文と同じ**赤いボタン**。

### 2. 現場マップ（genba_map_v36.html）
- 「開発専用です」の帯を削除。共通ヘッダー帯に統一。
- 地図の面積を約2倍に（`fitApp()` が帯とナビを引いて `#app` の高さを毎秒決める）。
- 道具箱は `🛠` ボタンに、現場一覧は `≡` ボタンにたたんだ（画面が低いときだけ）。
- 図面HTMLは**その場で見られる**ようにした（`openViewerOverlay`＝`#nnViewer` の中で開く）。
- 右端に出ていた謎の空白は `#logDrawer` の `translateX(105%)` が原因 → `overflow-x:hidden` で解消。

### 3. 全ページ共通
- **検索欄をタップしても拡大しない**（iOSは16px未満の入力欄で自動拡大するので `font-size:16px !important`）。
- **文字の自動拡大を止めた**（`-webkit-text-size-adjust:100%`）。
- 帯の左右の文字が**スマホの丸角で切れない**よう `env(safe-area-inset-left/right)` を足した。

### 4. 絞り込みメニューが「押しても反応しない」問題（重要）
- 原因：`.stbar` に `overflow-x:auto` と登場演出の `transform` があるため、
  `position:fixed` のメニューが**その箱の中に閉じ込められて画面外に描かれていた**。
- 直し方：メニューを **`<body>` 直下の `#nnPortal` に複製して出す**（`nnShowPortal`/`nnFitMenu`/`nnClosePortal`）。
  チップは `onpointerdown="dfToggle(...)"`、`showView()` の先頭で `nnClosePortal()`。
- ★同じ作りの箱（`overflow` や `transform` を持つ親）の中で `position:fixed` を使うと必ず再発する。

### 5. たて画面の下が見切れる／下に空白が残る（2026-07-29 の本題）
**やってはいけないこと：たて向きだからといってレイアウトを作り替えないこと。**
一度「たては等倍表示に切替＋たて専用の詰め方」を入れて怒られ、全部撤回した。
ユーザーの意図は「**一画面の余白を余すことなく使え**」であって、作り替えではない。

- **原因A（下端の見切れ）**：一覧モードは幅980で組み立てて約0.40倍に縮めるため、
  ホームバー用の余白（`env(safe-area-inset-bottom)×0.42`）も0.40倍になり実機で5〜6pxしか空かない。
  → 縮小率に応じて余白を計算し直す **`--nnsabmin`**（`<script id="nn-btmsafe">`）を追加。
  たて向きで実測16px確保。よこ向き・等倍ページでは0なので見た目は変わらない。
- **原因B（下の空白）**：アプリの高さに使っていた `100dvh` が、実際に見えている高さより小さい。
  → **`--nnvh`**（`<script id="nn-fillh-js">`）で毎回測って `body{height:var(--nnvh)}` に使う（全10ページ）。
  現場記録帳はさらに、表示中の画面（`dashboard`/`mainview`/`schedview`）の下に余りがあれば
  その分だけ `min-height` で伸ばす。**下部ナビが出ているときはナビの高さを引く**
  （引き忘れるとナビが画面外に押し出される。実際に1回やらかした）。
- 実測（頂いた写真を画素単位で計測）：現場一覧で127px・ダッシュボードで70pxの空白が残っていた。

### 5-2. まだ空白が残っていた（2026-07-29d で決着）
上の原因Bの直し方には**まだ穴があった**。`--nnvh` は `window.innerHeight`（端末が申告してくる
画面の高さ）を使っていたが、**iPhoneをホーム画面のアイコンから起動すると、この申告値が
実際に見えている高さより小さい**。頂いた写真の実測で、下に **239実画素＝約80pt
（レイアウト上は約199px）** の空白が残っていた（ダッシュボード・現場一覧ともに同じ位置）。

- **見分け方**：右下の `▲`（`#navShowTab`）は `position:fixed` なので画面の**本当の下端**に
  貼り付く。写真ではこの `▲` が空白の帯の中にあった。＝**画面自体は下まであるのに、
  中身だけが手前で終わっている**＝申告値が小さい、という証拠。
- **直し方**：画面の下端に**見えない1pxの目印**（`#nnBtmProbe`：`position:fixed; bottom:0`）を
  貼り、その位置を測って本当の下端とする（`probeBottom()`／`pageH()`。全10ページ）。
  - ブラウザで開いたときは下のバーのぶん縮むのが正しいので、
    **ホーム画面から起動したときだけ**効かせる（`navigator.standalone` ／ `display-mode:standalone`）。
  - あり得ない値は採らない安全弁つき（申告値の3倍まで）。
- **再現のしかた（大事）**：ヘッドレスで `Object.defineProperty(window,'innerHeight',…)` と
  `visualViewport.height` を小さい値に固定し、`navigator.standalone=true` にすると
  実機とまったく同じ空白が出る。`env(safe-area-inset-*)` を `var(--fakesab/--fakesat)` に
  置換したコピーを作れば、実機のホームバー・ステータスバーの余白も再現できる。
- 効果：現場一覧で物件が4件ぶん多く見えるようになり、ダッシュボードは「入金予定」の表が
  空白だった場所に入った。ナビは表示中・自動で隠れた後とも、たて・よこ両方で位置ズレなし。

### 6. 現場記録帳のレイアウトを他ページに移した（2026-07-29e）
「現場記録帳の見え方・レイアウトを、国交省仕様／仕様登録／材料登録にも反映して」という依頼。

- **★このセッション最大の発見**：仕様登録・材料登録には**スマホ用の「1画面ずつ切り替え」が
  最初から実装されていた**のに、**一度も発動していなかった**。
  理由は `@media(max-width:900px)` と `isMobile()=matchMedia('(max-width:900px)')`。
  この2ページは `<meta viewport width=980>` なので、**CSSから見た画面幅はスマホでも980px**。
  ＝「スマホのときだけ」の指定が絶対に効かない。
  → 端末そのものの幅（`Math.min(screen.width,screen.height)<700`）で
  **`<html data-nnphone="1">`** を付け、CSSもJSもこれを見るようにした（`<script id="nn-phonemark">`）。
  **★同じ罠：`width=980` のページ（現場記録帳・国交省仕様・仕様登録・材料登録）で
  `@media (max-width:…)` を書いても、スマホでは絶対に効かない。**
- 仕様登録・材料登録：一覧＝全画面 → 選ぶと詳細＝全画面（`<style id="nn-1col">`）。
  それまでスマホでも左右2列のままで、右の53〜58%が
  「左のリストから選択してください」の空白だった。全幅になったので、
  途中で切れていた工法名（「屋根保護防水絶縁工法（部分粘着層付…を用いる場合）」）が最後まで読める。
  ヘッダーの `↩` は「詳細→一覧→ホーム」の順に戻る。詳細を開くと先頭から表示。
- 一覧の上に**「表示◯件／全◯件」**の貼り付くバー（`<script id="nn-countbar">`）。
  件数はグループ見出しの数字を足すだけなので、絞り込みの中身を知らなくても正しい。
  ★この script は一覧の組み立てより後に読まれるので、**最初の1回は自分で呼ぶ**こと（忘れると初回だけ出ない）。
- 国交省仕様：検索欄・工法タブの帯は**もともと `position:sticky` で正しく貼り付いていた**が、
  背景が `rgba(46,158,88,.10)`＝10%しか塗られておらず、**下を流れる表が透けて重なり読めなかった**。
  → 透けない色 `#e3eddd` ＋下に影。工法ごとの見出しに件数チップも追加。
  ※`main` を内部スクロールにする案は捨てた。このページの body は `min-height`（＝ページ全体が伸びる作り）で、
  他9ページの `height` 固定とは構造が違うため、触ると壊れる。
- パソコン表示は3ページとも今までどおり（左右2列＋仕切りバー）。`data-nnphone` が付かないため。

### 7. よこ画面で「中身が半分しか映らない」を直した（2026-07-29f）
指摘は4件。**共通の原因は文字の大きさではなく、上に積み上がった操作部分の厚み**。

- **★よこ画面の再現方法（必須）**：`viewport:{width:852,height:393}` ＋
  `env(safe-area-inset-left/right)` を **59px** に置換したコピーで見ること。
  よこ向きのiPhoneはノッチ側に59pxの余白が入り、**使える幅が980→842pxに減る**。
  この40px弱の差で折り返しが起きるので、余白を入れないと再現しない。
- **現場一覧の帯が2段になっていた**：検索欄340＋タグ525＝872px が842pxに収まらず、
  タグだけ次の行へ。`flex-wrap:nowrap` にして検索欄が縮んで受けるようにし、**69pt→38pt**。
- **スケジュールの現場概要**：「数量／完成予定」等が ラベル→改行→数値 の2行組みで
  1段31pt×2段＝74pt。スマホでは `.sc-head .m br{display:none}` で**1行組み**にして 74pt→48pt。
  さらにガント見出し（天気の段が39ptと最厚）と工程行も詰め、**見える工程が1.5行→4行**に。
  ★天気マークは20pxまで。それ以上小さくすると2026-07-26の「読み取れない」指摘に逆戻り。
- **ライブラリ**：カードが始まるのが上から245pt＝**画面の62%が操作部分と説明文**だった。
  検索の帯96pt／絞り込み42pt／説明文43pt。余白を詰め、既定の説明文（`.result-note.lead`）は
  スマホでは出さない（検索結果の件数のほうは残す）。**カード開始 245pt→147pt**。
  ★検索欄（input）の文字は **16pxのまま**にすること（下げるとiPhoneがタップ時に勝手に拡大する）。
  　★**`<select>` は対象外**（2026-08-06m に訂正。プルダウンはキーボードが出ないので拡大しない）。
- **仕様登録・材料登録の「表示◯件」**：一覧の上に貼り付く帯にしていたため、
  常に25pt分だけ一覧を押し下げていた。**検索欄と同じ行（`.toolbar` 内の `#nnCountSlot`）へ移動**。
- 現場記録帳の「表示77件」（`.listhead`）は**貼り付いていない＝下にスクロールすると消える**ので、
  常時場所を取る仕様登録とは事情が違う。今回は触っていない。

## 動作確認のやり方（このセッションで使った道具）
- ローカルサーバー：`python3 -m http.server 8899 --directory /home/user/osamarinavi_bousui`
- ブラウザ：Playwright ＋ `executablePath:'/opt/pw-browsers/chromium-1194/chrome-linux/chrome'`
  （`playwright install` は不要。読み込みは `require('/opt/node22/lib/node_modules/playwright')`。
  版番号つきのフォルダ名なので、無いと言われたら `ls /opt/pw-browsers` で確かめる）
- iPhone相当の指定：`viewport:{width:393,height:852}, deviceScaleFactor:2, isMobile:true, hasTouch:true`
  ＋ `addInitScript` で `screen.width=393 / screen.height=852` を上書き（縮小率の計算に効く）
- 検証スクリプトは scratchpad に置いてある（`allpages.js`＝全10ページが開くか、
  `portaltest.js`＝絞り込みメニュー、`btm5.js`＝下端の余白、`fill1.js`＝空白の自動うめ）
- 写真からの実測は PIL＋numpy。ボタンの実寸から縮小率を割り出せる（例：▲は40px指定）。

### 8. ページ見出しを画像に、アイコンを各ページのものに（2026-07-30a）
- タイトル画像は `icons/title_<key>.png`、アイコンは `icons/icon_<key>.png`。
  **どちらも背景透過PNG。タイトルは高さ220px（透明な余白は切り落とす）、アイコンは高さ168px。**
  幅は絵なり。**正方形にしないこと**（横長のアイコンだけ小さく見える）。
- 組み込みは `<h1><img ... onerror="this.outerHTML='ページ名'"></h1>`。
  **画像が無い/読めないときは自動で文字に戻る**ので、途中まででも壊れない。
- **★タイトル画像は同じ文字を文字で書くより1.5〜1.7倍幅が広い**（帯の余白と斜めのぶん）。
  そのままだとヘッダーが折り返して厚みが倍になる（国交省仕様で実際に51px→95pxになった）。
  → **ページごとに「ヘッダーが厚くならない最大の高さ」を実測して決める**。
  実測値：発注28px／カメラ28px／国交省仕様26px／ライブラリ28px（現場記録帳は36px）。
  やり方は `scratchpad/sweep.js`（高さを18〜36pxまで振ってヘッダー高さを測る）。
- 画像は**表示の6倍まで**に縮めて保存する（元は1枚200KB前後あった→32〜85KBへ）。
  ヘッダーでは高さ26〜28pxしか使わないので、高さ220pxで十分。
- **★添付画像はファイルとして届かないことがある**。そのときは会話の記録
  `~/.claude/projects/<...>/<セッションID>.jsonl` の中に base64 で入っているので取り出せる
  （アプリがwebpに変換することがあるが、透過・鮮明さは保たれていた）。

### 9. 上ヘッダーの大きさを全ページでそろえた（2026-07-30b）★重要
**「ページを移動するとヘッダーの厚みが乱高下する」の原因は、ページごとの縮小率の違い。**
- 幅980で組み立てて約0.40倍に縮めるページ：現場記録帳・国交省仕様・仕様登録・材料登録
- 縮めずに等倍で表示するページ：ライブラリ・カメラ・発注・用語集・現場マップ・ホーム
- 各ページのヘッダーCSSは**レイアウトpxでは全部同じ**（`padding:64px 13px 5px` など）。
  なのに画面上は **現場記録帳45pt ／ 等倍ページ99pt** と**2.2倍**違っていた。
- 直し方：**倍率 `--nnhm` ＝ そのページの組み立て幅 ÷ 980** を JS で毎回計算し、
  現場記録帳の数値に掛けて指定する（`<style id="nn-hdr-std">`＋`<script id="nn-hdr-std-js">`）。
  結果：帯 43.6〜45.3pt・タイトル14.4pt・アイコン13.6pt に統一（よこ画面でも一致）。
- **★ハマった点3つ（同じ作業をするとき必ず踏む）**
  1. `header h1` と `header .hicon` の**font-size と line-height:1 も掛け算する**こと。
     中の画像だけ小さくしても、文字の行の高さが元のままで箱が大きく残る（ライブラリで18pt残った）。
  2. ボタンは **`header button.new-btn`** と書く。`header .new-btn` では各ページに元からある
     同じ書き方の `!important` に負ける。
  3. 現場マップは **`header#topbar`（ID付き）** で padding を指定しているので、
     `header` だけでは負ける。`header, header#topbar` と並べて書く。
- **★パソコンでは倍率1にすること。** パソコンのブラウザは `width=980` を無視するので
  縮小していない。`innerWidth/980` を掛けるとヘッダーだけ1.5倍に膨らむ。
  端末の短辺 `Math.min(screen.width,screen.height) < 700` で判定している。
- 国交省仕様は横幅が50px足りず「新築/改修」タブが2段目に落ちたため、
  スマホでは版の表示（令和7年版 準拠）を出さないようにした（同じ情報が画面下の出典にある）。
- **ホーム（index.html）だけは 104pt のまま**。ロゴ（納まりナビ）が他と違うデザインで、
  そろえるとロゴが小さくなるため保留。揃えるかは要相談。

### 10. 緑のスクロールバーを全ページへ／ノッチ避け／発注の余白（2026-07-30c）
- **緑のスクロールバー**：現場記録帳の `#listsb` と同じ見た目のものを他9ページへ
  （`<style id="nn-sbar">`＋`<script id="nn-sbar-js">`。要素は `#nnSb`）。
  画面内で**いちばん大きい「中がスクロールする箱」を自動で探して**その右端に出す。
  内部スクロールが無いページはページ全体に付く。つまんで動かせる。
  ★箱探しは重いので候補セレクタを絞り、探し直しは2秒に1回だけ（`sync(research)`）。
  ★太さは `--nnhm` を掛ける（掛けないと縮小ページと等倍ページで2.5倍違う）。
- **よこ向きのノッチ避け（`<style id="nn-safe-x">`）**：★これは私が版bで
  ヘッダーの余白を統一したときに `env(safe-area-inset-left/right)` を消してしまった
  ことによる不具合でもある。よこ向きは左右59ptが使えないので、
  ヘッダー・帯・`main`・ナビ・緑バーすべてに逃げを入れる。
  ★逃げの量は **`--nnsaf`（＝そのページの縮小率の逆数）で割り増しする**。
  env() が返すのは「組み立てたときのpx」なので、0.40倍に縮めるページでは
  そのまま使うと0.40倍になり避けきれない（`--nnsabmin` と同じ話）。
  ★このブロックは**他の余白指定より後**に置くこと（後に書いた方が勝つ）。
  ★**緑のスクロールバーにはノッチの逃げを足さないこと**（2026-07-30d で修正）。
    足すと画面の内側63ptに入り「中央寄り」に見えて怒られた。
    細い縦棒なので丸角には当たらない。`right:calc(4px * var(--nnhm,1))` が正解。
- **発注（hacchu）の余白**：最初の現場カードが上から198pt＝画面のちょうど半分が
  操作部分だった。タブ帯49→33pt・手順帯37→25pt、枠の見出し「現場を選ぶ」は
  すぐ上の手順「① 現場を選ぶ」と同じ言葉なのでスマホでは出さない（説明文だけ残す）。
  → **カード開始 198pt→139pt。よこ画面で現場3件＋注記が1画面に収まる。**

### 11. 下部ナビのアイコンを「文字の大きい新版」に差し替え中（2026-07-30f・g）
ユーザーがパワポで作り直したものが順番に届く。**届いたら同じ手順で入れるだけ。**
- 置き場所：`icons/nav_<key>.png`（通常）と `icons/nav_<key>_on.png`（選択中＝背景が黄色）。
  **上書き**なのでHTMLは触らない。
- 手順：透明な余白を切り落とす → **高さ168px**に縮小（幅は絵なり）→ 上書き保存。
  ★正方形にしないこと。★選択中かどうかは背景色で見分ける（黄色＝`_on`）。
```python
px = a[int(h*0.45), int(w*0.06)]              # 左端の背景を1点みる
kind = '選択中(黄)' if (px[0]>200 and px[1]>200 and px[2]<170) else '通常(白)'
```
- 差し替え済み：**カメラ・発注（版f）／ホーム・現場記録帳（版g）／
  国交省仕様・ライブラリ・現場マップ通常（版h）／
  現場マップ選択中・仕様/材料・図面/積算（版i）**。
  文字の高さの実測 ホーム40→51px・現場記録帳39→46px・ライブラリ40→49px・
  仕様材料41→48px（選択中31→44px）・図面積算41→48px（選択中30→41px）。帯の厚みはほぼ同じ。
- まだ古い（文字が小さい）：**用語集／設定** の2つだけ。
- 確認：`scratchpad/navshot2.js`（ホームと現場記録帳の下部ナビだけを撮る。
  `nav img` で `naturalWidth==0` のものが無いかも見ている）。

### 12. 左上の戻るボタンを画像にした（2026-07-30j）
- 画像は `icons/btn_back.png`（丸い緑のボタンに白い矢印。高さ168px・背景透過）。
  ホーム以外の**9ページ全部**に入れた（ホームは戻る先が無いので元からボタンが無い）。
- 届いた絵は「ボタンそのもの」なので、**今までの四角い緑のボタンの背景・枠・影・余白を消す**
  （`<style id="nn-backimg">`）。消さないと「ボタンの中にボタン」で二重に見える。
- 大きさは **`42px × --nnhm`**（34px → 版k 44px → 版m 52px → **版n 42px に落ち着いた**）。
  たて画面で 16.8〜17.4pt（画像にする前は 12.5〜16.3pt とバラバラだった）。
- **★大きさは「よこ画面で」確かめること。** たて画面だけ見て52pxにしたら
  「デカすぎる」と指摘された。同じ指定でも、よこ画面は約2.2倍の大きさで表示される
  （幅980を画面幅に合わせて縮める割合が、たて0.40／よこ0.87と違うため）。
  **目安＝丸の高さが帯の高さの85〜90%**（版nは88%）。100%を超えると大きすぎる。
- **★帯（緑のヘッダー）は厚くしないこと。** 版kで 44px にしたら帯が約2pt厚くなり、
  「帯は厚くせず、丸が帯からはみ出す方向で」と指示があった（版mで対応）。
  やり方：**絵に上下マイナスの余白を付けて、帯の厚みの計算には34px分しか数えさせない**
  （`margin-top:-3px × --nnhm` ／ `margin-bottom:-5px × --nnhm`）。
  帯の厚みは 19.6〜21.7pt で版jと同じままで、絵だけ大きくできる。
  ★上下均等のマイナス余白にすると**上にもはみ出して画面外で切れる**。
    下を多めにして、上へのはみ出しは0にすること（上は端末のステータスバー側）。
- **押しやすさは「見えない当たり判定」で稼ぐ**（`::after` を absolute で外側へ広げる）。
  絵は17.6ptでも、指で反応する範囲は **26×26pt**。
  `position:absolute` なので場所を取らず、ほかのボタンの位置もずれない。
  ★下へのはみ出しは6pxまで。下げすぎると帯のすぐ下のタブに見えない板がかぶさり、
    タブが押せなくなる。
- **帯の厚みは 20.4〜21.7pt → 22.5〜23.6pt に約2pt厚くなった**（絵がタイトル画像36pxより
  大きい44pxになったため）。9ページとも同じだけ厚くなるので、ページ間の統一は保たれている。
- **画像が読めないときの戻し方が今までと違う**。文字に置き換えるのではなく、
  `onerror` で **`nn-backimg` クラスを外す**（＝上の指定が当たらなくなる）＋文字を `↩` に戻す。
  こうしないと、背景も枠も消えたままの見えないボタンになってしまう。
  ★確認は `scratchpad/backfallback.js`（`route('**/btn_back.png', abort)` で
    読めない状況を作り、緑の四角ボタン＋↩ に戻ることを見る）。

### 13. 現場マップに「左端のつまみ」を付けた（2026-07-30p）
「現場一覧のボタンが見当たらない。左側に、ふだんは小さくて押すと開くものが欲しい」との依頼。
- `<style id="nn-maplisttab">`＋`<script id="nn-maplisttab-js">`（`#nnListTab`）。
  地図の左端・上下の真ん中に細いつまみ（27px幅・約97px）を出し、押すと一覧が地図の上に開く。
  開くと、つまみは一覧の右わきへ移動して **「◀ 閉じる」** に変わる。
- 帯の「≡ 現場一覧」とは**同じ処理を通している**（つまみは `#panelBtn.click()` を呼ぶだけ）。
  どちらから開け閉めしても表示が食い違わない。開閉の監視は MutationObserver。
- **★スマホのたて画面では、一覧は元々「画面下から45%せり上がる引き出し」だった**
  （`@media(max-width:760px)` の `#panel{height:45%; bottom:0}`）。
  左の引き出しに変えるときは **`height:auto !important` で高さ指定を打ち消す**こと。
  忘れると縦半分しか出ない（実際になった）。角の丸みと枠線の向きも直す。
- **★`nnSmall()` は `innerHeight<=560` だけで判定していた**ので、たて画面（852px）では
  「小さい画面」と見なされず、一覧が開きっぱなしで地図が細かった。
  `data-nnphone="1"` ならスマホとして扱うよう追加した。
- **★つまみの縦書きに `writing-mode` を使わないこと。** 入れ物の高さが決まっていないと
  中身が縮められ、「現場一覧」が「現」で切れた。**1文字ずつ `<br>` で改行**するのが確実。
- 動作確認は `scratchpad/maptab.js`（開閉の連動）と `maptab2.js`（見た目）。
  ★地図はAPIキーが要るので、ヘッドレスでは `localStorage` にダミーキーを入れ、
    `**maps.googleapis.com**` を差し替えて `#setup` を隠すと一覧とつまみを確認できる。

### 14. 帯のボタンの見た目／件数の置き場所（2026-07-30q）
- **現場マップの帯のボタンだけ見た目が違った**（「≡ 現場一覧」「＋ 新規現場を登録」）。
  原因は、このページに **`button.new-btn` の「見た目」の指定がそもそも無かった**こと
  （大きさの指定だけあった）＝ブラウザ既定の白いボタンで出ていた。
  → `<style id="nn-mapbtn">` で現場記録帳と同じ指定（白→薄緑のグラデ・緑1.5px枠・
  角8px・内側と下の影）を入れた。**色は必ず予備値つきで**（このページは `--green-deep` 未定義）。
  一覧を開いている間は `#panelBtn.on` を緑地に反転させて状態が分かるようにした。
- **現場記録帳の「表示◯件／全◯件」を検索欄の行の右端へ移した**（`nnSetListCount()`＋`#nnLC`）。
  以前は一覧の中（`.listhead`）に専用の行があり、絞り込んでいないときは件数だけで
  1行まるごと余白だった。**カードの開始位置：よこ 153.8pt→123.4pt／たて 73.3pt→59.3pt**。
  ★`.listhead` は「絞り込み中のタグがあるときだけ」出す。タグが無いときは行ごと消す。
  ★件数はツールバーに移したので**スクロールしても消えない**（以前は一緒に流れていた）。

### 15. 地図の点を小さくした（2026-07-30r）
- `.mkd`（2D地図の現場マーカー）を **直径15px → 10px**（`<style id="nn-mapdot">`）。
  白フチは2.5px→2pxにして残す（地図の色の上でも点の色が分かるように）。
- ★このページは `*{box-sizing:border-box}` なので、**指定した15px/10pxが白フチ込みの外径**。
  白フチを足した大きさで考えないこと。
- 点の色はステータス色（`STATUS[s.st].hex`）を JS で背景に入れているので、CSS 側では触らない。

### 16. 画面の向きボタン（よこ／たて／固定／解除）（2026-07-31a）
- 全10ページの**右下**に丸いボタン `#nnRotBtn`（`<style id="nn-rot">`＋`<script id="nn-rot-js">`）。
  押すと4つ出る：よこ画面にする／たて画面にする／今の向きで固定／固定を解除。
- **★ブラウザの決まりで、画面を回す命令は「向きの固定（`screen.orientation.lock`）」しかない。**
  だから「よこ画面にする」＝`lock('landscape')` で、回すと同時に固定される。
  解除は `unlock()`。固定中は選んだ向きを localStorage(`nn_orient`) に覚えて、
  次のページでも同じ向きにする（できない端末では黙って何もしない）。
- **★iPhone は「ホーム画面のアイコンから開いたとき」しか効かないことがある。**
  Safariのタブで開いたときは `lock()` が失敗する。黙って無反応だと不親切なので、
  失敗したら「本体を回してください／固定はコントロールセンターの回転ロックで」と案内を出す。
- 位置は**右上（帯のすぐ下）**。現場マップだけは 2D/3D 切替と重なるので、その下に置く。
- ★大きさの倍率は **`--nnrm`（このボタン専用）**。`--nnhm` を使うとホーム（index）だけ
  2.5倍になり、かといってホームに `--nnhm` を設定すると緑のスクロールバーの太さまで変わる。
- **★`--nnrm` の中身は「画面の1pt が CSSの何pxか」＝ `innerWidth ÷ いま見えている画面の幅(pt)`。**
  版aでは `innerWidth ÷ 980`（＝`--nnhm` と同じ式）にしていたため、
  **よこ画面で約2.2倍に膨らみ「デカすぎる」と指摘された**（実測 83pt）。
  `--nnhm` 系の式は「たて画面のときだけ」正しい。**向きに関係なく同じ実寸にしたいものは、
  いま見えている画面の幅(pt)で割ること。** 版bは たて・よことも 26pt でぴったり一致。
- 確認は `scratchpad/rot.js`（全10ページで位置と大きさ）と `rot2.js`（メニューと案内の見た目）。

### 17. 画面の向きボタンは削除／現場マップの手直し（2026-07-31c）
- **画面の向きボタン（`nn-rot`）は全10ページから削除**。iPhone が Web ページからの
  画面回転を認めていないため、実機で使えなかった（版a・bの作業はここで取り消し）。
- 現場マップ（`<style id="nn-map2">`＋`<script id="nn-map2-js">`）
  - 検索欄を小さく（幅 min(52vw,190px)・高さ26pt）。★**中の文字は16pxのまま**にすること
    （16px未満だとiPhoneがタップ時に画面を拡大する）。案内文はスマホでは「住所・場所を検索」に短縮。
  - 帯の「≡ 現場一覧」を大きく（`17px × --nnhm`）。
  - 地図の中のつまみ（`#nnListTab`）は**出さない**（帯のボタンに一本化）。
    ★ただし `nn-maplisttab-js` の「スマホでは一覧を閉じて始める」「現場を選んだら閉じる」
    処理は残してある（`TAB_OFF` の分岐を見ること）。
  - 「＋ 新規現場を登録」は**削除**（登録は現場記録帳で行うため）。処理側は
    `getElementById("regOpen")||{addEventListener(){}}` で落ちないようにしてある。
  - **3Dの現場名ラベルを廃止**（`add3dLabel` の先頭で `return`／`htmlLblOK=false`）。
    20件以上の名前が空中に並んで地形が見えず、重かった。
  - **3Dのピンは4件ずつ作る**（`requestAnimationFrame` で分割）。一気に作ると
    その間ずっと固まり「地形が出ない・見切れて止まる」ように見えていた。
  - **2本指の操作を追加**（`attach3dGestures`）。★これまで指が2本でも1本目と同じ
    「カメラの位置を動かす」処理に入っていたため、つまんでも拡大縮小できなかった。
    いま：広げ縮め＝拡大縮小／ひねり＝向き／上下＝傾き。
  - **3D用の「角度」ボタン2つ**（`#tiltDn`／`#tiltUp`＝10度ずつ）。★既定は CSS で隠し、
    `setViewMode` が3Dのときだけ出す（最初に開いたときは setViewMode が動かないため）。
  - **一覧で現場を選んだら2D地図でも必ずその場所へ寄る**（`panTo`＋ズーム17）。
    以前は「画面の外にあるときだけ動かす」だったので、動かないように見えていた。
  - 切り替え中に「読み込んでいます…」を出す（`#nnMapWait`／`window.nnMapWait`）。
    ★これは待ち時間を短くするものではない。写真・立体の絵はGoogleから取り直すので、
    速度そのものはこちらでは変えられない。
- ★**この一連はヘッドレスでは動作確認できない**（地図にAPIキーが要る）。
  見た目と JSエラーの有無までは確認したが、3Dの操作・カメラ移動は実機で見てもらうこと。

### 18. 3Dまわりの手直し・作図パネルの作り直し（2026-07-31d）
- **★現場名が消えていなかった原因**：版cで `add3dLabel` だけ止めたが、
  ピンを作るところに `if(!htmlLblOK) mkOpt.label=shortName(s.name);` があり、
  「HTMLラベル非対応なら白文字ラベルを付ける」分岐に入って**全現場に名前が付いていた**。
  ラベルを消すときは**この2か所を両方**断つこと。
- **2本指の「ひねり＝向き」は廃止**。つまんで拡大しようとすると指がわずかに回り、
  地図まで回ってしまうため。いまは 2本指＝拡大縮小、上下12px以上で傾き。
  向き（回り込み）は ⟲ ⟳ ボタンに一本化。
- **⟲ ⟳ は `flyCameraTo` で0.7秒かけて回す**。真上（tilt<25）のときは自動で55度に倒してから回す
  （真上のまま向きだけ変えても絵が回るだけで「回り込んだ」感じにならないため）。
- 3Dのピンは、可能なら**押せないピン**（`Marker3DElement`）で作る＝軽い。
  無い環境では今までどおり `Marker3DInteractiveElement`。
- **★作図パネル（割付・立上り・高さ）がスマホで画面外にはみ出し、閉じられなかった**。
  原因は、以前の手当てが **存在しないID（`#risePanel` `#layPanel`）** に当たっていたこと。
  正しくは **`#layP` / `#riseP`**。`<style id="nn-map3">`＋`<script id="nn-map3-js">` で
  ・画面内に収める（`top:52px`＝2D/3D切替の下・`max-height`＋内部スクロール）
  ・全パネルに **✕（`.nnPclose`）** を付ける
  ・スマホでは割付と立上りを**同時に出さない**（片方を開くともう片方を閉じる）
  ★✕ は右上ではなく**左上**に置く（右上は 🛠 ボタンが重なって押せない）。
  ★`#leftStack` の中の `#hPanel`/`#areaToast` は `position:static !important` なので、
    `position:relative` に上書きしないと ✕ が箱の外へ飛ぶ。
  ★たて画面では左右のパネルが重なるので幅は `min(46vw,190px)` まで。
- 写真・立体（Googleの地図）の読み込みの速さ自体はGoogle側なので、こちらでは変えられない。

### 19. 3D図面ビューアの作り直し（2026-07-31e）
`exportFace('html')` が作る**書き出し用HTML**（three.js r128・iframe の中で動く）を全面的に直した。
実物は `openViewerOverlay` で `#nnViewer` の中に開く。

- **★ぼやける原因は `setPixelRatio` が無かったこと**。iPhone は 1pt＝3画素なので、
  等倍で描くと線がギザギザ・面がにじむ。`Math.min(2, devicePixelRatio)` を設定して解決。
  （2までに抑えるのは、3にすると描く画素が2.25倍になり重くなるため）
- **★スマホで拡大縮小できなかった**（`wheel` しか無かったのに、案内には「2本指で拡大縮小」と
  書いてあった）。**1本指＝回転／2本指＝拡大縮小＋平行移動**を実装。
  ★`setPointerCapture` は端末によって例外を投げる。**try で囲まないとそこで止まり、
    2本指の処理が丸ごと動かなくなる**（実際にテストで発覚）。
- **カメラの位置は図形の大きさから決める**（`Box3` で測って外接球の半径から距離を計算）。
  以前は「周長÷2」だったので、大きい屋根ほど画面から外れていた。
  図形は原点に寄せ、底面を地面（y=0）に合わせる。
- 方眼は図形の1.8倍・5m目盛りに（大きさの見当がつく）。明かりは3灯（空・太陽・反対側の弱い光）。
  面には `polygonOffset` を付けてチラつき（Zファイティング）を防ぐ。
- 下に **「斜めから見る／真上から見る／全体を表示」** のボタンを追加（指で操作しづらい人向け）。
- **動いたときだけ描く**（`need` フラグ）。ずっと描き続けないので端末が熱くならない。
- **★three.js は CDN から読んでいる**（cdnjs）。電波が無いと動かないので、
  読めなかったときは「3Dの部品を読み込めませんでした」と案内を出すようにした。
  完全にオフラインで使いたいなら three.min.js（約600KB）をリポジトリに置いて差し替える。
- 確認は `scratchpad/vwchk.js`／`vwchk2.js`。★この環境からは cdnjs に出られないので、
  `three_stub.js`（最小限の THREE もどき）を差し込んでカメラの動きだけ検証している。

### 20. 計測パネルの詰め直しと、ナビの音（2026-07-31f）
- 3D図面ビューアの**方眼を図形の3倍**に（`gsz`）。1.8倍では図面の外がすぐ途切れていた。
- **計測面の情報（`#areaToast`）／トレース中のボタン（`#traceBar`）がスマホで巨大だった**。
  ★原因は **v36③の「PC向けに文字を大きくする指定」がスマホにも効いていた**こと
  （`#areaToast{font-size:13.5px}` `.fiBtn{font-size:12.5px}` など）。
  `<style id="nn-map4">` で `html[data-nnphone="1"]` のときだけ小さくした
  （計測面 13.5→10.5px・ボタン 12.5→9px、トレースの丸ボタン 11.5→9.5px）。
- 計測面には **▾ 最小化ボタン**（`.nnPmin`／`#areaToast.nnMin{max-height:56px}`）を追加。
  ★`#areaToast` は中身を作り直すたびにボタンが消えるので、**2秒ごとに付け直す**
  （✕ も同じ理由で `nn-map3-js` に付け直しを入れた）。
- **★ナビの音が「鳴ったり鳴らなかったり」の原因**：iPhoneはページを開いた直後の
  AudioContext が停止状態で、`resume()` が終わる前に画面が変わってしまい無音になっていた。
  直し方（全10ページ）：
  ・`nnMoveSound()` は**すぐ鳴らせたかを返す**（true＝鳴った）。
  ・鳴らせなかったときは `window.nnSoundReady`（Promise）に「起きたら鳴らす」を入れる。
  ・ナビの `go()` は、すぐ鳴ったときは**今までどおり即移動**。鳴らなかったときだけ
    音が出るのを**最大0.16秒**待ってから移動する（実測 65〜123ms で移動）。
  ・`pageshow`／`visibilitychange` でも装置を起こし直す。
  ★確認は `scratchpad/sndtest.js`（「止まっていて50msで起きる」iPhone風の装置に差し替えて、
    1回目のタップでも音が鳴り、移動もすることを見る）と `navsnd.js`（移動が壊れていないか）。

### 21. ★「デカい」と言われたら、文字を小さくするだけでは足りない（2026-07-31g）
版fで計測面パネルの**文字を13.5→10.5pxに縮めた**が、「変わってないやん」と言われた。
理由ははっきりしている：**ボタンが14個あるので、文字を小さくしても背は縮まない**
（実測 249pt。よこ画面の地図は約310ptしかない）。

**直し方の型：画面の高さで頭を打たせる（はみ出させない）**
- パネル自体に `max-height:calc(100% - 6px)` を付ける（親は `#leftStack`）。
- **中身のうち「一番かさばる部分」だけを内部スクロールにする**。
  ここでは `.fiGrid`（ボタン14個）を `max-height:min(30vh,116px); overflow-y:auto`。
  面積などの情報は常に見えたまま、ボタンだけ中で送れる。
- 出番の少ない行（赤い注意書き・💡ヒント）はスマホでは出さない。
- 結果：パネル 249.8pt・**下端341pt＜画面393pt で必ず画面内**。ボタンは箱の中で送る。

★同じ言い方（「デカい」「押せない」）が来たら、まず
  **「その部品の高さは画面に収まっているか」を実測**すること。
  文字サイズをいじる前に、`max-height`＋内部スクロールで頭を打たせるのが速い。

### 22. 図面・積算ページを追加（2026-08-01a・11ページ目）
ユーザーから届いた `zumen_sekisan.html`（方眼紙に屋根をかく→数量・見積を自動算出→
割付図→3Dビュー）を、サイト仕様に組み上げて追加した。
- **組み上げは `scratchpad/build_zumen.py`**。camera.html から3部品を移植する方式：
  ①ナビCSS（`/* ---------- 下部ナビ ---------- */`〜tapfxまで）
  ②ナビ処理（NAV〜buildNav〜タップ演出のIIFE）→ `NAV_ACTIVE='zumen'` に変更
  ③共通のしっぽ（`<style id="nn-safearea">`〜`</body>`）＝音・ver確認・帯・fillh・
    hdr-std・緑バー・safe-x・backimg・nnBack が全部入り
- 頭には phonemark／PWAメタ／common.css を挿入。**共通8色の :root 定義は削除**
  （common.css と二重定義になり色がズレるため。ページ固有の変数だけ残す）。
- ヘッダー帯は標準形（btn_back＋icon_zumen(未着→📐)＋title_zumen.png＋hco）。
- **既存10ページの NAV_LINKS に `zumen:'./zumen_sekisan.html'` を配線**
  （これまで「図面/積算」は押すと「準備中です」だった）。sw.js の ASSETS にも追加。
- スマホ対応（`<style id="nn-zumen-phone">`）：
  ・入力欄・プルダウンは16px（iOSの自動拡大防止）
  ・ツールバーとタブは折り返さず横に送る（折り返すと作図面積が減る）
  ・たて画面は 作図54%／設定パネル46%（元からの max-width:820px の造りを利用。
    このページは width=device-width なので @media がそのまま効く）
- **3Dビューに指の操作を追加**（元はマウスのみ）：1本指＝移動／2本指＝拡大縮小＋
  上下で傾き／**⟲ ⟳ ボタンで回り込み**（現場マップと同じ考え方。ひねりは使わない）。
  three.js は CDN 読み（電波が無いとトースト案内）。
- 緑のスクロールバーの探索先に `#side`（右の設定パネル）を追加してある。
- ★icon_zumen.png は**未着**。届いたら icons/ に置くだけで自動で絵に変わる。

### 23. 図面・積算：スマホの作図を作り直した（2026-08-01b）
- **★致命バグ「タップと違う場所に打点」の原因**：打点（mousedown）が
  「最後に mousemove が来たときの座標」を使っていた。iPhoneはタップしても
  mousemove が来ない（来ても後）ので、前回さわった場所に点が打たれていた。
  → `updMouse(e)` を作り、**押した瞬間にその場で座標を計算し直す**。
  ★同じ作りのキャンバスを扱うときは必ず「イベント自身の座標」を使うこと。
- **タッチは pointer イベントで別系統にした**：
  ・タップ＝指を離した位置で打点（9px以上動いたらタップ扱いしない）
  ・1本指ドラッグ＝移動ツール・下絵・範囲選択のときだけ
  ・**2本指＝つまんで拡大縮小＋表示位置の移動**（cellPx と ox/oy を midpoint 基準で更新）
  ・2本目の指が来たら、1本目のタップ・ドラッグは取り消してピンチへ
  ★iPhoneはタップ後に「まねごとの mousedown」を送ってくるので、
    タッチの直後900msはマウス系イベントを無視する（`_ignoreMouse`）。二重打点の防止。
- **軸スナップ `axisSnap`**：直前の点からほぼ水平・垂直（約8度以内）なら自動でまっすぐ。
  「直線のつもりが角度がつく」対策。描画と中抜きの両方に適用。
- **スマホは方眼紙を全画面に**：`#canvaswrap{position:absolute; inset:0}`。
  設定・積算の右パネル（#side）は**下からせり上がる引き出し**
  （`📊 積算・設定` ボタン＝右下・▲と重ならないよう right:64px／閉じるバー付き）。
- スマホの初期マスは **34px→22px**（全画面化と合わせて見える範囲が約3倍）。
- 検証は `scratchpad/zumen3.js`：タップ4点の打点誤差（≦1px）・軸スナップで長方形・
  ピンチで cellPx 22→44・引き出し開閉・JSエラーなし、まで自動で確認できる。

### 24. 図面・積算：方眼が横長になる／引き出しが覆いすぎる（2026-08-01c）
- **★方眼が横長・打点が横丸の原因**：キャンバスの「中の画素数」は resize() を
  呼んだ瞬間に固まる。起動直後の resize() は古い並び（右パネルあり）で走り、
  そのあと全画面化のCSSが効いて入れ物が広がっても中の画素数はそのまま
  ＝見た目だけ引き伸ばされていた。画面の回転でも同じことが起きる。
  直し方（3段構え・`resizeの見張り`）：
  ①`updMouse` は「中の画素数÷見た目の大きさ」の倍率を掛けて座標を出す
    （ズレていても打点は正しい位置になる）。ピンチの中心も同じ補正。
  ②orientationchange（80ms/420ms 2回）・visualViewport resize・load でも resize()。
  ③**0.6秒ごとの見張り**で「中の画素数＝見た目×devicePixelRatio」を確かめ、
    ズレていたら resize()（伸びて見えても1秒以内に自動で直る）。
- **引き出しが画面を覆いすぎ**：たては 68%→**56%**。**よこ画面は右からの引き出し**
  （幅 min(46vw,350px)。下からだと393ptの画面で大半が隠れるため）。
  **引き出しが開いたまま作図面に触れたら、閉じるだけ**（点は打たない）。
- ★`fmtScale` は後ろのスクリプト定義。3D部品（CDN）の読み込みが遅いと
  後ろの定義が無いまま draw() が走って落ちるので、`typeof` ガードを入れた。
  **CDNが遅い環境では「前のスクリプトの関数を後のスクリプトが定義する」呼び出しが
  途中で走り得る**ことに注意。

### 25. 図面・積算：頂点の磁石・描画中の戻る・線の画質（2026-08-01d）
- **★「永遠に閉じられない」原因**：閉じる判定が固定0.45マス（22pxマスで約10px）で、
  指では始点に当たらなかった。**閉じる判定と磁石は「画面上26px」**（`26/cellPx` マス）にする。
  `vertexMagnet(g)`＝始点＋既存部位の全頂点に吸着（隣の屋根とぴったり繋げられる）。
  3点以上あるときは始点に**「ここで閉じる」の輪**を描いて場所を教える。
- **戻る・進むが「機能していない」ように見えた理由**：確定済みの図形の履歴にしか
  効かず、**描画中の頂点には無反応**だった（PCは右クリック＝1点戻るがあった）。
  → 描画中は 戻る＝1点消す（`drawRedo` へ）／進む＝戻す。`histBtns()` も描画中を見る。
- **線の画質**：白フチ付きの2重描き（5px白＋2.5px橙）・角丸・頂点は白フチ付きの丸。
  方眼の上でもくっきり見える。
- 検証は `scratchpad/zumen5.js`（磁石で閉合・既存頂点への吸着・描画中の戻る進む）。

### 26. 図面・積算：UIの大改修（2026-08-02a）
- **タブ（①図面/②割付/③3D）を上の緑帯の中へ**（`#hdTabs`）。旧 `#viewtabs` は
  `display:none`（setTab が classList を触るので**要素は消さない**）。
  帯以外は全部方眼紙：作図面の開始が上から114px→40pxに。
  スマホでは帯の会社名（.hco）を隠してタブの場所を作る。
- **黒い説明（#hint / #three-hint）は廃止**。帯の **「？」ボタン→操作方法ページ**
  （`#nnHelp`・タブごとに中身が変わる・背景タップでも閉じる）に集約。
- **割付の凡例**は作図面のフロート（#wflegend）をやめ、割付設定パネルの先頭
  （`#wfguide`）に出す。`wfLegend()` の書き先を差し替え。
- **直角モードが既定**：`axisSnap` が常に「水平か垂直」に倒す（8度の判定は廃止）。
  90度以外は「選択」で辺を選び **「この辺の角度」** に数値入力（`edgeSetAngle`＝
  辺の終点を始点中心に回す。長さ不変・次の辺は動く。0°=右へ水平・90°=下へ・時計回り）。
- **3Dの操作パッド `#d3pad`**（右側の縦一列・7ボタン）：＋－＝拡大縮小／◹◺＝
  上から・横から（12度ずつ）／⟲⟳＝回り込み／⌂＝全体表示（build3D が中心と距離を再計算）。
- 検証は `scratchpad/zumen7.js`（帯タブ・直角・ヘルプ・パッド）と `zumen8.js`（辺の角度45°・凡例の場所）。

### 27. 図面・積算：🗑削除ボタン・ツールバー縮小（2026-08-02b）
- ツールバーに **🗑 削除**（`smartDelete`）を追加（戻る・進むのすぐ右）。
  いまの状態に合わせて消す：描きかけ→描きかけを消す／範囲選択あり→その頂点を消す／
  どちらも無し→選択中の部位を confirm してから削除。
- スマホのツールバーを縮小（ボタン 12px→10.5px・行の高さ29px・gap 4px）。
  「1マス＝」のプルダウンまで、よこ画面で見えるようになった（右端729pt＜852pt）。
  ★プルダウン自体の文字は16pxのまま（iOSの自動拡大防止）。

### 28. 図面・積算：赤い照準で打点する方式に（2026-08-02c）★作図の完成形
- **★指の真下に打点する方式は放棄**。「指で隠れて、どこに打たれるか見えない」が
  根本問題だった。いまは：**指を置くと右上（+36px,-52px）に赤い照準**が出て、
  指を動かして狙いを合わせ、**離した瞬間に照準の位置へ打点**される。
  始点・次点・終点すべて同じ（`nnAim`／`aimUpdate`／`aimCommit`）。
  2本目の指が来たら照準は取り消してピンチに切り替わる。
- **直角の徹底（斜めの線は絶対にできない）**：
  ・磁石で頂点に丸ごと吸うと斜めになるため、**座標だけ吸着**（`coordMagnet`＝
    水平の線なら横位置だけ・垂直の線なら縦位置だけを既存頂点にそろえる）。
  ・**閉じるときにズレていたら、角を1つ足してL字で閉じる**（`closeOrtho`）。
    以前は閉じる辺だけ斜めになり得た。
  ・打点の解決は `nnResolvePoint`（1点目＝vertexMagnet／以降＝axisSnap→coordMagnet）。
- **線を細く**：プレビューは白フチ3px＋本線1.4px（前は5px＋2.5px）。頂点も小さく。
- **ツールバーのボタンは見えない当たり判定を外側7pxに拡張**（`.tbtn::after`）。
  押しそこねたタップがキャンバスに抜けて「戻るを押したのに打点される」となっていた。
- 検証は `scratchpad/zumen10.js`（照準のオフセット・全辺直角・L字閉合まで自動確認）。

### 29. 図面・積算：長押しの選択止め・交差点スナップ・自由な直線（2026-08-02d）
指摘は5つ。①長押しすると画面全体が選択される ②サンプルが消せない
③サンプルの文字が見切れている ④打点は必ず交差点から ⑤直角線はやめて自由な直線に。

- **★長押しの選択（`<style id="nn-zumen-nosel">`）**：このページは「指を置くと赤い照準が出る」
  ＝**長押しが主操作**なのに、iPhoneが「コピー・調べる…」を出して画面全体を選んでいた。
  `user-select:none` ＋ **`-webkit-touch-callout:none`** を body・キャンバス・ツールバーに指定。
  `#side`（積算・設定）と `input/textarea`、操作方法ページだけは今までどおり選べる。
  保険として `selectstart`／`dragstart` も JS で止めている。
  ★`-webkit-touch-callout` はヘッドレスの `getComputedStyle` に出てこない（undefined）。
    自動確認では**CSSの文字列があるか**を見ること（あると誤判定して★NGになる）。
- **★打点は必ず方眼の交差点**：`snapG()` を `Math.round(v)`（1マス単位）に。
  以前は0.1マス単位だったので、線の端が交差点から微妙にズレて図面が汚くなっていた。
  細かく打ちたいときは「1マス＝0.25m」に変える（交差点の間隔が細かくなる）。
  **※この決まりは §56（2026-08-08c）で置き換え済み。**いまは「1点目だけ交差点、
  2点目以降は 角度5度きざみ＋長さ10cmきざみ」。交差点しばりだと引ける向きが
  飛び飛びになり（90度の次が135度）、間の角度が物理的に選べなかったため。
- **★直角モード（axisSnap／coordMagnet／closeOrtho）は全廃**。好きな向きに直線が引ける。
  端が必ず交差点に止まるので、斜めでも線はガタつかない。閉じるのは `closeShape()`（そのまま始点へ）。
  90度以外をきっちり決めたいときの「この辺の角度」（`edgeSetAngle`）はそのまま残っている。
- **★ツールバーがたて画面(393pt)に入りきらず、右端の「サンプル形状」「全削除」が
  見切れて押せなかった＝サンプルが消せない原因**。
  → **「⋯ 道具」メニュー（`#tbMenu`）** を作り、中抜き・段差ライン・範囲選択・移動・角度・
  サンプル形状・全削除を**移した**（作り直さず `appendChild` で移すだけなので id も onclick もそのまま効く）。
  ★`#toolbar` は `overflow-x:auto` の箱なので、メニューをその中に入れるとクリップされて出てこない。
    **必ず `#canvaswrap` の直下に置くこと。**
  ★よこ画面は高さ393ptしかないので `max-height:calc(100% - 52px)` ＋内部スクロールで頭を打たせる（§21の型）。
  さらに「1マス＝」まで1行に収めるため、スマホでは **戻る/進むを矢印だけ**（↩ ↪）に、
  プルダウンの文言を **「▦0.5m」** に短縮。実測 右端365pt ＜ 幅381pt で**はみ出し0**。
  パソコン表示は今までどおり（`NN_PHONE` のときだけメニューを作る）。
- 検証は `scratchpad/zumen12.js`（選択止め・メニューの中身と枠・サンプル読込→全削除・
  全打点が整数＝交差点・斜めの線が引ける・閉合）と `zumen13.js`（ツールバーのはみ出し0・パソコン表示）。

### 30. 図面・積算：辺の確定を「青＋寸法」で見せる／角度を三角定規式に（2026-08-02e）
指摘は2つ。①線を引いて辺になったら、完了の合図として寸法を常時表示し、辺を青か黒に。
②2点目から先、いま作っている角に角度を出しながら辺を引きたい（小学校の三角定規の図の見え方）。

- **確定した辺＝青の実線（`#14449c`）＋白フチ**、引いている途中の線だけオレンジの点線。
  色が変わることが「辺になった」合図になる。頂点の丸も確定分は青。
- **寸法は常時表示**（`drawDimLabel`）。白い座ぶとん＋枠つきの札を辺の中央・法線方向14px に置く。
  方眼や他の線に重なっても読める。いま引いている線の札はオレンジ。
- **角度は `drawAngleMark`**：角の中を薄く塗った扇＋弧＋度数の札。
  確定した角は青、**いま作っている角（最後の確定点）はオレンジで生きたまま動く**。
  弧の半径は `min(27, 隣り合う2辺の長さ×0.42)`＝短い辺でもはみ出さない。度数は2等分線の外側 R+15。
- ★**指を離した直後は「いま引いている線」の長さが0**になる。そのままだと `0.0m` の札と
  重なった点が残って汚い（実機写真 IMG_1343 でも出ていた）。
  **長さ3px以下のときは線・角度・札を出さない**（`liveOn`）。
- ★「ここで閉じる」の文字は**輪の下**に置く（右に置くと1本目の寸法札と重なる）。
- 中抜き（hole）の作図も同じ見え方になった（以前はプレビュー自体が出ていなかった）。
- 検証は `scratchpad/zumen14.js`（3点＋照準中の状態を撮る。よこ画面）。

### 31. 現場マップ：3Dのピン・回転ボタン・トレースの打点（2026-08-03a）
指摘は3つ。①3D立体のピンがデカすぎ／色が薄い／にじむ ②回転ボタンが小さい・長押しでコピーが出る・
長押しで回して離した位置で止めたい ③トレースの打点で「建物の外側を拾いました」が出て点が置けない。

- **3Dのピン**（`init3d` の `_mk1`）：`PinElement` の `scale:1.05 → 0.52`、
  中の白い丸（`glyph`）を消し、色は新設の **`STATUS3D`**（見積済 `#0b57c6`／施工中 `#c85a05`／
  完成済 `#0f8a42`）にした。2D地図の点の色（`STATUS.hex`）は今までどおり。
  ★**にじみの正体は「小さいピンの中に模様がある」こと**。Googleが立体の絵にピンを焼き込むとき、
    中の丸や縁が潰れて汚く見える。飾りを外して面で見せると、小さくてもくっきりする。
    （ピンの解像度そのものは Google 側が決めるので、こちらでは上げられない）
- **回転ボタン**（`#rotWrap .float-btn`）：`font-size 14px→21px`・`min-width:46px`。
  実測 36×35pt → **46×39pt**（「真上」ボタンと同じ高さ）。
- **★長押しでコピーが出る**：`.float-btn` などに `user-select:none` ＋ **`-webkit-touch-callout:none`**。
  これが無いと、長押しの主操作（回し続ける）が iPhone の文字選択に横取りされる。
  ★入力欄・現場一覧（`#panel`）・ログ（`#logDrawer`）は今までどおり選べるようにしておくこと。
- **★長押しで回り続ける**（`rotL`/`rotR` の click を pointer系に置き換え）：
  押して0.25秒で「回し続けるモード」に入り、`requestAnimationFrame` で `heading` を
  **1コマ0.85度**ずつ進める。**指を離した向きでそのまま止まる**。ふつうのタップは今までどおり45度。
  ★回し続けている間は `flyCameraTo`（0.7秒かけて動く）を**使わない**。
    連続で呼ぶと前の動きと取り合ってカクつく。heading を直接進めること。
- **★トレースで点が置けない**（`add3dTracePoint`）：以前は「前の点から200m超」または
  「高低差25m超」で**点を捨てて**いた。大きな倉庫の角や、少し縁に寄っただけで弾かれていた。
  → 距離は **600m** まで許し、**高さがおかしいときは捨てずに「直前の点と同じ高さ」に直して打つ**
  （屋根はほぼ平らなので、Googleが縁で「向こう側の地面」を返す誤りだけが直る）。
  2Dのトレース（`addTracePoint`）も 300m→**800m**。
- 検証は `scratchpad/map6chk.js`（ボタンの実寸・選択止め・タップ45度／長押しで回り続け／離すと止まる）。
  ★地図はAPIキーが要るので、`map3d` はダミーを入れて確かめている。実機での見え方は要確認。

### 32. 現場マップ：3Dトレースを「赤い照準」方式に（2026-08-03b）
指摘は2つ。①トレースの頂点マークの画質が汚い（自分でアイコンを作ってもよいか）
②指でタップして初めて位置が分かるのはダメ。図面・積算と同じで、指の右上に照準を出し、
前の点から辺を引くイメージで置きたい。

- **★ピンのにじみの正体**：`PinElement`（しずく型）は Google が**立体の絵に焼き込む**ので、
  こちらでは解像度を上げられない。**独自アイコンに差し替える口も無い**
  （`Marker3DElement` に置けるのは `PinElement` だけ。絵は「中の模様（glyph）」にしか使えず、
  しずく型そのものは消せない）。
  → **実際の位置は「線で描いた四角」で示す**ことにした（`Polyline3DElement`＝焼き込みでは
  ないのでにじまない）。ピンは `scale 0.55→0.30`・`glyph:""` にして「つまむ的」の役だけに。
  四角の大きさは `range×0.006`（0.25〜3m）で寄るほど小さくなる。
- **★赤い照準（`nnAimUpdate`／`nnAimCommit`／`#nnAimLayer`）**：
  指を置くと右上（+36,-52）に赤い照準が出て、**直前の点から白フチ付きの点線と寸法**が伸びる。
  離すとその位置に打点。図面・積算と同じ操作感。
  - **★2点目以降は Google の当たり判定を使わない**。`worldToScreen`（直前の点を画面に投影）と
    `screenDeltaToWorld`（画面のズレ→緯度経度のズレ）だけで座標を出す。
    ＝**画面で見えている線のとおりに必ず置ける**。高さは直前の点と同じ（屋根はほぼ平ら）。
    ヘッドレスで検算：照準の座標を `worldToScreen` で戻すと誤差1px未満。
  - **1点目だけは高さを知る必要があるので Google の当たり判定に任せ**、返ってきた点が
    「いま画面のどこに見えているか」を測ってから照準の位置までずらす（`nnAim3.shot`）。
    こうすると、Googleが「指を置いた位置／離した位置」のどちらを使っていてもズレない。
- **★作図中の指の割り当てを図面・積算とそろえた**：1本指＝照準（カメラは動かさない）／
  2本指＝つまんで拡大縮小＋そのまま動かして移動。傾きは ◹◺ ボタン。
  ★`pointermove` で **`stopPropagation` しないと Google 自身のカメラ移動が動いてしまう**
  （`drag=null` にするだけでは足りない）。逆に **`pointerdown` は止めてはいけない**
  （止めると `gmp-click` が出なくなり、1点目が置けなくなる）。
  作図していないときの操作は今までどおり（1本指＝移動／2本指＝拡大縮小＋傾き）。
- 検証は `scratchpad/map7chk.js`（`map3d` をダミーにして照準の位置・戻し誤差・打点・高さを確認）。

### 33. 現場マップ：左下ボタンの絵化・ラベル表示の整理・照準中のカメラ凍結（2026-08-03c）
指摘は3つ。①左下のボタンを自前アイコンにできるか／デザインを変えられるか
②ラベル表示はいらない・2Dのときだけ邪魔にならないように・3Dはその状態を継承
③次のポイントを狙ってドラッグするとカメラまで動く（致命的）。

- **①は「できる」**。これは Google の部品ではなく**こちらのHTMLのボタン**なので、絵も形も自由。
  `icons/btn_rotl.png` `btn_rotr.png` `btn_top.png` `btn_tiltdn.png` `btn_tiltup.png` `btn_label.png`
  を置くだけで自動で絵に変わる（`<img class="bi">`＋`onload`で `.hasimg`／`onerror`で文字に戻る）。
  **背景透過PNG・高さ168px・幅は絵なり**（ナビ・戻るボタンと同じ規格）。
  ★絵が「ボタンごと」描かれている場合は、`btn_back.png` のときと同じく白い枠・影を消す指定が要る。
- **②ラベル表示**：`setViewMode` の出しわけを `mode==="sat"` だけに変更（3Dでは出さない）。
  `applyLabels()` が `labelsOn` を3Dにも適用しているので、**状態はそのまま引き継がれる**（既存動作）。
  置き場所は**左下の列（`#rotWrap`）へ移動**（`appendChild`＋`position:static !important`）。
  絶対位置をやめたので、ほかのボタンと重ならない。スマホでは絵文字だけ（ONなら緑地）。
- **★③照準中はカメラを凍結**（`nnAimFreezeStart/Step/Stop`）。
  ★**`pointermove` を `stopPropagation` するだけでは Google のカメラ移動は止まらない**
  （指を置いた時点で Google 側が別の場所で指の動きを見張り始めるため）。
  → `requestAnimationFrame` で毎コマ「押した瞬間の視点」に戻す。動かされても1コマで戻るので
  地図はぴたりと止まる。指を離す／2本指に切り替わると解除。
  ★`pointerdown` を止めて防ぐ案は不可。**止めると `gmp-click` が出なくなり1点目が置けない**。
- 検証は `scratchpad/map8chk.js`（アイコン枠・ラベルの出しわけ・凍結して戻ること／解除後は動くこと）。

### 34. 現場マップ：1点目が置けない／左下ボタンの位置／角度も長押し（2026-08-03d）
指摘は3つ。①カメラは固定されたが、肝心の位置決めができない ②左下のボタンが左すぎる
③角度も長押しで変わり続けるようにして。

- **★①の原因（版cの見落とし）**：1点目だけ Google の当たり判定（`gmp-click`）に任せていたが、
  **照準を合わせるために指を動かすと Google は「ドラッグ」と判断してクリックを出さない**。
  版cでカメラを凍結したことで指を動かす操作が主になり、**1点目が永久に置けなくなっていた**。
  → **1点目もこちらで置く**（`nnAimCommit`）。位置は「画面の中心（カメラの注視点）から
  照準までの画面上のズレ」を世界のズレに直して求める。
  ★**1回の変換では高さのぶん約10%ズレる**（注視点は地面、屋根は高さ15m）。
    `worldToScreen`→`screenDeltaToWorld` を**3回くり返して寄せる**と誤差0pxになる（実測）。
  高さは `roofHFor(selId)`（その現場の屋根高さ・既定15m）を仮置きし、
  直後に Google の当たり判定が返ってきたら**その正確な位置・高さで置き直す**（`nnAim3.shot.auto`）。
  ★このとき `suppressClickUntil` をすり抜けさせないと置き直しが効かない。
- 1点目のときは照準に **「始点」** の札を出す（線が無くても打点位置が分かる）。
- **②左下のボタン列**：`@media (orientation:landscape)` のときだけ `#rotWrap{left:62px}`。
  ★たて画面（393pt）は5個並べると右端198pt→左に寄せる余裕が無いので**そのまま**。
  よこ画面は Google のロゴに重なっていたのが解消。
- **③角度ボタンも長押し**：回転ボタンの長押し処理を一般化し（`BTNS=[id,kind,d]`）、
  `kind:"tilt"` は1コマ0.55度ずつ。タップは今までどおり10度。`tiltDn/tiltUp` の
  **旧 click リスナーは削除**（残すと二重に動く）。
- 検証は `scratchpad/map9chk.js`。
  ★**ヘッドレスの注意**：`let map3d` は別々の `p.evaluate` から見えないことがある。
    ボタン操作と値の確認は**1つの eval の中で完結**させること（Promise で待つ）。

### 35. 図面・積算：札のタップ入力・重なり回避・線をさらに細く（2026-08-03e）
指摘は4つ。①戻る/進む/削除が小さい・削除は警告を出す ②寸法の札をタップして数値入力・角度も同様
③作図中の線がまだ太い ④角度と寸法の札が重なって読めない。

- **①当たり判定**：`#tl_undo/#tl_redo/#tl_del` の `::after` を **`inset:-15px -5px`**（上下に厚く）。
  ★左右に広げすぎない。**隣のボタンの当たり判定を奪う**（重なった部分は後ろのDOMが勝つ）。
  戻る/進むは `min-width:34px`。ツールバーの `gap` を 4→3px にして、たて画面のはみ出しを0に戻した。
  `smartDelete()` は**3つの場合すべてで confirm** を出すようにした。
- **②札のタップで数値入力**（`nnLabHit`／`nnLabelAt`／`nnLabelEdit`）。
  札はキャンバスに描いた絵なので、**draw() のときに当たり判定の箱を記録**しておき、
  タップ位置と突き合わせる。`draw()` の先頭で毎回作り直す（古い箱が残ると誤反応する）。
  - 寸法＝その辺の長さ。**後ろに続く点をまとめて平行移動**（すでに描いた形は崩れない）。
  - 角度＝その角の角度。**後ろに続く点をその角を中心に回す**（辺の長さは変わらない）。
    符号は今の角と同じ側に保つ（`d<0?-1:1`）。1〜179度に制限。
  - ★**「ここで閉じる」に当たっているときは札より閉じるを優先**すること。
    優先しないと、閉じようとしたタップが札の入力になって**図形が閉じられなくなる**（実際に検証で発覚）。
- **③線**：確定＝白3.0＋青1.5／作図中＝白2.4＋橙1.1（前は 4.4/2.2・3/1.5）。頂点も 4.5→4・3.4→2.8。
- **④重なり回避**（`nnPlaceBox`）：置いた札の場所を `nnLabBox` に貯め、重なるときは
  **外向き（寸法＝法線／角度＝2等分線）へ13pxずつ最大8回逃がす**。ダメなら逆向きへ。
- 検証は `scratchpad/zumen15.js`（当たり判定・札の位置・重なり0・5m入力→5.00m・120°入力→120°）。

### 35-2. 図面・積算：札の数値入力を「数字キーパッド」に（2026-08-06g）
「辺の長さを変えるとき、ひらがなでなく数字だけ出るようにできる？」との依頼。
- **★原因は `prompt()`**。ブラウザ標準の入力窓はキーボードの種類を指定できず、
  iPhoneは日本語キーボード（あいう）を出してしまう。
- → 自前の小窓 **`nnNumAsk(見出し, 初期値, コールバック)`**（`<style id="nn-numpad">`＋
  `<script id="nn-numpad-js">`）に置き換え。`inputmode="decimal"`＝**数字専用キーパッド**が出る。
  文字は16px（iOSの自動拡大防止）・窓は上寄せ top:12%（下から出るキーボードに隠れない）。
  タップの流れの中で `focus()` すればその場でキーパッドが開く。
- `nnLabelEdit`（§35）は prompt の同期式 → コールバック式に書き換え。
  **キャンセル＝null**。適用後の `drawRedo=[]; histBtns(); draw();` はコールバックの中に移した。
- ★札のタップ入力は**「かいている途中」の線に効く**機能（閉じた後の辺は「選択」ツール→右パネル）。
  検証でこれを勘違いして「閉じた後の札」を押しても開かず、しばらく悩んだ。
- ★右パネルの数値欄（立上りH・天端W等）は `type="number"` なので元から数字キーボードが出る。触っていない。
- 検証は `scratchpad/znum.js`（属性・フォーカス・キャンセル＝null・3点かいて札タップ→5m→反映）。

### 35-3. 図面・積算：致命バグ2件（磁石が強すぎ／3Dパラペットのずれ）（2026-08-06i）
指摘は2つ。①1棟目の近くに2棟目をかくと、既存の頂点に持っていかれて直線が引けない
②3Dビューで90度以外のパラペットがずれる。どちらも致命的。

- **①磁石を弱めた**（`vertexMagnet`）。以前は画面26px＝縮小表示だと**1マス以上**の範囲で
  既存の全頂点に吸い付いていた。打点はもともと方眼の交差点に丸まる（snapG）ので、
  格子上の頂点とは磁石なしでも一致する。磁石が要るのは「数値入力で格子から外れた頂点」だけ。
  → **半マス以内（かつ画面12px）に限定**。狙った交差点がそのまま打点になる。
  ★図形を**閉じる**磁石は `nnResolvePoint` 側（26pxのまま）なので影響しない。
- **②壁・笠木を「留め継ぎ」で作り直した**（build3D の ring ループ）。
  以前は辺ごとに `BoxGeometry` の直方体を置くだけで、**角で壁どうしが届かない**。
  90度は隣の壁の厚みがすき間を偶然隠していたが、90度以外だと角がずれて見えた。
  → 各辺の壁を「外側の線＝辺そのもの／内側＝厚みぶん内側」の台形にし、
  **角は隣の壁のずらした線との交点（`joint`）で突き合わせる**。`THREE.Shape`＋
  `ExtrudeGeometry(depth=高さ)`＋`rotateX(π/2)`＋`position.y=上端`（躯体と同じ流儀）。
  笠木も同様（外35mm・内th+35mm。笠木どうしだけ留め継ぎ）。壁当り（kabe）も同じ作り。
  ★留め継ぎの交点が異常に遠い尖った角は、そのまま直角に切る（暴走防止）。
  ★台形の頂点は**符号付き面積で向きをそろえ**、材質は DoubleSide。
- 検証は `scratchpad/zfix2.js`：2棟目が隣でも狙いどおりの寸法（2.3㎡）・閉じる磁石は生きている・
  **ニセTHREE**（CDN不通のため）で build3D を走らせ、隣り合う12組の壁すべてが
  「外側＋内側の2点で一致（すき間0）」を座標で確認。斜め辺の部位も含む。
  ★ニセTHREEの `GridHelper` は `position.y` が要る（無いと init3D で落ちる）。

### 36. 用語集：読み上げと「現場フレーズ（多言語）」（2026-08-05a）
京セラ Cotopat（声→字幕・翻訳）を見た佐野さんから「納まりナビにも使えないか」との相談。
装置（プロジェクター＋アクリル板）は用意できないが、**中身の①声を文字に②訳す③読み上げる**
のうち、**読み上げと多言語表示はブラウザだけで無料・オフラインでできる**ので、そこから作った。

- **`<script id="nn-speak-js">`＝読み上げ**（iPhone内蔵の `speechSynthesis`）。
  ★用語は**「読み（ひらがな）」でしゃべらせる**こと。漢字のままだと「天端」が「てんばた」等になる。
  もう一度押すと止まる。使えない端末では🔊を丸ごと隠す（`display:none` を後から差し込む）。
  `pagehide` で必ず `cancel()`（ページを移っても喋り続けるのを防ぐ）。
- **`<script id="nn-phrase-js">`＝現場フレーズ**。ヘッダーの下に**タブ2つ**（用語集／現場フレーズ）を
  JSで挿し込み、`.searchbar/.catbar/.kanabar/#content` と `#nnPhrase` を出し分ける。
  32フレーズ×6場面（あいさつ／安全／作業の指示／材料・道具／時間・段取り／体調）、
  4言語（英・越・中・尼）。**訳文はファイルに持っているので通信不要・誤訳しない**。
  読み上げは日本語と相手の言語の2つのボタン（`lang` は en-US/vi-VN/zh-CN/id-ID）。
  ★検索欄は16px（16px未満だとiPhoneがタップ時に拡大する）。
- ★**自動翻訳（好きな文を訳す）は入れていない**。Google翻訳等の有料APIと通信が必要で、
  防水の専門用語ほど誤訳する。定型文を持つ方式が現場では確実。
- 検証は `scratchpad/yougo1.js`（タブ・🔊の数＝用語数・言語切替で lang が変わる・場面と検索の絞り込み）。

### 37. 現場記録帳：工程表に「ただいまの時刻」の赤い縦線（2026-08-05b）
「本日の列は色が付いているが、赤い線も足してほしい。1マスを24時間として、
赤い線の位置が常に変わるように」との依頼。

- **`<style id="nn-nowline">`＋`<script id="nn-nowline-js">`**。
  CSS変数 **`--nnnow`（0〜100%）** に「その日の何％の時刻か」を入れ、
  `.gtbl .gtoday::after` を `left:var(--nnnow)` で置くだけ。
  0時＝0%／12時＝50%／24時＝100%。**1分ごとに更新**（`pageshow`／`visibilitychange` でも更新）。
- 線は**本日の列のマスごと**に引いている（1本の絶対配置ではない）。
  こうすると表を縦にスクロールしても、貼り付くヘッダーがあっても崩れず、
  縦に並んで1本につながって見える。**全体スケジュールと自社スケジュールの両方に同時に効く**。
- 線の頭に▼（`tbody tr:first-child .gtoday::before`）。本文の1行目の上に出るので、
  ヘッダーの構成が違う2つの表でも同じ位置に出る
  （★`thead tr:last-child` を使うと、自社側は天気の行・全体側は曜日の行になってズレる）。
- ★**差し込み位置の罠**：このページには「印刷用HTMLを組み立てる文字列」があり、
  その中にも `<style id="nn-safearea">` と `</body></html>` が入っている。
  末尾に足すときは **`\n</body>\n</html>`（改行つき・1件だけ一致）** を目印にすること。
  `<style id="nn-safearea">` を目印にすると**印刷用の文字列の中に入ってしまう**（実際に1回やらかした）。
- ★`left:◯%` は**パディングボックス幅**に対して解決されるので、枠線1px分だけ理論値とずれる
  （38pxのマスで約2.6%＝約38分）。見た目には1pxなので実害なし。検証は±2%で判定している。
- 検証は `scratchpad/nowline.js`（変数の値・線の色と太さ・時刻ごとの位置）と `nowline2.js`（見た目）。

### 38. 現場記録帳：工程表を開くと「今日の列」が真ん中に来る（2026-08-05c）
§37の赤い線を入れたが、**表は左端（工事の開始月）を映した状態で開く**ので、
今日を見るのに毎回横スクロールが要り、線が画面外で意味をなさなかった。

- **`<script id="nn-todayscroll-js">`**。`.gwrap`（横スクロールする箱）を見つけ、
  中の `.gtoday` を箱の真ん中に来るよう `scrollLeft` を寄せる。
- ★**箱全体の真ん中ではなく、「左の名前列（sticky）を除いた範囲」の真ん中**に合わせること。
  箱の中心にすると、貼り付いている `工程／開始・終了` 列（実測275px）のぶん**今日が左に寄って見える**。
- ★**5か月表示では日付・曜日の行が `display:none`** になるので、`querySelector('.gtoday')` で
  先頭を取ると**幅0の見えないマス**を掴む。**幅のあるものを選ぶ**こと
  （`[...w.querySelectorAll('.gtoday')].find(el=>rect.width>0)`）。
- 表は作り直されるたびに新しい箱になるので、**MutationObserver**（`#schedview`／`#mainview`／
  `#dashboard`）で見張り、`data-nncent` を目印に**1つの箱につき1回だけ**寄せる。
  幅が決まってから測るため `requestAnimationFrame` で1コマ待つ。
- 画面の向きが変わったら目印を消して寄せ直す。手動用に `window.nnCenterToday()`。
- 実測（よこ画面852pt）：自社・全体とも**全6カードで今日が見えている**。
  中央に寄るのは表の幅に余裕があるものだけで、端のカードはスクロール上限で止まる（正常）。
- 検証は `scratchpad/todayscroll.js`（ズレ・名前列に隠れていないか・1か月/5か月）。

### 39. 図面・積算：3Dのカメラ固定・ボタン列・割付の札／緑バー全廃（2026-08-05d）
指摘は6つ。①割り付け表示を押すとカメラが戻る ②右のボタンが見切れる ③長押しで動き続けてほしい
④スクロールバーが不要 ⑤割付でH300と寸法と割付が重なる ⑥戻る/進む/削除がまだ小さい・自作アイコンにしたい。

- **★①の原因**：`build3D()` の最後で**毎回** `T.tx/T.tz/T.r` を計算し直していた。
  `d3WariToggle()` も `build3D()` を呼ぶので、割り付けを切り替えるだけでカメラが初期位置に戻っていた。
  → **形（外接箱＋部位数）を文字列にした `d3FitKey` が変わったときと、⌂（`d3WantFit`）のときだけ**
  合わせ直す。割付の切替では箱が変わらないので動かない。検証で `tx/r` が保持されることを確認。
- **②`#d3pad`**：`top:52px; bottom:58px` ＋内部スクロールで頭を打たせる（§21の型）。
  `bottom` を空けるのは**右下の ▲（ナビを出すボタン）と重ねないため**。実測 上97pt〜下341pt（画面393pt）。
- **③長押し**：`d3_zin/zout/tup/tdn/rl/rr` に id を付け、**inline onclick は外して**JSでタップと長押しを分ける
  （残すと二重に動く）。1コマ＝拡大1.2%・傾き0.5度・回転0.9度。⌂ だけ onclick のまま。
- **④緑のスクロールバー（`nn-sbar`）を10ページから完全削除**（`<style>`と`<script>`ごと）。
  スクロールしない画面にも出ていて紛らわしく、2秒ごとの箱探しも止まるので少し軽くなる。
  ★現場記録帳の `#listsb`（一覧の内部バー）は別物なので残っている。
- **⑤割付の札の重なり**：`nnPlaceBox` の逃がし方を**4方向**（外・内・辺に沿って左右）に拡張。
  さらに**割付タブのときは H◯◯◯ も外側**に出す（屋根の中はシートの札で埋まっているため）。
  シート番号の札は帯から出せないので、**帯に沿ってだけずらす `nnSlideLabel`** を新設。
  検証：サンプル3部位で札60枚・**重なり0**（対策前は3件）。
- **⑥戻る/進む/削除**：`min-width` 34→46px（削除は56px）・padding 7px 8px。
  幅を稼ぐため「1マス＝」の**プルダウンをやめ、ボタン＋小さな一覧**にした
  （★当時は「プルダウンは16px未満にできない」と考えていたが、これは誤り。2026-08-06m に訂正）。
  実測 たて画面で右端379pt＜381pt・**はみ出し0**。
  **自作アイコン対応**：`icons/btn_undo.png` `btn_redo.png` `btn_del.png` を置くと絵に変わる
  （`<img class="tbi">`＋`onload`で `.hasimg`／`onerror`で文字に戻る。背景透過PNG・高さ168px）。
- ★three.js は CDN なのでこの環境では読めない。①の検証は **THREEの最小限のニセ部品**を差し込んで
  `build3D()` を走らせて行った（`scratchpad/zumen17.js`）。

### 40. 図面・積算：本物の平面図をPDFで出す（2026-08-05e）★元請提出用
「納まりナビで図面をかいたら、PDFで本物の平面図みたいにできないか。CADが使えない防水屋でも
元請に平面図を出せるのでは」という提案。**作った。**

- **`<style id="nn-plan">`＋`<script id="nn-plan-js">`**（zumen_sekisan.html の末尾）。
  積算・設定パネルの `#sekisan` の**上**に「📄 平面図（元請提出用）」の箱を差し込む。
  入力は 工事名／場所／元請・発注者／作成者／図面番号／北の向き／用紙（A3横・A4横）。
  **`localStorage('nn_zumen_plan_v1')` に保存**するので、次回は入れ直さなくてよい。
- **`window.nnPlanPDF()`** が図面を組み立てて、新しいタブに出して自動で印刷画面を開く。
  iPhoneは共有ボタンから「PDFとして保存」。
- **★中身はSVG（線の図）で描いている。** 画像ではないので、PDFに入っても
  **どれだけ拡大しても線がぼやけない**（元請がPDFを拡大して寸法を読める）。
  紙のmmをそのまま座標に使う（A3横＝420×297）。`@page{size:A3 landscape; margin:0}` で原寸印刷。
- **「本物の平面図らしさ」＝図面の作法をそろえたこと**。ここが肝。
  ①二重の図枠＋右の帯 ②**キリのいい縮尺**（1/10〜1/1000から自動で選ぶ。半端な倍率は使わない）
  ③スケールバー（1m/5m/10m刻みを縮尺で切替）④方位（北の向きは選べる）
  ⑤寸法線は**引出線＋端の45°斜線＋線の上に数値**、数値は線に沿って回す
  ⑥全体寸法を下と左に別で出す ⑦線の太さを使い分け（外形0.6／寸法0.2／補助0.15）
  ⑧ハッチング（工法の色の45°斜線）⑨凡例 ⑩数量表（部位ごと＋合計）⑪注記6行 ⑫表題欄
- **★H300/W250 のタグは全辺に書かない。** 最初そうしたら図面が文字で埋まって読めなかった。
  **部位でいちばん多い値を「共通」として部位名の下に1回だけ**書き、
  ちがう辺だけ辺の脇に書く（`poly._major`／`edgeTag()`）。実際の図面もこの書き方。
- **★部位名が重なる**（塔屋③ が 屋根① の名前と重なった）→ `nameBox` に置いた場所を貯めて
  **上下に8mmずつずらす**。名前の下に白いふとん（opacity .86）を敷いてハッチングと分離。
- **★これはCADの置き換えではない**。DXF/DWGは出せないので、元請側で編集はできない。
  出せるのは「**現地実測にもとづく施工用の平面図**」であって、建築確認申請図ではない。
  注記2にその旨を明記してある（後で揉めないため）。
- 検証は `scratchpad/zumen18.js`（`window.open` を横取りしてSVGを取り出し、
  図枠・寸法・縮尺・方位・凡例・数量表・注記・表題欄がそろっているかを確認。
  A4は `zumen18a4.js`）。見た目は `plan_shot.js` でSVGをPNGにして目視。

### 41. 図面・積算：断面図もPDFで出す（2026-08-05f）
「平面図に続いて断面図もいけないか」との依頼。**作った。**

- **`<script id="nn-sect-js">`**（`window.nnSectionPDF()`）。平面図の入力欄に
  **「断面の切り方（横に切る A-A'／縦に切る B-B'）」と「切る位置（スライダー 2〜98%）」**を追加し、
  ボタンを2つに（📄 平面図／📐 断面図）。設定は平面図と同じ `localStorage('nn_zumen_plan_v1')`。
- **平面図にも切断線が入る**（一点鎖線＋矢印＋A／A'）。2枚が対応する＝図面らしさ。
- 1枚の中身は4つ：①全体断面（上段・紙いっぱいの幅）②パラペット納まり詳細 1/10（下段左）
  ③切断位置キープラン（下段右）④工法の層構成・注記・表題欄（右の帯）。
- **★全体断面は「切断線と屋根の交わり」を計算して作る（`cutSegs`）。**
  交点を左から並べて**2つずつ組にする**と、その間が屋根の中身になる。
  ringsOf で穴も一緒に集めているので、**中抜きも自動で抜ける**（穴で部位が2つに切れる）。
  ★頂点にぴたり重なると計算が乱れるので、切る位置を **0.0007マスだけずらしている**。
- **★矢印の向きには理由がある。** 横に切ったときは**上向き**（＝北側から見る）。
  こうしないと断面図の左右が平面図と逆になる（右手系で確かめた）。縦に切ったときは左向き。
- 高さ関係：部位の `lv`（GL+◯m）で段差を作り、下の躯体は**すぐ下の屋根まで**描く。
  パラペットは辺の H（立上り）と W（天端幅）で外側に立てる。防水層は
  平場→立上り→天端を**1本の太線**（工法の色）でなぞる。壁当り（kabe）は上に破断線。
- **★同じ部位が穴で2つに切れると部位名が2回出る** → いちばん広い区間だけに書く（`widest`）。
- **★立上りHの札はパラペットの真ん中**に置く（内側だとレベル記号 ▽GL+9.0m と重なる）。
- 詳細図（1/10）は 躯体＋防水層＋アルミ笠木＋**層構成の模式図**（①〜⑤・帯の一覧と対応）。
  ★層構成は 1/10 では線1本にしかならないので、**厚さを誇張した模式図**にして
  「厚さは実寸ではありません」と明記した（注記4にも）。
- **★図枠からのはみ出しに注意**。詳細図は下に伸びる（パラペットの下・破断線）ので、
  紙の高さから逆算して置くこと。最初 230mm 下げていて図枠を突き抜けた。
- 縮尺は平面図と同じ考え方でキリのいい値を自動選択。実測：横切り 1/100・縦切り 1/75〜1/100。
  ★上段に紙いっぱいの幅を使う割り付けにしないと 1/150 まで落ちる（最初そうなっていた）。
- 検証は `scratchpad/zumen19.js`（`node zumen19.js h 50` / `v 25` のように向きと位置を渡す。
  17項目を自動確認）。見た目は `sect_shot.js`。

### 42. 図面まわりを作り直し：平面図・断面詳細図・施工層構成図（2026-08-05g）
指摘は4つ。①寸法の端を斜め線→**●**に ②真ん中の「A」が意味不明→削除
③レイアウトを一般的な建築図面（1階平面図）の形に ④断面図→**断面詳細図**に。
さらに「材料と工程が分かる別角度の図」（メーカーカタログのような絵）も新規で作った。

- **旧 `nn-plan-js` と `nn-sect-js` は廃止し、`<script id="nn-draw-js">` 1本に統合**。
  ボタンは3つ（📄平面図／📐断面詳細図／📦施工層構成図）。設定は今までどおり
  `localStorage('nn_zumen_plan_v1')`。**切断線 A-A' の設定（向き・位置）は削除**。
- **★寸法線の端は「●」**（`A.dim`）。斜め線（スラッシュ）は使わない。
  数値は線の上に、線に沿って回す。★回したあとの「上」は `(sin(ang), -cos(ang))`。
  これを間違えると、たて向きの寸法だけ数値が線の反対側に出る。
- **★レイアウトは一般的な建築図面に合わせた**（もらった「1階平面図 S:1/50」を実測）。
  ・**表題欄は下の横帯**（右の縦帯をやめた）。左＝作成者／中＝備考／右＝縮尺・作成年月日・
    図面番号 ＋ 工事名称・場所・図面名称の3行×2組。
  ・**図面名＋縮尺の箱**を表題欄のすぐ上・右端に置く（「平面図　S:1/75」）。
  ・**通り芯**（`axesOf`）＝頂点のx・yをまとめて X0…／Y0… と丸囲みで振る。一点鎖線で外に伸ばす。
    ★数が増えすぎると読めないので**14本で頭打ち**（間引く）。
  ・**寸法チェーン**＝通り芯どうしの間隔（上）＋総寸法（その外側）。たては右側に置く
    （左は通り芯の丸囲みが入るため）。
  ・**辺ごとの寸法は「斜めの辺」だけ**にした。縦横はチェーンで足りており、
    以前は総寸法と同じ数字が2回出て（26.5mが2つ）分かりにくかった。
- **断面図 → 断面詳細図（1/5）**。パラペットの納まりを大きく描き、引出線で材料名を書く。
  躯体（下は破断線）／入隅面木／防水層は**3本の平行線**（層のかさなり）／アルミ笠木＋シーリング。
  寸法は 立上りH・天端W・スラブ150。水勾配の記号（三角＋1/100）。下に層構成と注記。
  ★**引出線は右に1列そろえる**（指す先には●）。バラバラに置くと線が図面を横切って読めない。
- **新規：施工層構成図（アイソメ）**＝斜め上から見た絵。層を1枚ずつ**ずらして**描くので、
  どの材料がどの順で載るかが一目で分かる。右に工程表（工程・材料・範囲/数量）。
  ・アイソメの軸は `IX=[0.866,0.5] / IY=[-0.866,0.5] / z は上`。
    立体は「断面の輪郭(y,z)をxに押し出す」`prism()` で作る。
    ★**見える面だけ描く**＝辺の外向き法線 `(dz,-dy)` と `(0.7,0.7)` の内積が正のものだけ。
    輪郭の回り方が逆だと全部消えるので、**符号付き面積で向きをそろえてから**判定する。
  ・★**番号は「下から上へ」並べる**。上から並べると引出線が全部交差して読めない
    （下の層ほど手前・下に見えるため）。積む順そのものなので、かえって分かりやすい。
  ・★指す先は「その層の**見えている切り口**」。平場の上を指すと上の層に隠れる。
  ・厚みは誇張（実寸ではない）と図中と注記2に明記。縮尺は **NTS（尺度なし）**。
- ★**スケールバーは「紙に入る目盛り」を選ぶ**こと。縮尺で決め打ち（1/50以下なら1m等）にすると、
  1/75で5m刻み＝67mmの帯が4本＝267mmになり**紙からはみ出した**。
- 検証は `scratchpad/zumen20.js`（3種を一度に出して21項目を自動確認）と `shots.js`（PNG化して目視）。

### 43. 図面のPDFが「たて」で出る（2026-08-06a）
「PDF印刷して見たら、なぜかタテ表示になる」との指摘。実機は Windows / Edge。

- **★原因は `@page{size:A3 landscape}` という書き方**。この書き方は環境によって効かず、
  たて長の紙に**回転して押し込まれる**（Windows の「Microsoft Print to PDF」等は
  プリンタ側の用紙設定が優先されるため）。
  → **実寸で書く**（`@page{size:420mm 297mm; margin:0}`）。ヘッドレスで検算：
  `page.pdf({preferCSSPageSize:true})` の MediaBox が 1191×842pt＝420×297mm になった。
- それでもプリンタ側が「たて」しか受け付けない環境があるので、開いたタブの上に
  **緑の帯**（印刷時は消える）を付けた。
  ・「🖨 印刷 / PDFに保存」ボタン
  ・**「⟳ 用紙がたてになるとき」ボタン**＝`@page` をたて（297×420mm）に切り替え、
    SVGを `translateX(297mm) rotate(90deg)` で回して**たて紙いっぱいに入れる**。
    `<style>` を2つ置いて `sheet.disabled` で切り替えている。
  ・「印刷画面で 用紙：A3／向き：横 を選ぶ」という案内文
- 検証は `scratchpad/printchk.js`（両方の状態でPDFを作り MediaBox を見る）と `rotshot.js`（回転時の見た目）。

### 43-2. 書類の別タブに「← 戻る」を付けた（2026-08-06h）★致命バグ修正
「図面を起こすと、この画面から戻る画面がない」との指摘。書類（図面3種・工程表・報告書）は
`window.open` の別タブに開くが、**ホーム画面から起動したアプリ（PWA）にはブラウザの戻るが無く、
開いたら二度と戻れなかった**。
- 緑の帯の先頭に **「← 図面に戻る／← 記録帳に戻る」** ボタン（`nnDocBack()`）を追加。
  **3段構え**：①`window.close()`（scriptが開いたタブは閉じられる）→②閉じなければ `history.back()`
  →③それも効かなければ `location.replace(元ページのURL)`（開くときに `NN_BACK` として埋め込む）。
- 直した場所は3つ：zumen の `openSheet`（図面3種共通）／kirokucho の `openKoteiSheet`（工程表）／
  kirokucho の `exportPDF`（物件報告書。**ここは帯自体が無く自動印刷だけだった**ので、
  戻る＋印刷ボタンの帯を新設。`.noprint` クラスで印刷には出ない）。
- ★報告書はテンプレート文字列（バッククォート）の中なので、差し込む `<script>` の閉じは
  **`<\/script>` にすること**（そのまま書くと外側のscriptがそこで終わる）。
- 検証は `scratchpad/docback.js`（3系統とも：ボタンがある・押すと閉じる・JSエラーなし）。

### 44. 現場記録帳：工程表（バーチャート）をPDFで出す（2026-08-06b）★元請提出用
「現場に提出する資料ベースで考えると価値が上がる」という話から、まず工程表を作った。
図面（平面図・断面詳細図・アイソメ）に続く**4種類目の提出書類**。

- **`<style id="nn-kotei">`＋`<script id="nn-kotei-js">`**（kirokucho_demo.html の末尾）。
  `window.nnKoteiPDF(pid)`。ボタンは2か所：
  ・施工中カードの見出し（`scHead()` に `🖨 工程表PDF`）＝全体／自社スケジュール両方に出る
  ・物件詳細の「工程」タブ（`.gedit` の中）
- **★元請工程（`p.motoGantt`）と自社工程（`p.gantt`）を1枚に並べる**のが肝。
  これが「総合工程表」の形で、元請がそのまま使える。
  区切り行【元請工程（総合）】【自社工程（防水工事）】でグループ分け。
- 色分け：元請＝グレー／元請の防水工事＝赤／自社＝緑／防水施工（本工事）＝濃緑。
- **今日の位置に赤い縦線**（§37の工程表と同じ考え方。時刻ぶんの端数も反映）。
- 1枚の構成：表題「工程表（バーチャート）」→ 工事概要の帯（8項目）→ 表（工程名／開始〜終了（日数）／
  日付グリッド）→ 凡例 → 備考 → 表題欄（図面と同じ下の横帯）。
- ★**日付の目盛りは幅で切り替える**。1日あたり2.2mm未満になったら日付・曜日を出さず、
  月曜ごとの目盛りだけにする（`unit='week'`）。長い工事でも潰れない。
- ★**長い文字は枠からはみ出す**。工事名が表題欄を突き抜けたので `A.fit(文字,サイズ,幅)` を追加
  （日本語は1文字＝文字サイズ、英数は0.55倍として見積もり、入らない分は「…」で切る）。
- ★**行の高さの上限は 11mm**。7.4mmにしていたら表が紙の上半分で終わり、下が真っ白だった。
- ★**金額は載せない**（請負額・粗利）。現場に出す紙なので、意図せず単価が出回るのを避ける。
- 印刷の向きは §43 と同じ作り（実寸の `@page` ＋ 案内の帯 ＋ 90°回転ボタン）。
- 作成者は図面と同じ `localStorage('nn_zumen_plan_v1')` から読む（同じサイトなので共有される）。
- 検証は `scratchpad/kotei.js`（16項目）と `kotei_shot.js`（PNG化して目視）。

### 45. 検索避け（noindex）と、公開リポジトリからの個人情報の削除（2026-08-06c）
「発信すると競合を増やすだけ。隠れてやりたい」との方針を受けて。

- **★このリポジトリは `public`（誰でも閲覧可）**。GitHub API で確認済み。
  つまり HTML 全ソース・**CLAUDE.md（このファイル）**・コミット履歴が全部読める。
  機能を真似されるより、**「なぜそうしたか」が書いてある CLAUDE.md を読まれるほうが痛い**。
- **CLAUDE.md から個人情報を削除**：閲覧制限の取り決めの具体的な内容（サイト名・管理者・
  設定方法）と、自宅PCのフルパス（`C:\Users\...`）。**ルール自体は短い形で残してある**
  （守る必要があるため）。★このファイルは公開先に載る。**個人的な内容は書かないこと。**
- **全11ページの `<head>` に `noindex, nofollow, noarchive, nosnippet, noimageindex`**
  （`scratchpad/add_noindex.py`）。Google・Bing の検索結果から外れる。
  ★**印刷用HTMLを組み立てる文字列の中にも `<meta charset>` `<meta name="viewport">` がある**
  （kirokucho は3件・zumen と genba_map は2件）ので、**必ず最初の1つの前にだけ**差し込む。
- `robots.txt` も置いたが、**github.io のサブパスでは効かない**（robots.txt はドメイン直下しか
  読まれず、そこは GitHub の持ち物）。**実際に効いているのは meta の noindex のほう。**
  独自ドメインにしたときのための保険として置いてある。
- 残っている宿題：①コミット履歴には消す前の CLAUDE.md が残っている（強制pushで消せる）
  ②リポジトリを非公開にするには GitHub Pro（月4ドル程度）が要る。どちらも本人の判断待ち。
- ★**ブラウザだけで動くアプリは、サイトを見た人が「ソースを表示」すれば中身が読める。**
  リポジトリを非公開にしても変わらない。本当に隠すなら計算をサーバー側に置くしかない（大工事）。

### 46. データの保存・復元（書き出し／読み込み）を追加（2026-08-06d）★引っ越しの前提
引き継ぎの優先1「データの書き出し／読み込み（JSONファイル）」を作った。
Cloudflare への引っ越し（URLが変わると localStorage が別扱いになり消える）や機種変更に備えるもの。

- **入口はホームの「設定」ボタン**（今まで「準備中です」だった）。`navGo` の settei 分岐で
  `nnDataOpen()` を呼ぶ。中身は index.html 末尾の `<style id="nn-databk">`＋`<script id="nn-databk-js">`。
  パネルはJSで組み立てる（HTMLには置いていない）。下部ナビに「設定」は無いのでホームだけでよい。
- **書き出し**：全保存キーを1つのJSONにまとめてダウンロード
  （`osamarinavi_data_2026-08-06.json` のような日付入りの名前）。
  形は `{app,kind:'nn_backup',ver:1,date,data:{キー:生の文字列}}`。
  **値は生の文字列のまま**入れる（解釈しないので、各ページの形式が変わっても壊れない）。
- **読み込み**：ファイル選択 → 中身の種類数と書き出し日を confirm で見せてから上書き。
  `kind:'nn_backup'` でないファイルは断る。**知らないキーは書き込まない**（許可制）。
  書き込み失敗（容量オーバー。写真が多いと起こり得る）は1キーずつ拾って件数を案内する。
- **★対象キーの一覧は `nn-databk-js` の `LIST`（24キー・日本語ラベル付き。§47で1キー追加済み）。
  新しい保存キーを増やしたら、ここにも1行足すこと（足れないと引っ越しでそのデータだけ消える）。**
  内訳：記録帳の工程編集／図面・積算の作図＋入力欄／仕様／材料／ライブラリ写真＋転用／
  発注の履歴＋自社情報／用語のお気に入り／現場マップ一式（現場・屋根・段差・写真・位置・
  高さ×2・APIキー・住所控え）／表示設定4つ。
- 件数の表示は「配列→◯件、`{items:[…]}`→◯件、その他のオブジェクト→キー数、それ以外→保存あり」。
- 検証は `scratchpad/databk.js`（20項目：開閉・件数・実ダウンロードと中身・全消去→復元・
  別ファイルの拒否）。★一覧の innerText はラベルと件数の間に改行が入るので、
  正規表現は `[\s\S]*?` で跨ぐこと。
- ★ヘッドレスで index を開くと favicon.ico の404が console に出るが、**もともと無いファイル**
  （実機は apple-touch-icon を使う）。今回の変更とは無関係。

### 47. 現場記録帳：不具合・現場条件タグ（2026-08-06e）★佐野さん発案・アイコン画像待ち
「膨れ・口開き・漏水…をアイコンにして現場記録帳に置けば視認性が上がる」という佐野さんの発案。
アイコンは**佐野さんがパワポで自作して順次届く**。先に受け皿を全部作った（文字チップで動いている）。

- **できること**：物件に不具合タグを付ける →「①一覧カードに表示 ②物件詳細に表示＋『✎ タグを編集』
  ③絞り込みバーの『不具合別』で絞り込み（グループ見出し付きメニュー・件数表示・複数=OR）」。
- **タグは24種・4グループ**（グループごとに色分け。症状=赤／原因・環境=茶／部位の注意=緑／対応・宿題=黄）。
  キーは `nn-defect-js` の `DEFS` に定義：
  症状=fukure膨れ・kuchiaki口開き・tsuppari突っ張り・rosui漏水・chokingチョーキング・genmo減耗・
  sunaochi_geki砂落ち激しい・sunaochi砂落ち・gamahadaガマ肌・poolプール・drainドレン詰まり・
  osamari納まり不良・toriai_furyo取り合い不良・tanmatsu端末不具合／
  原因=shitaji下地起因・togai凍害／部位=fukuzatsu複雑形状・kyosho狭小部位・toriai取り合い発生／
  宿題=settyaku要接着試験・hikinuki要引き抜き試験・makerメーカー相談・maker_chu相談中・maker_zumi相談済
- **★アイコン画像の入れ方（届いたら）**：`icons/def_<キー>.png` に置くだけ（HTMLは触らない）。
  規格はナビと同じ＝**背景透過PNG・透明な余白を切り落とす・高さ168px・幅は絵なり・正方形にしない**。
  文字チップの左に絵が出る。無い間・読めない間は文字だけ（onerrorで自動非表示）。
- **保存**：`localStorage('nn_kirokucho_def_v1')`＝`{"J001":["fukure",…]}`（物件番号キー）。
  保存が無い物件はサンプル `SAMPLE`（17物件・24タグ全種を網羅）。
  ★一覧の先頭は完成済が50件並ぶので、**先頭の完成済にも数件付けてある**（J001・J002・J004・J007・J008）。
  付けないと「最初の画面で何も変わっていない＝反映されていない」ように見える（版eで実際に言われた）。
  編集した物件は保存が優先（サンプルには戻らない）。**バックアップのLIST（§46）にも追加済み**。
- **作り**：本文への差し込み7か所（カード・詳細・filtered・lfTags・lfDrop・lfResetAll・buildChips）＋
  末尾の `<style id="nn-defect">`＋`<script id="nn-defect-js">`。差し込みは `scratchpad/apply_defect.py`
  （置換が各1件ずつ成功したら書き込む方式）。絞り込みチップは既存の `lfToggle`／`nnShowPortal` に
  相乗りしている（`data-k="defect"` の `.dfil` を `buildChips` の末尾に足すだけ）。
  ★`lfOpen` などのページ側の `let` 変数は、後ろの `<script>` からそのまま読み書きできる
  （top-level の let/const は同じページ内なら別スクリプトからも見える）。
  ★`nnShowPortal` は **`#nnPortal` 自身が `.dfmenu` になる**（中に .dfmenu ができるのではない）。
  検証スクリプトでセレクタを間違えた。
- 検証は `scratchpad/kdef.js`（13項目：カード表示・メニュー・絞り込み4件・リセット・編集・保存・
  再読み込み後も残る）と `kdef2.js`（たて画面でチップ帯が1段のまま＝高さ44px）。

### 48. 絞り込みバーの検索欄つぶれ／ライブラリを一覧に（2026-08-06j）
指摘は2つ。①現場記録帳の絞り込みバーで「年度別」の後ろに何かがあって被っている
②ライブラリだけカード形式。一覧にしつつ画像も分かるように、選んだら詳細。

- **①原因は §47 で「不具合別」が増えてチップが7個になったこと**。ツールバーは
  `#chips{flex:none}`（縮まない）／`.search{flex:1 1 150px; min-width:0}`（いくらでも縮む）
  だったため、チップの合計幅が広がったぶん**検索欄が数pxまで潰れ、白い塊として顔を出していた**
  （よこ画面はノッチ59px×2ぶん使える幅が減るのでさらに顕著）。
  → 検索欄に**最低幅104px**を与え、**チップ帯のほうを横スクロール**（`flex:1 1 200px` ＋
  `overflow-x:auto`）にした。件数の札（`#nnLC`）もスマホでは少し詰めた。
  ★`#chips` を overflow の箱にしても**絞り込みメニューは閉じ込められない**。
    §4 のとおりメニューは `#nnPortal`（body直下）に複製して出しているため。検証で確認済み。
  ★再現には **よこ画面＋`env(safe-area-inset-*)` を59pxに置換したコピー**が必要（§7の型）。
    ノッチ無しでは幅が足りてしまい再現しない。
- **②ライブラリを一覧（行）形式に**（`renderGrid` の中身＋末尾の `<style id="nn-liblist">`）。
  行＝[サムネイル112×72（スマホ86×58・工法色＋📷、`it.thumb` があれば実写真）]＋
  [タイトル／タグ（注意あり・部位・工法・区分・共有）]＋[日付・転用回数]＋[›]。
  タップで詳細（既存の `openDetail`／`detailId`）が全画面で開く＝作りは変えていない。
  ★**「⚠ 注意あり」はタイトル行に置かないこと**。タイトルが折り返して読みにくくなる（実際になった）。
    タグ行の先頭に赤いタグとして置く。
  ★よこ画面（高さ393pt）は行が高いと3件しか入らないので、
    `@media (orientation:landscape) and (max-height:520px)` で行を詰めて**4件**にした。
  実測：たて7件／よこ4件が1画面。カードのとき（たて約4件）より増えた。
- 検証は `scratchpad/ui2chk.js`（22項目：検索欄の幅・重なり・チップ1段・メニューが画面内に1つだけ／
  ライブラリの行数・サムネ寸法・横はみ出し0・タップで詳細・戻ると一覧。たて／よこ両方）。

### 49. 図面・積算：流し方向を部位ごとに／数値欄の日本語キーボード（2026-08-06k）
指摘は2つ。①ルーフィングの割り付け方向が全部同じなので、短手に流す割り付けも選べるように
②GL+ の欄でひらがなが入力できてしまう。

- **①流し方向を「部位ごと」に**（`dirOf(poly)`／`shortDir(poly)`／`setPolyDir`／`renderDirRows`）。
  ・全体の設定（`state.dir`）に **`short`（短手に流す）** を追加＝3択に。
  ・部位ごとの上書き `poly.dir`＝`''`(全体に従う)／`h`／`v`／`short`。
    割付設定パネルに「部位ごとに変える」の一覧を出す（**部位が2つ以上のときだけ**）。
    各行に [全体][横][縦][短手] と、いま実際に流れる向き（横流し／縦流し）を表示。
  ・**短手＝外接四角形の短い辺に沿って流す**（横長の屋根→縦流し／縦長→横流し）。
    状態としては 'short' のまま持ち、描くたびに計算するので、形を変えれば自動で追従する。
  ・★`state.dir` を見ていた箇所を**全部** `dirOf(poly)` に差し替えること（5か所）：
    割付図 `drawBands`／数量集計 `wariAgg`／3Dの継目 `build3D`／
    シートのクリック判定 `wfPick`／当たり判定に積む `wfHit.dir`。
    **`wfPick` は部位ごとに向きが違うので、帯に保存した `w.dir` で座標を読み替える**
    （ループの外で1回だけ計算していたものを、ループの中へ移す）。
  ・`loadState` で `p.dir` が 'h'/'v'/'short' 以外なら消す（旧データ互換）。
- **②数値の入力欄はすべて `inputmode="decimal"` に**（このページの `type="number"` 12か所）。
  ★**`type="number"` だけではiPhoneは日本語キーボードを出すことがある**（直前に日本語入力を
  使っていると残る）。§35-2 の札の入力と同じ対処。見た目も動作も変わらず、キーボードだけ数字になる。
- 検証は `scratchpad/zdir.js`（12項目：inputmode・部位ごとの行と4択・2つ目だけ縦流し・
  短手の自動判定・**ニセTHREEで3Dの継目に縦横が混在**・全体「短手」）。

### 50. ★プルダウンだけ文字が異様に大きかった（2026-08-06m）★全ページの取り違え
「部位：すべて とかの文字デカすぎじゃない？ なんでこれだけどうでも良いものなのに異様にでかいの？」
ライブラリの絞り込みで指摘。**もっともな指摘で、原因はこちらの取り違えだった。**

- **原因**：全ページ共通の「入力欄は16px以上にする」指定（iOSの自動拡大よけ）に、
  **`select` まで含めて `!important` を付けていた**。
  そのためページ側の指定（ライブラリ `.filterbar select{12.5px}`／仕様登録 `.estep select{12.5px}`／
  材料登録 `.qty-line select{13px}`／現場マップ `#movePad select{10px}`）が**全部打ち消され**、
  脇役のプルダウンだけが一覧の見出し（13px）より大きい16pxで出ていた。
- **★正しい知識**：iPhoneで画面が勝手に拡大するのは **キーボードが出る入力欄**
  （text/number/tel/email/search/textarea）をタップしたときだけ。
  **`<select>` はピッカーが出るだけなので拡大しない＝16pxにする必要はない。**
  これまでの引き継ぎメモ（§7・§39）の「selectも16pxのまま」という記述は誤りなので訂正した。
- 直し方：全11ページの `input:not(...), select, textarea{font-size:16px !important}` から
  **`select` を外す**（12箇所。現場記録帳だけ2箇所ある）。
  外すと各ページの本来の指定が効く。指定が無いページはブラウザ既定（約13px）になる。
  ★意図して16pxにしている select（図面・積算 `#nnPlanBox`＝入力欄と並ぶ設定欄、
    現場記録帳・仕様登録・材料登録の `.modal`）はそのまま16pxで正しい。
- あわせて**ライブラリの検索例タグ5個（「パラペットが低い時のトーチ端末」等）を削除**（本人の指示）。
  一覧の開始位置：**245pt → 118pt**。よこ画面で3件が丸ごと見えるようになった。
- ★**入力欄（検索欄）の文字は16pxのまま**。プレースホルダだけ小さくすることは可能だが、
  入力を始めると16pxに戻るので大きさが変わって見える。要相談。
- 検証は `scratchpad/selchk.js`（全11ページの select の実効サイズ・ライブラリのタグ0個・
  プルダウン12px＜一覧の見出し13px）。

### 51. 図面・積算：方眼を水色に／夜モード／断面図モード（2026-08-06n）
指摘は4つ。①方眼の線が灰色→本物のように薄い水色に ②ダークモード（直射日光の屋上）
③断面図モード ④写真取り込みモード（写真をなぞってアイソメの既存図を起こす）。
**①②③を実装。④は次のセッションで**（規模が大きいため。設計は下に残す）。

- **①方眼の色**：`#c9d4c6/#e3eae0`（灰）→ **`#a6cee4/#dcecf7`（薄い水色）**。
  canvas はCSSが効かないので、色は `NNTH` というオブジェクトに持たせて `draw()` から使う。
- **②夜モード**（`nnSetTheme('dark')`／ツールバーの「🌙 夜」ボタン。スマホは「⋯ 道具」の中）。
  ・canvas 側＝`NNTH.dark`（地 `#0d161d`・方眼 `#1b3140/#3a7a99`）
  ・CSS 側＝`<style id="nn-theme">` の `html[data-nntheme="dark"]`（共通8色の変数を上書き＋
    白背景を直書きしている要素だけ個別に上書き）。**両方に色が要る**のがポイント。
  ・`localStorage('nn_zumen_theme')` に保存。3Dビューの背景も合わせて変える。
- **③断面詳細図モード**（タブが `①図面 ②割付 ③断面 ④3D` の4つに。`<style id="nn-sect">`＋`<script id="nn-sect-js">`）。
  ★最初「全体断面（屋上を切った全景）」を作って**やり直しになった**。欲しかったのは
  **パラペット立上りの納まりを大きく描く「断面詳細図」**（PDFの断面詳細図と同じ中身を画面で見るもの）。
  ・上のバーで **部位と辺**を選ぶ（既定は「いちばん多い納まり（代表）」＝`nnMajorHW()`）。
    辺を選ぶとその辺の H・W で描き直す。「📐 PDFで出す」＝既存の `nnSectionPDF()`。
  ・中身：躯体（左端は**破断線**）／入隅の面木75×75／防水層を3本の平行線で／アルミ笠木＋シーリング／
    寸法（立上りH・天端W・スラブt）／引出線＋材料名（右に1列そろえる）／層構成（`nnLayersOf`）。
  ・★**文字はすべて横書き**。縦寸法の数値も回さない（回すと読みにくいと指摘された）。
  ・★**`X(mm)=ox+mm*K` の ox は「原点(0mm)の画面位置」**。図の左端 `x0` を余白に合わせるには
    `-x0*K` を足すこと。忘れると図が画面の外に出る（実際に出た）。
  ・★**よこ画面（高さ393pt）は縦の余裕が無い**。層構成まで入れると図が豆粒（1mm=0.04px）になるので、
    `(Hh-barH)<430` のときは層構成を出さず引出線も4つに絞る（1mm=0.14px＝3.5倍に）。
  ・★上の操作バーと下部ナビの高さを余白に入れること（`barH`／`navH` を実測）。
  ・層構成と代表寸法は nn-draw-js 側の `layersOf`／`majorHW` を **`window.nnLayersOf`／`window.nnMajorHW`
    として公開**して使い回す（PDFと画面で中身がずれないように1箇所で管理）。
  ・たて画面でもヘッダーのタブ4個＋？は1段に収まる（実測：右端388.6 ＜ 幅393）。
- 検証は `scratchpad/thchk.js`（方眼が水色・夜モードの画素・保存）と
  `sec2chk.js`（8項目：詳細図が描ける・部位と辺の選択・PDFボタン・夜モード）。

### 52. 図面・積算：写真取り込みモード（2026-08-06q）★斜め写真から平面図を起こす
現場写真をなぞって「既存の屋上」を図面に起こす機能。ツールバー（スマホは「⋯ 道具」）の
**「📷 写真から起こす」**から、画面全体の専用モードが開く（`<style id="nn-photo">`＋`<script id="nn-photo-js">`）。

- **手順は5段階**（画面下に案内文が出る）：①写真を選ぶ ②平場の四隅を左上→右上→右下→左下の
  順にタップ ③その四角形の実際の幅・奥行き（m）を入れる ④屋根の輪郭をなぞる（始点で閉じる）
  ⑤「図面に取り込む」→ `state.polys` に部位として入る。
- **★中身は射影変換（homography）**。四隅の4点対応から3×3行列を解き（`homography`＝
  8元1次連立をガウス消去 `solve8` で解く）、写真上の点を平面の座標(m)に直す（`applyH`）。
  これで**斜めから撮った写真でも真上から見た正しい形**になる。
  検証：台形に置いた四隅が誤差**0m**で長方形に戻り、なぞった四角形の面積が 12m×9m＝**108㎡**ちょうど。
  台形の重心が長方形の中心(4.5)ではなく6.0に写る＝遠近がちゃんと効いている証拠。
- なぞっている最中、**各辺に変換後の実寸（◯◯m）が出る**ので、その場で寸法の妥当性が分かる。
- 置いた点は**指でドラッグして直せる**（四隅も輪郭も）。2本指で拡大・移動。
- 取り込むと既存の部位と重ならない位置へ自動でずらす。立上りHは既定値（右パネルの defH/defW）。
  → **③断面詳細図・④3Dビューでそのまま既存の姿が見られる**（新しい描画は作っていない）。
- ★**写真そのものは保存しない**（localStorage は容量が厳しく、写真1枚で溢れる）。
  取り込んだ形だけが図面に残る。作業途中で閉じると写真は消える。
- ★スマホのたて画面はボタンの文字が長いとバーが3段になり写真が小さくなるので、
  `NN_PHONE` のときはラベルを短くする（「写真を選ぶ」→「📁 写真」等）。
- 検証は `scratchpad/photochk.js`（13項目：変換の誤差・面積・取り込み・3Dに出る）と
  `photoshot.js`（たて／よこのバーの段数とはみ出し）。
- ★注意（使う人向け）：写真は「屋上全体が入る1枚」で。四隅は**平らな面の角**を選ぶ
  （段差の上下をまたがない）。四隅が実際に長方形でないと精度が落ちる。

### 53. 4件まとめて（スケールバー削除・中抜きのGL・地図ピン・ライブラリ絞り込み）（2026-08-06r）
- **①平面図PDFのスケールバー（凡例の下の白黒の棒）を削除**。縮尺は図面名の箱と表題欄に出ている。
- **②中抜きすると3Dで高さがおかしくなる**（写真で穴のまわりが白い壁で囲まれていた）。
  ★原因は `closeHole` が穴の縁の既定を **`k:'kabe'`（壁当り）** にしていたこと。
  build3D の壁当りは「上の階と接していなければ **既定2.6mの外壁**を立てる」作りなので、
  中抜きしただけで穴のまわりが壁で囲まれ、GLが変わったように見えていた。
  → **(a) 新規の中抜きの既定を `para`（パラペット・立上り＋天端）に変更**
    **(b) 既存データ対策として、`ring.r>=0`（＝穴の縁）の壁当りは上の階と接していなければ壁を立てない**
    （`wallUp=0`）。これで前に作った図面も直る。
  ★`ringsOf` は `r:-1`＝外周／`r:0..`＝穴 を返すので、穴かどうかはこれで判定できる。
- **②夜モードのボタン名を「🌙 夜／☀ 昼」→「🌙 ダークモード／☀ 通常モード」に**（本人の指示）。
- **③現場マップの2Dマーカーを自作アイコン対応に**（`dotContent`＋`<style id="nn-pinimg">`）。
  `icons/pin_quote.png`（見積済）／`pin_work.png`（施工中）／`pin_done.png`（完成済）を置くと絵になる。
  規格＝背景透過PNG・高さ168px・幅は絵なり。置いていなければ今までの丸い点のまま。
  ★**3D立体モードのピンは Google の部品（PinElement）なので絵に差し替えられない**（§32で調査済み）。
    2D地図・2D写真モードだけ自作アイコンが効く。
    ★**2026-08-15m 追記**：しずく型の代わりに **Polygon3DElement で「ポチ（丸）」を描く**方法で
      置き換えられた（§117）。焼き込みではないのでにじまない。
- **④ライブラリの絞り込みを現場記録帳と同じ方式に**（`<style id="nn-libfil">`＋`<script id="nn-libfil-js">`）。
  検索欄（短く）＋部位／工法／区分／並びのチップ＋件数を**1行**に。チップは
  **チェックボックス付きメニュー（複数選べる・件数つき）**で、メニューは `#nnLibPortal`（body直下）に出す。
  「すべて」の文言は廃止。旧プルダウン（`.filterbar`）は `display:none` で残してある
  （`filtered()` が今も参照しているため、消すと壊れる）。
  ★たて画面（393pt）は「検索欄＋AI検索の文字＋チップ4つ＋件数」が入りきらず**ボタンとチップが重なった**。
    `@media (max-width:560px)` で AI検索を**虫めがねだけ**にして幅を空けた（実測 帯42px・1段・はみ出し0）。
- 検証は `scratchpad/r4chk.js`（17項目）と `libshot.js`（たて／よこの帯の段数とはみ出し）。

### 54. 図面・積算：3Dの継目のはみ出し／ダークモードの導線／マスの表示切替（2026-08-06s）
- **①3Dで短手（エンドラップ）の継目線が屋根からはみ出す**。
  ★原因：エンドラップの線だけ**「帯の幅ぶんの棒」を屋根の形と無関係に置いていた**
  （`bw=Math.min(b.hi-b.lo, sp.sheetW)`）。帯の範囲は外接矩形ベースなので、
  L字など凸凹した屋根では帯が屋根の外にはみ出す＝線もはみ出す。
  → **その位置で屋根の中に入っている区間を `scanSegsH` で求め、帯の範囲と重なる分だけ描く**。
  ★サイドラップ線は最初から `scanSegsH` を使っていたので正しかった。
  ★座標の入れ替えは2種類要る：**T2＝帯に沿う向き／T3＝それと直交する向き**（HDIRで逆になる）。
  検証は `scratchpad/seamcmp.js`（**L字形を作って旧方式と新方式を数値で比較**。
  旧は端点46個中1個が屋根の外→新は0個。※長方形のサンプルでは差が出ないので必ずL字で試すこと）。
- **②ダークモードの切替を4か所すべてから**：図面／割付＝ツールバー（`tl_theme`）、
  3D＝操作パッド（`d3_theme`）、断面＝上のバー（`sec_theme`）。
  ★`init3D` の `scene.background` を**テーマから決める**ように変更。
    以前は 0xe8ece6 固定だったので、3Dを開く前にダークにしていると背景だけ明るいままだった。
- **③方眼（マス）の表示・非表示**（`nnToggleGrid`／`nnGridOn`／ツールバーの「▦ マス」）。
  消すと方眼紙ではなく「図面」に見えるので、清書や元請への画面見せに向く。
  ★**打点は消しても今までどおり交差点に止まる**（`snapG` は方眼の表示と無関係）。
  左下の案内も「マス非表示（1マス＝◯m のまま。打点は交差点に止まります）」に変わる。
  `localStorage('nn_zumen_grid')` に保存。スマホでは「⋯ 道具」の中。
- 検証は `scratchpad/f3chk.js`（10項目：継目のはみ出し0・切替ボタン3か所・3Dの背景・
  マスの消去と復帰・非表示でも打点が整数・保存）。
- **★2026-08-06t 追記：3Dの地面の網（GridHelper 2枚）は削除**。一面に細かい線が並ぶと
  目が疲れる（特にダークモードで白い線が目立つ）との指摘。形と割り付けを見るのが目的なので
  地面の網は無くても困らない。**図面タブの方眼（▦ マス）とは別物**なので、そちらは今までどおり。
  検証は `scratchpad/gridchk.js`（GridHelper が0個・建物は描かれる・図面のマスは無事）。

### 55. 現場記録帳：進捗・予算のバーを「筒状の光沢バー」に（2026-08-08a）
ゲーム（パワプロ）の達成度バーの写真をもらい、「筒状で、足りない部分は黒、満たしている
部分は光沢のある緑または赤にしてほしい」との依頼。

- **`<style id="nn-glossbar">`**（kirokucho_demo.html の末尾）。既存のCSSは触らず、
  上から `!important` でかぶせる方式。直したのは**横向きの進捗・予算バー6種だけ**：
  `.prog` / `.pline .prog`（一覧の進捗／予算消化・要対応のミニバー）／`.sc-prog .bar`
  （施工中の現場）／`.ptrack`（物件詳細の予実）／`.pace .pt2`（工事の進み／予算の減り）／
  `.houbar`（工法別の比率）。
- **筒に見せる作り**は3つの組み合わせ。
  ①地を黒の縦グラデ（`#0a0d0a→#2c332c→#040604`）②内側に濃い影（`inset 0 2px 4px`）
  ③**バー全体にかぶせる白いつや**（`::after`・上から46%・`pointer-events:none`）。
  ★つやは「満ちている部分」ではなく**バー全体**にかける。筒そのものの照り返しなので、
  黒い部分にも乗っているほうが本物らしく見える。
- 満ちている部分は緑（`#97e86f→#45c94e→#0a6a21`）、**予算のバーと超過は赤**。
  ★`.ptrack i` と `.pace .pt2 i` は**JSがインラインで色を入れている**ので `!important` が要る。
    そのうち「赤にしたいもの」だけは **`i[style*="f0a99c"]` のように属性で選ぶ**
    （インラインの色コードを手がかりにする。CSSだけで出し分けられる）。
- **★黒地になるので、バーの上に重なる文字は白＋濃い影に変えること**（`.pline .pv`／`.houbar b`）。
  元は「濃い文字＋白いフチ」で、黒い筒の上では読めなくなる。
  ★超過のときの `.pv.ov` は**黄色**にする。赤系にすると**赤いバーの上で沈んで読めない**（実際になった）。
  ★つや（`::after`）は文字より後ろに来るので、文字側に `z-index:2` が要る。
- **★費目の積み上げバー（`.cbar`）と月別グラフ（`.bars2`）は触っていない。**
  あちらは「色で費目を見分ける」ためのもので、黒＋緑にすると意味が消える。
- **★2026-08-08f 追記：角の丸みは廃止**（`border-radius:0`）。「丸みを帯びているから普通の
  長方形にして。質感・色はそのまま」との指示。地・光沢・つや・文字色は版aのまま。
  直したのは3か所（筒・満ちている部分・つやの `border-radius`）だけ。
- **★2026-08-08g 追記：つやを「パワプロの評価バー」と同じタッチに**（実物の写真をもらった）。
  ★ゲームのバーが締まって見えるのは**にじみが無い**から。要点は3つ。
  ①つやを**上下2段のはっきりした切り替え**にする（上半分＝明るい／46〜47%でスパッと段差／
    下端に少しだけ明るさを戻す＝反射のもどり）。ぼかしたグラデを重ねると眠くなる。
  ②まわりに**くっきりした濃い輪郭**（`border:1.5px solid #05070a`）。
  ③**外側の光（グロー）はやめる**（`0 0 5px …` を削除）。ゲームのバーは光がにじまない。
  地・満ちている部分（緑／赤）とも同じ作り方で、上下の段差の位置（46/47%）をそろえてある。
  ★色の値が変わったので、検証スクリプトの色コードも直すこと（緑 `#22a63a`／赤 `#e02a1a`）。
- 検証は `scratchpad/gloss.js`（筒の地・角・内側の影・つや・緑／赤・文字色）と
  `gloss2.js`（施工中カード・予実・工事の進み／予算の減り）、見た目は `glossshot.js`／`gs3.js`。

### 56. 図面・積算：どこからでも真下・真横に線が引ける（2026-08-08b）★致命バグ
「0.2m 引いた後の頂点から、本当は真っ直ぐ真下に下げたいのに引けない。
基本的に十字の線はどんな位置でも引けるようにして」との指摘。

- **★原因は「打点は必ず方眼の交差点」（§29 の `snapG`＝1マス単位に丸める）だけだったこと。**
  直前の点が交差点の**上に無い**とき ―― 札のタップで長さを数値入力した頂点や、
  0.2m のような半端な位置の頂点 ―― そこから真下へ狙っても、打点は必ず交差点に落ちるので
  **x が必ずズレる＝100%斜めになる**。実機では 90度のつもりが 113度になっていた。
  §29 で直角モードを全廃したこと自体は正しいが、**垂直・水平が「引けない」状態**になっていた。
- **直し方＝`nnSnapPt(rawx,rawy)`（`snapG` のすぐ下）**。
  打点の「よこ位置」「たて位置」を**別々に**決め、それぞれ
  **①方眼の交差点／②直前の点と同じ位置** の**近いほう**を採る。
  ・直前の点のほぼ真下 → x がそろって**完全な垂直線**
  ・ほぼ真横 → y がそろって**完全な水平線**
  ・それ以外 → 今までどおり交差点（斜めの線は自由なまま）
  ★**片方の座標だけ**をそろえるのがポイント。両方そろえると直前の点に重なるので、
    そのときは何もしない。同点は直前の点を採る（まっすぐを優先）。
  ★**固定の半径（○px以内）にしないこと。** 縮小表示だと1マス以上を飲み込み、
    交差点そのものが打てなくなる。「近いほうを採る」なら**交差点は必ず残る**
    （吸い込む範囲は自動的に、交差点との中間まで）。
- 差し込みは2か所だけ：`updMouse`（パソコンのマウス）と `aimUpdate`（スマホの赤い照準）。
  どちらも `snapG(...)` を `nnSnapPt(...)` に替えるだけ。`tool` が draw/hole のときだけ効く
  （移動・範囲選択・段差ラインには影響しない）。
- 検証は `scratchpad/cross.js`（真下・真横・真上・斜め・クリック実打）と
  `cross2.js`（**スマホの照準でも**垂直/水平/斜め）。

### 57. 図面・積算：線は「角度5度きざみ・長さ10cmきざみ」に（2026-08-08c）★作図の完成形
版bで垂直・水平は引けるようになったが、**その間の角度が引けない**という指摘。
「90度、そして次はマス目の交点となる135度です。これでは
『線が思ったところに引けないWEBアプリだな』と思われる。角度は各5度で引けるように。
あとから変更するのは手間なので、常に反応するように」。**もっともな指摘。**

- **★根本原因は §29 の「打点は必ず方眼の交差点」そのもの。**
  交差点にしか打てないと、直前の点から引ける向きは**交差点の並びで決まってしまう**。
  1マス先なら 45度きざみ、2マス先でも 26.6度・45度…と**飛び飛び**で、
  100度や110度は**物理的に存在しなかった**。版bの「真下・真横にそろえる」は
  その飛び飛びの中に0度と90度を差し込んだだけで、間は埋まっていなかった。
- **いまの決まり（`nnSnapPt` を全面書き換え）**
  ・**1点目**＝今までどおり方眼の交差点（図面が方眼に対してまっすぐ始まる）
  ・**2点目以降**＝直前の点から見て
      **角度＝5度きざみ**（`NN_ANG_STEP=5`。0度＝右・90度＝下・72通り）
      **長さ＝10cmきざみ**（`nnLenStepM()`。1マスが0.25m以下なら5cmきざみ）
  ・0/90/180/270度も5の倍数なので、**真横・真下は今までどおり必ず引ける**。
  ・既存の角への吸着（`vertexMagnet`）と「ここで閉じる」（26px）は今までどおり優先。
  ・**モードではなく常時この動き**（切り替えボタンは作っていない）。
  ・画面左下の案内に「線は 5度きざみ・長さ 10cmきざみ」と出るようにした。
- ★**寸法も角度もキリのいい数字になる**のが利点。長さは必ず 0.1m の倍数、
  角の角度は必ず 5度の倍数（5度きざみの辺どうしの差だから）。
- ★**検証スクリプトの注意（3本直した）**：打点が「直前の点からの相対」になったので、
  **画面の適当な位置を続けてクリックすると2点目以降が数度ズレる**
  （1点目が交差点へ寄ったぶん、狙いの基準がズレるため）。
  実機ではユーザーが「打たれた点」を見て狙うのでズレないが、自動テストは見ないので
  **方眼の交差点を画面座標に直してクリックする**こと（`gclick(gx,gy)`）。
  `zfix2.js`／`f3chk.js`／`cross.js` をこの方式に直した。
- 検証は `scratchpad/ang5.js`（720通りの狙いで**角度が5の倍数・長さが0.1mの倍数**・
  72通りの向きが全部出せる・**95〜130度も引ける**・真下/真上/真横・1点目は交差点・
  交差点クリックの長方形は頂点ぴったり・25度も引ける）。見た目は `ang5shot.js`。
  回帰は `cross.js`／`cross2.js`／`zfix2.js`／`f3chk.js`／`zdir.js`／`znum.js`／
  `photochk.js` とも全部○。

### 58. 図面・積算：4件（3Dの継目・操作パッド・割付の寸法・頂点移動）（2026-08-08d）
指摘は4つ。①3Dで短手（エンドラップ）の継目がはみ出る（前回直したはずなのに）
②3Dの右のボタン列が右下の▲と重なる ③割付の1枚ごとの寸法は不要（出し入れボタンを付けて）
④頂点の位置を直すとき1マス単位では粗すぎる。

- **★①の見落とし（§54 の続き）**：§54 で直したのは「**屋根の外**へのはみ出し」。
  今回のは別物で、**次のシートの下に隠れるはずの部分まで線を描いていた**。
  シートは幅ぜんぶ（`b.lo`〜`b.hi`）あるが、次のシートは `b.lo+eff`（＝`b.hi−サイドラップ`）
  から上に載るので、その先の切断線は本来見えない。
  2Dは後の帯の塗りで隠れるが、**3Dは全部同じ高さ（lv+0.02）に置いていたので全部見えていた**。
  → `visHi = 次の帯があれば min(b.hi, 次.lo)` で頭を止める。
  ★はみ出し量はちょうど**サイドラップ（100mm）**。写真で「帯の幅の1割ほど飛び出す」ように見えたのはこれ。
- **②`#d3pad` は画面が低いとき2列に**（`@media (max-height:560px)`＝よこ画面）。
  1列8個＝355pxはよこ画面（393pt）に入らず、下2〜3個が▲の下に潜っていた。
  2列なら4段＝175pxで確実に収まる。`bottom` に `env(safe-area-inset-bottom)` も足した。
  ★ボタンが増えるたびに再発するので、**増やしたら必ずよこ画面で測ること**。
- **③割付のシート札は既定で番号だけ**（`nnWfDimOn()`／`nn_zumen_wfdim`）。
  「📏 寸法なし／📏 寸法あり」ボタンで切替。**スマホでは「⋯ 道具」の中**に入れてある
  （ツールバーに足すと たて画面で右端457pt＞393pt になり はみ出した）。
  割付タブのときだけ出す（`setTab` で display を切替）。凡例の文言も状態に合わせて変わる。
- **④頂点の移動は 0.1m 単位**（`nnSnapMove`。1マス0.25m以下なら0.05m単位）。
  以前は `snapG`＝1マス単位で、1マス＝1mだと1mずつしか動かせなかった。
  差し込みは `rdrag` の2か所（マウス・タッチ）。**打点（`nnSnapPt`）とは別物**なので混同しないこと。
- 検証は `scratchpad/f4chk.js`（継目のはみ出し0・寸法トグルと保存・0.1m単位の丸めと実ドラッグ）と
  `f4lay.js`（たて／よこで▲とのかぶり0・ツールバー1段・はみ出し0）、見た目は `f4shot.js`。
  ★継目が屋根の外に出ていないかは、**点の内外判定ではなく「その位置で屋根が占める区間」と比べる**こと
  （端点が屋根の縁ぴったりに来るので、内外判定だと誤って「外」と出る。実際に4件の誤判定が出た）。

### 59. パソコンのときだけナビを画面の左へ（2026-08-08e・全11ページ）
「PC版に限り、メインアイコンを左側に配置して。上からホーム、最後に仕様・材料になるように」との指示。

- **`<style id="nn-leftnav">`**（全11ページの末尾）。追加したのは**CSSだけ**。
  `html:not([data-nnphone="1"])` で**パソコンのときだけ**効かせる
  （`data-nnphone` は端末の短辺が700px未満のときに付く＝スマホ。§6 の仕組みを使い回した）。
  ・`nav` を `position:fixed; left:0; top:0; bottom:0; width:86px; flex-direction:column`
  ・`body{padding-left:86px}` で本文をナビのぶん右へ寄せる（`*{box-sizing:border-box}` なので幅は崩れない）
  ・並び順は `NAV` の配列そのまま（ホーム→…→仕様・材料）。**JSは触っていない**。
- ★**自動で隠れる動き（オートハイド）はCSSで打ち消している**。
  あの処理は `nav.style.marginBottom` と `transform:translateY(120%)` を**インラインで**入れるので、
  スタイルシート側に `margin:0 !important; transform:none !important;` を書けば勝てる
  （CSSの `!important` はインラインより強い）。**JSを止める必要はない。**
  右下の ▲（`#navShowTab`）もパソコンでは `display:none !important`。
- ★**画面いっぱいの固定部品は左へずらす**：`#nnver`（版表示）・`#nnbar`（上端の進行バー）・
  `#setup`（現場マップのAPIキー画面）。
- ★**「ナビの高さ」を引いている計算を2か所直した**（これを忘れると画面が壊れる）
  ・現場マップ `fitApp()`：`nav.offsetHeight` を引いて地図の高さを決めていたが、
    左のたて列は**高さが画面いっぱい**なので、地図が最小の160pxまで潰れた。
  ・図面・積算 `navH`（断面詳細図の下余白）も同じ。
  → どちらも **`nav.offsetWidth < innerWidth*0.5` なら「左のたて列」とみなして 0** にする。
  ★`--nnvh` の `fill()` は元から「position:fixed のナビは数えない」ので直す必要なし。
- 検証は `scratchpad/leftnav.js`（全11ページ×6項目：左に縦並び・並び順・本文が潜らない・
  横はみ出し0・▲非表示・JSエラー／スマホ3ページは今までどおり下の帯）と
  `lnmap.js`（現場マップの地図の高さ＝850/900）、見た目は `leftshot.js`。

### 60. 現場記録帳：工法別 受注・利益の「並び順」を分かるようにした（2026-08-08h）
「面積優先にしたとき、面積で並べているのが分かりづらい。あと比率バーは選んだ項目の比率にしてほしい」との指摘。

- **★比率バーは元から「選んだ列の比率」になっていた**（`hmKey`＝`dashSort.hou.col`。§の rev.13 で実装済み）。
  問題は**そう見えないこと**だったので、伝え方だけを直した。
- **見出しの下に帯**（`.housort`）：`並び順 [面積 の大きい順]　バーの比率 [面積]　↺ 並びを戻す`。
  黄色＝並び順／緑＝バーの対象、と色を分けている。
- **並び替え中の列を黄色く**（`th.hsorted`／`td.hsorted`）。見出しだけでなく**中身のセルも**塗ると、
  横に長い表でも「どの列で並んでいるか」が一目で分かる。
  ★class の足し方：td はすでに `class="num"` を持っているものがあるので、
  `<td class="` があれば中に差し込み、無ければ新しく付ける（`hAddCls`）。単純な置換だと class が2つになって効かない。
- **比率の列見出しを2段に**（上＝小さく「比率」／下＝大きく対象名）。対象名が主役になるように。
- **平均利益率だけは「比率」ではなく「大きさ」**と出す（利益率を足して100%にしても意味がないため。
  値そのものを表示する既存の動きは正しい）。
- `houSortReset()`＝既定（平均利益率の高い順・バーは受注額）に戻す。
- ★**残っている問題（本人の判断待ち）**：ウレタン塗膜系は `㎡` 19件と `m` 9件（シーリング等）が混在していて、
  面積の列は**単位の違うものを足している**。面積で並べ替えたときの比率が正しくない。
  直すなら「㎡だけで面積を集計し、m のものは別に併記」が妥当。
- 検証は `scratchpad/housort.js`（帯の文言・列の色・比率の合計が100%・実績数/平均利益率の切替・戻す）と
  `hsshot.js`（見た目。スマホたてで横はみ出し0）。

### 61. パソコンの「表示の大きさ」をモニターに関わらずそろえた（2026-08-11a・全11ページ）
「大きいモニターでも普通のモニターでも同じ見え方にしたい（いずれも拡大100%）。
あと全体的に文字が小さすぎる」との指摘。あわせて工法別の説明文の削除と、パネル見出しの拡大。

- **★なぜ大きいモニターほど小さく見えるのか**：拡大100%でも、横に広いモニターほど
  1画面に入る px が多い。ページの中身は px で作ってあるので、**画面に占める割合が小さくなる**＝
  相対的に小さく見える。文字サイズを上げるだけでは、モニターごとの差は消えない。
- **`<style id="nn-pczoom">`＋`<script id="nn-pczoom-js">`**（全11ページの末尾）。
  **倍率 ＝ 画面の横幅 ÷ 1600px（1.00〜1.60）** を `html` に `zoom` でかける。
  1920 → 1.20／2560 → 1.60。1600以下は1.00（これ以上小さくはしない）。
  ★**スマホは対象外**（`data-nnphone="1"` なら何もしない）。
  ★**2026-08-11b：手動の倍率ボタンは削除**（「パソコンはブラウザの拡大縮小が使えるから要らない」との指示）。
    いまは自動の倍率だけ。`localStorage('nn_pczoom')` も使っていない。
- **★`zoom` を使うときの注意（実際にハマった）**
  1. `getBoundingClientRect()` は**倍率をかけた後のpx**、`offsetWidth/Height` は**かける前のpx**を返す。
     混ぜて計算すると必ず狂う。
  2. `innerHeight` は倍率をかけない実画面のpx。`body{height:var(--nnvh)}` は CSS の px なので、
     **`--nnvh` は倍率で割る**こと（`nn-fillh-js` の `fill()` を全11ページで直した）。
     割らないと body が倍率ぶん高くなり、下に余分なスクロールが出る（実測 1080→1404px）。
  3. 倍率をかけているときは**下の余白うめ（minHeight）をしない**。あれは iPhone 用の処理で、
     `getBoundingClientRect` と混ざって二重にズレる。
  4. `transform:scale` ではなく **`zoom`** を使うこと。scale は `position:fixed` の基準を壊す（§の警告）。
- **`.httl`（パネル見出し）を「全物件 77」チップと同じ見た目に**（`<style id="nn-pchead">`・現場記録帳）。
  ★最初はKPIの帯（14px）に合わせたが、「**いちばん上の『全物件』と同じ色・同じデザインにして**」との指示で
  `.stchip.on` に合わせ直した：明るい緑のグラデ（`#48c274→#2e9e58`）＋濃い緑の枠（`#124a28`）＋角16px。
  ★**先頭の ●／■ の印は出さない**（「全物件」に無いため）。§の「●＝集計系／■＝表・グラフ系」の
  区別はここで廃止。戻したくなったら `.httl::before` の `display:none` を外すだけ。
  大きさもチップと同じ 13.5px（パソコン）。スマホは今までどおり。
- **★2026-08-11c 見出しの手直し3点**（`.httl` と `.stchip.on` の両方に同じ指定）
  ①**文字を白 → 濃い緑（#0e3c20）**。明るい緑の地には濃い文字のほうが読みやすい。
    ★コントラスト比で確かめると **白 2.4 → 濃い緑 5.2**（4.5以上が読みやすさの目安）。
    「白抜きのほうが締まる」は暗い地のときの話で、明るい地では逆になる。
  ②**上下の余白を同じに**。`display:inline-flex` ＋ `align-items:center` ＋ `line-height:1` ＋
    上下同じ padding。★`inline-block` のままだと、日本語の字は行の下寄りに置かれて
    「下の余白が狭い」と見える（実測でも文字が下に寄っていた）。
    ※`h4{display:flex}` の中にあるので、computedStyle は `inline-flex` ではなく `flex` を返す。
      自動確認でここを `inline-flex` で判定すると誤って★NGになる。
  ③**光沢**：上半分を明るく・段差・上端に白い線・下端に影（§55のバーと同じ考え方）。
- **★2026-08-11d の手直し**：「真ん中に境界線があるように見える／文字が見えにくい」との指摘。
  ★**段差は 46/47% → 63/65% に下げた**。バー（§55）は上下2分割で締まって見えるが、
    **丸いピルは真ん中に段差があると「線が引いてある」ように見える**。
    明るい部分を広げて影を下1/3に寄せると、線に見えず光沢だけが残る。
  ★文字は **太さ 800→900・色 #0e3c20→#082e18**、白い影は .38→.28 に薄くした
    （太字にすると白フチが太って輪郭がぼけるため）。

### 62. パネル見出しを「パワプロ 栄冠ナイン」のメニューボタン風に（2026-08-11e）★作り直し
「全然わかりにくい。ガラッと変える。パワプロのメニュー（フレンド・プロフィール）みたいな
平行四辺形の格好いいやつを、書体も形もそのままパクるくらいの感じで。色は緑。光沢も同じ。
のちのちイラストを平行四辺形の左側に入れるからスペースを空けて」との指示。

- **`.httl`（`<style id="nn-pchead">`）を全面的に作り直した**。
  ・**形は `::before` の斜めの箱**（`transform:skewX(-10deg)`）で作り、**文字は本体に置く**。
    こうすると背景だけ平行四辺形になり、**文字はまっすぐ**のまま読みやすい。
    ★`.httl{position:relative; z-index:0}` ＋ `::before{z-index:-1}` にすること。
      これで文字より後ろ・パネルの背景より前に描かれる（z-index:0 が無いと背景の裏に回る）。
  ・**太い白フチ**は `box-shadow` の多段で作る（`0 0 0 3.5px #fff, 0 0 0 4.8px rgba(...)`）。
    transform を掛けた要素の影なので、白フチも平行四辺形になる。
  ・**光沢は上下2段のはっきりした切り替え**（45%→47%でスパッと。§55のバーと同じ考え方）。
    ★丸いピルでは「線に見える」ので下げたが（§61）、**ゲームUIの角ばった形では真ん中の段差が正解**。
  ・**書体は丸ゴシック**：`Hiragino Maru Gothic ProN`（iPhone/Mac）→ `HGMaruGothicMPRO`（Windows＋Office）
    → 通常のゴシック、の順。**Webフォントは読み込まない**（§の速度の決着を守る）。
  ・**左に 48px（スマホ34px）のあき**。届いたイラストは `.httl > img.hpic` に入れれば
    平行四辺形の左端に重なって出る（位置・大きさの指定は済み）。
- **「全物件」チップ（`.stchip.on`）は丸いピルのまま**にした（別物なので形は分けた）。

**★2026-08-11f の作り直し**（「1枚目と全然違う。1枚目はもっと角ばっているのに丸みを帯びている。
1枚目と全く同じにして。影の入れ方も1枚目と同じに。文字フォントも変」との指摘）。
版eは**丸み5px＋外側にふわっと広がる白いハロー**だったので、輪郭がぼやけて別物に見えていた。
直したのは3点。

1. **角を立てる**：`::before` の `border-radius: 5px → 2px`。ゲームUIのボタンは角が立っている。
2. **★影の入れ方をゲームUI式に**。にじむ影を全部やめ、**輪郭がはっきりした3層の重ね方**にする。
   ・**白い縁**＝`box-shadow` の太いハローではなく **`border:2px solid #fff`**（スマホは1.5px）
   ・**その内側に濃い緑の細い線**＝`inset 0 0 0 1.5px #128f4a`（縁と地の境目を締める）
   ・**右下に「ずらしただけ」の落ち影**＝`3px 4px 0 rgba(20,60,38,.22)`（ぼかし0＝板が浮いて見える）
     ＋ごく弱い接地影 `0 2px 4px rgba(0,0,0,.20)`
   ★**ぼかした影を重ねると必ず眠くなる**（§55のバーで得た教訓と同じ）。
     ゲームのボタンが締まって見えるのは「ぼかしていない」から。
3. **書体は `font-family:inherit`（ページの標準）に戻した**。版eは丸ゴシックを指定していたが、
   **端末に丸ゴが無いと別の字体に化けて「フォントが変」になる**（Windowsは HGMaruGothicMPRO が
   Office同梱で、入っていない環境が多い）。Webフォントは読まない決まりなので、
   **確実に同じに見える「他の文字とそろえる」が正解**。太さ900・字間 .04em で見出しらしさを出す。

- 光沢の段差は 46%→48%（版eの 45%→47% から微調整）。上下2段のスパッとした切り替えは変えていない。

**★2026-08-11g（決着）：見本の画像を1画素ずつ測ってから作り直した**
版e・fは「見本を見て似せる」やり方だったので毎回外していた。**測ったら4つとも違っていた。**
指摘は「配色と影色の位置が違う／文字が細すぎる／クリックしたところの右下がクリックされている」。

**★やり方：見本の画像を実測すること。目で似せない。**
添付画像は会話の記録（`~/.claude/projects/<...>/<セッションID>.jsonl`）に base64 で入っている（§8）。
取り出して PIL＋numpy で、**縦1列・横1行の色の変わり目**を並べれば、境目の位置と色が全部出る。
形は「背景でない画素の左端・右端」を行ごとに出せば、かたむきが割り出せる。

| | 見本の実測 | 版f（外していた） |
|---|---|---|
| かたむき | 上端L=35→下端L=19（46行で16px）＝**約19度** | 10度（傾きが足りず「角ばっていない」と見えた） |
| まわりの線 | 地色より少し**濃い緑**（同じ色相）2px | **白**2px＋内側に濃い緑の線（見本に無い） |
| 地の色 | 上から**38%までがほぼ白** → スパッと段 → 下は**平らな中間色** | 下半分が濃い緑＝**明暗が見本とほぼ逆** |
| 影 | 右下に**やわらかい灰色**2〜3px（ぼかしあり） | ぼかし0で 3px 4px ＝**同じ板がもう1枚ずれて見えた** |

- ★**「クリックしたところの右下がクリックされている」の正体は影**。ぼかし0の濃い影は
  「押されて沈んだ板」「もう1枚の板」に見える。**ゲームUIでもぼかしはゼロではない**
  （§62の版fで「ぼかすと眠くなる」と書いたのは行き過ぎだった。正しくは**2〜3pxの弱いぼかし**）。
  ★念のため `::before` に **`pointer-events:none`** も付けた（飾りを当たり判定に入れない）。
  なお**当たり判定そのものはズレていない**（zoom 1.2 で実クリックと `elementFromPoint` が一致するのを確認済み）。
- ★**色は「色相だけ」水色→緑に回すと決めやすい**。明るさと鮮やかさは見本の数値をそのまま使う
  （`colorsys.rgb_to_hls` で分解 → 色相を145度にして戻す）。そのままだと薄い若草色になるので、
  読みやすさのため少しだけ濃くした（候補A/B/Cを並べて撮り、**B**を採用）。
- ★**文字が細い → 書体は変えずに `-webkit-text-stroke:.5px` で太らせる**（スマホは .35px）。
  Webフォントは読まない決まりなので、太いフォントに替える手は使えない。
  **0.9px は字がつぶれる**ので上げすぎないこと（並べて撮って確かめた）。
  あわせて `text-shadow` は消した（白い影は輪郭をぼかして細く見せる）。
- ★**かたむきを19度にすると左右に約7pxずつはみ出す**（高さ43px × tan19° ÷ 2）。
  10度のときの倍なので、`margin` を左右 9/10px → **14/15px** に広げないと隣のボタンに当たる。
- 検証は `scratchpad/pwchk.js`（かたむき19度・ふちが濃い緑・影は1枚でぼかしあり・
  角が2px以下・上38%までが白・当たり判定に入れない・text-stroke・左のあき・
  書体がbodyと一致・9個ともパネル内・スマホでも収まる）。
  ★`hdchk.js` は役目を終えたので削除（pwchk.js が後継）。
  見た目は `pwshot3.js`（6倍で1個だけ撮る）／候補くらべは `cand.html`＋`candshot.js`。

**★2026-08-11h：文字を黒に・見出しにイラストを差し込み**
- 文字は **黒（#000）**（`-webkit-text-stroke` の色も黒）。
- **イラストは `icons/hpic_<キー>.png`**（背景透過PNG・透明な余白を切り落とす・**高さ168px**・
  幅は絵なり）。差し込みは見出しの中に
  `<img class="hpic" src="./icons/hpic_◯.png" alt="" onerror="this.remove()">` を置くだけ。
  読めないときは `onerror` で消えるので、文字だけになって壊れない。
  入っているのは3つ：`hpic_yojitsu`（完成工事高の予実・棒グラフ）／`hpic_taio`（要対応・注意マーク）／
  `hpic_juchu`（受注率・チェックマーク）。**残り6つの見出しは、あきだけ残して空**。
- ★**絵の形はまちまち**（棒グラフは横長・他は正方形）。高さだけ指定すると横長の絵が文字に重なるので、
  **`max-width:48px`（スマホ33px）＋ `object-fit:contain`** で幅にも頭を打たせる。
- ★**`white-space:nowrap` と `flex:0 0 auto` が要る**。入れないと、横に説明文（`.cta`）が
  並ぶ見出しで箱が縮められ、**「受注率」が「受注／率」の2行**になった（実際になった）。
- ★**sw.js の ASSETS にも足すこと**（オフライン用の保存一覧）。
- ★**検証スクリプトで大きさを測るときは倍率で割ること**（§61の落とし穴）。
  `getBoundingClientRect()` は倍率**後**のpx、`getComputedStyle` は倍率**前**のpx。
  混ぜたまま比べて「折り返している」「絵が文字に重なる」と誤検知した（`Z=window.nnPZ` で割って解決）。
- ★**AI で作った絵には「AI-Generated」の札が焼き込まれていることがある**。
  その範囲の透明度を0にしてから余白を切り落とす（`a[y0:y1,x0:x1,3]=0`）。

**★2026-08-11i：文字の大きさを「見積済」の帯にそろえた／読みやすく／イラストを大きく**
- ★★**この日いちばん大事な発見：頂いたスクリーンショットは「iPhoneのよこ向き」だった。**
  パソコンの画面だと思い込んで PC 側の数値をいじりかけた。**見分け方（必ずやること）**
  ・**画像の縦横比**を端末の寸法と突き合わせる（1999×922 は 852×393 と 932×430 の
    どちらでも縦横の倍率がぴったり一致＝iPhoneよこ）。
  ・**左右に約59ptの余白**（ノッチ）があるか。中身の左端が 59pt ぶん内側なら、たぶんiPhoneよこ。
  ・**左のたてナビが無い**（パソコンなら幅86pxの帯が必ず左端にある。§59）。
  ★スマホとパソコンで数値が別々（`html[data-nnphone="1"]`）なので、**どちらの画面の話かを
    先に確かめないと、直す場所を間違える**。
- **文字の大きさは「見積済」の帯（`.kgh`＝14px）に合わせた**。
  実測（iPhoneよこ）で 見積済27px に対し 見出し25px と小さかったので、スマホの `.httl` を
  **12.5px → 14px**（実測で両方とも同じ高さになるのを確認）。**パソコンは17pxのまま**
  （そちらは元から `.kgh` より大きい。指摘はスマホの画面の話だった）。
- **黒文字が見えにくい → 3つ直した**（コントラスト比は元から14:1あり、暗さの問題ではなかった）
  ①**縁取りを 0.5→0.35px（スマホ 0.3px）に細く**。太らせすぎると漢字の中の空きがつぶれて
    かえって読みにくい（「実」「高」で顕著）。**大きさで見やすくするほうが効く**。
  ②地の**下半分を明るく**（`#84d2a2→#96e3b6` など）。文字は地の段差をまたぐので、
    下側が濃いと字の下半分が沈む。
  ③**白い下影**（`0 1px 0 rgba(255,255,255,.60)`）で字を地から浮かせる。
- **イラストを大きく**：あき 52→70px（スマホ 36→58px）、`max-width` 48→66px（スマホ 33→54px）。
  ★**横に長い絵は「幅」で頭打ちになる**ので、大きくしたいときは高さではなく
    `max-width` とあき（padding-left）を上げること。実測 33×23 → 54×42px（スマホ）。
- 検証は `scratchpad/land.js`（**iPhoneよこ向きの再現**＝`env(safe-area-inset-left/right)` を
  59pxに置換したコピーを 852×393・deviceScaleFactor 3 で開き、見積済と見出しの文字の高さを
  画素で測って一致を確かめる。たて向きも同時に見る）。`pwchk.js` にも同じ判定を追加した。
- 工法別の帯から説明文（`.hsnote`）を削除（§60）。
- 検証は `scratchpad/pczoom.js`（1366/1600/1920/2560 の4サイズ×はみ出し・余分なスクロール・
  1600px以上で見え方が±5%以内にそろう・＋−自動・保存・他5ページ・スマホは倍率なし）と
  `pzshot.js`（1920と2560の見た目が同じ配置になるか）。

### 63. 3Dビューの絵を作り直した（2026-08-11j）★three.js を同梱・PBR・影
「BIMで施工図をつくるチーム」の3D動画（Instagram）を見て「せめてこのレベルにしたい。
Blenderを使えば可能か？」との相談。**Blenderはパソコンのソフトでブラウザの中では動かない**ので、
まず**表示側（three.js）を今風にする**ところから着手した（相談の①）。

- **★three.js を CDN → リポジトリに同梱**（`vendor/three.min.js`＝**r159**・668KB・MIT）。
  ・r159 は **`build/three.min.js`（グローバル `THREE`）が入っている最後の版**。r160で削除された。
    それ以降を使うにはページ全体をESM（import）に書き換える必要があるので、ここで止めるのが正解。
  ・入手は npm（`https://registry.npmjs.org/three/-/three-0.159.0.tgz`）。**この環境から cdnjs と
    unpkg には出られないが、npm レジストリには出られる**（curl で確認済み）。
  ・★**先頭の「非推奨」警告を消すときの罠**：このファイルの先頭は
    `console.warn(...) , 以降ぜんぶ` という**1つのカンマ式**。console.warn ごと消すと
    `Function statements require a function name` で丸ごと動かなくなる（実際になった）。
    **`void 0` に置き換えてカンマは残す**こと。
  ・**3Dタブを最初に開いたときだけ読む**（`nnLoadThree`）。ページを開く速さは今までどおり。
  ・★**これで電波が無くても3Dが動く**ようになった（今までは圏外だと3Dが一切出なかった）。
    ★ただし**現場マップの書き出し用3Dビューア（`exportFace('html')`）はまだCDN読み**。別物なので未対応。
- **材質を PBR に**（`MeshLambertMaterial` → `MeshStandardMaterial`）。`nnMat(種類,色,両面)` に集約。
  躯体 rough .95／外壁 .90／シート防水 .78／塩ビ .50／ウレタン .55／**アルミ笠木 rough .34・metal .85**。
  ★分けないと、コンクリートもアルミ笠木も同じのっぺりした色板に見える。
- **空の光（環境光）を「その場で作る」**（`nnMakeEnv`）。箱の内側に明るい面を並べた小部屋を
  `PMREMGenerator` で写して `scene.environment` にする。**HDRI画像（1〜3MB）を読まないので通信も容量も増えない。**
  ★古い端末で作れないことがあるので `null` を返して普通の明かりを足す作りにしてある（落とさない）。
- **影**：`shadowMap`＋`PCFSoft`。**影の範囲は建物の大きさから毎回決める**（決め打ちだと大きい屋根で切れる）。
  **影を受ける地面**は `ShadowMaterial`＝**板は見えず影だけ映る**。★2026-08-06t にやめた「地面の網」とは別物（線は1本も描かない）。
- **明るさ**：`ACESFilmicToneMapping`＋`toneMappingExposure=0.82`、`outputColorSpace=SRGBColorSpace`。
  ★最初 exposure 1.0＋環境光を強くしたら**真っ白に飛んだ**。環境の「部屋の地」を暗く（`#b6bfbc`）して
    上下の差を作ると陰影が出る。
- **重さ対策（これが無いと熱くなる）**
  ・**画素密度は2倍まで**（`Math.min(2,devicePixelRatio)`）。iPhoneは3倍なので描く量が2.25倍違う。
    ★このとき**大きさの見張り（`el.width!==w*devicePixelRatio`）も同じ倍率に直すこと**。
      直さないと毎コマ作り直しになって逆に重くなる。
  ・**動いたときだけ描く**：視点の値＋組み直し回数を1本の文字列（`T.sig`）にして、変わったときだけ2コマ描く。
    実測（CPU6倍負荷・ソフト描画）**止めている間 22回/秒 → 1回/秒**。
  ・**影は毎コマ計算しない**（`sun.shadow.autoUpdate=false`／組み直しとテーマ切替のときだけ `needsUpdate=true`）。
- ★**測り方の注意**：この環境のブラウザは **GPUなし（SwiftShader＝ソフト描画）**。
  回している間のコマ数は 20→8 と出るが、**実機のGPUではPBRと影1枚は軽い**ので当てにならない。
  **なめらかさは実機で見てもらうこと。**「止めている間の描画回数」だけは環境に関係なく効く。
- 検証は `scratchpad/d3chk.js`（18項目：開いた時点でthreeを読まない・CDNを見に行かない・3Dタブで同梱版を読む・
  r159・Lambertが残っていない・ざらつきが部材ごと・金属あり・影・影の範囲・環境光・ACES・sRGB・
  画素密度2倍まで・ダークで光も変わる・絵が描かれている・JSエラーなし）、
  `d3mo.js`（iPhoneよこでの読み込み時間とコマ数）、`fps2.js`（前後のコマ数くらべ）、
  見た目は `d3shot.js`／並べた画像は `d3_compare.png`。
  ★`thchk.js` の「☀ 昼」は §53 で「☀ 通常モード」に変わっていた（古い期待値だったので直した）。

### 64. パネルの枠を「栄冠クロス」風に／3DがPCで粗い件／イラスト3枚追加（2026-08-11k）
指摘は3つ。①ダッシュボードのパネルを覆う白い枠を、パワプロのメニューの枠にして。四隅にバインダーの
フチ。平行四辺形の帯のすぐ下に境界線 ②3DがPCだと画質が荒い（スマホでは気にならない）
③施工中の現場・ステータス分布・入金予定にもイラスト。

- **①パネルの枠（`<style id="nn-panelframe">`）**。対象は **`#dashboard` の中だけ**
  （`.dpanel` は物件詳細でも使われているので、そちらは今までどおり）。
  ・外に太い白のフチ（3px）／内側に細い青灰色の線（inset影）／角14px
  ・**四隅の角あて（バインダーの金具）は `::before`＋`::after` の2枚重ね**。
    ★白い枠の上に白いLを置いても見えない。下に「少し大きい輪郭のL（#9fb5a7）」を敷き、
      その上に「明るい面のL」を載せると金具に見える。
    ★Lの字は `linear-gradient` を8個（4隅×縦横）並べて背景の絵として描く。要素は増やさない。
  ・**帯のすぐ下の境界線**は `h4{border-bottom:2px solid #bcd3c4}`＋下に白い線（`box-shadow`）。
- **★②の原因は「パソコンの表示倍率（zoom）」**（§61）。3Dの絵はCSSのpxで作るので、
  倍率のぶんだけ引き伸ばされて粗くなる。**実測：1920のモニター（倍率1.20）で必要な画素の83%、
  2560（倍率1.60）で63%しか描いていなかった**。スマホは倍率1なので気づきにくい。
  → **`nnPixRatio()`＝`min(2.5, min(2,devicePixelRatio) × nnPZ)`**。
    倍率が変わったとき（窓の大きさを変えたとき）も毎コマ見張って合わせ直す。
    直後の実測で 1366／1920／2560 とも **1画面画素あたり1.00描画画素**になった。
  ★**`zoom` を使っているページで canvas を扱うときは必ずこれを踏む**。canvas に限らず、
    「CSSのpxで作って画面に出す」ものは全部同じ問題を持つ。
- **③イラスト**：`hpic_sekou`（施工中の現場・黄色い標識）／`hpic_status`（ステータス分布・円グラフ）／
  `hpic_nyukin`（入金予定・お金とカレンダー）。**これで9枚中6枚が絵入り**。
  残りは 月別の推移／自社の実績歩掛／工法別 受注・利益 の3つ。
- 検証は `scratchpad/d3sharp.js`（1366/1920/2560 で「描いている画素 ÷ 画面に出ている大きさ」を出す）と
  `pfshot.js`／`pfall.js`（枠と角あての見た目・PCとスマホ・横はみ出し0・JSエラー）。

### 65. パネルの枠を作り直し（2回目）／つかんだパネルがズレる／継目を黒く（2026-08-11m）
指摘は3つ。①四隅の角がおかしい（見本と違う）②またクリックしたところと違うところが光る
③3Dのルーフィングの継目をもっと黒く。
あわせて「見本と全く同じ枠はCSSでは限界か／自分でイラストした方がよいか」という質問。

- **★①版kの失敗の理由**：角あてを**別の板として枠の上に重ねた**ので、浮いた四角が
  貼り付いて見えた。見本（パワプロ）を実測すると、枠は**1本つながっていて角だけ太い**。
  → **作り方を逆にした**：①まず枠を「角の太さ」でぐるりと引く（`border:10px`）
  ②**辺の真ん中だけ、内側を中の色で塗り戻して細く見せる**（`::before` に8個のグラデーション）。
  こうすると段差が枠の一部になり、切れ目のない連続した枠になる。
  ★**枠の絵を1枚もらえば、9分割（`border-image`）で完全に一致させられる**。
    `icons/frame_panel.png` を置くだけで自動で切り替わる仕掛けを入れてある（`nn-panelframe-js`）。
    絵の作り方：正方形PNG（例104×104・背景透過）、四隅26pxが「角」、その間が「辺」（伸ばして使う）、
    真ん中は中身の地。**この方式なら角の形はパネルの大きさに関係なく崩れない。**
- **★②「クリックしたところと違うところが光る」の正体（今度は本物の不具合）**
  パネルの長押し並べ替えの `lift()` が **`getBoundingClientRect()`（倍率**後**のpx）を
  `style.left/top/width`（倍率**前**のpx）にそのまま書いていた**（§61の落とし穴）。
  実測：**2560のモニター（倍率1.6）で 右へ93px・下へ267px・幅は835px大きく**なっていた。
  → 書き込む直前に `window.nnPZ` で割る。`pointermove` も同じ。実測でズレ3px以内に。
  ★**当たり判定そのものはズレていない**（`elementFromPoint` と実クリックは全サイズで一致。
    拡大ボタン9個も全部「押したパネル」が開くことを確認済み）。**ズレていたのは「つかんだ後の位置」だけ。**
  ★`zoom` を使っているページで **`style.left/top/width` に rect の値を入れる処理は全部同じ罠**を持つ。
- **③3Dの継目**（`seamC`）を `#4a4038` → **`#14120f`（ほぼ黒）**。実際のアスファルトが黒いため。
  面の色（`sp.fill`）は工法を見分ける色なので変えない。
- 検証は `scratchpad/drag1.js`（1366/1920/2560 でつかんだ後の位置ズレ）、
  `hit3.js`（押せる部品の中心が別物を指していないか）、`hit5.js`（拡大ボタン9個）、
  `pfshot.js`／`pfall.js`（枠の見た目・角の太さ10px・辺は塗り戻し8枚・内側の線）。

### 66. パネルの枠を「描いてもらった絵」に差し替えた（2026-08-11n）★9分割
§65 で「絵を1枚もらえれば完全に一致させられる」と伝えたところ、
**パワポで左上の角1枚**（756×756・背景透過）が届いた。

- **★もらうのは「左上の角」1枚でよい。** 4隅は鏡で反転し、辺は「まっすぐな部分の断面」を
  引き伸ばして、こちらで組み立てる（`scratchpad/build_frame.py`／`icons/README_frame.md`）。
- **★実測して分かった、そのままでは使えない点2つ**
  1. **まっすぐな帯が画像のフチまで届いていなかった**（余白があった）。
     → 絵のある範囲を測って切り出す（`getbbox` 相当）。
  2. **横の腕とたての腕で太さも色も違った**（横130px／たて137px。たては下へ行くほど暗くなる
     グラデ）。鏡で反転すると**上下の辺と左右の辺で明るさが違って見える**。
     → **対角線で折り返して**（`i<=j` なら元、そうでなければ転置）完全に同じにそろえた。
  ★**腕の先端は「ふさぎ」（端が閉じた線）になっている**ので、いちばん端の断面は使えない。
    まっすぐな部分の途中（角から300px）で切ること。
- 組み立て後：**648×648px**（角300px＋中央48pxが引き伸ばされる部分）。
- CSS は `border-image:url(...) 300 / 14px / 0 stretch` ＋ `border-radius:9px`。
  ★**数値の決め方**：絵は 角300px・帯130px・外側の丸み194px。画面で帯を約6pxにしたいので
    **6 ÷ (130/300) ≒ 14px** を border-width に。背景の角は **194×(14/300) ≒ 9px** で丸める
    （`border-image` は `border-radius` に切り取られないが、**背景は切り取られる**ので、
     合わせておかないと帯の外に白い角がはみ出す）。
  ★`fill` は付けない（中央は元の背景を使う）。
- **絵が無いときは §65 のCSSの枠のまま**（`nn-panelframe-js` が読めたら
  `html.nnhasframe` が付いて切り替わる）。描き直したいときは
  `icons/frame_panel_src_corner.png` を差し替えて `build_frame.py` を流し直すだけ。

**★2026-08-11p の作り直し（ここが決着）**
版nは**全部の辺を太い帯**にしていたが、本人の希望は
**「辺は細く、四隅だけ太い（段が付く）」**だった（見本の角を拡大した画像が届いた）。
- もらった絵は**太い帯だけ**なので、**同じ色で細い帯をこちらで作る**
  （フチの太さ・フチの色・中身の色を断面から測り、細い帯＝太い帯の48%）。
- **角から238pxの位置で段を付け**、そこから先を細い帯にしてから、鏡で4隅に展開。
  辺は**細い帯の断面**を引き伸ばす。
- ★**段は「内側」に付ける**（外側の線は通しのまま）。外側に段を付けると枠が波打って見える。
- 寸法は 太い帯135px／細い帯65px／外側の丸み194px／角300px。
  画面で細い帯を約4pxにしたいので **4 ÷ (65/300) ≒ 18px**、丸みは **194×(18/300) ≒ 12px**。
- ★**もらう絵は「太い帯の角1枚」でよい**。細い帯と段はこちらで作れる。
  ただし**まっすぐな部分を少し長めに**描いてもらうこと（断面を測る場所が要る）。

**★2026-08-11q：「変わっていない」の原因＝画像のキャッシュ**
版pを公開したのに実機は版nのままだった。**HTMLは新しくなっても、
`frame_panel.png` は「同じファイル名で中身だけ差し替えた」ので
ブラウザが古い絵を使い続けていた**（版名4点セットはHTMLしか面倒を見ない）。
- 直し方：**URLに版名を付ける**。`nn-panelframe-js` が
  `--nnframe: url('./icons/frame_panel.png?v='+NN_VER)` を入れ、CSSは
  `border-image:var(--nnframe) 300 / 18px / 0 stretch`。読み込み確認も同じURLで行う。
- ★**同じ名前で画像を差し替えたら、必ずURLに版名を付けること。**
  新しく増やす画像（`hpic_*.png` など）は名前が新しいので問題ない。
  **差し替えるときだけ**この罠がある。
- ★この環境から github.io には出られない（公開先を直接見に行けない）。
  **公開できているかは GitHub API（`mcp__github__get_file_contents` で `ver.txt`）で確かめる。**
  実機で古いまま見えているときは、①Pagesの再構築待ち ②ブラウザのキャッシュ の順に疑う。
  パソコンは **Ctrl＋Shift＋R**（強制再読み込み）で確かめてもらうのが早い。

### 67. パネルの枠：絵は四隅だけ・大きく（2026-08-11r）★ここが本人の意図
**版n〜qは意図を取り違えていた。** 本人の指示は最初から
**「四隅の金具だけ絵にする。辺には使わない」**だった。
版n〜qは `border-image` で**枠ぜんぶ**を絵にしていたため、
「4隅の金具がクワガタの顎ぐらいしかない」「4隅以外は絵を使うな」と指摘された。

- **`border-image` はやめた。** 枠は**細い線1本**（`border:1.5px solid #cfdcd3`）に戻し、
  角の絵は `::before` の背景として**四隅に4枚置く**（`frame_c_tl/tr/bl/br.png`・300×300）。
  → 大きさを `--cs`（PC 46px／スマホ 34px）で自由に決められる。border-image では
    枠の太さと角の大きさが連動してしまい、角だけ大きくできなかった。
- **★`border-radius` は角の絵の外側の丸みに合わせること。**
  絵は 外側の丸み194px／角300px なので、46pxで置くなら `194×46÷300 ≒ 30px`。
  合わせないと**角の絵の外に枠線がはみ出して二重に見える**。
- **★左右と下の余白は、角の腕の太さ（角の大きさの約46%）より大きくとること。**
  足りないと腕が中身の文字に重なる（実際に「費目の増減」に重なった）。
  **上は詰めたまま**（「見出しの上が空きすぎ」との指摘。28px→10px）。
- 4隅の絵は `scratchpad/build_frame.py` が元絵から鏡で反転して作る。
  **対角線で折り返すなどの加工はしない**（もらった絵をそのまま使う）。
- ★**この件の反省**：見本の一部（角の拡大写真）を見て「枠全体の作り」を推測したのが誤り。
  **どこに使うのかを先に確かめること。** 指示にない部分（辺）まで作り込んだせいで
  作り直しが3回発生した。

**★2026-08-11s：太すぎた原因は「比率」（ここで決着）**
版rは絵をそのまま46pxで置いたので「塩ビパイプみたい」と指摘された。**測ったら答えが出た。**

| | 帯の太さ ÷ 角の大きさ |
|---|---|
| 見本（パワプロ・`ref2/r2.png` を実測） | **13%**（帯30px／角235px） |
| もらった絵 | **45%**（帯135px／角300px）＝3.5倍太い |

★**もらった絵は「拡大縮小」では直せない**。小さくすれば角が消え（版q＝クワガタの顎）、
  大きくすれば帯が太くなる（版r＝塩ビパイプ）。**比率が固定だから両立しない。**
→ **色だけ元絵から採り（フチ `#a4acad`／中身 `#c4cbcc`）、比率は見本に合わせて描き起こす**。
  `build_frame.py` が `ImageDraw.rounded_rectangle` で
  「外側の角丸」から「内側の角丸」を引いてL字の帯を作る（帯13%・フチは帯の15%・丸み19.5%）。
  画面46pxで 帯6px・丸み9px。パネルの `border-radius` もこの9pxに合わせる。
★**同じ相談が来たら、まず「帯の太さ ÷ 角の大きさ」を見本と比べること。**
  見た目の印象で大きさを上下させても永久に合わない。

**★★2026-08-11t：「変わってない」と言われたら、まず公開作業の状態を見ること（最重要）**
版sを公開した直後に「変わってない」と言われたが、**コードは正しく、GitHub Pages の
ビルドがまだ終わっていなかっただけ**だった（実測：push から完了まで **3〜7分**）。
- **確認方法**：`mcp__github__actions_list`（method: `list_workflow_runs`）で
  「pages build and deployment」の `status` と `head_sha` を見る。
  `in_progress` なら**まだ届いていない**。`completed/success` になって初めて公開されている。
  ★戻り値が巨大なので、保存されたファイルを `json.loads` して必要な項目だけ出すこと。
- ★**この環境から github.io には出られない**ので、公開の可否はこの方法でしか確かめられない。
- **順番**：①Pagesのビルドが終わっているか →②ブラウザのキャッシュ（Ctrl+Shift+R）→③コード
  の順に疑う。いきなりコードを直しにいかないこと（今回それで無駄な作り直しをしかけた）。
- ついでに見つけた不具合：**自動更新の対象一覧 `NN_PAGES` に `zumen_sekisan.html` が
  入っていなかった**（2026-08-01 に増えたページ）。図面・積算だけ、版が新しくなっても
  古いものが出続ける状態だった。全11ページで追加した。
  ★**ページを増やしたら `NN_PAGES` にも足すこと。**（版名4点セットと同じくらい大事）

### 68. 3Dの屋根の納まりを実際の現場に合わせた（2026-08-12b・c）★本人の現場写真より
本人から**実際の屋上の写真**が届き、それに合わせて作り直した。指示は3回に分かれている。

**版b（1回目）**
- **パラペットの外壁の色を建物と同じに**（`lv>0?0xcdc6ba:0xb9bcb4`）。
- **笠木（アルミ）は廃止**。「現段階では不要」との指示。
- **立上り防水層を追加**。★**最初は壁の中に埋まって見えなかった**。
  原因は `nrm`（辺の法線）が**内向き**なので、そこへさらに +0.012 すると
  厚さ80mm以上の壁の**内部**に入ってしまうこと。**`fo=th+0.006`（壁の内面より少し手前）**が正解。
  パラペット（para）と壁当り（kabe）の**両方**を直すこと。
- **継目は「線」ではなく「丸く盛り上がった棒」に**（`CylinderGeometry` 半径12mm）。
  溶融アスファルトのはみ出しアスは、実物も丸く盛り上がっている。
- ★**外壁に黒い点がぽつぽつ出る**：棒の端が外壁の面とぴったり同じ位置にあり、
  切り口が壁から顔を出していた。**端を30mm内側で止める**（`BEAD_IN`）。
- 砂付ルーフィングの色は `nnMemColor(sp)`＝工法ごと（シート 0x8f948d ほか）に統一。

**版c（2回目）＝ここが本題**
指摘は「①天端の防水層とはみ出しアスが無い ②立上りの継目の間隔が広すぎる
③平場と立上りで継目が同じ場所に来たら漏水する。あと平場に少し立上りを貼りかけている」。
★**③は施工の常識で、絵の問題ではない。** 平場と立上りの継目が重なると水みちができる。
- **天端に防水層を張った**。壁と同じ留め継ぎ（`joint`）で作り、**外へ20mm出す**。
- **はみ出しアスを2本足した**：①貼りかけの端（平場の上・壁から120mm）②天端の外側の端。
- ★**立上りの縦の継目は「平場の継目の位置」を流用してはいけない**。
  版bは `seamCuts`（平場のサイドラップ・エンドラップの位置）をそのまま立上りに回していたので、
  **間隔がまちまち**になり、しかも**平場の継目とぴったり同じ場所**に来ていた（＝漏水の形）。
  → **シート幅から自前で決める**：`pitch = sheetW − ラップ`（既定 1.0−0.1＝**0.9m**）。
  **開始位置を `pitch*0.5` ずらす**ことで平場の継目と重ならない（千鳥）。
- 縦の継目は**立上り面 → 平場に貼りかけた部分 → 天端**の3か所を通す（実物と同じく1本につながる）。
- ★**斜めに寝かせた棒は `m.rotation.y` で向きを付けている**（`beadBetween`）。
  検証で向きを無視すると「x方向の棒」として測ってしまい、誤って「屋根の外に出た」と出る。

**★検証スクリプトの「ニセTHREE」を共通化した（`scratchpad/stub3.js`）**
- §63 で PBR・影・環境光を入れたため、各スクリプトに内蔵していた古いニセTHREEでは
  `renderer.shadowMap.enabled` で止まり、**4本（zfix2/f3chk/f4chk/zdir）が丸ごと落ちていた**
  （さらに photochk も）。**1本のファイルにまとめ、5本から読ませる**ようにした。
  渡し方：`await p.evaluate(s=>{window.__STUB=s;}, '('+require('./stub3.js').toString()+')()')`
  → 使う場所で `eval(window.__STUB)`。
- ★**受け皿の名前がスクリプトによって違った**（`__zGroups` と `__g`）。両方に積むようにしてある。
- ★**期待値も古くなっていた**：継目は Box→Cylinder に変わり、笠木は廃止された。
  **product を直す前に「テストが古いだけではないか」を必ず疑うこと。**
  今回は5本とも product は正しく、テストだけが古かった。

### 69. 3D：建物の外に防水層／継目が切れる（2026-08-12d）★根っこは古いバグ
本人の指摘3点。①建物の外になぜか防水層が施工されている ②はみ出しアスの継目が
つながっていない（いたるところ）③天端の出隅で防水層・はみ出しアスが切れる。
あと平場のはみ出しアスが立上りの入隅の際まで貫通しているのはおかしい。

- **★①の真犯人は `pointInPoly` が10年物のバグだったこと。**
  ```js
  if(((yi>y)!==(yj>y)) && (x < …)) inside=true;   // ← 誤り
  if(((yi>y)!==(yj>y)) && (x < …)) inside=!inside; // ← 正しい
  ```
  交わるたびに**裏返す**のが正しいのに、`true` を入れっぱなしにしていた。
  そのため**部位の左側にある点がすべて「中」と判定**され、
  `ringNormal` の「内向きに直す」判定が**左の辺だけ効かなかった**。
  ＝**左の辺の立上り防水層が、建物の外側の面に貼られていた。**
  ★見つけ方：内側へずらした輪郭を作ったら、左の辺だけ外へ出た（12.00→11.73）。
    法線を印字したら (0,1)(-1,0)(0,-1)**(-1,0)** と、4本目だけ符号が逆だった。
  ★この関数は5か所から呼ばれるが、**どれも正しい内外判定を欲しがっている**ので
    直して困るところは無い（中抜きを描く場所の判定・分割時の穴の割り振り等も一緒に直る）。
- **★②継目のすき間**：版cで端を30mmずつ短くしていた（外壁から顔を出すのを隠すため）。
  そのせいで**交わるところに60mmのすき間**ができていた。短くするのはやめた。
- **★平場の継目は「立上りが貼りかかる線」で止める**（新設 `nnInsetOutline`）。
  立上りのシートは平場に120mm貼りかかるので、**その下の継目は隠れて見えないのが実物**。
  割り付けの計算（`wari`）は今までどおり屋根いっぱいで行い、**描くときだけ**内側の輪郭で切る。
  端が外壁から顔を出す問題も、これで自動的に解決する（短くする細工が要らなくなった）。
- **★③角で切れる**：貼りかけ・天端のはみ出しアスを、辺ごとに垂直に出していた。
  壁と同じ **`joint()`（留め継ぎ）** で角の点を出すようにして、ぐるりとつながるようにした。
- **天端の防水層を建物の外へ出さない**（外へ20mm出していたのをやめた）。
  出していると、遠目に「外壁に防水層が貼ってある」ように見える。
- 平場の継目には **`m.name='nnFlatSeam'`** を付けた。検証でパラペットの継目と見分けるため。

★**検証で気をつけること（今回3回踏んだ）**
1. **端はちょうど境界線の上に来る**ので、内外判定はぶれる。2mm内側に寄せた点で見ること。
2. **帯どうしはサイドラップぶん重なっている**ので、「継目の中心がどの帯か」では決まらない。
   「まるごと収まる帯が1つでもあるか」で見る。
3. 継目を数えるときは **`name==='nnFlatSeam'` で平場のものだけに絞る**。
   絞らないとパラペットの継目まで数えて、誤って★NGが出る。
- 検証は `scratchpad/lapchk.js`（平場の継目が貼りかけの線の内側で止まる）を新設。
  `f3chk.js`／`f4chk.js` も上の3点に合わせて直した。

### 70. 3D：出隅の納まり・はみ出しアスの質感（2026-08-12e）
本人の指摘4点。①天端に謎のはみ出しアスの残骸 ②天端の出隅が切断している・立上りと天端の
防水層も切れて見える ③はみ出しアスが黒いパイプにしか見えない ④立上り出隅の施工を
現場写真のとおりに（最初からそうなるように）。

- **①天端の外側の端に出していた「はみ出しアス」を削除**。そこはシートの端が納まるだけで
  重ね合わせが無い＝アスファルトは出ない。出していたので天端に謎の帯が残って見えていた。
- **★②出隅で立上り防水層が切れていた原因**：`face` を **`PlaneGeometry(len, hh)` を辺の中央に
  置く**作りにしていたこと。内側へ `fo` ずらすと、角では板どうしが**届かない**
  （ずらした量ぶん手前で終わる）。
  → 壁と同じ **`joint()`（留め継ぎ）で角の点を出し、角から角までの板**にした。
  ★**「辺の長さぶんの板」を内側にずらす作りは、角で必ずすき間ができる。**
    厚みのあるものを内側に置くときは、長さも留め継ぎで伸ばすこと。
- **★③はみ出しアスの質感（`nnBeadGeom`）**：まっすぐな円柱は「黒いパイプ」にしか見えない。
  頂点の半径を**位置から決まるゆらぎ**で±38%動かして、太さを不揃いにした。
  ★**乱数を使わない**（`Math.random` だと開くたびに絵が変わる。そもそも作図中は使えない）。
  ★ゆらぎは **x,z から作る**こと。角度から作ると円柱のつなぎ目で段差が出る。
  ★平らな面の上に出るものは `gm.scale(1,0.72,1)` で**少しつぶす**（実物も平たく広がる）。
    立上りの縦のものは壁の面に沿ってつぶす（`scale(1,1,0.72)`＋`rotation.y=-ang`）。
- **★④出隅の納まりを最初から作る**。角ではシートをそのまま曲げられないので切って重ねる
  （きんちゃく折り）。実物の角には必ず
  **・角に沿った縦の継目 ・天端を渡る継目 ・平場側へ斜めに出る継目（重ねた三角のふち）**
  が出る。この3本を `next` のある角（両側パラペット）に自動で入れる。
  斜めの継目は**角の二等分線の向き**に、貼りかけの線を50mm越えるまで。
  ★長くしすぎると図面が線だらけになる（最初160mm伸ばして長すぎた）。
- ★検証スクリプトのニセTHREE（`stub3.js`）に **`attributes.position`／`scale`／
  `computeVertexNormals`** を足した。`nnBeadGeom` がそこを触るため、無いと5本が丸ごと落ちる。

### 71. 3D：立上りの継目を天端まで通す／不具合と見出しの絵（2026-08-12f）
- **★立上りの縦の継目が「謎の黒い線」に見えて、天端の手前で切れていた**。原因は2つ。
  ①`gm.scale(1,1,0.72)` で**つぶしていた**こと。立上りは垂直面なので上からの光が当たらず、
    つぶすと陰影が出ず**平たい黒い線**にしか見えない。→ つぶさず丸いままにする。
  ②高さが `hh-0.01` で、**天端（lv+hh+0.012）より22mm低かった**。→ 平場の継目の高さ（lv+0.016）から
    天端の上まで**1本で通す**。天端の横の継目の終点も `th` → `fo`（＝縦の真上）にそろえた。
  ★**平らな面の上の継目はつぶす／垂直面の継目はつぶさない**、と覚える。
- **★出隅で「はみ出しアスが斜め上に飛び出て見える」→ 天端を斜めに渡る継目を削除**。
  角では天端を対角に横切るので、見る向きによっては空中に飛び出したように見える。
  角の縦の継目と、平場へ出る斜めの継目だけで、重ねてあることは十分わかる。
- **不具合タグに「植物繁茂（shokubutsu）」を追加**（症状グループ・25種になった）。
  絵は `icons/def_shokubutsu.png`。あわせて `def_kuchiaki.png`／`def_fukure.png` も差し替えた。
  ★**`DEFS` を増やしたら `scratchpad/kdef.js` の「24行」「24個」も直すこと**（今回引っかかった）。
- **自社の実績歩掛の見出しに絵**（`icons/hpic_bugakari.png`）。これで9個中7個が絵入り。
  残りは 月別の受注金額の推移 の1つだけ。
- ★**不具合の絵は背景が透過でないもの（現場の絵）も混ざる**。透過PNGなら透明部分で、
  そうでなければ**四隅の色を背景とみなして**切り落としてから高さ168pxにする
  （`scratchpad` の変換スクリプト参照）。
- ★**icons を増やしたら `sw.js` の ASSETS にも足すこと。**

### 72. 3D：出隅の天端はL字に2本／2本指はズームだけ（2026-08-12g）
- **★出隅の「謎の段」の正体**：角の縦の継目が**天端の面ちょうどで切りっぱなし**になっていた。
  天端の横の継目は「軸が天端の面」なので**8mmだけ上に出る**。縦は面で止まる。この差が段に見えた。
  さらに横の継目だけ `scale(1,0.72,1)` で**つぶして**いたので、断面が合わず合わせ目に段ができた。
  → **天端の継目はつぶさない**（`beadBetween(...,round=true)` を追加）。縦は天端の面で止める。
  ★**突き合わせる継目どうしは、断面（つぶし方）をそろえること。**
- **★角の天端を「斜めに渡す」のは2回とも失敗**（空中の塊に見える／飛び出て見える）。
  → **両側の辺それぞれに直角の継目を1本ずつ**（L字）に変えた。角では両側のシートがそこで
  終わるので、実物もL字に2本出る。**向きが他の天端の継目とそろうので浮いて見えない。**
  ★見る人が「変」と言うのは、たいてい**他と向きがそろっていない**とき。
- **平場へ出る斜めの継目は `joint(...,LAPIN,...)`（貼りかけの線の角）でぴたりと止める**。
  以前は二等分線に決め打ちの長さで伸ばしていたので、止まるべきところを通り越していた。
- **★2本指は「拡大縮小だけ」に**（`pointermove` の `T.phi` 更新を削除）。
  つまんで拡大しようとすると指が上下にわずかに動き、そのたびにアングルが変わっていた。
  傾きは ◹◺ ボタンに一本化（現場マップで「ひねり＝向き」を廃止したのと同じ理由・§18）。
  検証は `scratchpad/pinch.js`（PointerEvent を自前で流し、r だけ変わり phi/theta が動かないことを見る）。

### 73. 割付図PDF（5種類目）と、役物・架台の登録（2026-08-12h）★本人の提案

**①割付図PDF（`window.nnWariPDF()`）**
- 出す場所：図面・積算 →「📊 積算・設定」→ **🧵 割付図（平場のシート割り）**。
- 中身は**画面の②割付タブと同じ計算（`wari`）をそのまま紙に流し込む**だけ。
  1枚のシートごとに四角＋「帯番号-枚番号」と長さ、右に 施工条件／割付数量／必要巻数／凡例／注記。
- ★**帯は外接四角形ベースなので、そのままだと屋根の外へはみ出す**。
  SVGの `clipPath`（`clip-rule="evenodd"`）で**屋根の形に切り抜く**こと。穴も一緒に抜ける。
- ★**部位名の白いふとんの幅は文字数から決める**（決め打ちだと長い名前がはみ出して読めない）。
- 縮尺・図枠・表題欄は他の4種と同じ helper（`sheetGeom`/`frame`/`titleStrip`/`nameBox`/`openSheet`）。
  図面番号は末尾に `-W` を付ける。検証は `scratchpad/warichk.js`／見た目は `warishot.js`。

**②役物・架台の登録（`<style id="nn-parts">`＋`<script id="nn-parts-js">`）**
- 置き場所：`localStorage('nn_zumen_parts_v1')`＝`{items:[{id,name,kind,w,d,h,price,addM2,sealM,memo}]}`。
  **暇なときに登録しておけば次の現場でもそのまま使える**、という本人の発案。見本3件を最初から入れてある。
- 置いたものは **`state.parts=[{p:役物id, x, y, r}]`**（作図データと同じ入れ物なので保存も復元も自動）。
- ★**本体のコードはほとんど触らず「相乗り」で作った**。ここが肝。
  ・`draw` / `build3D` / `recalc` を **`window.◯◯` の差し替えで包む**（元を呼んでから自分の描画を足す）。
    top-level の `function draw(){}` はグローバルの性質なので、`window.draw=…` で差し替えられる。
  ・キャンバスの当たり判定は **capture フェーズの `pointerdown`** で先取りし、
    「置く最中」と「選択ツールで役物を叩いたとき」だけ `stopPropagation` する。それ以外は素通り。
- ★**キャンバスの id は `cv`**（`canvas` ではない）。最初 `getElementById('canvas')` と書いて動かなかった。
- ★**高さ0の役物（ドレン等）は3Dで箱を出さない**のが正しい。検証で高さのある「架台」で試すこと。
- 積算は既存の表の下に別表で足す（`#nnPartsQt`）。**増し貼り㎡・シールm も1個あたりで持たせている**。
  役物は「置いた数」より**まわりの手間**でお金が変わるため、ここを持つと現場に近くなる。
- ★**バックアップの対象一覧（index.html の `LIST`）にも足すこと**（§46）。足さないと引っ越しで消える。
- 検証は `scratchpad/partschk.js`（15項目：置く・回す・複製・積算・3D・登録・保存・再読み込み・削除）と
  `partsmo.js`（スマホたて／よこ：引き出しに収まる・「置く」で引き出しが閉じる・操作バーが画面内）。

### 74. 3D：継ぎ目の「段」は玉で溶かす（2026-08-12i）★3回直してやっと決着
出隅の段を版f・gと2回直したのに「やっぱり段ができたまま」と言われた。
**目で見て直すのをやめ、数値で全部の継目を調べたら、位置は完全に正しかった。**
（`scratchpad/beadaudit.js`：天端の継目は外の面から 0.03〜0.256m、縦の上端は全部 0.312m
　＝天端の面とぴったり。問題0件）

- **★段の正体は「位置」ではなく「丸い棒どうしの突き合わせ」そのもの。**
  ・天端の継目＝**軸が天端の面**にある横向きの円柱。上に半径ぶん（12mm）出ている。
  ・立上りの縦の継目＝上端が天端の面で終わる円柱。**上端は平らな面**。
  → 同じ点で出会っても、**横は12mm高く、縦は平らに切れている**。これが段。
  **位置をいくら合わせても、断面の形が違うかぎり段は消えない。**
- **★直し方＝継ぎ目に「玉」を置く**（`beadBall`＝`SphereGeometry`）。
  玉の中に両方の棒が埋まるので、つながって見える。実物の継目も角では盛り上がる。
  ・玉の太さは **棒の1.08倍**。1.3倍にしたら「こぶ」に見えて作り直した。
  ・★**継ぎ目にだけ置くこと。** 棒の自由端（何も繋がらない端）に置くと、
    そこだけ丸くふくらんでかえって目立つ（天端の外側の端・角のL字の端で実際にそうなった）。
  ・置く場所は4つ：立上りの縦の上端（天端との継ぎ目）と下端（平場との継ぎ目）、
    角の縦の上端と下端。
- ★**同じ高さで交わるT字（平場の継目どうし・貼りかけ）には玉は要らない。** 段にならない。
- ★検証スクリプトのニセTHREE（`stub3.js`）に **`SphereGeometry`** を足すこと。

### 75. 3D：立上りと天端の「面」の段（2026-08-12j）
版iで棒の継ぎ目は玉で解決したが「まだ段がある。なぞの継ぎ目の部分」と言われた。
**今度は棒ではなく「面」だった。**

- **★立上り防水層が壁の上端（lv+hh）で終わっていた。** その上に天端防水層の
  **厚み12mm・6mm奥まった小口**が出て、パラペットの全長に走る帯（段）になっていた。
  → 立上りの板を **天端の上の面（lv+hh+0.012）まで**伸ばす。小口が裏に隠れる。
- **★天端の板の内側の端は「壁の内面（th）」で止めること。**
  立上りの板（fo＝th+6mm）と**同じ位置**にすると、面がぴったり重なって
  どちらを描くか毎回入れ替わり、天端の際に**ちらつく帯**が出る（Zファイティング）。
  一度そう直して悪化させた。**重ねるのではなく、裏に隠すのが正解。**
- **★角のすぐ横に縦の継目を立てない**（`dd<0.30 || dd>len-0.30` は飛ばす）。
  角には「角の縦の継目」がすでにあるので、0.3m以内にもう1本立つと、
  天端の上で継目の端が2〜3本重なって塊に見える。実際の現場でも角の際では継がない。
- ★検証：`scratchpad/facegap.js`（隣り合う立上りの板の端点が一致しているか＝すきま0、
  高さ・上端・下端がそろっているか）。`beadaudit.js` と合わせて使う。

### 76. 3D：天端の面取り（2026-08-12k）★本人の指摘
「実際、天端って面が取られてるでしょう？面も再現して」

- 実物のパラペットは、型枠に**面木**を入れて天端の外側の角を45度に落としてある。
- **★`strip`（押し出し）では斜面が作れない。** 押し出しは垂直の面しか作らないので、
  面取りには**4点それぞれに高さを持てる板**が要る → 新設 `slope(p1,y1,…,p4,y4,色,種類)`
  （`BufferGeometry` に三角形2枚を直接置く。`computeVertexNormals` で陰影も付く）。
- 壁は**3つに分けて組む**：
  ①本体（下端〜面取りの下・高さ hh−CH）②天端寄りの帯（CH・外側へ CH 内側に寄せる）
  ③その間の斜面（`slope`）。**CH＝min(20mm, 天端幅の1/4, 立上りの1/4)**。
- 防水層も同じ形に回す：天端の板は **CH〜th**、面取りの斜面は `slope` で外の面まで下ろす。
- ★**`zfix2.js` の「壁は12枚」は本体の depth/上端が `hh` から `hh−CH` に変わる**ので、
  期待値を 0.3 → 0.28 に直すこと（今回引っかかった）。
- ★検証スクリプトのニセTHREE（`stub3.js`）に **`BufferGeometry`／`BufferAttribute`** を足すこと。

### 77. 3D：出隅（縦の角）の面取り（2026-08-12m）
★版kで「天端の面取り＝上の角」と受け取ったが、本人が言っていたのは
**出隅の縦の角**（角ばっているところ）だった。写真に丸を付けてもらって判明。
**「面取り」だけでは上の角か縦の角か決まらない。次からどちらか確かめること。**

- やり方：**両側の壁の外側の線を、角から CV（既定30mm）だけ手前で止め、その間を三角柱でふさぐ**。
  ・`CV=Math.min(0.03, len*0.25, th)`
  ・角には 本体（hh−CH）／天端寄りの帯（CH）／その間の斜面／天端の防水層／防水層の斜面 の
    **5つを同じ三角形の形で**足す。1つの角につき1回だけ（`cvB && next` のときに作る）。
- **★`strip` は4点固定だったので `stripN`（何角形でもよい）に一般化**した。
  `strip` は `stripN` を呼ぶだけの薄い包み。三角柱はこれで作る。
- **★出隅かどうかの判定**：多角形の**回り方（符号付き面積）**と、その角の**外積の符号**が
  同じなら出っぱった角＝出隅。入隅（へこんだ角）は今までどおり留め継ぎのまま。
- ★**`zfix2.js` の「壁は12枚・隣どうし2点一致」は前提が変わる**。
  面取りを入れると隣の壁とは**内側の1点しか共有しない**（外側は三角がつなぐ）。
  → 「**どの三角も両側の壁と2点ずつ共有しているか**」に判定を変えた（12個・不一致0）。

### 78. 3D：天端の内側の角の面取り（2026-08-12n）★「面取り」で3回外した
本人がオレンジで塗って示したのは **天端と立上りが出会う内側の角**だった。
・版k＝天端の**外側**の角 ・版m＝出隅の**縦**の角 ・版n＝天端の**内側**の角（これが本命）
★**「面取りして」だけでは、どの角か決まらない。必ず場所を確かめること。**
　パラペットには面取りできる角が最低3つある（外の上・内の上・縦の出隅）。

- コンクリート：天端寄りの帯の内側の端を `th−CH` にして、`I`（th・下端側）との間を斜面でつなぐ。
  ★**`joint` の offset は「内向き（屋根側）に何m」なので、面取りは `th−CH`。**
    `th+CH` にすると屋根側へ出っ張って、面取りではなく**ひさし**になる（実際に1回やらかした）。
- 防水層：天端の板の内側の端も `th−CH`。立上りの板は **`hh−CH` まで**にして、
  その上は内側の斜面（`slope`）が受ける。＝立上り→内の面取り→天端→外の面取り、と1枚で回る。
- 出隅の三角柱の内側の頂点も `th−CH`（`Imk`）にそろえる。

### 79. 3D：継目も面取りに沿わせる（2026-08-12p）
版nで天端の内側に面取りを入れたが、**継目（はみ出しアス）は直さなかった**ので、
縦の継目が面取りの上で**宙に浮いて**いた（本人の指摘）。

- **★形を変えたら、その上を通る継目も必ず一緒に直すこと。** 面・継目・玉は3点セット。
- 新設 **`beadSlope(p1,y1,p2,y2)`**：斜めに置く継目。
  `m.quaternion.setFromUnitVectors(new Vector3(0,1,0), 向きの単位ベクトル)` で棒を3Dで倒す
  （`rotateZ`／`rotateX` では45度の斜面に合わせられない）。
- 継目の通り道：**平場 → 立上りの縦（`hh−CH` まで）→ 面取りの斜め → 天端（`th−CH` から）**。
  折れる2か所には玉（`beadBall`）を置いて段を消す。
- ★検証スクリプトの注意：`beadaudit.js` の「縦の上端＝天端」は前提が変わる。
  いまは **縦の上端＝`hh−CH`**、そこから `nnChamBead`（面取りを渡る継目）が天端へ渡る。
  ニセTHREE（`stub3.js`）には **`quaternion.setFromUnitVectors`／`name`** が要る。
- ★**`nCH` は「出隅の三角柱」ブロックのローカル変数**。継目のブロックからは見えないので、
  必要なら同じ式でその場に定義し直すこと（`nCH2`）。

### 80. 3D：現場写真に近づけた（2026-08-12q）★はみ出しアスを細く・表面の質感
「実際の写真と比べてどう感じる？」と聞かれ、正直に差を挙げたうえで
**①はみ出しアスを細く・平たく・不揃いに ②砂付とコンクリートの質感**をやることになった。

- **①はみ出しアス（`BEAD_R` 0.012 → 0.006）**。直径24mm→12mm。実物のはみ出しアスは
  こんなに太くない。あわせて**平場の上のものは `scale(1,0.52,1)`**（前は0.72）で
  もっと平たくつぶす（溶けて広がるので、実物は太さより「幅」が出る）。
  ★**太さのゆらぎを大きくした**（`1+n*0.38` → `Math.max(0.30, 1+n*0.62)`＝0.3〜1.6倍）。
    ゆらぎは**4つのサイン波を混ぜて**作り、輪切りの数も `len/0.035` に細かくした。
    **`Math.random` は使わない**（開くたびに絵が変わってしまう。§70と同じ）。
- **②表面の質感は「その場で描いて」貼る**（`nnMakeTex`／`nnSandTex`／`nnConcTex`）。
  ★**画像ファイルを1枚も増やさない**のが肝。256×256の canvas に JS で粒を描いて
  `THREE.CanvasTexture` にする。**容量ゼロ・通信ゼロ**（写真を貼ると1枚1〜3MBかかる）。
  ・砂付ルーフィング＝細かい粒（255〜197）＋大きめの粒2600個
  ・コンクリート＝粗い粒＋骨材420個＋ムラ26個（`radialGradient`）
  ・`map`（色）と `bumpMap`（凹凸）に**同じ絵**を入れる。凹凸があると光の当たり方が変わり、
    ただの色板が「ざらざらした面」に見える（砂 0.35／コンクリート 0.18）。
  ・**乱数は自前の数列（LCG）**で作る（`nnNoiseSeq`）。毎回同じ絵になる。
- **★UVを「メートル」にそろえること**（`nnUvMetric`）。ここが一番ハマる。
  ・`PlaneGeometry` の UV は **0〜1**（大きさに関係なく1周）
  ・`ShapeGeometry`／`ExtrudeGeometry` の UV は **メートルの値がそのまま**入る
  → そのままだと、屋根の平場は粒が細かく、立上りだけ粒が巨大、とバラバラになる。
  `NN_TEX_PER_M=4`（1タイル25cm）を掛けてそろえる。直す場所は3つ：
  平場の `ShapeGeometry`／`stripN` の `ExtrudeGeometry`／立上りの `PlaneGeometry`
  （こちらは0〜1なので `fL*4`／`fH*4` を掛ける）。
- ★検証スクリプトのニセTHREE（`stub3.js`）に **`CanvasTexture`／`RepeatWrapping`** を足すこと。
- 回帰は13本（d3chk／zfix2／f3chk／f4chk／zdir／lapchk／sec2chk／partschk／warichk／
  facegap／beadaudit／photochk／pinch）とも★NGなし。

### 81. アイコンの差し替えまとめ（2026-08-12t・u）★ファイル名の一覧はここ
- **ホームの 設定・用語集**（`nav_settei.png`／`nav_yougo.png`＋`_on`）と、
  **ホームのメニューボタン**（`btn_menu.png`・新規）、
  **ダッシュボードの見出し**（`hpic_yojitsu.png`／`hpic_status.png` を差し替え、
  `hpic_tsukibetsu.png` を新規追加）。**これで見出し9個すべてに絵が入った。**
- **見出しの絵のファイル名（`icons/hpic_<キー>.png`）**
  | 見出し | ファイル |
  |---|---|
  | 完成工事高の予実 | `hpic_yojitsu.png` |
  | 受注率 | `hpic_juchu.png` |
  | 要対応 | `hpic_taio.png` |
  | 月別 受注金額の推移 | `hpic_tsukibetsu.png` |
  | 施工中の現場 | `hpic_sekou.png` |
  | ステータス分布 | `hpic_status.png` |
  | 歩掛実績（旧「自社の実績歩掛」） | `hpic_bugakari.png` |
  | 入金予定 | `hpic_nyukin.png` |
  | 工法別 受注・利益 | `hpic_hou.png` |
- **★添付画像はアプリが webp に変換することがあり、そのとき透過が失われる。**
  透過を表す**市松模様（254と242の薄い灰色の格子）がそのまま絵として焼き込まれる**。
  → `scratchpad/dechecker.py`：**画像のフチからつながっている範囲だけを塗りつぶしで背景と判定**する
  （色で一括判定すると、絵の中の白＝本のページや金属のつやまで消える）。
  絵に黒い輪郭があれば内側までは届かないので安全。フチのギザギザは半透明にしてやわらげる。
  ★**透過が保たれることもある**（今回は4枚中3枚は無事だった）。毎回 `mode`／alpha を見て判断すること。
- **★ホームの 設定・用語集は「絵の作りが変わった」ので CSS も直した。**
  以前の絵は**白いタイルと緑ラベルが焼き込まれていて、上から65%だけを切り出して**出していた
  （`.icwrap{height:34px; overflow:hidden}` ＋ `img{height:52px}`）。
  新しい絵は**絵だけ**なので、そのままだと下が切れる。
  → **白いタイルは CSS で作る**（`.icwrap` に `width:68px; height:34px` と白のグラデ）、
  絵は `height:30px` で丸ごと出す。**大きさは差し替え前の実測（69×34）に合わせた**ので周りは動かない。
- **★メニューボタン（`#menuBtn`）は CSS で描いた青い丸だった。**
  絵は「ボタンそのもの」なので、`onload` で `hasimg` を付けて**丸・枠・影・白い三本線を消す**
  （`btn_back.png` と同じ考え方・§12）。`onerror` なら絵を外すだけで青い丸に戻る。
- **★同じファイル名で中身だけ差し替えたら、URLに版名を付けること**（§66の罠）。
  今回は `src="./icons/hpic_yojitsu.png?v=2026-08-12u"` のように直接書いた。
  **新しい名前の絵には不要**（`hpic_tsukibetsu.png`／`btn_menu.png` は付けていない）。
- 検証は `scratchpad/newic.js`（設定・用語集）と `ic4chk.js`
  （メニューボタンの背景・枠・線が消えているか／見出し9個の絵／**見出しが折り返していないか**／
  版名つきURL／横はみ出し0。PC・たて・よこの3通り）。見た目は `ic4shot.js`。
  ★**絵の大きさを測るときは `window.nnPZ` で割ること**（PCは zoom がかかっている・§61）。

### 82. 物件データの総入れ替えと、一覧カードのゲームUI化（2026-08-12v）★大改修
指示は2つ。①現場マップと現場記録帳の物件を一致させる（記録帳がベース。シーリング系は物件ごと削除。
旭川8・苫小牧4・帯広3・函館3・恵庭2・千歳2・北広島3・東京4・横浜3 の32件を追加＝**計100件**）
②一覧カードの丸みをやめ、パワプロ風の**状態色の枠**＋**左に現場写真**。

**①データ（`scratchpad/newdata.py`＝記録帳／`mapsync.py`＝マップ。再実行可能）**
- 削除したシーリング9件：J001/J003/J004/J016/J018/J019/J037/J048/J076。
  ★**不具合タグの見本 `SAMPLE` が J001/J004 を指していた**ので J005/J006 に付け替えた。
  RAWの物件を消すときは **SAMPLE と grep で突き合わせる**こと。
- 追加32件は**実在の建物**（フィール旭川・とかちプラザ・函館アリーナ・東京流通センター等）で、
  **各地の防水業者からの2次請け**という設定（元請＝「道北防水工業（株）」等の一般的な社名）。
  工法はメーカー実在の商品名（リベットルーフ／サラセーヌ／DSカラー・ゼロ／ポリマリット／
  オルタックスカイ／ロンプルーフ・エコAL／ガムクール／クイックルーフィング）。
- 数値の作法は既存と同じ：y=[材料31%,労務29%±,外注6%,経費14%]≒受注額の80%、nin≒労務/25000。
  kan は a（実績）あり・kou は a=予算×進捗率。
- **★住所に東京・横浜が増えたので直した箇所2つ**：
  `p.area` の正規表現に `^東京都[^区]+区` を追加（無いと東京だけ地域=その他になる）／
  `wxCity` に `横浜` を追加（天気を東京側に）。
- **現場マップは `BASE_SITES` を記録帳の100件から自動生成**（id=RAWの並び順+1）。
  状態は kan→done／kou→work／他→quote の3値に丸める。座標はおよそ（区の中心＋番号ズレ、
  追加分は建物ごとの実座標）を入れ、実機ではジオコーディングが正確な位置に直す。
  ★ジオコーディングの「北海道札幌市」決め打ちをやめた（東京都→そのまま／横浜市→神奈川県を前置き／他→北海道）。
  ★**POS_STORE を `osamari_pos_v2` に版上げ**（データ総入れ替えで旧のピン位置補正が別物件に当たるため）。
    index.html のバックアップ `LIST` のキーも合わせて変更した。
  ★記録帳の物件を変えたら **`mapsync.py` を流し直してマップも作り直す**こと（片方だけ変えるとまたズレる）。

**②一覧カード（`<style id="nn-pwcard">`＋renderListの雛形）**
- `pcard st-<stRaw>` クラスで枠色：施工中=橙 #ff8a1e／見積済・調査済=青 #2f9dff／完成済=緑 #21a53f／
  引合い=黄 #f0c419／**契約済=赤 #d84343（ユーザー未指定・こちらで選定）**。
  `border-radius:0 !important`＋内側に白2px（`box-shadow:inset`）＝ゲームUIの白フチ。
- **左の写真**：`<div class="pph"><img src="./icons/roofph_${(p.id%6)+1}.jpg">`。
  roofph_1〜6.jpg は **PILで描いた仮の屋上写真**（`scratchpad/roofphoto.py`・各5〜6KB）。
  実写が来たら同じ名前で置き換えるだけ（★そのときはURLに版名を付けること・§66）。
  読めないときは `onerror` で写真枠ごと消えて文字だけになる。
- スマホは写真96px・本文の余白を詰めた。一覧は元から横いっぱいなのでレイアウト変更なし。
- ★上書きは `#list .pcard`（ID付き）で書く。ページ内の `.pcard`（クラスのみ）に必ず勝つ。

### 83. 現場一覧カードを見本レイアウトに・その場で編集（2026-08-13a）
本人からレイアウトの見本画像が届き、それに合わせて作り直した。指摘は
「絞り込みチップをもっと左へ」「枠が太すぎる」「スマホは上のタブ・検索の行がタテに太い」
「表面上の情報はその場で入力・編集できるように」。

- **カードの中身（renderList の雛形）**：
  1行目=[物件名][状態▾][請負単価][元請][請負金額][材料金額][工賃][他]／
  2行目=[住所]＋日付＋右端に利益予定額／3行目=[工法▾][数量][記号バッジ][メーカーチップ]／
  4行目=不具合タグ（nnDefRow）。囲みの項目＝編集できる項目。
- **その場編集（`<script id="nn-inline-edit">`）**：
  ・囲み（`.ebox`）をタップ→その場が入力欄に。Enter か外タップで確定、Escで取り消し。
  ・状態と工法はプルダウン（工法は KO_MASTER＋実データの和集合。変えると記号・メーカーも連動）。
  ・確定で props を直接書き換え recalcProp → 一覧・利益・絞り込み件数・詳細まで追従。
  ・金額系は `inputmode=decimal`（iPhoneで数字キーパッド）。入力欄は16px（自動拡大よけ）。
  ・**永続化はしていない**（既存の編集フォームと同じ「開いている間だけ」。トーストで明言）。
  ★**カードの「1回目タップ=黄色選択」と喧嘩する**ので、document の **captureフェーズ**で
    `.ebox`/`.stsel` のタップを先取りして stopPropagation する。#list のリスナーより必ず先に動く。
  ★「他」は 外注+経費 の合計を表示。編集したら `src[2]=入力-経費` で外注側に寄せる。
  ★材料/工賃/他の編集先は **`p.a||p.y`（表示と同じ出どころ）**。yだけ直すと画面が変わらない。
- **枠は 3px→1.5px に細く**（「太すぎ」の指摘）。白の inset は廃止。色は §82 のまま。
- **絞り込みチップ**：`.chips{margin-left:auto}` が右寄せの犯人。`#toolbar .chips{margin-left:0}` で
  検索欄のすぐ右へ。件数の札（#nnLC）は自前の margin-left:auto で右端に残る。
- **スマホの上まわり**：`#viewtabs` のボタン padding 9px→4px・font 14.5→12px、
  `.toolbar` padding 3px 8px。検索欄の中の文字は16pxのまま。
- 検証は `scratchpad/edit1.js`（単価・材料金額・状態・工法・Esc取消・写真タップの2回移動が生きている）
  と `pwlist.js`（枠色・角なし・100件・はみ出し0）。

**★2026-08-13b：よこ向きスマホの左右余白を詰めた**（「左右のスペースがもったいない」の指摘）。
- ノッチの逃げ（`nn-safe-x`＝env×--nnsaf を丸ごと）を、main・toolbar・viewtabs で **0.4倍**に縮めた。
  一覧はスクロールする中身なので4割逃げれば丸角にかからない。カードの左端 約80px→35px。
  ★上書きは nn-pwcard の中（nn-safe-x より後ろ）に同じ `!important` で書く。後に書いた方が勝つ。
- `#list` の余白 12px→8px 4px。写真 108→**150px**。カードの中の余白・行間も1段詰めた。
- 件数の札（#nnLC）は文字10pxに縮め、**左に影**を付けてチップ帯が「札の下をくぐる」見た目に
  （チップ帯は横スクロールの箱なので、切れ目をなくすことはできない）。
- 検証は `scratchpad/landfit.js`（env→59px置換コピーで、カードの左端・写真の幅・はみ出しを実測）。

### 84. 一覧のメモ欄・メーカー別絞り込み・囲みの色分け／図面の2画面3D（2026-08-13c）
指示は「①囲みが白だと分かりにくいのでステータス色を薄くした地色に・メーカー別チップ追加・
カードにメモ欄 ②図面をかきながらリアルタイムで3Dが見たい（2画面）」。

**①現場記録帳**
- 囲み（.ebox）の地色＝状態色の薄色（`--stcL`：橙#fff3e6／青#eaf4ff／緑#eaf8ee／黄#fdf8e2／赤#fdecec）。
- 絞り込みに **メーカー別** を追加（LIST_FIL に1行足すだけ。buildChips は LIST_FIL から自動生成）。
- **メモ欄**＝カード下段の右側。`p.tb`（備考）にその場書き込み（textarea・Escで取消）。
  下段は `.pmain`＝[左（住所・工法・タグ）｜メモ（flex:1）｜利益予定額] の3列。スマホではメモが下の行に回る。
- ★**編集中に絞り込みチップを押すと確定されないバグ**を踏んだ。チップは pointerdown で
  preventDefault するため**フォーカスが移らず blur が出ない**。captureフェーズの pointerdown で
  「編集中の入力欄の外を押したら自分で blur を呼ぶ」処理を入れた（nn-inline-edit）。

**②図面・積算：2画面（上=作図56%／下=3D 44%）**
- ツールバー「⿻ 2画面」（`tl_2pane`・スマホは⋯道具の中）。①図面・②割付タブで有効。
  ③断面・④3Dタブでは解除（④は今までどおり全画面）。`localStorage('nn_zumen_split')` に保存。
- **リアルタイム反映＝`commit()` に相乗り**（§73と同じ window.commit 差し替え）。打点の確定・
  頂点移動・数値入力・削除は全部 commit を通るので、そこで 0.4秒の間を置いて build3D()。
- ★ハマった点2つ：
  1. `resize()` が**親（canvaswrap）の大きさ**でキャンバスを測っていた。2画面では #cv{height:56%}
     なので**キャンバス自身の getBoundingClientRect** に変更（通常時は同じ値）。見張りの setInterval も同様。
  2. 3Dの描画ループが **`if(tab!=='d3')return`** で止まっていた（分割ペインが 300x150 の初期サイズのまま）。
     `body.nnsplit2` のときも通すよう条件を足した。
- 検証は `scratchpad/split1.js`（16項目：ON/OFF・56/44・画素数の追従・自動組み直し・debounce・
  タブ連動・保存・復元）と `splitshot2.js`（実物の three.js で絵が出るか）。
  ★回帰15本は **ローカルサーバー(8899)を立ててから**流すこと（http:// 前提のスクリプトがある）。

### 85. 図面・積算：10件まとめて改修（2026-08-13d）
本人の指摘10件。①〜⑨は完了、⑩は第1弾（箱の押し出し）まで。

- **①全削除で3Dに影が残る**：影の再計算（`sun.shadow.needsUpdate`）が
  「部位があるとき」しか走らなかった。空になったときも1回立てる（else節を追加）。
- **②初期値 立上り高さ/天端幅が既存の辺に効かない**：`nnDefApply`＝変更時に
  「既にかいた辺にも反映しますか？」の confirm を出して一括適用（キャンセル＝新しい辺のみ）。
- **③ズームで寄るとカメラが見上げになる**：カメラの高さに**注視点の高さ(0.4m)を
  足していなかった**。寄るほどカメラが地面に沈み、注視点を見上げていた。
  `0.4 + T.r*cos(phi)` に修正＋ズーム下限 0.25→0.6m（3か所）。
- **④保存・開く**：`nn_zumen_saves_v1`（最大30件・stateのJSONを丸ごと）。
  ツールバー「💾 保存」「📂 開く」。**現場記録帳の「作成図面」タブに保存一覧**（`nnZuSaves()`）
  →「開く →」＝`zumen_sekisan.html?open=<id>`（KEYに書き込んで reload）。
  バックアップLIST（index）にも追加。
- **⑤寸法・角度の文字が小さい（PC）**：NN_PHONEでないとき 11.5→15px／札12→15.5px／
  H札10→12.5px。箱の高さも連動（h=19→23）。
- **⑥完成した形の寸法が消える**：出す条件が epx≥44・cellPx≥13 と厳しかった → 34/8 に緩和＋
  **「📏 寸法」トグル**（`nnDimsOn`・既定ON・`nn_zumen_dims`）。
- **⑦役物がどこに置かれたか分からない**：★**`drawParts` のタブ名が `'draw'` になっていた**
  （正しくは `'zu'`）。①図面タブで役物が一切描かれていなかった。あわせて小さい役物は
  名前を箱の上に出す。
- **⑧エッジ作成の段階から3Dへ**：2画面中、描きかけ3点以上を0.45秒ごとに監視し、
  「立上りなしの仮部位」として state.polys に push→build3D→pop。
  ★**カメラ（T.tx/tz/r と d3FitKey）を保存・復元**しないと、組み直すたび全体表示に戻って作図の邪魔。
- **⑨Shift＝90度**：`nnSnapPt` の角度きざみを `mouse.shift` のとき90に。
  ヒント文の「Shift＝1マススナップ」（実装されていなかった）を差し替え。
- **⑩3D面から押し出し（第1弾）**：3Dペインの「⬛ 押し出し」→ 面をタップ（Raycaster）→
  幅・奥行・高さを nnNumAsk で入力 → **役物ライブラリに自動登録して state.parts に配置**
  （§73の仕組みに相乗り。2D・積算・移動・削除がそのまま効く）。
  面に自由な形を描いての押し出しは未着手（次の段階）。
  ★役物ライブラリへ外から足す口として `window.nnPartsAddLib` を追加。
- 検証は `scratchpad/fix10chk.js`（18項目）。★localStorage を2ページで共有して確かめるとき、
  `browser.newPage()` は**別コンテキスト＝別localStorage**になる（注入して回避）。
- 回帰18本＋記録帳2本 すべて○。

### 86. 一覧の囲みを白＋緑ラベルに／ツールバーのアイコン差し替え口（2026-08-13e）
- **現場記録帳の囲み（`.ebox`）**：全体を薄緑にしていたのを、**値の部分＝白／項目名（`i`）＝
  ステータス色の薄色**に分けた（「入力するところが分かりにくい」の指摘）。
  `display:inline-flex` にして `i` を左端まで伸ばす（`margin-left:-8px`＋`padding`）。
- **スマホの文字を1段小さく**：本文12→11px・物件名13.5→12.5px・項目名10.5→9.5px・
  メーカー/日付/メモも連動。カードの高さが下がり、1画面に入る件数が増える。
- **★図面・積算のツールバー：全ボタンを自作アイコンに差し替えられるようにした**
  （`window.NN_TB_ICONS` の対応表＋`nnTbIcons()`）。`icons/btn_<キー>.png` を置くだけ。
  ・**パソコン＝絵＋文字／スマホ＝絵だけ**（`.tbtn.hasimg .tbtx{display:none}`）。
  ・**絵が無ければ `onerror` で img を消して文字のまま**＝置く前でも壊れない。
  ・ボタンを増やしたら `NN_TB_ICONS` に1行足すこと。id は `tl_◯◯` に統一してある。
  ・★未着の間は `icons/btn_*.png` が404になる（仕様）。検証スクリプトの
    「コンソールのエラーなし」はこの404を除外すること（d3chk.js を修正済み）。
- ★**テストが古くなった2件**：`memo1.js` の「囲みが薄色」は仕様変更で白に、
  `pwlist.js` の2回タップは**カードの中央が編集できる囲みに当たるようになった**ため
  写真（`.pph`）をタップするよう変更。どちらも product は正しくテストが古いだけだった（§68の教訓と同じ）。

### 87. 一覧カード：既存の状態・写真の長押し取り込み・チップ短縮（2026-08-13f）
- **絞り込みチップの「〇〇別▼」→「〇〇▼」**（`LIST_FIL` の label と、利益・不具合の2か所）。
  幅が詰まり、右端の件数の札との重なりが解消。★`lfTags()` は label から「別」を消す作りだったので
  そのままで正しく出る（消す処理が空振りするだけ）。
- **写真の下に「既存の状態」**（`.pcol` ＝ 写真＋`.pexist`）。3つとも囲みタップで選べる：
  ・既存防水 `p.kizon`（`NN_KIZON` 11種）・区分 `p.kind`（新築/改修/増改築/部分補修）
  ・構造体 `p.kouzou`（RC/S/ALC/W/RCS）
  ★`p.kind` は元から '改修' が入っていた項目を流用。kizon/kouzou は新規（既定 不明／RC）。
- **写真を長押し（0.5秒）でスマホの写真を取り込み**（`nnPhotoPick`）。
  ★**横640pxのJPEG(q0.75)に縮めてから保存**する（原寸だと保存容量をすぐ超える）。
  保存先 `nn_kirokucho_photo_v1`＝`{物件コード:dataURL}`。**入りきらないときは画面だけ差し替えて
  その旨をトーストで出す**（黙って消えないように）。バックアップの `LIST` にも追加済み。
  ★長押しの直後のタップが「カード選択→詳細へ移動」にならないよう、`_nnPhotoLP` の印で1回だけ止める。
- **物件名を大きく**（PC 15.5→18px／スマホ 12.5→14.5px）。
- ★**その場編集の不具合を2つ直した**（この作業中に発覚）
  1. **編集中の囲みから別の囲みへ直接タップすると、2つ目が開かない**。
     外を押した扱いで先に `render()` が走り、掴んでいた要素が作り直されて画面から消えるため。
     → `startEdit` の先頭で「もう画面に無い」なら、同じ物件・同じ項目の生きている囲みを取り直す。
  2. プルダウンが **Esc で取り消せなかった**（入力欄だけ対応していた）→ `sel.onkeydown` を追加。
- 検証は `scratchpad/exist1.js`（チップ名・3項目・写真の下・枠の中・選択肢・保存・長押しで
  画像選択が開く。PC／スマホ）。
  ★テストが古くなった3本を直した：`memo1.js`（チップ名）・`ebox2.js`（物件名を大きくした）・
  `kdef.js`（チップ名と、§82の総入れ替えで **77→100件**）。product は正しくテストが古いだけ。

### 88. 一覧カード：写真を横長に・上下2段組み・写真の取り込みが効かない件（2026-08-13g）
指摘は2つ。①写真の長押しで**iPhoneの「共有／“写真”に保存／コピー」**が出て、新しい写真を
添付できない ②写真が細長い（現場写真はタテで撮らない）・枠の使い方が悪い・既存防水の名前が
見切れる・メモが無駄に長い。

- **★①の原因は iOS の長押しメニュー**（§29・§31と同じ罠）。画像の上で長押しすると
  Safari が自前のメニューを出し、こちらの長押しを横取りする。
  → `.pph` と中の `img` に **`-webkit-touch-callout:none`＋`user-select:none`**、
  `img` に `draggable="false"`／`oncontextmenu="return false"`／`pointer-events:none`。
  ★**さらに逃げ道を作った**：写真の下の帯（📷 写真を変える）は**1回タップで写真を選べる**
  （`pointerdown` の y が下20px以内なら即 `nnPhotoPick`）。長押しが効かない端末でも必ず添付できる。
  ★`-webkit-touch-callout` は**ヘッドレスの getComputedStyle に出てこない**ので、
    検証はCSSの文字列で見ること（§29と同じ。card4chk.js で対応済み）。
- **②レイアウトを2段組みに**
  ・上段（`.rhead`）＝**現場名＋ステータス＋利益予定額**。その下に**ステータス色の境界線**（2px）。
  ・下段＝元請・請負単価・請負金額・材料金額・工賃・他／住所・日付／工法・数量・記号・メーカー／
    不具合タグ／メモ。
  ・**写真を横長に**（PC 185→**258px幅**・高さ118px以上／スマホ 118→**172px**）。
    これで既存防水の一番長い名前「露出アスファルト防水」も見切れない。
  ・**メモは幅を抑えた**（PC 最大340px・高さ30px以上）。スマホは幅いっぱいのまま背だけ低く。
- 検証は `scratchpad/card4chk.js`（横長・同じ行・境界線・見切れ0・メモ幅・帯タップ・長押し・
  callout指定・はみ出し0。PC／スマホ）。

### 89. 写真の取り込みが効かない件の決着・チップ縮小・並び替え・提出書類（2026-08-13h）

**★写真が入らない本当の原因（2回外していた）**
`-webkit-touch-callout:none` を入れても直らなかった。真因は別で、
**iOSは「利用者の操作の中」でしか写真の選択窓を開けない**こと。
長押しの `setTimeout`（0.5秒後）から `input.click()` を呼んでも、その時点では
操作の外なので**ブラウザに無視される**。＝どれだけ長押しを直しても開かなかった。
- **直し方＝本物の `<input type="file">` と `<label for>` をカードの中に置く。**
  ブラウザ自身が開くのでJSを通さず、確実に動く（`nnPhotoSet(pid, inp)` は結果を受け取るだけ）。
- 長押しも残したが、**開くのは「指を離した瞬間」**（pointerup＝操作の中）に変更。
  長押し中はタイマーで印を付けるだけにして、離したときに `inp.click()` を呼ぶ。
- ★**JSから選択窓・カメラ・全画面・音を出すときは、必ず click/pointerup など
  「操作そのもの」のハンドラの中で呼ぶこと。** setTimeout をまたぐと無効になる。
- ★検証の注意：`HTMLInputElement.prototype.click` を見張る方法では**labelは検出できない**
  （ブラウザが内部で開くため）。label と input の結び付きで確かめること（card4chk.js を修正）。

**チップが大きすぎて見切れる** → `#toolbar .chips .stchip` を 13.5→11.5px（スマホ10.5px）、
padding も縮小。8個が1行に収まり、件数の札とも重ならない。

**並び替えのUIを作り直した**（「本当に効率的か考えて」との問い）
- 以前は「利益」チップに利益2種だけ＝**並び替えという機能自体が見つけにくかった**。
- チップ名を **「並び替え」** に変え、選ぶと **「並び：受注金額」** のようにいま何順かを出す。
  絞り込み（どれを見るか）は複数選択＋件数、並び替え（どの順に見るか）は単一選択、と役割を分けた。
- 中身は7種：日付が新しい順／古い順／受注金額／利益額／利益率／数量／物件名の五十音順。

**カード右端に「提出書類」**（`.pdocs`）：見積・報告書・比較表・他資料の4つ。
- 報告書＝既存の `exportPDF(pid)`、他資料＝物件詳細の「保管資料」タブへ。
  見積・比較表は準備中（押すと何を作る予定かをトーストで出す）。
- スマホでは下の行に横並びで入る。

### 90. 写真がクリックで入らない件の真因・レイアウト調整・ツールバーのアイコン4枚（2026-08-13i）

**★写真がクリックで入らない真因（3回目でようやく特定）**
`<label for>` 方式にしたのは正しかったが、**自分で入れた `e.preventDefault()` が
label 本来の「選択窓を開く」まで打ち消していた**。
- 写真の帯を押したとき `window._nnPhotoLP=true` を立て、あとの capture click で
  `stopPropagation()＋preventDefault()` していた。**preventDefault は絶対にしないこと。**
- ★**さらにもう1段あった**：カードの選択（arm）は **document の capture** で受けており
  （`#list` ではない）、**あとから足した見張りより先に動く**。
  後から `stopPropagation()` しても手遅れなので、**arm する側に「これは操作するものだから
  カードの選択をしない」除外を書く**のが正しい（`.phhint/.phinp/.ebox/.stsel/.pdocs`）。
- ★**除外を `.pph`（写真ぜんぶ）まで広げてはいけない**。写真をタップして
  カードを開く動き（2回タップ）まで消える（実際に消して回帰6件が落ちた）。
  帯（`.phhint`）と入力欄（`.phinp`）だけにすること。
- ★同じ理由で、capture click の `stopPropagation()` も帯だけに絞る。
  `.pph` 全体で止めると、**bubble 側の移動処理まで届かなくなる**（document capture で
  止めると target・bubble のどちらにも届かない）。

**レイアウト**
- `.pbd` の `justify-content:center` をやめて `flex-start`＋上の padding 7→3px
  （物件名の上の余白が無駄だった）。
- **メモを不具合タグの右へ**（`.rtag` の中に移動）。上限460px。

**ツールバーのアイコン4枚**（`icons/btn_*.png`。§86の受け皿にそのまま入る）
- `btn_ang`（紫の∠＝角度）／`btn_draw`（青の鉛筆＝描画）／
  `btn_rect`（水色の破線の四角＝範囲選択）／`btn_pan`（黄の四方向矢印＝移動）。
- ★4枚中3枚は webp 変換で**白い地が焼き込まれていた**ので `dechecker.py` で塗りつぶして透過に戻した
  （市松模様でなく白でも同じ手が使える）。sw.js の ASSETS にも追加。
- 検証は `scratchpad/photo2.js`（帯のクリックが打ち消されない・写真が入る・カードが選択されない・
  物件名の上の余白・メモの位置・アイコン4枚）。

### 91. ★倍率(zoom)まわりの取りこぼし2件を一掃（2026-08-13j）
「絞り込みメニューが謎の場所に出る」「クリックすると関係ない右下が光る（ずっと続いている）」の2件は
**どちらも §61 の同じ落とし穴**だった。**`getBoundingClientRect()`／`clientX,clientY` は
倍率を「かけた後」のpx、`style.left/top`／`offsetWidth`／`getComputedStyle` は「かける前」のpx。**
混ぜると、大きいモニター（倍率1.2〜1.6）ほど大きくずれる。

- **絞り込みメニュー（`nnFitMenu`）**：rect で計算した位置をそのまま `style.top/left` に入れていた。
  → 「かけた後のpx」で計算し、**最後に倍率で割って返す**。`m.offsetWidth`／`m.scrollHeight` は
  かける前なので**倍率を掛けてから**比較する。`maxHeight` は CSS 値なので割って入れる。
  実測：1600/2000/2560px のどれでも チップとの横ズレ0px・縦4px。ダッシュボードも同じ。
- **タップ発光（`glow(x,y)`）**：`clientX/Y` をそのまま `style.left/top` に入れていた。
  → `/(window.nnPZ||1)`。**全10ページ**に同じコードがあるので一括で直した（現場マップには無い）。
  実測：5ページ×3サイズで押した場所とのズレ0px。
- ★**この形のコードを見たら必ず疑うこと**：`style.left/top = rect や clientX の値`。
  すでに §61（--nnvh）・§64（canvas）・§65（パネルの並べ替え）でも同じ罠を踏んでいる。

**タグの文字を大きく（枠は据え置き）**
- ★**不具合タグの実体は `.defchip`**（`.dtag` は別の機能で、当てても効かない）。
  12→13.5px にし、**padding を 2px→1px に詰めて枠の高さを据え置き**（実測22px）。
- 記号バッジ・メーカー・ステータス・日付・メモ・既存の状態も 12→12.5px に。

**写真の下のプルダウンが大きすぎる**
- `#list .pcard .ebox select` が 16px だった。★§50 のとおり **`<select>` は16pxにする必要がない**
  （キーボードが出ないのでiPhoneは拡大しない）。12px（既存の状態は11px）に下げ、
  開いた一覧が画面を覆う問題も解消。

### 92. 一覧の写真：＋で拡大・ドラッグで挿入・写真は詳細へ飛ばさない（2026-08-13k）
- **写真が入っているとき**＝右上に **＋**（`.phzoom`）。**1クリックで拡大表示**（`nnPhotoZoom`＝
  全画面のライトボックス。背景タップ・✕・Escで閉じる）。案内は「📷 写真を変える」。
- **写真が無いとき**（見本のまま）＝**＋を出さない**。写真ぜんぶが「入れる」入口で、
  破線で囲って薄く表示し、案内は「📷 写真を入れる」。
- **写真ぜんぶが `<label class="phfull">`**（絶対配置で全面を覆う・z-index:1）。
  **＋はその上（z-index:3）** に置くので、＋を押しても選択窓は開かない。
- **ドラッグ＆ドロップ**でも入る（`dragover/drop` を document で受け、`.pph` の上だけ処理）。
  ドラッグ中は枠が光る（`.drop`）。画像以外を落としたら断る。
- **★写真は物件の選択・詳細への移動に使わない**（本人の指示）。カードの選択を受ける
  document capture の受け口から `.pph` を丸ごと除外した。
  ★このため**中立な場所が減る**ので、検証スクリプトのカード選択は
  **日付（`.pdate`）をタップ**する方式に変えた（`pwlist.js`／`edit1.js`）。
- ★**capture の `stopPropagation()` は目的の要素にも届かなくする**。
  写真まわりで止めていたら **＋の `onclick` が動かなかった**（実際にハマった）。
  カードの選択は「選択する側で除外」しているので、ここでは止めなくてよい。
- 長押しの仕掛けは廃止（1タップで開くようになったため）。
- 検証は `scratchpad/photo4.js`（14項目：＋の有無・拡大・案内文・選択されない・
  2回タップで飛ばない・入れ替え・ドラッグ＆ドロップ。PC／スマホ）。

### 93. 図面・積算：始点・角の「真上・真下・真横」に自動でそろう（2026-08-13m）★閉じられない欠陥
「スマホで図面作成する際、最後の終点に合わせたいのに、垂直方向のところに止まらない。
これは欠陥」との指摘。**そのとおりの欠陥だった。**

- **★原因は §57（角度5度・長さ10cmきざみ）に「相手にそろえる」考えが無かったこと。**
  打点は**直前の点から見た向きと長さ**だけで決まるので、
  「始点の真下（＝x が始点とぴったり同じ）」に止められる保証が**まったく無かった**。
  頂いた写真でも、最後の1点を合わせようとして 0.5m→0.4m→0.3m と刻んでいた。
  §56 で「直前の点の真下・真横」は入れたが、**相手が始点のときは何も無かった**。
- **直し方＝`nnAlignSnap`（`nnSnapPt` の最後で呼ぶ）**。CADの「オブジェクトスナップ追跡」と同じ考え方。
  ・そろえる相手＝**始点／作図中の他の点／既存の部位の角**。
    ★**直前の点は相手に入れない**（真下・真横は 90度・0度の角度きざみでもともと引けるし、
      入れると5度の線が勝手に垂直へ倒れる）。
  ・狙いが相手の「たての線（x=◯）／よこの線（y=◯）」から**画面12px以内**なら、その線に止める。
    ①**線と交わる向き**に引いているとき＝角度はそのまま・**長さだけ**合わせる
    ②**線とほぼ平行**（真下へ下ろす等・|方向成分|≦0.2）＝**横へ寄せて**線に載せる
  ・**★たてとよこ、両方に相手がいるときは「その交わる点」に置く**。ここが肝。
    片方だけ選ぶと、もう片方が数cmずれて**結局閉じない**。
    交わる点なら「いま引く辺は水平・閉じる辺は垂直」が同時に成り立つ。
  ・始点をやや優先（`pri`＝始点0／作図中1／既存2。score＝距離×(1+pri*0.15)）。
- ★**このときだけ「長さ10cmきざみ」を外れる**。狙ったところに止まることのほうが大事
  （AutoCAD等も同じで、スナップは増分より優先する）。
- **画面には紫の細い線**（相手までの点線＋相手に小さな四角＋「たてよこ そろえ」）を出す。
  なぜそこに止まったかが見えないと、勝手に動いたようにしか見えない。
  ★札は画面の端で切れるので `cv.width/devicePixelRatio` で内側に寄せること。
  ★スマホは下の案内文の1行がぎりぎりなので、そろえの説明は**パソコンのときだけ**足す。
- ★**`nnAlignHit` は `var` で宣言する**。`draw()` はこの行より前にあるので、
  `let` だと（先に呼ばれたとき）TDZで落ちる。
- ★**テストが古くなる**（§68）：`ang5.js` は `drawPts=[{x:0,y:0},{x:2.4,y:1.3}]` と
  **始点が狙いのすぐ近く**にあったため、そろえが効いて「長さが0.1mの倍数でない」と出た。
  product は正しい。**始点を (-60,-60) へ動かして「角度・長さのきざみだけ」を見るテストに直した**
  （そろえの動きは新しい `align1.js` で見る）。
- 検証は `scratchpad/align1.js`（16項目：始点の真下／真横・平行に下ろす場合・既存の角・
  遠いときは今までどおり・閉じる磁石が優先・1点目は交差点・紫の目印・**実打で最後の点が x=0**。
  PC／スマホたての両方）。見た目は `alignshot.js`。
  回帰は zumen 18本＋記録帳3本とも★NGなし。

### 94. 現場記録帳：工法アイコン枠・メーカー省略名・保管写真10枚・提出書類3つ追加（2026-08-13n）
本人からレイアウトの指示画像（赤字の書き込み）で4件。

- **①工法アイコンの画像枠**（`<style id="nn-kicon">`＋`nnKiconHtml`）。現場名の行・ステータスの右に
  54×34px（スマホ40×26）の枠を置いた。絵はこれから作るので、いまは**点線の枠に工法名**だけ。
  ★**版nでは仕様記号（AS-J1）でファイル名を作っていたが、これは取り違え**
    （本人の指摘「AS-J1のアイコンでなく、常温粘着工法のアイコンです」）。
    **アイコンは「工法」のもの**なので、記号が違っても同じ工法なら同じ絵を使う
    （AS-T1 と AS-T2 はどちらもトーチ工法＝1枚でよい）。
  **置き場所＝`icons/kou_<工法キー>.png`**。背景透過PNG・高さ168px・幅は絵なり。
  キーは工法名から自動で決まる（`KOU_RULES`。名前に「トーチ」「通気緩衝」等が入っていれば当たるので、
  工法を増やしても直さなくてよい）。**いま要るのは7枚**：

  | ファイル | 工法 | 記号 |
  |---|---|---|
  | `kou_nenchaku.png` | 常温粘着工法 | AS-J1 |
  | `kou_torch.png` | トーチ工法 | AS-T1・AS-T2 |
  | `kou_ure_tsuki.png` | ウレタン 通気緩衝工法 | X-1 |
  | `kou_ure_micchaku.png` | ウレタン 密着工法 | X-2 |
  | `kou_ure_fukitsuke.png` | 超速硬化ウレタン 吹付工法 | X-2H |
  | `kou_enbi_kikai.png` | 塩ビシート 機械固定 | S-M1・S-M2 |
  | `kou_frp.png` | FRP防水 | L-FF |

  （将来ぶん：`kou_netsu.png` 熱工法／`kou_enbi_setchaku.png` 接着工法／`kou_sealing.png` シーリング）
  ★**絵があるかは「キーごとに1回だけ」確かめる**（`KI_STATE`＋`kiProbe`）。
    カードに `<img>` を直接置くと、絵が無い間**100件ぶん404を取りに行く**。キーは7種類なので
    7回だけ確かめ、あったキーだけ絵にして `render()` し直す。
- **②メーカーはすべて省略名**（田島ルーフィング→**田島**／ディックプルーフィング→**ディック**／
  アーキヤマデ→**ヤマデ**／AGCポリマー建材→**AGC**／ロンシール工業→**ロンシール**）。
  ★**表示のたびに変換するのではなく、データそのもの（`p.maker`）を省略名にした。**
    表示だけ変えると、絞り込みメニューの中身（`p[k]` を直に見ている）と食い違う。
    **正式名は `p.makerFull` に残し、元請へ出す「物件報告書」だけは正式名**を使う。
  対応表は `MK_ABBR`＋`mkShort()`（props を作る直前）。**メーカーを増やしたらここに1行足すこと。**
  ★施工中の表にあった**ローカルの `MK_ABBR` は削除**（props 側が短くなったので二重変換は不要）。
- **③保管済みの写真を一覧に出す（パソコンのみ・10枚まで）**（`nnShotRow`／`nnShotList`／`nnShotZoom`）。
  ★**版nは高さ26pxの「帯」にしてしまい、やり直しになった**（本人の指摘
    「なぜこんなに写真10枚が帯みたいな長方形なの？ なぜ枠いっぱいに高さを調整しないの？」）。
    **目的は「トップ画像だけでなく、他の候補写真がどうなのかすぐ視認できること」**。
    中身が見えない大きさでは目的を果たさない。→ 1枚ずつを **`aspect-ratio:4/3`**（実測 129×97px）に
    して、カードの高さを使い切る（カードの背は245→370pxになるが、それが目的）。
  ★**大きさを決めるときは「何のために見せるのか」から決めること。** 空いた場所に収めるのが目的ではない。
  カードの現場写真＋「写真」タブの写真を集めて5×2の枠に。**写真があれば押すと拡大**、
  **空き枠は押すと写真を選ぶ窓**（`nnShotAdd`→既存の `pickFiles`）。右の**「他」で写真タブ**へ。
  ★**空き枠から詳細ページへ飛ばさない**こと（§92の指示「画像は画像で、詳細に飛ばさない」）。
    飛ばすのは「他」ボタンだけ。
  ★`.pshots` は**カードの選択を受ける document capture の除外リストに足す**こと。
    ここで `stopPropagation` してはいけない（§92：目的の onclick にも届かなくなる）。
- **④提出書類に 図面／写真台帳／施工要領書 を追加**（2段になって計7つ）。
  図面＝作成図面タブ／写真台帳・施工要領書＝準備中のトースト（中身は今後）。
- **⑤一覧カードの「押すと沈む」演出を廃止**（本人の指摘「ものすごく沈み込んだような動き。気持ち悪い」）。
  ★原因は `.pcard:active{transform:translateY(1px) scale(.985)}`。**カードが小さかったころの指定**で、
  いまは横1900px・縦370pxあるので、押すたびに**全体が横28px縮んで「ぐにゃっ」と沈んで**いた。
  写真を押しても親のカードが縮むので同じ動きになる。
  → `#list .pcard` では **transform を none** にし、代わりに**内側の細い枠**（`box-shadow:inset`）で
  手応えを出す（位置は1pxも動かない）。選択中（`.armed`）の浮き上がりも廃止（黄色の枠と地色で十分）。
  ★**大きい要素に `scale()` を掛けないこと。** 縮む量は「大きさ×率」なので、
  同じ指定でも小さいボタンでは1px、大きいカードでは数十pxになる。
- **⑥メインの現場写真が縦長になった**（版pの副作用。本人の指摘「なんでメインの画像が縦長になるんだよ」）。
  ★原因は `.pph{flex:1 1 auto}`。**カードが高くなるほど写真の枠も縦に伸び**、
  横長の現場写真が `object-fit:cover` で上下だけ残して切り取られていた。
  → パソコンでは **`flex:0 0 auto` ＋ `aspect-ratio:4/3` で写真の形に固定**（300×225px）。
  ★**入れ物の高さを中身に合わせるのではなく、写真の形を先に決めること。**
  ★列の幅は **300px**（実測で決めた）。340pxにすると、そのぶん本文が狭くなって
    1行目（元請〜他の6項目）が**2段に折り返す**（1600・1920px幅で確認）。
  写真の下に残る余白は「既存の状態」の3行（既存・区分・構造）を伸ばして使い切る。
- 検証は `scratchpad/kai1chk.js`（PC 22項目／スマホ 19項目：省略名・絞り込みメニュー・枠の大きさと
  点線・絵が来たら差し替わる・確かめるのは9回だけ・写真10枚＋他・空き枠で窓が開く・詳細へ飛ばない・
  提出書類7つ・横はみ出し0）。見た目は `kai1shot.js`。
  ★**テストが古くなった2件**（§68）：`edit1.js` の「メーカーも変わる＝アーキヤマデ」→**ヤマデ**、
    `card5chk.js` の「4つのアイコン」→**7つ**。どちらも product は正しい。

### 95. 反応の速さ／よこ向きスマホでダッシュボードが見切れる（2026-08-14a）★速度の決着その2
指摘は2つ。①スマホ版の上のタグが見切れている ②ボタン押してからの反応が全体的に遅い（PC・スマホとも）。

**①見切れ＝ノッチの逃げが効いていない画面があった**
- ★**原因**：ノッチの逃げ（`nn-safe-x`）は **`main`** に付けていたが、
  **ダッシュボードと工程表は `main` の外**（`#dashboard`／`#schedview`）にあるため、
  逃げが1ptも効いていなかった。実測で KPIカードの左端が **14px**（ノッチは68px）＝35px潜っていた。
  さらに §83b で `main` の逃げを4割に減らしたため、一覧以外がよけい目立っていた。
  → `#dashboard`／`#schedview` に**丸ごとの逃げ**を足した（中の帯は二重に逃がさない）。
- ★**帯（`.stbar`）が横に見切れる**のは `flex-wrap:nowrap; overflow-x:auto` のため。
  スマホでは**折り返し可**に戻した（メニューは `#nnPortal` に出すので閉じ込めの心配はない・§4）。
  あわせてチップ名を **「〇〇別」→「〇〇」**（現場一覧と統一・§89）。
  実測：帯の中身 1216px→830px＝**はみ出し0**。
- ★**画面の外にある画面にも余白の指定が要る**。`main` だけ直して満足しないこと。

**②遅さ＝100件のカードの「組み立て」だった**
- ★**まず測る**（CPU4倍の負荷で）：`render()`（100件）に **1151ms**。
  内訳は **HTMLを作るのが17ms／画面の組み立て（レイアウト）が835ms**。
  ＝文字を作るのは速い。**100枚ぶんの箱の位置を全部計算するのが遅い。**
- **直し方＝`content-visibility:auto`＋`contain-intrinsic-size`**（`#list .pcard`）。
  画面に映っていないカードは組み立てを後回しにする。スクロールしたぶんだけ組み立てる。
  | | 前 | 後 |
  |---|---|---|
  | 現場一覧を開く | 1270ms | **195ms** |
  | 一覧の作り直し | 981ms | **125ms** |
  | 検索で1文字 | 949ms | **1ms**（打っている間は待たせない） |
- ★**`flex:0 0 auto` を一緒に付けること。** `#list` は「たてに並べる flex」なので、
  後回しにしたカードは中身が空っぽ扱いになり、**縮められて高さ2pxまで潰れる**。
  そのせいでスクロールバーが実際の1/12の長さになっていた（実測 2913px→**37693px**）。
- ★**「たぶんこの高さ」は実測に合わせる**（PC 370px／スマホ 220px）。
  ズレているとスクロール中にバーの長さが変わる。`auto` を付けておくと一度見た実寸を覚える。
- ★**印刷のときは切ること**（`@media print{content-visibility:visible}`）。
  切らないと、見えていない部分が白紙で出る。
- 検索欄は1文字ごとに作り直していた → **`nnRenderSoon()`**（0.12秒後にまとめて1回）。
- ★**画像は「見えたときに読む」ようになる**（offscreen は読み込まない）。
  検証で「写真が全カードで読めている」を見ていた `pwlist.js` は、**映っているカードだけ**見るよう直した。
- 他のページ（ライブラリ・用語集・仕様登録・材料登録ほか）は DOM が100〜1600節点で軽く、
  この対策は不要だった（現場記録帳だけ **10727節点**と桁違い）。
- 検証は `scratchpad/fast1.js`（`node fast1.js` ／ `node fast1.js land`。
  ★land は **`env(safe-area-inset-*)` を59pxに置換したコピー `_land.html`** を作ってから流すこと・§7）。
  速さの内訳を測るのは `perf1.js`／`perf2.js`（HTML作りとレイアウトの切り分け）。

### 96. 覚えのない「写真の選択画面」が出る（2026-08-14b）★誤タップ
ホーム画面の上に iPhone の「写真ライブラリ／写真を撮る／ファイルを選択」が出ている写真が届いた。

- ★**原因は現場一覧の現場写真**。§89 で「クリックで写真が入らない」を直したとき、
  **写真ぜんぶを `<label for>` にした**（ブラウザ自身が選択窓を開く＝確実に動く作り）。
  そのため**指が少し当たっただけで選択窓が開く**。開いたあと別のページへ移っても
  iPhone のこの画面は残るので、ホームの上に出ているように見えていた。
  ★調べ方：`grep 'type="file"' *.html`。ホーム（index.html）の入力欄は JSON 用で
    accept が `.json` なので、写真の選択画面は出ない＝別のページから来たと分かる。
    ほかのページの写真入力は全部 `display:none`＋ボタンから開く作りで、誤タップの余地がない。
- **直し方（スマホだけ）**：`label` を**下の帯（📷 写真を入れる／変える）だけ**に縮めた
  （`top:auto; bottom:0; height:30px`）。写真そのものをタップしたときは
  **写真が入っていれば大きく表示、入っていなければ何も起きない**。
  帯の文字も 9.5px→11px に大きくして押しやすくした。
  ★**パソコンは今までどおり写真ぜんぶで開く**（マウスの「押す」ははっきりしているため）。
- ★**「1タップで確実に開く」と「誤タップで開かない」は両立しない。**
  どちらを取るかは端末で分ける（指＝狭い的／マウス＝広い的）。
- 検証は `scratchpad/phtap.js`（`node phtap.js`／`node phtap.js pc`。
  写真の真ん中と下の帯をそれぞれ叩いて、`elementFromPoint` が label かどうかで判定する）。

### 97. 検索の帯が消える／進捗バー／写真を小さく（2026-08-14c）
指示画像（赤字の書き込み）で3件。

- **★①検索・絞り込みの帯が出てこない**（「スマホ版同様に、検索タグを入れなさい」）。
  ★**帯は最初から在る。いちど物件の詳細を開くと、二度と戻らなくなっていた**のが正体。
  `body.nn-detail` が付くと `#toolbar{display:none !important}`（詳細では邪魔なので隠す指定）。
  ところが `showView('list')` は `#mainview` の `show-detail` しか外しておらず、
  **`body.nn-detail` を外していなかった**ので、タブで一覧に戻っても帯が隠れたままだった。
  → `showView()` の中で必ず `body.nn-detail` を外す。
  ★**「2か所に状態を持つ」と必ずこうなる。** 片方だけ戻す書き方をしないこと。
  ★再現手順：一覧 → 物件を2回タップ（詳細）→ タブ「現場一覧」→ 帯が消えたまま。
- **②進捗バーを現場名の行に追加**（`.pprog`。ダッシュボードと同じ黒い筒＋光沢＝§55の見た目）。
  工法アイコンの右、190×20px（スマホ 120×17）。中身は `p.prog`。
  **完成済はデータが100なので自動的に満タン**（サンプルは全件100）。施工中は実際の進捗（例55%）。
- **③写真を一回り小さく**（「物件ひとつで画面を取りすぎ」）。
  メイン写真 300×225 → **250×188**、保管写真の1枚 129×97 → **109×82**。
  **カードの高さ 368 → 338px**（1画面に2.3件）。スマホは215pxのまま。
  ★1行目（元請〜他）が折り返さないことを 1600/1999px で実測して決めた。
- 検証は `scratchpad/kai3chk.js`（PC 18項目／スマホ 14項目：詳細を開いて戻る・ダッシュボード経由でも
  戻る・バーの位置と100%・筒のグラデ・写真とカードの大きさ・折り返し・はみ出し0）。

### 98. 項目名が消える／写真の重複／日付の欄（2026-08-14d）
指摘は4件＋追加1件。

- **★①プルダウンを開くと左の項目名（既存・区分・構造）が消える。**
  原因は `startEdit` の **`box.innerHTML=''`**。囲みを作り直すときに、
  左の緑のラベル（`<i>`）ごと消していた。メモ欄（`<b>`）だけは残す作りになっていたのに、
  プルダウンと入力欄では残していなかった。
  → **`<i>` を取っておいて、入れ直してから `<select>`／`<input>` を足す**。
  ★**「作り直す前に、残すものを取っておく」**。innerHTML='' は中身を全部消す。
- **②メインの写真と、10枚の1枚目が同じだった** → `nnShotList` から**カードの現場写真を外した**。
  ここに並ぶのは「写真」タブに保管した写真だけ。
- **③④項目名を追加**：住所／改修仕様（工法）／数量。r1（元請〜他）だけラベルがあり、
  住所と工法の行だけ無かったので不ぞろいだった。
- **⑤着工日・完成日を追加**（`.pdate` を3つに）。契約（着手）／着工／完成。
  ★**完成していない物件は「完成予定」**にする。
  ★**`p.kansei` の有無で判定してはいけない**。施工中の物件にも `d.fb`（予定の完成日）が
  入っているので、全部「完成」と出てしまう。**`p.stRaw==='kan'` で判定**し、
  それ以外は `p.yotei`（完成予定）を優先して出す。
  日付は表示のみ（変更は「✎ 編集」フォームから）。
  ★`.pdate` は `.ebox` にしないこと。**カードを選ぶための中立な場所**として
    検証スクリプト（`pwlist.js`／`edit1.js`）がタップに使っている。
- 検証は `scratchpad/kai4chk.js`（PC 16項目／スマホ 14項目）。

### 99. 1物件のたてを0.8倍に／値が枠の上に寄る（2026-08-14e）
- **★たての長さを0.8倍**（よこはそのまま・**写真のたてよこ比は 4:3 のまま**）。
  実測 **338 → 275px**（1画面に2.7件）。
  ★**比率を変えずに高さを縮めるには、写真の「幅」を縮めるしかない。**
    メイン写真 250×188 → **200×150**／保管写真 109×82 → **80×60**（どちらも 4:3）。
  ★それでも足りない分は**写真ではなく「すき間」で詰める**
    （`.pbd` gap 4→2・padding 3/5→2/3、`.pleft` gap 4→2、メモの最低の高さ 30→24）。
    写真をこれ以上小さくすると、せっかく見やすくした保管写真が読めなくなる。
  ★**物件名の文字（18px）は縮めないこと。** §87 で「小さいので大きくして」と言われて上げたばかり。
    3px 稼ぐために戻すのは本末転倒（実際に一度16.5pxにして `exist1.js` が落ち、戻した）。
  ★**`contain-intrinsic-size` も直すこと**（370→280px）。カードの高さを変えたら、
    §95 の「たぶんこの高さ」もズレる（放っておくとスクロールバーの長さが狂う）。
- **★既存・区分・構造の値が枠の上に寄っていた**。`.ebox` は `align-items:stretch` なので、
  背の高い枠では文字が上端に付く。この3つだけ **`align-items:center`**（左の緑ラベルは
  `align-self:stretch` で上下いっぱいのまま）。実測 上11.6px・下12.2px＝ほぼ中央。

### 100. タブの余白・進行度の見出し・既存/区分/構造の絵の枠（2026-08-14f）
- **①上のタブ（見出しタグ）の上下の余白を詰めた**（パソコンだけ。スマホ版に寄せた）。
  `#viewtabs` padding 10px→3px・ボタンの上下 9px→5px・文字 14.5→14px。
  実測 **帯 55→33px／一覧の開始 153→131px**。
- **②進行度の見出し**をバーの上に（`.plbl`）。`.pprog` をたて並び（label＋bar）にした。
  高さ 20→31px だが、現場名の行（39px）に収まるのでカードは275pxのまま。
- **③既存・区分・構造にも絵の枠**（`.pexrow`＋`.exicon`。38px幅・点線の枠）。
  **置き場所＝`icons/<種類>_<キー>.png`**（背景透過PNG・高さ168px・幅は絵なり）。
  | 種類 | キー |
  |---|---|
  | `kizon_` | fumei／as_roshutsu／osae／kaishitsu／enbi／gomu／ure／frp／kinzoku／mortar／shinsetsu |
  | `kind_` | shinchiku／kaishu／zoukaichiku／bubun |
  | `kouzou_` | rc／s／alc／w／rcs |
  （例：塩ビシート防水＝`icons/kizon_enbi.png`／改修＝`icons/kind_kaishu.png`／RC＝`icons/kouzou_rc.png`）
  ★工法アイコンと同じく**キーごとに1回だけ**絵の有無を確かめる（`EX_STATE`／`exProbe`）。
    画面に出ている種類ぶんしか取りに行かない（実測3種）。
- 検証は `scratchpad/kai5chk.js`（PC 12項目／スマホ 11項目）。

### 101. 新規物件登録フォームを一覧の中身に合わせた（2026-08-14g）
「新規物件作成の項目と、実際の物件内容に不一致・不要項目・必要項目の欠如がある」との指摘。
実データと突き合わせて調べたら、**本当に3種類のズレ**があった。

- **★①工法の一覧が実データと食い違っていた（いちばん重い不具合）。**
  `KO_MASTER`（フォームで選べる工法）に **S-M1／X-2H／AS-T1 の3つが無かった**のに、
  実データ（RAW）では **20件**がその工法を使っていた。
  そのため**その物件を「✎ 編集」で開くと、工法が勝手に別のものへすり替わり**、
  保存すると記号（AS-T1 等）もメーカーも書き換わっていた。
  → KO_MASTER に3つ追加（メーカー・主材は実データに合わせた）。
  ★**フォームの `<option>` は手書きをやめ、`KO_MASTER` から毎回作る**ようにした
    （手書きの一覧はまた食い違う）。編集で開いた物件の工法が一覧に無いときは、その工法も足して選択を保つ。
  ★**RAW に工法を増やしたら KO_MASTER にも足すこと。**
- **②一覧で選べるのにフォームに無かった3項目を追加**：**既存防水・区分・構造体**。
  今までは新規作成すると `kind:'改修'` 固定、既存防水と構造体は空（表示だけ「不明」「RC」）だった。
  中身は一覧カードと同じ `NN_KIZON`／`NN_KIND`／`NN_KOUZOU` から作るので、ズレようがない。
- **③不具合・現場条件タグの入口**を「現場情報」に追加（`nnFormDefEdit`）。
  一覧・詳細と同じ編集画面を開く。**新規（未保存）のときは押せない**
  （物件IDが無いとタグを紐づけられないため。ボタンの文言も「保存したあとに選べます」に変わる）。
- 見出しの文言も一覧に合わせた（「防水仕様」→**「改修仕様（工法）」**）。
- ★**不要な項目は無かった**。締め処理日・入金予定日・支払条件は入金予定パネル、
  実績4費目は予実、投入人工は歩掛で使っている。消すと別の画面が壊れる。
- 検証は `scratchpad/formchk.js`（22項目：実データの工法が全部選べる・追加3項目の中身と保存・
  新規登録した物件が一覧に正しく出る・編集で開いても工法がすり替わらない）。

### 102. スマホの絞り込みの帯が2段になり、あいだに大きな空白（2026-08-14h）
「なぜ上の検索タグと日付が二段構成になっている？ もっと画面いっぱいに使え」との指摘。

- ★**原因は `.stbar .rng{margin-left:auto}`**（期間の組を右端へ押しやる指定）。
  実測でその余白は **288px**。§95 で帯を折り返し可にしたので、
  押しやられた期間の組が2段目に落ち、1段目の右半分と2段目の左半分が空白になっていた。
- 直し方（スマホだけ）：`margin-left:0` にして**チップのすぐ右に続ける**。
  あわせて日付の欄 172→**104px**、チップ 13.5→12px、帯の余白も詰めた。
  実測：中身 **985px → 795px**（使える幅812px）＝**よこ向きで1段に収まる**。
  **帯の厚み 66→35px**（KPIカードが33px上がった）。
- ★**日付の欄（`input[type=date]`）は16px未満でよい。** iPhoneは日付をホイールで選ぶので、
  文字入力の欄と違って画面が勝手に拡大しない（§50 の `<select>` と同じ理由）。
- ★パソコンは今までどおり右寄せのまま（幅に余裕があり、詰める必要がない）。
- 検証は `scratchpad/fast1.js land` に4項目追加（1段・厚み・空白・margin-left）と `stbar1.js`（実測）。

### 103. 新規物件登録の窓が画面からはみ出す／見出しタグの文字（2026-08-14i）
「文字サイズがでかすぎる。入力枠でスペース取りすぎ。見切れていて物件名が入力できない」
「見出しタグの中の文字と枠までの余白が無駄。もっと文字を大きく」との指摘。

- **★物件名が入力できない原因は `max-height:88vh` に倍率（zoom）が二重にかかること**（§61）。
  実測：2000×1010の画面（倍率1.25）で **88vh=888px × 1.25 = 1111px**＝画面より高い。
  窓は真ん中ぞろえなので **上が −50px にはみ出し**、そこにある「工事名」に指が届かなかった。
  → **①`max-height:calc(92vh / var(--nnpzr,1))`**（倍率で割る。`--nnpzr` は `nn-pczoom-js` が入れている）
    **②窓の外側（`#modalbg`）も `overflow-y:auto` ＋ 上ぞろえ**にして、
    どんな高さでも上から読める（`margin:auto` なので入るときは今までどおり中央）。
  ★**`vh`／`vw` を使う指定は、zoom を掛けているページでは必ず倍率で割ること。**
    `getBoundingClientRect` と同じ落とし穴（§61）。
- **パソコンだけ文字と枠を詰めた**：入力欄 16→**14px**・高さ 35→**28px**、
  ラベル 14→12px、見出し 16→14.5px、余白も圧縮。
  ★**16px の指定（iPhoneの自動拡大よけ・§50）は `!important` なので、
    パソコン側も `!important` で上書きしないと効かない。** スマホは16pxのまま。
- **見出しタグ（上のタブ）**：枠は小さいまま、**文字 14→17px**・左右の余白 20→11px。
  実測 ボタン 147×32 →**151×30**（文字だけ大きく、箱はむしろ小さい）。
- 検証は `scratchpad/formchk.js` に5項目追加（はみ出し・見出しと工事名が見える・入力欄14px・高さ）と
  `modalchk.js`（1366/1600/2000で実測）／`modalph.js`（スマホは16pxのまま）。

### 104. よこ向きスマホの左右の余白を全ページで4割に（2026-08-14j）
「ダッシュボードの左右に無駄な余白。ホーム以外のすべてのメイン機能で見直せ。
特に発注・カメラ・ライブラリは余白多すぎ」との指摘。

- ★**余白の正体はノッチの逃げ（`nn-safe-x`）が「丸ごと（59pt×--nnsaf）」だったこと。**
  左右あわせて **約140px** が常に空白になっていた。§83b で現場一覧だけ4割にしたが、
  他のページ・ダッシュボードは丸ごとのままだった。
- **全10ページ（ホーム以外）の `nn-safe-x` の逃げを 0.42倍に**（main と帯の両方）。
  ★**下部ナビ（nav）だけは丸ごとのまま**（丸角・ホームバーに近いため。
    media query の閉じかっこの前に nav 専用の丸ごと指定を足し直した）。
  ★kirokucho は §95 で足した `#dashboard`／`#schedview` の丸ごと逃げも 0.42 に。
  実測（よこ向き852pt）：本文の左端 69〜83px → **35〜46px**（ダッシュボードのKPIは 74→35px）。
  絞り込みチップ＋期間も1段に収まり、KPIカードが画面幅いっぱいに。
- ★**スクロールする中身は4割で丸角にかからない**（§83bで実証済み）。ヘッダーの逃げは別の
  ブロックが持っているので触っていない。
- ★図面・積算のよこ向きで body の scrollWidth が361pxはみ出て見えるのは**元からの仕様**
  （#side＝画面外に隠してある引き出し。変更前後で同一なのを確認済み）。
- 検証は `scratchpad/pad10.js`（10ページの左右余白を実測）・`allpages2.js`（全ページ×両向きで
  JSエラー・はみ出し）・`fast1.js land`（期待値を「余白は saf×0.3〜0.75」に更新）。

### 105. 自主点検で見つけた2件（天気-10%・発注の現場が旧ダミー）（2026-08-14k）
「まだ改善の余地が大量にあるはず。画面を見ながら修正しなさい」との指示。
全ページをたて・よこで撮影して見比べ、実害のある2件を直した。

- **①工程表の天気に「-10%」が出る**（降水確率がマイナス）。
  ★原因：`wxHash` は `>>>0` で符号なしにしてあるが、そのあとの **`>>`（符号ありシフト）** で
  h が 2^31 を超えると負に戻り、`%2` が **-1** を返して 0+(-1)×10=-10 になっていた。
  → シフトを全部 `>>>` に（降水・最高気温・最低気温の3か所）。
  ★**`>>>0` した値でも、その後の `>>` でまた符号が戻る。** ハッシュ値のシフトは必ず `>>>`。
  検証：672日×2都市の全組み合わせで異常値0件（`scratchpad/wxchk.js`）。
- **②発注ページの現場が旧ダミーのまま**（「〇〇第2ビル」「◇◇工務店」等）。
  §82 で現場記録帳を100件に総入れ替えしたのに、発注の現場3件が置き去りだった
  （「現場記録帳がベース」という本人の方針に反する）。
  → 記録帳の実データに同期：g1=サン太平レジデンス（施工中・X-1 844㎡・管理組合直）／
  g2=発寒南倉庫（契約済・S-M2 980㎡・丸彦渡辺建設）／g3=篠路工場（見積済・回答待ち・岩倉建設）。
  ★材料明細（items）はそのまま＝g1のウレタン材・g2の塩ビ材と工法が合う物件を選んである。
  ★状態の名前も記録帳に統一（受注済→契約済／見積中→見積済）。判定式2か所も追従。
  ★自社情報の既定を「（株）〇〇防水工業」→**株式会社三浦工業**に（全ページのヘッダーと同じ会社）。
    保存済みが旧の見本のままの端末は、読み込み時に新しい既定へ置き換える。
  ★**記録帳の物件を変えたら、現場マップ（mapsync.py）と発注（GENBA）の両方を合わせること。**
- ★**「同期演出」ではなく本当に同じ物件になった**ので、デモで「記録帳のこの現場に発注する」
  という説明がそのまま通る。検証は `scratchpad/hacchk.js`（10項目：一致・発注フロー・再発注・自社名）。
- ほか、撮影で確認して問題なしと判断：詳細ページ（実タップでは正常。eval同期呼びだと
  ヘッドレスの描画が飛ぶことがあるが実機無関係）・ライブラリ／仕様材料／用語集のよこ向き。

### 106. カメラ：エッジ描きを作り直した（2026-08-14m）★「クリックした場所から始まらない」
mapry建築の「3Dスキャン→図面作成」の画像を見せられ、「今のは壊滅。そもそもクリックした
ところからエッジが始まらない。ちゃんと作りこんで」との指摘。

- **★打点がズレる原因は `cvPos` の `(clientX-左端)×DPR` 決め打ち**。
  パソコンは表示倍率（zoom・§61）がかかるため、倍率1.25なら25%右下にズレていた。
  → **`(clientX-左端)×(cv.width÷rect.width)`**（キャンバスの中の画素数÷見た目の大きさ）。
  倍率・DPR・縮尺のどれが変わっても必ず指した場所に打てる（図面・積算 §24 と同じ式）。
  ★**canvas を持つページはこの式以外を使わないこと。** DPR 決め打ちは zoom で必ず壊れる。
- **操作系を図面・積算と同じ作法に統一**（`wireCanvas`）：
  ・パソコン＝マウスを動かすと**予告線**（白い点線）→クリックで打点／**頂点はつかんで動かせる**／
    **右クリック＝1点戻す**
  ・スマホ＝**指の右上に赤い照準**（+36,-52）→動かして狙い→**離した位置に打点**（指で隠れない）。
    照準がキャンバス外なら打たない。頂点はつまんで修正。
  ・配線は要素につき1回（`cv._wired`）。resize で setupCanvas が再走しても二重にならない。
- **窓の大きさ・向きが変わったら、打った点とレイヤーも同じ割合で追従**（`oldW→新W` の比で座標を掛ける）。
- **線の見た目は mapry のスキャン風**：白のにじむ光（shadowBlur）＋白下地＋色の芯
  （作図中=赤／確定=黄）の3層。写真のどんな色の上でもくっきり浮かぶ。頂点は白リング＋色の芯。
- 検証は `scratchpad/camchk.js`（8項目：**倍率1.25のPCで打点誤差0px**・頂点ドラッグ+60,-40・
  右クリック戻し・確定→材料適用・スマホ照準の位置=打点・JSエラーなし）。
  見た目は `camshot2.js`（予告線・照準）。
- ★防水層の「絵」（Blender等で作る納まり部品）は今後。いまは手続き描画のまま
  （シートの折れ・ラップライン・塗膜・シール・金物は描ける）。

## 次にやること（3Dの続き・本人と相談済み）
- **②Blenderで「納まり部品」を作って `.glb` で同梱**（パラペット／入隅・出隅／ドレン／貫通／端末）。
  Blenderは**部品を作る工場**として使う。**その場で生成させない**（サーバーが要る・毎月お金がかかる）。
- 質感の写真（コンクリート・シートの目）を足すとさらに近づくが、ファイルが増えるので
  **3Dタブを開いたときだけ読む**こと。

## 最初の1回だけ遅い件（2026-07-30c 調査）
- 各HTMLは 61〜396KB の1ファイル（CSS・JS・データを全部内蔵）。
  **最初のタップでこれを丸ごと通信で取るので待つ。2回目以降はSafariの保存分で速い。**
- **仕方なくはない。`sw.js` は既に全10ページを保存一覧（ASSETS）に持っている。**
  各HTMLの `NN_USE_SW=false` を **true** にすれば、最初のページを開いた直後に
  裏側（別スレッド）で全ページを取りに行くので、初回タップも速くなる。
- ただし切り替えると**更新の届き方が変わる**（保存分を先に返す方式）。
  「直したのに変わってない」を何度も踏んでいる案件なので、
  **ユーザーの了解を取ってから切り替えること。** 版名4点セットの更新は必須。
- なお `index.html` だけ `<link rel="prefetch">` が無い（他9ページにはある）。
  ただしiOS Safariは prefetch をあまり効かせないので、本命はサービスワーカー。

---

# ★次のセッションへの引き継ぎ（2026-08-06c 時点）

## いま出せる「元請提出用」の書類は4種類
| 書類 | 出す場所 | 関数 |
|---|---|---|
| 平面図 | 図面・積算 →「📊 積算・設定」→ 📄 平面図 | `nnPlanPDF()` |
| 断面詳細図（パラペット納まり 1/5） | 同上 → 📐 断面詳細図 | `nnSectionPDF()` |
| 施工層構成図（アイソメ） | 同上 → 📦 施工層構成図 | `nnIsoPDF()` |
| 工程表（バーチャート） | 現場記録帳 → 施工中カードの「🖨 工程表PDF」／物件詳細の「工程」タブ | `nnKoteiPDF(pid)` |
- 4種とも **A3横・SVG・下の横帯の表題欄**で見た目をそろえてある（§42・§44）。
- 印刷の向きは §43 の作り（実寸 `@page` ＋ 案内の帯 ＋ 90°回転ボタン）。**4種すべて同じ**。
- 図面の入力（工事名・場所・元請・作成者・図面番号・北・用紙）は
  `localStorage('nn_zumen_plan_v1')` に保存。**工程表もここから作成者を読む**（同じサイトなので共有）。

## 公開範囲についての方針（2026-08-06 決定）
- **公開の場（SNS等）で機能を宣伝しない。** 買う人（元請・現場・材料屋・同業）に直接見せる方針。
- §45 のとおり **noindex を全ページに入れ、CLAUDE.md から個人的な記述を消した**。
- ★**このファイルは公開リポジトリに載る。個人情報・込み入った事情はここに書かないこと。**

## 本人の判断待ち（次のセッションで最初に聞くこと）
1. **コミット履歴の書き換え**（消す前の CLAUDE.md が過去のコミットに残っている）。強制pushで消せる。
2. **公開の置き場所をどうするか**。2案を提示済み。
   - GitHub Pro（$4/月・年払い$40＝**日本円で年6,000円前後**）→ リポジトリを非公開のまま Pages が使える。
     ただし**サイト自体はURLを知っていれば誰でも見られる**ままなので、隠す効果は半分。
   - **Cloudflare Pages（0円）へ引っ越し ＋ Cloudflare Access（無料枠50人）**
     → GitHubからも見えず、**サイトも許可した人しか開けない**。こちらが本命。
     ★ただし **URLが変わる＝localStorage が別扱いになり、保存データが消える**
     （工程表の編集・図面の入力欄・作図データ）。**先にデータの書き出し／読み込みを作ること。**
3. サービスワーカー（`NN_USE_SW=true`）に切り替えるか。**了解を取ってから**（§「最初の1回だけ遅い件」）。

## 次に作る予定（優先順）
1. ~~データの書き出し／読み込み（JSONファイル）~~ → **済（2026-08-06d・§46）**。ホームの「設定」から。
2. **配合計算**（主剤：硬化剤の比率 → 必要kg・セット数）。**単体で作らず、材料登録・仕様登録と繋げて
   「この現場の数量 → 必要セット数 → 発注」まで通すこと。** 単機能では差がつかない。
3. **施工計画書PDF** … 中身の8割は既にある（工事概要・数量・工法・材料・工程・体制）。
   元請が必ず要求する書類で、作るのが一番面倒＝価値が高い。
4. 以降の候補：使用材料一覧／材料承認願、施工要領書、火気使用届（トーチ工法は必須の現場が多い）、
   KY記録、施工写真台帳（カメラ画面と連携）、完成図、保証書、維持管理の手引き。

## まだ残っている宿題
- **現場マップの2Dピンのアイコン3枚**（§53）。`icons/pin_quote.png`／`pin_work.png`／`pin_done.png`。
- **不具合タグのアイコン24枚**（§47・佐野さんが自作して順次届く）。届いたら
  `icons/def_<キー>.png` に置くだけ（透過PNG・高さ168px・幅は絵なり）。キー一覧は§47。
- **画像がまだ来ていないページ**：用語集のタイトル `title_yougo.png` とアイコン `icon_yougo.png` の2枚だけ。
  届いたら「透明な余白を切り落とす → タイトルは高さ220px／アイコンは高さ168px に縮小 →
  `<h1>` を `<img ... onerror>` に差し替え、`.hicon img` の src を差し替え」で入る。
  ★高さの指定は不要（`nn-hdr-std` が `36px × --nnhm` で自動的にそろえる）。
- 図面・積算のヘッダー用アイコン `icon_zumen.png` が未着（今は絵文字📐で代用。届いたら置くだけ）。
- カメラのタイトルは帯が他より21%厚い（文字が他ページより17%小さく見える）。
  作り直すなら帯の上下の余白を詰めてもらう。
- サービスワーカーは停止中（`NN_USE_SW=false`）。オフライン対応が要るときだけ true に戻す。

### 107. 図面・積算の3D：出隅の3件（2026-08-15a）★本人の写真に赤丸で指摘
指摘は3件。①「なぞのはみ出しアス。外壁側の出隅に発生している。削除して。不要。」
②「謎の面取り？なんかおかしい。不要。削除せよ。」（パラペットの足元・スラブとの境目）
③「天端の面取り部分が砂付仕上げになっていない。改善して」。

- **★まず絵を実測して「どの部品か」を特定した**（§62の型）。添付は会話の記録の中に
  base64 で入っているので取り出し（§8）、同じ形（GL±0の長方形・立上り300・天端250）を
  ヘッドレスで作って**同じ向き・同じ寄りで撮り、赤丸の中身と突き合わせた**。
  3件とも1枚の絵で再現できたので、当てずっぽうで直さずに済んだ（`scratchpad/d3corner.js`）。
- **①角の天端に出していた継目2本（§72 の「L字に2本」）を削除**。
  角では2本が外側の縁でV字に出会うので、**外壁側の出隅にアスが乗っているようにしか見えない**。
  角の縦の継目と、平場へ斜めに出る継目は残っているので、重ねてあることは十分わかる。
- **②出隅（縦の角）の面取り（§77・CV=30mm）を廃止**。角は留め継ぎ（`joint`）で
  両側の壁が直接突き合う形に戻した。角の三角柱（`stripN([A,Bp,Im],…)`）も削除。
  ★**「謎の面取り」の正体は、面取りがパラペットの足元で終わるところにできる小さな三角**。
    壁の途中で終わる面取りは、実物には無い形なので必ず違和感になる。
- **★③の原因は `slope()`（面取りの斜面）に UV が無かったこと。**
  `BufferGeometry` に position だけ入れて作っていたので、**three.js は模様（砂の粒）を
  貼れず、のっぺりした色板になっていた**。他の面と同じ実寸（`NN_TEX_PER_M`）で
  uv を作って渡すと、天端の面取りにも砂付の粒が乗る。
  ★**自前で `BufferGeometry` を組むときは、position・法線だけでなく uv も要る。**
    `PlaneGeometry`／`ExtrudeGeometry`／`ShapeGeometry` は uv を自動で作るので気づきにくい。
- 検証は `scratchpad/chamchk.js`（`node chamchk.js` ／ 直す前と比べるときは
  `git show HEAD:zumen_sekisan.html > _before.html` して `node chamchk.js _before.html`）。
  4項目：角の天端の継目0本（前は8本）／壁が角まで届く（前は30mm手前で止まっていた）／
  模様つきの材料で uv の無い面が0枚（前は24枚）／面取りの斜面すべてに uv。
- ★**テストが古くなった1件**（§68）：`zfix2.js` の「出隅の面取り（三角）が12個」は
  面取りの廃止で 0個が正しくなった。すき間の確かめ方も
  「隣り合う壁が外側＋内側の2点で一致」に戻した（面取りを入れる前の形）。
  product は正しく、テストだけが古かった。
- 回帰は zumen の19本（zfix2／f3chk／f4chk／zdir／lapchk／facegap／beadaudit／d3chk／
  sec2chk／partschk／warichk／photochk／pinch／split1／align1／ang5／cross／cross2／znum）
  とも★NGなし。

### 108. 屋根が20面ある物件の見せ方（2026-08-15b）★試作・本人の判断待ち
「屋根が1つの物件だけど、屋根が20個あったら？ 数量の項目が20個ほしい」との相談。
A＝詳細で見る／B＝一覧にも全部出す／C＝他案、のうち**C（合わせ技）**で試作した。

- **一覧は「合計＋面数」だけ、押すとカードの中で内訳が開く**。詳細ページには飛ばさない
  （飛ばすと戻ったときにスクロール位置が変わり、20件見比べるのに20往復が要るため）。
  ・数量＝`合計 3,100㎡＋180m（20面）` ＋「内訳 ▾」ボタン
  ・改修仕様＝`塩ビシート 機械的固定工法 ほか4工法`（面積のいちばん大きい工法が主）
  ・開くと**3面まで**＋「ほか17面をすべて見る ▾」＋工法ごとの小計
  ・**面が1つの物件は今までどおりの見た目**（内訳ボタンを出さない）。100件中98件はこちら。
  ・カードの高さ：閉 275px（今までどおり）→ 3面 408px → 全20面 754px。
- **★データは `faces_sample.js` に置き、現場記録帳と現場マップの両方が読む**。
  片方だけ直すと数字が食い違う（§105 の発注と同じ失敗をしないため）。
  マップ側は `nnQ(s)`／`nnK(s)` を通して4か所（ピンの吹き出し・現場一覧・下のカード・見出し）に出す。
  ★**キーはいま「物件名」**。本番にするときは物件番号（J100）にし、
    BASE_SITES にも `code` を持たせること（mapsync.py で作れる）。
  ★**新しいファイルを足したら `sw.js` の ASSETS にも足す**こと（追加済み）。
- 試作データは2件だけ：`三井アウトレットパーク札幌北広島`（20面・5工法・シールだけ単位が m）と
  `東京流通センター 物流ビルB棟`（6面）。★**単位の違う面（m）をわざと混ぜてある**。
  合計を1つの数字にできないこと＝工法ごとの小計が要ることが、これで見て分かる。
- ★内訳のボタンと表は、**カードの選択を受ける document capture の除外リストに追加**した
  （`.fcbtn` `.pfaces`）。除外しないと、内訳を押すたびに物件が選択・詳細へ移動してしまう（§92）。
- **本番にするなら**：①物件データに面の一覧を持たせる ②図面・積算の部位（`state.polys`）を
  そのまま内訳として受け取る（二重入力を無くす）③工法別の集計を面ごとに足す（㎡と m の
  混在＝§60 の宿題もここで解ける）。
- 検証は `scratchpad/faces1.js`（記録帳・PC/スマホ 各16項目）と `faces2.js`（マップ側の一致）。

### 109. 新規物件登録：屋根・部位の内訳の入力（2026-08-15c）★§108の続き
「見え方は良い。そうなると新規物件作成に複数屋根の入力項目が必要」との指示で作った。

- **フォームに「屋根・部位の内訳」の枠**（既存防水〜構造体の下・`#f_faces`）。
  「＋ 面を追加」で1行＝［部位名／工法（KO_MASTERから）／数量／単位㎡・m／✕］。
  ★行が1つでもあると、上の**「数量」「単位」「工法」は合計から自動で決まり、手で直せなくなる**
  （`readOnly`＋薄地 `.ffauto`）。内訳と合計が食い違うと集計が壊れるため。
  行を全部消せば今までどおりの手入力（1面の物件）に戻る。
  ・数量＝㎡の合計（mなど別単位は「＋120m」で案内に併記）／工法＝面積がいちばん大きいもの
  ・案内文「合計 1,000㎡＋120m（3面・3工法）→ 一覧には内訳ボタン付きで出ます」が常時更新
- **保存先は `localStorage('nn_kirokucho_faces_v1')`＝`{物件名:[{n,ko,sp,q,un}]}`**。
  読み書きは **faces_sample.js に集約**（`nnFacesSave`／`nnFacesReload`。保存分＞見本の優先）。
  現場マップも同じファイルを読むので、**フォームで入れた内訳がマップにもそのまま出る**（検証済み）。
  ★**空配列＝「見本を消した」印**としてそのまま持つ。見本に無い物件で空ならキーごと消す。
  ★物件名を変えたら古いキーは消す（`oldName` を渡す）。**キーは物件名**（§108の注のとおり、
    本番は物件番号にするのが筋）。
  ★バックアップの `LIST`（index.html）にも追加済み（§46）。
- **openModal／saveProperty は関数の差し替えで包んだ**（§73の型。元を呼んでから内訳の分を足す）。
  ★保存の成否は「窓（#modalbg）が閉じたか」で判定。名前未入力で alert のときは窓が開いたままなので、
    内訳を保存しない（半端な状態を作らない）。
- ★**パソコンの窓は幅560px**。行の部品の寸法を詰めないと単位と✕だけ2行目に落ちる
  （部位名 flex 110・工法 flex 150・数量74px・単位52px で1行に収まる）。スマホは元から1行。
- ★**新規登録した物件そのものは保存されない**（従来どおりメモリ上だけ・再読み込みで消える）。
  内訳（faces）だけが localStorage に残る。テストを書くときはこの差に注意
  （faces3.js で「再読み込み後」は物件でなくデータ関数で確かめている）。
- 検証は `scratchpad/faces3.js`（16項目：自動合計・工法自動・行削除の追従・保存・編集で復元・
  再読み込み・マップから同じ数字・全部消すとゴミが残らない）と `ffshot.js`（PC/スマホの見た目）。
  回帰：faces1（PC/ph）・faces2・formchk・modalchk・modalph・databk・pwlist・edit1・kai3chk・kai4chk 全部○。

### 110. 新規物件登録：1画面で完結（横長3列）・✕・下書きの自動セーブ（2026-08-15d）
「入力画面をスクロールするのはストレス。文字も枠もコンパクトに。ヨコ型にして構わないから
1画面で完結させて。下書き保存も追加。自動セーブも。右上に✕」との指示。

- **パソコンは横長3列**（`.mwrap`＋`.mcol`×3・`<style id="nn-modalwide">`）。
  列1＝基本情報／列2＝屋根・部位の内訳＋金額＋日程・支払／列3＝予算＋実績＋現場情報。
  文字と枠をさらに圧縮（入力欄 14px/28px → **12.5px/25px**・ラベル10.5px・余白も半分）。
  実測：**1366×720（いちばん低い画面）でもスクロール0**・全欄と保存ボタンが同時に見える。
  1600×900・2000×1010（zoom 1.25）でも同じ。窓の幅は `min(1250px,96vw)`。
  ★列の中は約400pxしかないので、内訳の行は **`flex-wrap:nowrap`＋部位名/工法を縮めて受ける**
  （折り返すと✕だけ2行目に落ちる。実際に落ちた）。
  ★スマホは今までどおり1列（この横長はかえって使えない）。✕と下書きはスマホでも効く。
- **右上に✕**（`.mclose`）＝閉じるだけ。自動セーブがあるので閉じても入力は消えない。
- **下書きの自動セーブ**（`<script id="nn-modaldraft-js">`・`nn_kirokucho_draft_v1`）
  ・**新規登録のときだけ**。打つそばから0.4秒のまとめで保存し、ボタン行の左に
    「✓ 下書きを自動保存（HH:MM）」と出す。✕・キャンセルで閉じても残り、
    次に「＋新規作成」を開くと**復元**（トーストでも案内）。再読み込み後も残る。
  ・「📝 下書き保存して閉じる」ボタン＝同じ保存＋確認のトースト＋閉じる（安心感のため）。
  ・**「保存する」が通ったら下書きは消す**。成否は「窓が閉じたか」で判定（§109と同じ）。
  ・**編集（既存物件）は対象外**＝自動セーブせず、下書きボタンも出さない。
    編集まで下書きにすると「どの物件の下書きか」問題が出るため。
  ・空っぽ（実質何も打っていない）なら保存しない・消す（hasBody 判定。
    ステータス等の既定値だけでは「中身」と見なさない）。
  ・バックアップの `LIST`（index.html）にも追加。
- 入力欄 `f_m`（数量）に `min="0"`（頂いた画面で -3 が入っていた）。
- ★openModal／saveProperty は**3重の包み**になった（元→§109の内訳→§110の下書き）。
  順序が大事：下書きの復元は内訳の行が消された**後**に走る。この構造を崩さないこと。
- ★テストの期待値更新：`formchk.js` の「入力欄14px」→12.5px＋「スクロールなしで完結」を追加。
- 検証は `scratchpad/draft1.js`（1366×720／1600×900／2000×1010・各15項目）と
  `mwshot.js`（PC/スマホの見た目）。回帰：faces1・faces3・formchk・modalph・edit1 全部○。

### 111. 一覧カード：詳細へボタン・1クリックで選択肢・沈み演出の一掃（2026-08-15e）
指摘は3つ。①「詳細へ」のボタンをどこかに追加（場所は任せる）②既存・区分・構造が
「1クリック目で黄色になり、2クリック目でようやく選択肢」→1クリックで選択肢を
③「なぞの沈み込む演出いらんって言ったよね？」（2回目の指摘）。

- **①「詳細へ ›」ボタン**（`.pgo`）＝現場名の行の右端（利益予定額の右）。緑のグラデ。
  `nnGoDetail(pid)`＝selectedId を入れて `openDetailFull()`。
  ★**カード選択の除外リスト（document capture・2か所）に `.pgo` を足すこと**。
  従来の「2回タップで詳細」も生きている（消していない）。
- **②1クリックで選択肢**：`startEdit` のプルダウン型で **`sel.showPicker()`** を呼ぶ。
  ★focus() だけではプルダウンの一覧は開かない（これが「2度目のクリックでようやく出る」の正体）。
  ★showPicker は「利用者の操作（クリックのハンドラ）の中」でしか呼べない。ここは click
    ハンドラの中なので使える。無い端末では try で握りつぶし（もう1回タップで開く＝今まで通り）。
  ・**行のどこを押しても開く**：`.pexrow`（ラベル・アイコン・余白）で受けて中の `.ebox` に渡す。
    囲みを狙い損ねてカードが黄色になるだけ、を無くした。除外リストにも `.pexrow` を追加。
- **③沈み・浮きの動きの正体は2つ残っていた**：
  ・**`.pcard:hover{transform:translateY(-1px)}`**（マウスを乗せるたび1px浮く）←これが本体
  ・カード内の部品（`.pshmore` `.phzoom` `.doc`）の `:active{translateY(1px)}`
  → `#list .pcard:hover{transform:none !important}` と **`#list .pcard :active{transform:none !important}`**
  （子孫まるごと）で一掃。`transition` からも transform を外した。手応えは色と影の変化だけ。
  ★「押すと動く」系の指摘が3回目。**カードの中に新しい部品を足すときは transform の動きを入れない。**
- ★**テストが古くなった1件**（§68）：`exist1.js`。選択肢が1クリックで開くようになったため、
  **開きっぱなしの一覧（ネイティブのプルダウン）が次のクリックを食い**、次の囲みが開けず
  NGになっていた。**Escape＋blur＋検索欄クリックで閉じてから次へ**進む形に直した。
  ★同じ理由で、**プルダウンを触るテストは「閉じる」手順を挟むこと**（go1.js に同じ処理）。
  ★中立なクリック先に画面上部（y≈70）を使わないこと（タブに当たって画面が切り替わる）。
- 検証は `scratchpad/go1.js`（PC/スマホ 各10項目：ボタン100個・1クリックで移動・
  1クリックで選択肢・黄色にならない・行の余白でも開く・カードも中の部品も動かない）。
  回帰：pwlist・edit1・exist1・kai3chk・kai4chk・kai5chk・photo4・faces1・memo1・card4chk 全部○。

### 112. 一覧カード：押しても着色されない（2026-08-15f）★原因は2つ
「物件一覧をクリックしても色が着色されなくなった」との指摘。**測ったら原因が2つあった。**

- **★原因①：カードの70%が「押しても無反応の場所」になっていた。**
  カードの選択をしない場所（document capture の除外リスト）に、
  **写真10枚の帯（`.pshots`）と提出書類の枠（`.pdocs`）を「枠ごと」**入れていたため、
  中のボタンだけでなく**帯の余白・見出し・すき間まで全部が無反応**だった。
  カードが大きくなるほど悪化する（§94で写真10枚、§111で `.pgo`/`.pexrow` を追加した積み重ね）。
  → **除外は「実際に押せる部品」だけ**にした（`NN_CARD_SKIP` に一本化）。
  枠ごと除くのは `.pph`（写真）だけ＝本人の指示「画像は画像で、詳細に飛ばさない」（§92）。
  実測：無反応の面積 **70% → 42%**（残りは写真・囲み・ボタンなど、押すと別の動きをする所だけ）。
- **★原因②：黄色は `armed` に付いていて、2.6秒で自動的に消えていた。**
  `armed` は「もう一度押すと詳細へ進む」ための一時的な状態。色をそこに付けていたので、
  少し待つと色が消える＝「押したのに着色されない」ように見えていた。
  → **色は `sel`（選ばれているカード）に持たせ、次のカードを押すまで残す**
  （薄い黄色 `#fffdf0`＋黄色い枠）。`armed` はその上に濃い黄色 `#fff6d6` を重ねる。
- **JS側：着色（`sel`）と2回タップの待機（`armed`）を分けた。**
  ・`sel`＝カードの**どこを押しても**必ず付く（編集の囲みでも写真でもボタンでも）。実測 無反応0箇所。
  ・`armed`＝今までどおり「何も無い場所を押したとき」だけ。編集の囲みなどを押したときは
    **`armed` を解く**（うっかり詳細へ進まないように）。
  ★**`selectedId` は「すでに選ばれているとき」も必ず入れ直すこと。**
    入れ忘れると、一覧が作り直された瞬間（その場編集の確定など）に別のカードへ色が移り、
    押したカードが白いままに見える（実際に踏んだ）。
- ★**テストが古くなった1件**（§68）：`faces1.js` の「内訳を押しても物件は選択されない」。
  着色はどこを押しても付く仕様に変えたので、**見るべきは「詳細へ飛ばないこと」**に直した。
- 検証は `scratchpad/selcolor.js`（PC/スマホ 各10項目：カード全面を格子で押して無反応0・
  物件名/日付/写真枠/提出書類/既存の囲み それぞれで着色・3秒後も色が残る・
  着色は1枚だけ・2回タップで詳細）。
  回帰：pwlist・edit1・exist1・go1・kai3〜5chk・photo4・faces1・memo1・card4chk 全部○。

### 113. 現場マップ：一覧の行に防水アイコン・数量/工法/仕様の枠・角を四角に（2026-08-15g）
「防水アイコンをステータスの上に配置したい。面積と工法名と仕様の文字が少し大きいから、
レイアウトに合うようにその枠を作って。あと現場一覧表みたいに、角を丸枠でなくて四角に」との指示。

- **1行の作り**（`renderList` の雛形＋`<style id="nn-maplist">`）
  ・左＝ステータス色の縦棒／中＝物件名＋小枠3つ／右＝**アイコンの下にステータス**（`.rcol`）
  ・小枠は `nnRowChips(s)`＝[数量][工法名][仕様記号]。工法名の末尾の（AS-J1）を
    `nnSplitKo` で切り出して別の枠にした（記号だけ色を変えて見分けやすくする）。
  ・文字は 10.5px → **9.5px**（スマホ9px）。物件名（12.5px）より小さくして主従をはっきりさせた。
  ・**角は丸めない**（`.site{border-radius:0}`・ステータスも2px）＝現場記録帳の一覧と同じ見え方。
- ★**小枠は折り返さない**（`flex-wrap:nowrap`）。折り返すと行の高さが揃わず一覧が読みにくい。
  入りきらない工法名は「…」で切り、**全文は title（マウスを乗せると出る）**。実測 行の高さ69〜73px。
- **★防水アイコンのキーの決め方（工法名 → `icons/kou_<キー>.png`）を `faces_sample.js` に集約**した。
  現場記録帳と現場マップの両方が読むので、**同じ工法なら必ず同じ絵**になる（§105の失敗を繰り返さない）。
  記録帳の `nnKouKey` は共通版（`nnKouKeyOf`）を先に見て、無ければ従来の表に落ちる。
  ★絵はまだ未着なので、いまは**点線の枠に短い工法名**（常温粘着・トーチ 等）が出る。
  ★絵があるかは**キーごとに1回だけ**確かめる（`NN_KIC`）。100件ぶん404を取りに行かないため。
- ★**テストが古くなった1件**（§68）：`kai1chk.js` の「写真の枠を押しても物件は選ばれない」。
  §112で「どこを押しても着色」に変えたので、見るべきは**詳細へ飛ばないこと**に直した。
- 検証は `scratchpad/maprow.js`（PC/スマホ 各15項目：角・枠・文字の大小・アイコンがステータスの上・
  同じ列・行の高さがそろう・はみ出し0・共通キー・行を押すと選択できる）。
  ★**ヘッドレスでは `#setup`（APIキー画面）が前面に出るので隠してから撮ること**（真っ白な絵になる）。
  回帰：faces1・faces2・mapchk1・lnmap・kai1chk 全部○。

### 114. 現場マップ：地図がぼやける（2026-08-15h）★原因はパソコンの表示倍率
「1枚目が現場マップのGoogleマップ、2枚目がGoogleマップ本家。画質が違うから同じにして」との指摘。
並べて見ると、こちらの地図だけ**文字も線も一回り大きく、にじんでいた**。

- **★原因は §61 の表示倍率（zoom）**。パソコンではどのモニターでも同じ見え方になるよう
  `html` に zoom 1.00〜1.60 を掛けている（本人の2000pxのモニターでは1.25倍）。
  文字やボタン（HTML）は倍率をかけても**描き直される**のでぼやけないが、
  **地図はGoogleが描いた1枚の絵（WebGL・`renderingType:"VECTOR"`）**なので、
  倍率のぶん**引き伸ばされて画素が足りなくなる**。§64（3Dビューが粗い）とまったく同じ話。
- **直し方＝地図の面だけ倍率を打ち消す**（`<style id="nn-mapsharp">`）。
  `#map2d`／`gmp-map-3d` に **`zoom: calc(1/var(--nnpzr))` ＋ `width/height:100%`**。
  打ち消すと中の広さ（CSSのpx）が倍率のぶん増え、Googleはその画素数で描き直す＝本家と同じ細かさ。
  **画面に占める大きさは変わらない**（実測：1366/1600/2000/2560px のどれでも
  「1 CSSpx ＝ 1 画面px」・地図の枠は親とぴったり同じ）。
  ★中に置いている自前のピン（`.mkd`）・吹き出し（`.mkx`）は `zoom: var(--nnpzr)` で
    今までと同じ大きさに戻す（DOMなので倍率を掛けてもぼやけない）。
  ★スマホは倍率を掛けていないので何も変わらない。
- ★**「地図の中の座標」もむしろ安全になる**：打ち消し後、地図の中は倍率1.0（画面と1:1）になる。
  Google の内部計算は倍率のかかっていない状態を前提にしているので、今までより素直な状態。
- ★**同じ理由でぼやけるものは他にもある**。「CSSのpxで作って画面に出す絵」
  （canvas・WebGL・拡大した画像）は全部この対象。**倍率を掛けているページで絵を扱うときは、
  `nnPixRatio()`（§64）で描く画素を増やすか、この節のように面だけ等倍に戻すこと。**
- 検証は `scratchpad/mapsharp.js`（4サイズ×5項目＋スマホ：等倍・枠いっぱい・ピンと吹き出しの
  大きさが今までどおり・JSエラーなし）。回帰：maprow（PC/スマホ）・mapchk1・lnmap・faces2 全部○。
  ★実際の細かさ（絵のにじみ）はAPIキーが要るのでヘッドレスでは見られない。
    **「1 CSSpx ＝ 1 画面px」になっていることが、本家と同じ細かさで描かれる条件**。

### 115. 現場マップ：記録帳と同じ質感に／地図の見出しを白地に／施工中は稼働バー（2026-08-15j）
指摘は4つ。①防水工法のところだけ影で強調されて見える ②全体的にAI感が強すぎ・現場記録帳を参考に
③2D地図の見出しは黒が強すぎ。背景は白・物件名は黒 ④施工中は稼働バー（これまでと同じ緑）を付けて。

- **①②「AI感」の正体＝淡い色・グラデ・角丸・浮いた影**。記録帳の一覧はそれを避けて
  **白地・地味なグレーの1px枠・角0・影なし**で作ってある（`.ebox`）。マップの一覧も同じにした。
  ・小枠（数量・工法名）＝白地・`#b9c2b6` 1px・角0・影なし
  ・**仕様記号は記録帳と同じ「記号バッジ」**（色を塗って白文字）。色の決め方も
    `badgeCls` と同じ（AS→#8c2f2f／X→#1f66c0／ASI→#a04a20／その他→#5c6670）＝`nnSpecGrp`
  ・ステータスも半透明の淡色をやめ、記録帳と同じ**色を塗って白文字**
  ・行の影（`box-shadow`）を削除。選択中は薄い黄色＋左に緑の線（記録帳と同じ考え方）
  ★**影は実際には入っていなかった**（実測 `box-shadow:none`）。
    「工法名の枠だけ薄い色の地で横に長い」ため浮いて見えていた。**白地にすると消える。**
- **③地図の見出し（`.mk-chip`）を白地・物件名は黒に**（`<style id="nn-mapchip">`）。
  枠は状態色2px・角3px・影は `drop-shadow(0 1px 3px …)` まで弱めた。
  ★黒地は地図の上で強すぎて、**地図そのものが読めなくなる**（本人の指摘）。
  ステータスの文字だけは状態色のまま（白地の上で識別に必要）。
- **④施工中の見出しに稼働バー**（`.mk-prog`）＝記録帳と同じ**黒い筒＋緑のグラデ**（§55）。
  幅は `s.prog`。施工中以外には出さない。
- 検証は `scratchpad/maprow.js`（PC/スマホ 各24項目。②の質感＝白地・影なし・記号バッジ・
  ステータスの塗り、③白地と黒文字、④バーの有無と幅まで見る）。
  回帰：mapsharp・mapchk1・lnmap・faces2 全部○。

### 116. 現場マップ：吹き出しに✕・全体を記録帳の作法で組み直し（2026-08-15k）
「まだAI感が出ている。現場名のマッピングのウィンドウ内になんで閉じる(✕)がないの？
デザインも現場記録帳を参考に組み直して」との指摘。

- **★吹き出し（`expandedContent`）を記録帳の一覧カードと同じ組み方に**（`<style id="nn-mapx">`）。
  ・上段＝物件名＋ステータス＋**✕（閉じる）**、その下に**状態色の境界線**（記録帳の `.rhead` と同じ）
  ・左端に**状態色の縦棒**（`border-left:4px`）＝記録帳のカードと同じ見分け方
  ・中身＝白地・`#b9c2b6` 1px・角0の**囲み**（数量／工法／稼働）＋**記号バッジ**（色を塗って白文字）
  ・進行度＝記録帳と同じ**黒い筒＋緑**（§55）
  ・「📒 記録帳を開く」＝単色の緑・角2px（グラデをやめた）
  ★**✕は `showAllSites()` を呼ぶ**＝帯の下の「選択解除して全現場を表示」と同じ処理。
    別の閉じ方を作らない（2か所に状態を持たない・§97の教訓）。
  ★**人形の絵（`.x-workers .w`）は廃止**し「稼働 本日◯人」の囲みにした。
    他の画面に無い表現で、そこだけ浮いて見えていた。
- **「AI感」＝大きい丸み・ふわっとした影・半透明・淡いグラデ**。地図まわりも記録帳の作法にそろえた
  （`<style id="nn-mapui">`）：タブ（丸いピル→**角2px・白地・選択は緑の塗り**）、
  地図の上のボタン（角10px→**3px**・影は1pxの縁だけ）、検索バー・「選択解除」も角3px。
- ★検証スクリプトの注意：**`selId` は top-level の `let` なので `window.selId` では読めない**
  （§47と同じ）。evaluate の中では**そのままの名前**で読むこと。
- ★**テストが古くなった1件**（§68）：`mapsharp.js` の「吹き出しの幅232px」。
  組み直しで 262px になったので期待値を更新（倍率どおりに拡大するかを見る趣旨は変えていない）。
- 検証は `scratchpad/maprow.js`（PC/スマホ 各32項目。✕の有無と押したときの解除・白地・角0・
  囲み・黒い筒＋緑・人形0個・タブとボタンの角まで見る）。
  回帰：mapsharp・mapchk1・lnmap・faces2 全部○。

### 117. 現場マップ：3Dの現場のしるしを「2Dと同じただのポチ」に（2026-08-15m）
「3Dの表示点のアイコンが気持ち悪いので、2Dの表示アイコン（ただのポチ）に変更して」との指示。

- **★しずく型のピン（`PinElement`）は Google が立体の絵に焼き込む**ので、寄ると巨大・にじむ・
  形が浮く（§31・§32で色や大きさを何度も調整したが、形そのものは変えられなかった）。
  → **`Polygon3DElement` で「白フチ付きの丸」を描く**方式に変更（`nnMakeDot3d`／`nnDotRing`）。
  線と面で描き直される部品なので**にじまない**。色は2Dの点と同じ状態色（`STATUS3D`）。
  ★§32の「3Dのピンは差し替えられない」は、この方法で**解決できる**（メモを更新済み）。
- ★**地面に貼ると建物の陰に隠れる**ので、`altitudeMode:"RELATIVE_TO_GROUND"` ＋
  高さ＝`altFor(s.id)`（ピンと同じ高さ＝屋根の上）に置く。ピンをドラッグしたとき・
  高さのスライダーを動かしたときも、ポチが一緒に動くようにした（3か所）。
- ★**大きさはカメラの寄り（`map3d.range`）に合わせる**（`range×0.010`・1.2〜90m）。
  遠景で見えなくならないように。**0.35秒ごとに見て、1.4倍以上変わったときだけ**座標を作り直す
  （100件×19点を毎コマ作り直すと重い）。実測：range 200m→2m／600m→6m／5000m→50m。
- **`PinElement` は「押すための的」として透明・極小で残す**（`gmp-click` で現場を選べるように）。
  これを消すと3Dで現場を押して選べなくなる。
- ★3Dを作り直すとき（モード切替）は**古いポチを消してから**作る（`init3d` の先頭）。
  消さないと二重に残る。
- 検証は `scratchpad/dot3d.js`（9項目：丸の形・状態色・白フチ・ピンと同じ高さ・
  高さ変更に追従・寄り引きで大きさが変わる・しずく型の絵を出していない）。
  ★**地図はAPIキーが要るので、`lib3d` をニセ物に差し替えて確かめる**（実機での見え方は要確認）。
  回帰：maprow（PC/スマホ）・mapsharp・mapchk1・lnmap・faces2 全部○。

**★2026-08-15n の追い直し**（「変わってないよ」との指摘）
公開（`2dc04ea`）は completed/success で届いていた（§67tの手順で確認）。頂いた写真を拡大すると
**しずく型の緑ピン＋地面まで伸びる白い棒**がそのまま出ていた。原因になり得るのは2つ。
- **①ピンの色を透明にしただけでは消えないことがある**（Google が色を無視して既定色で描く）。
  → **大きさも極小（`scale:0.01`）**にして二重に効かせる。押すための的はマーカー自身が持つので選べる。
- **②白い棒は `extruded:true`**（地面からマーカーまで線を引く指定）。ポチにするなら不要なので false に。
★実機のキャッシュの可能性も残るので、**確認は「ホーム左上の版表示」で**（v2026-08-15 n になっていれば新しい版）。

**★2026-08-15p の是正**（「ポチがでかすぎる／地上から伸びた線が無くなっている」）
- **大きさの係数が大きすぎた**（`range×0.010`）。引いた画（range 4,000m）で半径40m＝直径80m になり、
  街区ごと覆っていた。**`range×0.0045`（0.5〜45m）**に変更。
  ★**目安は「画面の上で2Dのポチ（直径10px）と同じに見えること」**。
    画面の高さ900pxに range のおよそ0.9倍が写るので `直径px ≒ 半径×2×900÷(range×0.9)`。
    この式で見ると 0.0045 はどの寄り引きでも**約9px**になる（実測：200m/600m/5000m とも9.0px）。
- **地上から伸びる線（`extruded:true`）は戻した**。どの建物のことか分かるので必要（本人の指摘）。
  版nで「棒も消す」と判断したのは行き過ぎだった。

**★2026-08-15q ポチの大きさは「実測」で決める（ここで決着）**
版p（`range×0.0045`）でも「まだデカすぎる」と言われた。**range で決める考え方そのものが誤り**。
- **3Dは斜めから見るので、range が同じでも傾き（tilt）で地面の見え方が何倍も変わる**。
  実測：range 600m・tilt 0° では 1px＝0.77m だが、tilt 70° では **1px＝2.26m**（約3倍）。
  だから「range に係数を掛ける」式は、傾けた瞬間に破綻する（頂いた写真では直径44mになっていた）。
- **直し方＝画面の1pxが地面の何mかを毎回測る**（`nnMeterPerPx`）。
  画面中央と「そこから北へ1m」の点を `worldToScreenRaw` で投影し、間隔（px）を見るだけ。
  半径＝`1pxのm × 10px ÷ 2`。実測：寄り・引き・傾き70°のどれでも**画面上10px**にそろう。
- ★**「画面の上で何pxに見えるか」を保証したいときは、必ず投影して測ること。**
  カメラの数値（range・zoom）から逆算する式は、傾きや画角が絡むと必ずズレる。
- 検証は `scratchpad/dot3d.js`（12項目。寄り／引き／傾き70°の3通りで画面px を実測）。

**★2026-08-15r：測れなかったときの目安の式も1/4に**
`nnMeterPerPx()` は `worldToScreenRaw` が使えないと `null` を返す（地図がまだ立ち上がっていない等）。
そのときの保険の式が版pのまま（`range×0.0045`・上限45m）だったので、本人の「4分の1でいい」に合わせて
**`range×0.0011`・上限12m** にした。★**実測できたときの上限（60m）は下げないこと。**
下げると引いた画で画面3.7pxまで縮み、こんどは見えなくなる（実測方式では画面px側で頭打ちになるので、
実寸の上限は暴走防止のためだけの数字）。

**★★2026-08-15s：ポチが巨大なままだった本当の原因（版q・rの測り方が誤り）**
版q・rでも実機は**直径56m（画面180px）**のままだった。頂いた写真を実測して決着した
（縮尺バー 50m＝162px → 1px＝0.309m、ポチの幅 181px＝56m）。
- **★原因は `nnMeterPerPx` の測り方**。「画面の中心と、そこから**北へ1m**の点を投影して
  間隔を測る」やり方にしていたが、**斜めから見ると北向きは画面上でつぶれて見える**。
  つぶれたぶん「1pxは何mか」が何倍にも出て、そのままポチの半径になっていた（実測18倍）。
  ★**一方向だけ測って縮尺にしないこと。** 傾き（tilt）で向きごとに縮み方が違う。
- **直し方＝このページが前から使っている式に戻す**（`screenDeltaToWorld` と同じ）：
  **`1pxのm ＝ カメラまでの距離(range) × fovFactor ÷ 地図の高さ(px)`**。
  横方向の縮尺なので**傾きの影響を受けない**。実機の画（range 233m・高さ870px）で
  1px＝0.309m ＝ 写真の実測とぴったり一致した。半径1.55m＝**画面10px**になる。
- 入れ替えに失敗したときはポチを作り直す（黙って古い大きさで残さない）。見直しの敷居も1.25倍→1.15倍。
- ★**検証がザルだった**：版qのテストは「`nnMeterPerPx` で割り戻して10px」と見ていただけで、
  **同じ誤差で割るので必ず10pxと出る**（product が18倍でも通る）。
  いまは**ページ自身の投影（`worldToScreenRaw`）でポチの輪を画面に落として幅を測る**。
  ★**検算に、検査したい当の関数を使わないこと。**

**★★2026-08-15t：カメラの数値から縮尺を出すのは、この端末では当てにならない（決着）**
版sでも実機は**直径28m（画面111px）**のままだった。頂いた写真の実測で、ようやく数字が出た。

| | 実機（写真の実測） | こちらの計算 |
|---|---|---|
| 画面の1px は地面の何m | **0.255m**（Googleの縮尺バー 50m＝196px） | **2.84m** |

**＝11倍の開き。** つまり `range × fovFactor ÷ 地図の高さ` という式（このページが
`screenDeltaToWorld` などで前から使っている式）が、**実機では11倍ずれている**。
`map3d.range` か `fovFactor` か `clientHeight` のどれかが想定と違う（ヘッドレスでは
本物の地図が動かないので、どれかは特定できていない）。
- ★**直し方＝計算をやめるのではなく、「地面での大きさ」に上限・下限を付ける**
  （`NN_DOT_MIN_M=0.8` 〜 `NN_DOT_MAX_M=2.5`。半径）。
  こうすると、**計算が何倍ずれてもポチは直径5m（車1台ぶん）を超えない**＝建物を覆えない。
  縮尺が正しい環境では、寄った画で2Dのポチと同じ画面10px前後になる。
- ★**教訓：外部の部品（Googleの地図）の数値を信じた計算で「見た目の大きさ」を決めない。**
  ずれたときに歯止めが無いと、青天井で巨大化する。実寸で頭を打たせておけば最悪でも軽傷。
- ★**検算に、検査したい当の関数を使わないこと**（版qのテストは `nnMeterPerPx` で
  割り戻していたので、product が18倍でも○になっていた）。
  いまは①ページ自身の投影で幅を測る②**実機で外れた条件を再現して上限を確かめる**の2本立て。
- ★**写真からの実測が唯一の頼り**。Googleの縮尺バー（例：50m）の長さを画素で測れば
  「1pxが何m」が分かる。あとは部品の幅を測って割るだけ（`scratchpad` で PIL＋numpy）。

### 118. 図面・積算：ツールバー大改修（2026-08-16a）★本人のモック準拠・アイコン待ち
「UIと操作性を飛躍的に向上。極力右側を使わずに作図できるように。上のアイコンと右のダブりは削除」
との指示（モック画像つき）。アイコンは本人がパワポで自作して次のターンで届く。

- **ツールバーを3つのかたまりに**（`#tbgA`＝作図の基本3行／`#tbgB`＝機能・部材3行／
  `#tbgC`＝夜・昼、**右上に absolute**）。行は `.tbrow`。
  ★スマホは **`.tbg`/`.tbrow` を `display:contents`** で「無かったこと」にし、
  今までどおり1列＋「⋯道具」メニュー（§29）の作りをそのまま使う。**この一行が肝**。
  ★`tb.insertBefore(more, anchor)` は入れ子になったので **`anchor.parentNode` に挿す**
  （tl_scale ボタンも同じ）。忘れると NotFoundError で ⋯道具ごと動かない。
- **新ボタン**：ヨコ割付/タテ割付（`nnWariDir('h'/'v')`＝setDir＋割付タブへ。dirBtns が点灯を同期）／
  **部材スタンプ6個**（引込み・押出し・笠木・脱気筒・タテドレン・ヨコドレン＝`nnStamp(key)`。
  §73の役物システムに相乗り：名前で登録を探し、無ければ `NN_STAMPS` の見本で自動登録→置くモード。
  寸法・単価は役物パネルの✎編集で直せば以後それが使われる）／
  **夜画面・昼画面**（`nnSetTheme` 直呼び。旧「🌙 ダークモード」トグル（tl_theme）は削除。
  3D側の `d3_theme`・断面の `sec_theme` のトグルは今までどおり）。
- **ダブり削除**：割付設定の「横流し/縦流し」ボタンは **display:none で残す**
  （消すと dirBtns・状態復元が空振りする）。案内文「横・縦は上のヨコ割付／タテ割付で」。
- **★ボタンの文字を変えるときは `nnBtnTx(btn,文字)`** を使うこと。textContent 直書きは
  アイコン化の包み（.tbtx＋img）ごと消す（tl_grid で実際に起きていた）。
- ★**PCの幅の頭打ちに注意**：本人のモニター2000px＝倍率1.25＝**CSS幅1600px**しかない。
  A579+B674+C91 は入らず B が下に落ちた → 文字12px・padding縮小・保存をB3行目へ・Cをabsoluteに。
  ★ツールバーが3段になったぶん、**キャンバス上部~150pxはボタンの下**。既存テストの
  打点座標が被って2本落ちた（cross.js は狙いを下へ、sec2chk はボタン名「サンプル」に追従）。
- アイコンは `icons/btn_<キー>.png`（§86の仕組みそのまま・置くだけで絵になる）。
  新キー：btn_wari_h/btn_wari_v/btn_p_hikomi/btn_p_oshidashi/btn_p_kasagi/btn_p_dakki/
  btn_p_tatedrain/btn_p_yokodrain/btn_night/btn_day。
- 検証は `scratchpad/tb2.js`（PC 17項目／スマホ ph 15項目）。回帰21本すべて○
  （f3chk・thchk は tl_theme 前提だったので夜画面ボタンに更新＝product は正しくテストが古い）。

**★2026-08-16b：ツールバーのアイコン5枚が届いた（全削除・削除・寸法表示・選択・マス表示）**
- `icons/btn_clear.png` `btn_del.png` `btn_dims.png` `btn_sel.png` `btn_grid.png`（透過webpで届いた
  →フチの透明を確認して切り出し・高さ168px。今回は市松の焼き込みなし）。sw.js の ASSETS にも追加。
- ★**絵が付いたら、文字の先頭の絵文字（🗑 ▦ など）は消す**（`nnStripEmoji`）。
  消さないと「赤いゴミ箱の絵＋🗑 削除」と絵文字が二重になる（実際になった）。
  `nnBtnTx` も hasimg のとき同じ処理を通すので、マス表示の切替でも絵文字は戻らない
  （ON/OFF は緑の点灯で見分ける＝モックと同じ）。

**★2026-08-16c：ツールバーのアイコン、さらに15枚（計21/28）**
- 新規12枚：`btn_split` `btn_2pane` `btn_wari_h` `btn_wari_v` `btn_hole`
  `btn_p_hikomi` `btn_p_oshidashi` `btn_p_kasagi` `btn_p_dakki` `btn_p_tatedrain`
  `btn_p_yokodrain` `btn_night`。sw.js の ASSETS にも追加。
- **差し替え3枚**（`btn_ang` `btn_rect` `btn_pan`＝新しい丸ボタン調に統一）：
  ★同名差し替えなので **NN_TB_ICONS の3つ目に版名**を持たせ、URLに `?v=` を付けた（§66の罠）。
  `nnTbIcons` が `[id, file, ver]` の3要素に対応。
- ★**アイコンが付くとボタンが横に広がる**。1600px（2000pxモニター÷倍率1.25）で
  Bグループがまた下に落ちた → 絵の高さ 25→22px・gap/padding を1段詰めて解消。
  **アイコンを足すたびに `node tb2.js` で「AとBが横に並ぶ」を確かめること。**
- 未着7枚：btn_undo／btn_redo／btn_sample／btn_save／btn_open／btn_photo／btn_day。

**★2026-08-16d：アイコン4枚追加（昼画面・写真から起こす・サンプル・保存）＝計25/28**
- `btn_day` `btn_photo` `btn_sample` `btn_save`。btn_photo は白背景の焼き込みあり
  → フチからの塗りつぶしで透過に（§81 の手）。sw.js の ASSETS にも追加。
- **未着は3枚だけ**：btn_undo（戻る）／btn_redo（進む）／btn_open（開く）。

**★2026-08-16e：戻る・進むのアイコン追加＝計27/28。未着は btn_open（開く）だけ。**

---

### 119. 図面・積算：3Dで辺をタップ→その辺だけ編集／水切りアゴを新設（2026-08-16f）
依頼は「**指定した箇所に、ユーザーが求める編集がしたい**。3Dでさらに高度な編集ができれば
一目おかれて導入の意思決定につながる」。モックは「**このパラペットだけ立上りを500に／アゴありに**」。

- **考え方＝新しい編集画面は作らない。** 図面タブに元からある辺編集
  （`sel` ＋ `edgeKind`/`edgeSet`/`edgeApplyAll`）を、3Dから触れるようにしただけ。
  図面と3Dが同じ1つの状態を見るので、どちらで直しても食い違わない。
- **★build3D には一切手を入れていない。** 3Dの組み立ては §55（2026-08-06i）の留め継ぎ方式で
  かなり込み入っているので、**「押し出し」（§⑩ hookExt）と同じく末尾に独立ブロック
  `nn-3dedit-js` を足し、できあがった3Dの上に後から飾りを載せる**方式にした。
  ★この作りにしておくと、3Dの組み立てが今後変わってもこの機能は壊れない。
  実際、この改修中に main 側で3Dが作り直されており、直接書き換える版は全部当たらなくなった。
- **飾りは専用のまとまり `deco`（THREE.Group）に入れ、`T.scene` に直接ぶら下げる。**
  ★`T.group` は build3D の先頭で毎回空にされるので、そこに入れると消える。
  `build3D` と `setTab` を**包んで**（`_nn3d` 印で二重包み防止）、組み直しのたびに
  `nn3dSync()` で飾りを作り直す。最後に `T.rev` を1つ増やして描き直させる
  （§「変わったときだけ描く」の合図）。
- **辺のタップ**：全辺に**見えない当たり判定の板**（`opacity:0` の Box・`userData.pick={p,r,e}`）を置き、
  `Raycaster` で拾う。★`visible=false` は使わないこと（版によってレイキャストの対象から外れる）。
  ★立上り0の辺（フリー端末）も選べるよう、**当たり判定は高さに関係なく必ず作る**。
  ★指を8px以上動かした／0.7秒以上押したら視点操作とみなして拾わない。
  ★「押し出し」モード中（`window._nnExtOn`）は譲る。
- **選択の見せ方**：半透明オレンジ（`opacity .45`・`depthTest:false`・`renderOrder 999`）を
  かぶせる。本体の材質を書き換えないので、3D側の材質・影の作りに影響しない。
- **水切りアゴ**を辺の属性として新設（`e.ago` / `e.agoD` 既定100mm・垂れ `AGO_LIP=40mm`）。
  ・3D＝天端の外側に庇状の出っ張り＋先端の垂れ＋**裏に回した防水層**（`nnMat`/`nnShade` で既存と同じ質感）
  ・積算＝「**水切りアゴ廻り**」の行が自動で増える（面積＝長さ×(出＋垂れ)、単価は立上りと同じ）
  ・図面(2D)＝外側に青い点線、寸法タグが `H500+アゴ` になる
  ・**パラペット以外に変えると自動で外れる**（壁当り・立上りなしにアゴは無い）
- **編集カード `#d3edit`**（3Dの左下・JSで生成）：種別3つ／立上りH（**300・500のワンタップ**付き）／
  天端W／水切りアゴ／アゴの出／「全辺へ」／✕。右パネルの「選択中の辺」にもアゴの行を足してある。
  ★スマホは幅が足りず**500ボタンが切れて押せなかった**。`flex-wrap`＋ラベル58px・入力52pxで解決（§21の型）。
- 検証は `scratchpad/zumen3d.js`。**同梱の three.js r159（`vendor/three.min.js`）で本物のレイキャスト**まで
  確認している（辺の壁を画面に投影 → その座標に pointerdown/up → 狙った辺が選ばれる）。
  ★**ヘッドレスの注意：`let T` は window に載らない**。`window.T` で見ると常に false で、
  「3Dが初期化されない」と誤診する。**`typeof T!=='undefined' && T` で見ること**（実際に1回ハマった）。
  ★Chromium は `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader` で WebGL が出る。

### 120. 図面・積算：役物が選べない／3Dの選択解除・ツールバー・範囲編集（2026-08-16g）
指摘は4件。①役物系（引込み・笠木・ドレン等）が選択できない・削除で消えない
②3Dでパラペットを一度選択したら戻れない ③3Dでもツールバーのボタンをそのまま使いたい
④パラペットや辺の単位でなく、**ユーザーが決めた範囲**（例：2.5m だけ）で編集したい。

- **★①の原因＝ツール名の取り違え**。「選択」ツールの id は **`'sel'`** なのに、
  役物側の `onCanvasDown` は **`tool==='select'`** を見ていた。一致しないので
  **役物の当たり判定に永久に入らず、選ぶことも消すこともできなかった**（§73の作りっぱなし）。
  → `(tool==='sel'||tool==='select')` に。あわせて `smartDelete()` の先頭に
  「役物を選んでいるならそれを消す」分岐を足した（🗑削除で消える）。
  ★役物のまとまり（IIFE）の中の `sel` は**その中だけの番号**で、辺選択の `sel`（オブジェクト）とは別物。
    外から触るために `window.nnPartSelIdx/nnPartSelName/nnPartDel/nnPlacingId/nnPlaceAtGrid` を公開した。
- **②選択を戻す口を3つに**：カードの ✕／**何もない所をタップ**／**Escキー**。
  ★空タップの判定は「当たり判定（`userData.pick`）に当たらなかったら解除」。
    nn-3dedit-js より**後**に登録するので、辺を拾ったときはあちらが先に処理する。
- **③3Dでもツールバーを出す**（`setTab` の `display` 条件から `d3` を外し、`body.nn3dbar` を付ける）。
  ・2D専用の道具（描画・中抜き・範囲選択など）を3Dで押したら、**図面タブへ移ってからその道具**にする
    （`setTool` を包む）。
  ・**戻る／進むを3Dにも効かせる**（`undoStep`/`redoStep` を包んで `build3D()`。
    `applyHist` は3Dを組み直さないので、押しても立体が古いままだった）。
  ・**部材スタンプは3Dでもタップした場所に置ける**（`nnPlacingId()` があれば
    レイキャストの当たった点 → `nnPlaceAtGrid(gx,gy)`）。
  ・★`#d3wari`／`#d3ext`／`#d3pad` はツールバーと同じ場所にいたので、
    ツールバーの高さを毎回測って `--nn3dtop` に入れ、その下へ逃がす（重なりを実測で0に）。
- **④範囲で編集（`nnEdgeSplitRange(開始m, 長さm)`／`nnEdgeRangeAsk()`）**
  ★**新しい仕組みは作らない**のが肝。**辺に頂点を2つ足して、その部分を1本の辺にする**だけ。
  こうすると 図面・3D・積算・PDF が今までどおりそのまま正しく動く（分けた部分に
  立上り500やアゴを付けても、他の部分は元のまま）。
  ・入口は2つ：右パネル「📏 この辺の一部だけ変える」と、3Dの編集カード下の同じボタン。
  ・`edges[i]` は「pts[i]→pts[i+1]」の辺。挿入は `pts.splice(i+1,0,…)` と
    `edges.splice(i+1,0,…)` を**同じ位置に同じ数**入れ、選択は
    開始が0mなら `i`、そうでなければ `i+1`。
  ・実測：11mの辺を「1.0mから2.5m」で分けると、頂点が2つ増えて選択部分が2.50mになる。
- **★あわせて実バグを1つ修正**：`renderEdgeEdit()` は、選んでいた辺が無くなっていると
  `ring.pts[sel.e]` が undefined で落ちた（「戻る」や部位削除のあとに古い選択が残ると再現）。
  先頭で確かめて、無ければ選択を外すようにした。
- 検証は `scratchpad/fix4.js`（22項目：役物を置く→選ぶ→🗑で消える／3Dのツールバーと重なり0／
  2Dの道具で図面へ／3Dでも戻るが効く／✕・空タップ・Escで解除／範囲で分けて2.5m・
  その部分だけ500＋アゴ・他は変わらない）。

### 121. 図面・積算：3Dの「選んでもすぐ外れる」を根治・全ボタンスイープ（2026-08-16i 相当・版はh）
指摘は「割付表示を削除」「選択ボタンで3Dから図面に戻される」「パラペットをクリックしても
何も起こらない」「こういう変な動きが大量に埋まってるはず。先回りして直せ」。

- **★「クリックしても何も起こらない」の真犯人＝タップの受け口が2本あったこと。**
  §120で足した「空タップで選択解除」の受け口が、§119の「辺を選ぶ」受け口と別々だった。
  辺をタップ→①選ばれる→②選択の飾り（deco）を**その場で作り直す**→③2本目の受け口が
  レイキャスト→**作り直した直後の箱はまだ位置計算（matrixWorld）が済んでいない**ので
  全部すり抜け→「何も無い所を押した」と誤判定→**即解除**。結果「選んでも一瞬で外れる」
  ＝人間の目には「何も起こらない」。
  ★**合成イベントのテストでは再現しなかった**（①だけ確かめて②③の競走を見ていなかった）。
    **本物の `page.mouse.click()` で再現**し、`pick3` を見張ると
    `pick3({p,r,e})` → 直後に `pick3(null)` の2連発が記録された。これが動かぬ証拠。
  → **受け口を nn-3dedit-js の1本に統合**（スタンプ配置・辺選択・役物選択・空タップ解除を
    この順で1つの pointerup でさばく）。nn-3dtools-js 側の受け口は削除。
    レイキャストの前に **`deco.updateMatrixWorld(true)`** を呼ぶ（作り直し直後でも当たる）。
  ★教訓：**同じタップを2か所で別々にレイキャストしない**。後から足すときは必ず既存の
    受け口に分岐を足すこと。★もう1つ：**「選択→即解除」の競走はスパイ（関数を包んで
    呼び出し履歴を記録）で見つける**。見た目のテストだけでは①の成功しか見えない。
- **「選択」「移動」ボタンで3Dから弾き出される問題**：道具の性質で分けた。
  ・選択・移動＝3Dに元からある操作なので**タブは動かさない**。使い方をトーストで案内
    （「3Dではパラペットや壁・役物を直接タップすると選べます」）。
  ・描画・中抜き・範囲選択・段差ライン＝図面でしか使えないので図面タブへ。
    ただし**黙って移らず「①図面タブに移りました（◯◯は図面でかくときの道具です）」**と出す。
    ★黙ってタブが変わるのが「変な動き」の正体。移るなら理由を言う。
- **「🧵 割り付け表示」ボタンは削除**（本人の指示）。継目は常時表示。`d3WariToggle` は
  互換のため残し null ガード。「⬛ 押し出し」を left:10px へ。
- **2画面ボタンを3Dで押したら「図面＋3Dの2画面」に切り替える**。以前は状態だけ変わって
  見た目が動かず、無反応に見えた（スイープで発見）。
- **役物も3Dでタップして選べるようにした**：`parts3D` の箱に `userData.partIdx` を持たせ、
  タップで `nnPartSelect(i)`（新設）→ 下に既存の操作バー（回す・複製・削除）。
  空タップで役物の選択も外れる。案内文も「笠木・ドレン等をタップ＝下のバーで操作」に更新。
- **★全ボタンスイープ（`scratchpad/sweep3d.js`）**：3Dタブでツールバーの31ボタンを
  実際に押して回り、タブ移動・JSエラー・無反応を一覧で出す。**ボタンを増やしたら流すこと。**
- ★検証スクリプトの罠2つ（今回自分で踏んだ）：
  ①**倍率(zoom)環境では rect も clientX も「かけた後のpx」**。page.mouse の座標に
    倍率を掛け直すと25%ズレて空振りする（§61の裏返し）。
  ②サンプル形状は GL+9 の塔屋があり、**屋根が画面上部＝ツールバーの裏に映る**。
    3Dでタップするテストは `T.phi=0.5`（上から見る）にしてから狙うこと。
- 検証：`clickchk.js`（本物のマウスで 倍率1.25/等倍×素直・選択ボタン後・手ぶれ2px の6通り）／
  `part3dchk.js`（3Dで置く→選ぶ→バーで削除→空タップ解除）／`sweep3d.js`／
  回帰 `fix4.js`・`zumen3d.js` すべて○。

**★2026-08-16i：スマホは「⋯道具」廃止・全アイコン直置き・枠なし（本人の指示）**
- 「道具コマンドいらないから、そのままアイコン出して。ボタンの外にある枠はいらん。スマホ版は」
- §29 の tbMenu（⋯道具）は**丸ごと廃止**。スマホIIFEに残るのは文字の短縮だけ。
- スマホの `#toolbar` は **`flex-wrap:wrap`** に戻した（絵だけの小さいボタンなので、
  折り返しても作図面積をほとんど食わない。たて3段・よこ2段で全27個が画面内）。
- **絵のあるボタン（.hasimg）は白い枠・背景・影を全部消す**（nn-tb2 の `!important` 上書き。
  `#tl_undo` 等のID指定の min-width にも個別に勝たせる）。
  ★枠が無くなると**選択中のツールが見分けられなくなる**ので、`.on` は
  「薄い緑の地＋緑の内側リング」で見せる。絵の無いボタン（開く・▦0.5m）は今までどおり白い枠。
- テスト：tb2.js ph は「メニューがある」前提だったので全面更新（枠なし・3段以内・
  はみ出しなし・選択中の緑まで見る）。f4lay・partsmo など既存回帰は○のまま。

**★2026-08-16j：現場記録帳スマホ版に保管写真10枚を追加／進行バーが細すぎた件（本人の指示）**
- `nnShotRow` の `if(PHONE) return ''`（§94「パソコンのみ」）を撤廃。スマホにも5列×2段＋「他」。
  1枚はスマホ用CSSで一回り小さく、**並び（.pshgrid）は max-width:560px で頭を打たせる**
  （よこ画面はカードが広く、上限が無いと1枚が巨大になる）。
  `contain-intrinsic-size` は 220→330px（§95。カードが高くなったら必ず直す）。
- **★進行バーが細かった真犯人は「入れ物の固定高さ」**。`.pprog` は §100 から
  「ラベル＋バー」のたて2段（flex column）なのに、スマホは `height:17px` 固定のままだった。
  ラベルとバーで17pxを取り合い、**バーが flex-shrink でつぶれて半分になっていた**。
  → 入れ物は `height:auto`、バー本体（.prog）だけ `height:24px; flex:none`。
  ★flexの入れ物に固定高さ＋中身2段は、中身がつぶれる定番の形。高さは中身側に付けること。
- テスト2本が仕様変更で古くなった：kai3chk ph（カード260px以内→440px）・
  kai1chk ph（写真を出さない→10個出す）。selcolor の1回きりのNGはタイミングの揺れ（再実行2回○）。

**★2026-08-16k：スマホの保管写真は小さく（「写真予備軍は小さくて全然おっけー」）**
- 1枚 109×82 → **75×56**（.pshgrid max-width 560→390px・min-height 26px）。
  カードの高さ 412→360px。contain-intrinsic-size は 330→300px。
- ★「見やすい大きさ」は §94 でパソコン向けに決めた話。スマホは「枠をデカくしない」が優先。

**★2026-08-16m：スマホの写真予備軍とバーをさらに小さく（2回目の指摘）**
- 写真 75×56 → **49×37**（.pshgrid max-width 390→256px・gap 3px）。
  ★この大きさでは「写真1」の文字は入らないので **font-size:0 で消し**、
    空き枠＝薄い**点線**の四角／写真あり＝実線＋絵、で見分ける。押す動作は今までどおり。
- 進行バー 24px → **15px**（幅170→132px）。12px＝つぶれて細い・24px＝太い、の中間。
- カードの高さ よこ 360→**311px**／たて 338→**289px**。contain-intrinsic-size は295px。
- ★**大きさの指摘は2回来る前提で、1回目から思い切って落とすこと。**
  今回は 109→75（不足）→49 と2手かかった。

**★2026-08-16n：写真予備軍は「1段に10枚」（本人の指示「無理やり2段にするな」）**
- スマホの `.pshgrid` を **grid(5列) → flex 1段・横に送る**（`overflow-x:auto`・幅は `flex:0 1 auto`）。
  ★`flex:1 1 auto` のままだと帯が横いっぱいに伸び、「他」ボタンが右端へ飛ぶ。
- 1枚 46×35。カードの高さ よこ 311→**269px**／たて 289→**247px**（たては1画面に3.5件）。
  contain-intrinsic-size は 255px。
- ★**画面が狭いからと段に折り返すと、かえって「デカい」と見える。** 1段＋横送りが正解。

**★★2026-08-16p：現場記録帳がiPhoneで「問題が繰り返し起きました」と落ちた（緊急修正）**
- 原因＝16n で写真予備軍の帯に **`-webkit-overflow-scrolling:touch`** を付けたこと。
  カード100件×100個の**慣性スクロール部品が一気に作られ、メモリ切れでWebKitごと落ちる**
  （iOSで昔から知られたクラッシュの型）。クラッシュの開始時刻が16n公開直後と一致。
- 直し方＝この1行を消すだけ。**いまのiPhoneは `overflow:auto` だけで慣性スクロールが効く**ので
  そもそも不要だった。10枚×46px≒490pxは帯の幅に収まるので、ふだんは送る必要すらない。
- ★**「一覧の1行ごと」に付くものに `-webkit-overflow-scrolling:touch` を書かないこと。**
  1〜2個なら平気でも、100件の一覧では即死する。

**★2026-08-16q：スマホの写真予備軍は5枚・提出書類と同じ行に（本人の指示）**
- 「10枚でなくて5枚。そのかわり関連書類を写真予備軍と同じ行に。二段にしないで」
- ★**置き場所を変えるのが肝**。`.pshots` は `.pleft` の中、`.pdocs` は `.pleft` の兄弟（`.pmain` 直下）
  なので、そのままでは絶対に同じ行にならない（スマホは `.pdocs{width:100%}` で改行していた）。
  → `nnShotRow(p, where)` に引数を足し、**スマホのときだけ `.pmain` 直下（提出書類の直前）に出す**。
    パソコンは今までどおり `.pleft` の中で10枚（§94の「見比べる」目的を守る）。
  → CSS は `.pdocs{width:auto; flex:0 1 auto}` にするだけで横に並ぶ。
- カードの高さ よこ 269→**230px**／たて 247→**215px**。contain-intrinsic-size は225px。
- ★`selcolor.js` は前から**たまに1件だけ落ちる**（クラスを直接触る①の格子テストと render の競走）。
  3回連続で通れば product は正常。

### 122. 付箋タグの見出しを全ページへ／図面・積算の下絵の説明をヘルプへ（2026-08-16r）
指示は2つ。①「現場一覧のページにある、付箋タグとの境界線。これ他のページにも付与して」
②「下絵がどうのこうの、の説明は基本説明書ボタンでわかるようにするから右側の内容は不要」。

- **①現場記録帳の `.httl`（パワプロ風の平行四辺形・§62g）＋その下の境界線を、他9ページへ**
  （`<style id="nn-httl">`＋`<script id="nn-httl-js">`。差し込みは `scratchpad/add_httl.py`）。
  ・対象の見出し＝`.panel > h4` / `.wr-box > h4` / `.dpanel > h4` / `.proc-card > h4`
  ・**JSで見出しの文字だけを `<span class="httl">` に包む**。右端の補助（`.r` `.cta` `.sub` 等）は
    包まない＝タグの外に残す（包むと「仕様登録から」までタグの中に入って間延びする）。
  ・★**大きさ（font-size）は指定しない**。各ページの h4 の文字サイズをそのまま使うので、
    ページごとの統一感を壊さない（現場記録帳だけは従来どおり17px指定）。
  ・★**中身を作り直すページがある**（仕様登録・材料登録・ライブラリ・国交省仕様は
    項目を選んで初めて見出しが生まれる）。**MutationObserver（0.2秒のまとめ）で包み直す**。
    ★検証も「一覧の行をクリックしてから数える」こと。読み込み直後は0個で正しい。
  ・斜めのぶん左右に約5pxはみ出すので、`margin` を左右10〜12px確保している（§62g と同じ話）。
- **②図面・積算の右パネルから「下絵の使い方」の緑の箱を削除**し、
  帯の「？ 操作方法」の①図面タブに **🖼 下絵（航空写真をなぞる）** の項として移した。
  右側には操作するボタンだけが残る。
- ★`selchk.js` は**この変更の前から1件★NG**（図面・積算の `#nnPlanBox` の select/入力欄が16px。
  §50 で意図してそうしている）。今回とは無関係。

### 123. 図面・積算：隣り合う屋根の境界を自動でつなぐ（2026-08-16s）★本人の指示
「2つめの屋根を作るとき、屋根が隣接する境界のエッジをわざわざ引かなくてよいようにして。
屋根1が2階・屋根2が1階のとき、その境界に立上りパラペットが2つできるケースは稀なので不要」。

- **`<script id="nn-adj-js">`**（本体は書き換えず、末尾で `nnResolvePoint`／`closePoly`／`draw` を包む）。
- **①境界の自動補完**：作図中、**始点が既存の屋根の輪郭の上**にあり、いま置く点も
  **同じ屋根の輪郭の上**なら「ここで閉じる（境界は自動）」になる。
  足りない境界は、その屋根の輪郭を**なぞって自動で足す**（紫の点線で予告表示）。
  ★**どちら回りでなぞるか＝「できあがる面積が小さいほう」**。逆回りは既存の屋根を
    ぐるりと囲むので面積がその分だけ大きくなり、これだけで確実に見分けられる。
  ★輪郭の上に乗ったかの判定は `ringHit`（画面6px）。`vertexMagnet`（角に吸う）とは別物で、
    **辺の途中でも乗る**。
- **②立上りを二重にしない**（`nnSyncSharedEdges`）。部位どうしで**半分以上重なっている辺**を
  境界とみなし、
  ・高さ（GL）が同じ … 両方「立上りなし」（同じ屋根面の切れ目）
  ・高さが違う … **低いほう＝壁当り／高いほう＝パラペット**
  手で境界をなぞって描いたときも同じ（closePoly のあとに1回だけ走る）。
  GL+ の欄を変えたときも呼び直す（部位リストの onchange に追加）。
- 検証は `scratchpad/adjchk.js`（10項目）。
  ★**テストの落とし穴**：`setTool()` は `drawPts` を空にするので、
    テストで点を仕込むときは **setTool を先に呼ぶこと**（ここで1回ハマった）。
- ★zfix2／f3chk／f4chk の★NG（計4件）は**この変更の前から同じ**（別セッションの3D改修に
  検証スクリプトのニセTHREEが追いついていない）。今回とは無関係。

**★★2026-08-16t：現場記録帳がまた落ちた（16pの直しが不十分だった）**
- 16p で `-webkit-overflow-scrolling:touch` だけを消したが、**`overflow-x:auto` が残っていた**ため
  16q でまた落ちた。**原因は「慣性の指定」ではなく「スクロールする箱そのもの」**。
- ★**実測で確定**：一覧を開いた状態で `#list` の中に **overflow:auto の要素が100個**
  （カード1件につき1個）。iOSはこれを1つずつスクロール部品として持つのでメモリを使い切る。
  → 写真予備軍の帯から overflow を外して **100個 → 0個**。写真5枚＝約242pxで幅に収まるので
    そもそもスクロールは要らなかった。
- ★**「一覧の1行ごと」に `overflow:auto`／`scroll` を書かないこと。**
  1〜2個なら平気でも、100件の一覧では落ちる。
  （他ページの `-webkit-overflow-scrolling` はタブ・表・ナビなど**固定の1〜数個**なので問題ない）
- ★調べ方：`[...document.querySelectorAll('#list *')].filter(e=>{const s=getComputedStyle(e);
  return /auto|scroll/.test(s.overflowX+s.overflowY);}).length` で数える。

### 124. 図面・積算：辺を方眼と同じ太さに・色は濃く（2026-08-16u）★本人の指示
「作成した辺が太い。辺はマス目と同じ太さに。ただし色は濃く。
**太いと細かい描写移行の際に対応できないから**」。

- **太さ＝方眼の太線と同じ 1px**（`ctx.lineWidth`）。
  ・確定した辺 … 1.8〜2.5px（壁当り3・選択4）→ **1px**（選択中だけ1.6px）
  ・作図中の線 … 白フチ2.4＋橙1.1 → **橙1px の点線だけ**
  ・確定済みの下書き線 … 白フチ3.0＋青1.5 → **青1px**
- ★**「太く見える」いちばんの原因は白フチ（下地の太い白線）だった**（§30・§35で足したもの）。
  細い線に白フチを付けると、見た目の太さは**フチの太さ**になる。細くするときは
  **フチごと外す**こと。方眼が薄い水色なので、色を濃くすれば白フチ無しでも十分読める。
- **色は濃く**：確定 `#1e5c3a`→**`#0a3c1f`**／選択外 `#7a8a7e`→**`#39463d`**／
  壁当り `#8a5a20`→**`#5c3408`**／作図中 `#d9660a`→**`#b34c00`**／下書き `#14449c`→**`#0b2c6b`**。
- 頂点の丸も線に合わせて小さく（4.5/3.2→**3.2/2.4**、白フチ1.5→1px）。
  ★**つまむ当たり判定は変えていない**ので、指での操作感は今までどおり。
- ★選択中の辺だけ1.6pxにしたのは、同じ太さだと「いまどの辺を編集しているか」が
  分からなくなるため（色＝オレンジだけでは細い線で見分けにくい）。
- 回帰8本（adjchk／ang5／cross／cross2／align1／zdir／warichk／sec2chk）とも○。

### 125. 現場記録帳の一覧カード：4件まとめて（2026-08-16v）★本人の指示
指示は4つ。①写真候補5枚と提出書類が被っている ②メモを不具合の行に（被らないように）
③進捗バーを半分に短縮し、空いた場所に請負金額を入れる（**この行は絶対に1行**）
④「材料金額」→「材料代」。差し込みは末尾の `<style id="nn-card16v">`。

- **★③でいちばんハマったところ：バーの本体には昔からの `min-width:100px` があった。**
  入れ物（`.pprog`）を 132→66px に細くしても、**中のバー（`.prog`）だけ100pxのまま右へはみ出し、
  となりに置いた請負金額の上に重なっていた**（実測 523〜623px／請負金額は593から）。
  → `#list .pcard .rhead .pprog .prog{min-width:0 !important; width:100% !important;}`。
  ★**入れ物を細くするときは、中身の `min-width` も必ず外すこと。**
  ★この不具合は**数値のテストをすり抜けた**（行からのはみ出しは0だったため）。
    画面を撮って目で見て気づいた。→ `card6.js` に**部品どうしの重なりを総当たりで見る判定**を追加した。
- **③1行の保証**：`flex-wrap:nowrap` にするだけでは足りない（折り返さない代わりにはみ出す）。
  **縮んでよい部品を1つだけ決める**のが正解＝物件名だけ `flex:0 1 auto` ＋ `text-overflow:ellipsis`、
  ほかは全部 `flex:none`。1366〜2560px・スマホたて／よことも1行を実測で確認。
  現場名の行の並び：物件名／ステータス／工法アイコン／進行度バー／**請負金額**／利益予定額／詳細へ。
  請負金額は下段（r1）からは外した（同じものを2か所に出さない）。
- **★①「被る」の正体**：提出書類の枠（`.pdocs`）が `flex:0 1 auto`＝**縮んでよい**設定だった。
  幅が足りないと枠だけが縮み、**中のボタン（最小40px）が枠の外へあふれて**左どなりの
  写真の帯に乗る。枠は縮んでもボタンは縮まないので、こうなる。
  → 写真の帯も提出書類も **どちらも `flex:0 0 auto`**（縮ませない）。
  こうすると、万一幅が足りなくても**重なるのではなく行が変わる**だけで済む。
  あわせてボタンを一回り細く（min-width 40→34px・gap 4→3px）し、境目に点線＋10pxのあきを入れた。
  実測：写真の帯とのすき間18px・同じ行・はみ出し0。
  ★**「縮んでよい枠」の中に「縮まない中身」を入れると、あふれて必ず重なる。**
    どちらか片方に統一すること。
- **②メモ**は `flex:1 1 100%`（＝必ず1行まるごと）をやめ、不具合タグの右に置いた
  （スマホで実測 591px幅）。入りきらないときは折り返す＝重ならない。
- カードの高さ よこ・たてとも **215px**（16qの230pxからさらに低い）。
  `contain-intrinsic-size` は据え置き（225px・§95）。
- 検証は `scratchpad/card6.js`（`node card6.js` ／ `ph` ／ `land`。13項目）と
  `card6b.js`（1366〜2560pxで1行・その場編集）／`scrollcnt.js`（★iPhoneが落ちない確認＝
  一覧の中のスクロールする箱が0個・§123t）。
  回帰は記録帳の14本（pwlist／edit1／exist1／go1／kai1〜5chk／photo4／faces1／memo1／
  card4chk／card5chk／kdef）＋fast1 land とも★NGなし。

### 126. ★一覧の「灰色の空白」「文字の見え隠れ」「行のばらつき」は全部 content-visibility が原因（2026-08-16w）
本人の指摘3件。①下部の灰色みたいなのは何？ ②現場写真と提出書類が同じ行にならない物件がある
③写真の隙間に謎の言葉が見え隠れする。＋④トップ画面の写真が縦長・写真候補と同じ4:3にして。

- **★①②③の根っこは同じ＝§95 で入れた `content-visibility:auto`。**
  これは「画面に映っていないカードは組み立てない」指定で、100件の一覧を1270ms→195msにした立役者。
  ただしブラウザの判断は**「画面に入っているか」だけ**なので、
  **画面のすぐ下にあるカードは組み立てられず、灰色の空白のまま見えていた**。
  組み立ての途中が見えると、文字が見え隠れしたり、行の並びが違って見える。
  ★たて画面は1画面に**約10件**入る（幅980を0.40倍に縮めて出しているため、
    画面の高さは CSS で約2100px ある）。だから空白が何枚も見えていた。
- **直し方＝「後回しにするのは画面から離れたカードだけ」**（`<script id="nn-cvwin-js">`）。
  ・CSSは `#list .pcard.nnoff{content-visibility:auto; …}`＝**クラスが付いた分だけ**後回し。
  ・カードを組み立てるとき、**先頭4件以外に最初から `.nnoff` を付ける**
    （★後から付けると1回目の組み立てで100件ぶん計算してしまう。実測 176ms→1069ms）。
  ・IntersectionObserver（逃げしろ2000px）で、近づいたカードの `.nnoff` を外す。
    実測：常に20件前後が組み立て済み。スクロールしても画面内の空白は0件。
  ★**罠1：`#list` 自身が「中がスクロールする箱」**。root を指定しないと箱に切り取られ、
    **逃げしろがまったく効かない**（1枚も外れなかった）。探すときは**自分自身から**見ること。
  ★**罠2：見張りを始めるのは「画面に出したあと」**（`setTimeout`→`rAF`×2）。
    すぐ始めると位置が決まる前に判定され、96/100件が後回しのまま残った。
  ・代償：一覧を開く時間が 176ms→**約500ms**（CPU4倍の負荷。実機はこれより速い）。
    灰色の空白を出さないためなので、これでよい。テストの上限も 350→450ms に直した。
- **②は仕上げにもう1つ**：左の本文（`.pleft`）が「中身の幅ぶん」しか取らない設定だったので、
  中身の短い物件では本文の右に写真の帯が入り込み、提出書類だけ次の行へ落ちていた。
  → `flex:1 1 100%` で**必ず1行まるごと**使わせる。写真の帯と提出書類は必ず2行目に並ぶ。
- **③は元から文字を書かない**ようにした。空き枠の「写真1」は CSS の `font-size:0` で
  隠していたが、iPhoneでは見え隠れした。**スマホでは最初から文字を出さない**（説明は title に残す）。
- **④メインの現場写真を 4:3 に**（172×150→**172×129**。写真候補5枚と同じ比率）。
  高さを決め打ちにせず `aspect-ratio:4/3` で決める。カードの高さ 215→**194px**。
  `contain-intrinsic-size` も 195px に合わせた（★カードの高さを変えたら必ず直すこと・§95）。
- 検証は `scratchpad/card6.js`（16項目に増えた。4:3・画面内に空白0・空き枠に文字0を追加）と
  `cvscroll.js`（**スクロールしながら**画面内の空白を数える）・`cv2.js`／`cvspeed.js`（速さ比べ）。
  ★`fast1.js` の content-visibility の判定は `.pcard` → **`.pcard.nnoff`** に直した（仕様変更）。

### 127. 物件名は絶対に隠さない／写真候補の「謎の文字」の正体（2026-08-16x）
本人の指摘2件。①（写真の隙間の謎の言葉が）変わっていない ②現場名が全部入らない・隠れている。危険。

- **★②「1行に全部を横並び」は物理的に無理だった。** 実測（よこ画面）：
  この行に使える幅 **718px**／いちばん長い物件名 **465px**／
  ほかの部品（ステータス58＋工法40＋バー66＋請負金額136＋利益134＋詳細へ60＋すき間24）＝**518px**。
  名前に残るのは200pxしかなく、**100件中25件しか収まらない**。
  版vで「入りきらない分は … で切る」にしたのは、**読めなくなる＝危険**という指摘のとおり。
  → **物件名だけ折り返してよい**ことにした（`white-space:normal`＋`flex:1 1 auto`）。
    ほかの部品は横に並んだままなので、**行が2つに割れることはない**（指示どおり1行のまま）。
    長い名前のときだけ名前の箱が2段になり、この行が少し高くなる。
    実測：たて画面は 64件が1段・36件が2段／カードの高さは 194→196px。**隠れる文字は0**。
  ★**「1行に収める」と「全部見せる」は、幅が足りなければ両立しない。**
    どちらを取るかは中身の大事さで決める（物件名＞見た目の揃い）。
- **★①「謎の言葉」の正体＝写真が読めなかった枠に残る番号（1〜5）。**
  写真ありの枠は `<img>＋<i>番号</i>` で作っている。保存した写真の置き場所が変わるなどして
  **絵が出ないと、右下の小さな番号だけが残る**＝「文字が見え隠れする」ように見えていた。
  版wで空き枠の文字（写真1…）を消したが、**そちらは別物**だったので直っていなかった。
  → `onerror="nnShotBad(this,pid)"` で**枠を空き枠に戻す**（番号も消え、押すと写真を入れられる）。
    あわせてスマホでは番号自体を出さない（46pxの枠には小さすぎる）＋`overflow:hidden`。
  ★**ヘッドレスでは再現しない**（サンプルは写真0枚なので `<i>` が作られない）。
    実機だけで出る不具合は、**HTMLの組み立て方から「どんなときに出るか」を逆算する**こと。
- 検証は `scratchpad/namefit.js`（**全100件**で「名前が1文字も隠れていない」「行が2つに割れない」
  「部品どうしが重ならない」を、たて／よこ／パソコンで見る）。
  回帰は記録帳15本＋fast1（PC/land）＋cvscroll＋scrollcnt とも★NGなし。

### 128. 現場名の行は4つだけ・メモと「詳細」を最下段へ（2026-08-16y）★カードの並びの決着
本人の指示3つ。①物件名が2行になるのは気持ち悪い。物件名・ステータス・アイコン・請負金額を
一番上にして、**それ以外を下にずらし、なんとしてでも1行で完結**させる
②「詳細へ」→「詳細」にして右下へ ③メモは一番下の行へ。

- **カードの並び（上から）**
  1. `.rhead` … 物件名／ステータス／工法アイコン／**請負金額**（この4つだけ）
  2. `.rsub` … 進行度バー／利益予定額（右寄せ）　← 下ろした行
  3. `.r1`〜`.r3` … 元請・請負単価・材料代・工賃・他／住所・日付／改修仕様・数量・記号・メーカー
  4. `.rtag` … 不具合タグ（**タグが無い物件では行ごと出さない**）
  5. 写真候補5枚／提出書類（スマホ）
  6. `.rbot` … **メモ（幅いっぱい）＋右端に「詳細」ボタン**＝カードの右下
- **★なぜ①で解決するか（実測）**：この行に使える幅は **718px**。
  前は 名前以外の部品で **518px** 使っていたので名前には200pxしか残らず、
  いちばん長い名前（465px）は**どうやっても入らなかった**（100件中25件しか収まらない）。
  進行度・利益予定額・詳細へ（計290px）を下ろすと名前に **472px** 残るので、
  **100件すべてが1行**に収まる（実測・たて／よこ／パソコンとも折り返し0）。
- 進行度の見出しは**バーの上ではなく左**に置く（たて2段だと13px背が高くなるため）。
  バーは 110px（スマホ）／150px（パソコン）。
- カードの高さ：スマホ 194→**234px**／パソコン 275→**339px**。行が2つ増えたぶん。
  ★「1行に収める」と「全部見せる」を両立させる代償なので、ここは戻さないこと。
- ★**テストが古くなった6本**（§68。product は正しい）：
  `go1`（「詳細へ ›」→「詳細」・置き場所が .rhead → .rbot）／`kai3chk`（バーが .rhead → .rsub）／
  `kai4chk`／`kai5chk`／`faces1`（カードの高さの上限 290/300 → 345）／
  `card6`（バーの位置・メモの位置・詳細ボタン）。
- ★`selcolor` の「たまに1件だけ落ちる」は**テスト側の待ち不足**だった（§112では原因不明としていた）。
  カードの色は少しずつ変わる指定（transition）なので、250msでは**途中の白**を拾っていた。
  `sel` クラスは毎回ちゃんと付いていた。待ちを700msにして解消。

### 129. カードの行構成を確定・物件名は「縮めて必ず入れる」・ピンチ拡大の崩れ（2026-08-16z）
本人の指示（詳細なルール付き）。①物件名の枠は**情報枠の半分まで**。それより長い名前は
2行にせず**文字を小さくして必ず枠に入れる** ②2行目＝元請・数量・メーカー・仕様（「改修仕様」→「仕様」）
③3行目＝請負金額・単価（「請負単価」→「単価」）・材料代・工賃・他
④4行目＝住所・契約（材工/材のみ/工のみ）・契約日/予定・着工日/予定・完成日/予定
（日か予定かは**ステータスから自動**）。進行度は**写真の下（既存・区分・構造の下）**へ。
＋「ズームしたら画面が変になる」不具合。

- **①物件名の自動縮小（`<script id="nn-ttlfit-js">`）**：CSSは `max-width:50%`＋`overflow:hidden`。
  あふれる名前だけ `scrollWidth÷clientWidth` の比で font-size を縮める（下限8px）。
  呼びどころは3つ：一覧の組み立て後（nn-cvwin-js の scan）／**後回しカードが組み立てられた瞬間**
  （IntersectionObserver。★1コマ待ってから測ること）／画面の向きが変わったとき。
  実測：よこ画面で最小11.4px・全100件あふれ0・全カード1行。
- **④日/予定はステータスで決める**：kan＝契約日/着工日/完成日／kou＝〜/〜/完成予定／
  keiyaku＝契約日/着工予定/完成予定／それ以外＝すべて予定（`_dk` 配列）。
  契約区分は新フィールド `p.kbn`（既定「材工」）。その場編集のプルダウンに追加
  （startEdit の分岐・commitSel・選択肢 材工/材のみ/工のみ）。
- **進行度は `.pcol`（写真の列）の最後**。見出しはバーの左・％はバーの右端の上
  （％の白文字は黒い筒の上でしか読めない）。
- **★★ピンチ拡大の崩れの原因＝`nn-fillh-js` の fill()**。
  拡大中は getBoundingClientRect の座標が「見えている範囲」とずれるため、
  そのまま minHeight を計算すると盛大に狂い、**画面の下に巨大な空白**が出ていた。
  → fill() の先頭で **`visualViewport.width < innerWidth×0.96` なら何もしない**
  （＝拡大中。やめれば resize が来て測り直される）。
  ★検証は「rect.bottom を偽装して、①拡大中は minHeight が付かない
  ②等倍なら同じ偽装で付く」の**両方**を見る（検算に fill 自身を使わない・§117sの教訓）。
- ★**テストが古くなった6本**：kai3chk（バーの場所）／kai4chk（仕様・着工日・完成日）／
  kai5chk（見出しはバーの左）／faces1（qsum が r3→r1）／card6（請負金額はr2先頭・バーは写真の下）／
  namefit（後回しを外したら nnTtlFitAll を呼ぶ）。product は card7.js／kbn1.js で先に検証済み。
- ★selcolor の残っていた揺れ＝編集の囲みを開いた直後に一覧が作り直され、
  **色の変化（transition）が最初からやり直しになる**ことがあるため。白のままなら0.6秒待ち足す。
- 検証は `scratchpad/card7.js`（①〜⑤・たて/よこ/PC 各15項目）と `kbn1.js`（契約の編集・
  長い名前の縮小・バーが枠内）。回帰は記録帳18本＋fast1×2＋cvscroll＋scrollcnt 全部○。

### 130. 一覧カード：モック準拠の配置替え・詳細はボタンからだけ（2026-08-17a）★本人のモック画像つき
指示は8つ。①現場名の行のアイコンの隣に仕様（記号バッジ） ②住所の下に進行度＋少しあけて不具合タグ
③④利益予定額の下に**大きな「詳細」ボタン** ⑤写真予備軍と提出書類を**1行**（PCは10枚を1段）
⑥メモは最下段 ⑦**詳細へはボタンを押したときだけ**移る ⑧下部に余白を出さない。

- **カードの並び（確定形）**：rhead（物件名・ステータス・工法アイコン・記号バッジ・利益予定額）→
  `.pmid`＝左 `.pmidL`（r1〜r3・`.rprog`＝進行度＋不具合タグ・faces）＋右 `.pgo`（大きな詳細）→
  `.rfiles`（写真の帯＋提出書類7ボタン1行）→ `.rbot`（メモ幅いっぱい）。
  提出書類の2つの `.db` は1つに統合。`nnShotRow` の where 引数は廃止（PC10枚／スマホ5枚とも1本の帯）。
- **⑦2回タップの移動と待機状態（armed）を廃止**。カードのタップは「選ぶ（黄色）」だけ。
  document capture の pointerdown から armed 系を削除し、click リスナーごと撤去。
- ★**古い「5列×2段」時代の `max-width:415px` が生きていて、写真10枚が提出書類の下に潜った**。
  1段化するときは旧レイアウトの幅の上限を必ず消すこと。
- ★**写真マスの4:3が崩れた**：`.pshgrid` が stretch のままだと**「他」ボタンの背に合わせて
  マスが伸びる**。`align-items:center` にすると flex-basis 62px×aspect-ratio で 62×46.5 になる。
- ★検証スクリプトの注意：2000px幅のPCは zoom 1.25。**probe が返す 77.5×58.125 は
  62×46.5 の1.25倍**＝正しい（§61。割り忘れて「4:3が崩れた」と誤診しかけた）。
- ★**テストが古くなった8本**（product は go2.js／shot17.js で先に検証済み）：
  pwlist・edit1・selcolor（2回タップ→詳細ボタン）／go1（.rbot→.pmid）／
  kai3chk（バー .pcol→.rprog・写真4:3）／kai1chk（写真マスは小さく・4:3で判定）／
  card6・card7（バー330/220px・詳細の位置）。
- カードの高さ：PC 238px／スマホよこ 208px（下の余白4px＝⑧OK）。
- 検証は `scratchpad/go2.js`（⑦：何度タップしても移動しない・ボタンでだけ移る・④大きさ）と
  `shot17.js`（配置の実測）。回帰21本＋fast1×2＋cvscroll＋scrollcnt すべて○。

### 131. 現場一覧の検索タグを拡充（2026-08-17b）★「これだと足りない」
本人の指示「検索タグが足りない。増やしたり位置を調整したり、使いやすくなるように」。

- **絞り込みタグを6種追加**（`LIST_FIL` に足すだけで buildChips〜lfPass まで全部つながる）：
  **既存**（既存防水）／**区分**（新築・改修…）／**構造**（RC・S…）／**契約**（材工・材のみ・工のみ）／
  **金額**（請負金額を5つの幅で：100万円未満〜1000万円以上）／**数量**（㎡を4つの幅＋m単位）。
  ★金額・数量は**幅（バンド）で区切る**こと。1件ずつの値では絞り込みにならない。
    値の並びも件数順ではなく**小→大**にする（`nnBandSort`。件数順だと100万未満が真ん中に来る）。
- **並び順をよく使う順に**：ステータス・工法・メーカー・元請・地域・年度・既存・区分・構造・
  契約・金額・数量・不具合、**並び替えだけ右端**（点線の仕切りつき）。
  ★不具合チップは buildChips の innerHTML の**中**（nnDefChipHTML）、並び替え（profit）はその前に
    入っているので、buildChips を包んで**並び替えチップを appendChild で末尾へ動かす**
    （`nn-fil17-js`）。★包んだだけでは初回に効かない（nn-defect-js が読み込み時に1回描いている）
    ので、**包んだ直後に1回呼ぶ**こと。
- **スマホはチップの横スクロールをやめて折り返し**（2段）。12種に増えると横送りでは
  「見えないタグ」ができて存在に気づけない。折り返せば全タグが最初から見える。
  メニューは #nnPortal に出すので折り返しても閉じ込められない（§4）。
  帯の高さ 43→56px（1段ぶん増）。パソコンは1段のまま（実測2000pxで収まる）。
- ★検査の注意：`.dfmenu`（チップごとの隠れメニュー）は overflow:auto を持つが**固定14個**なので
  §123t（1行ごとに100個で落ちる）の対象外。見るべきは「チップの帯そのもの」がスクロール箱でないこと。
- ★一覧は「最初の14件→残りは次のコマ」の2段組み立て（§95）なので、絞り込み直後に
  件数を数えるテストは**700ms待ってから**数えること（待たないと14件と誤カウント）。
- 検証は `scratchpad/fil17.js`（PC／たて／よこ 各16項目：14個・並び順・隠れタグ0・
  新タグ6種で実際に絞り込める・金額の並びが小→大・メニューが画面内）。
  回帰22本＋fast1×2＋cvscroll＋scrollcnt すべて○。

### 132. 検索タグの高さ統一・絞り込み中バーの余白・実機だけの「1」と被り（2026-08-17c）
本人のスクショ図示4件。①検索タグの高さを最初から（選択中と同じ）小ささに ②絞り込み中バーの
余白を詰める（PC/スマホ共通） ③写真マスに謎の文字「1」 ④写真マスが提出書類に被る。

- **①タグの高さ**：実測で**選択中のチップ（21px）のほうが未選択（25px）より小さかった**。
  → 全チップを height:21px（スマホ19px）に固定＋`display:inline-flex`。
  選んでも高さが変わらない＝画面がガタつかない。
- **②絞り込み中バー**：`.listhead` の margin を負に（上-5/下-3）・`.lfil` `.ftag` `.freset` の
  padding を1段詰め。バー34→24px・上下の余白 計17→9px（PC）。
- **★③④はヘッドレスで再現しない実機（iOS）だけの症状**。会話の記録から実機のスクショを
  取り出して拡大し（§8）、「1」＝写真番号の `<i>1</i>` らしき斜体の数字・被り＝写真マスが
  提出書類のボタンの上に描かれる、と特定。原因の完全特定はできないため**根こそぎ断つ**方針：
  ・スマホでは番号 `<i>` を **CSSで隠すのではなく最初から出力しない**（PHONE分岐）
  ・写真の名前を title に入れない（**HTMLに使い手のデータを混ぜない**＝quoteで壊れようがない）
  ・提出書類を `position:relative; z-index:1`＝万一かぶっても**提出書類が上**になり隠れない
  ・写真の帯は `overflow:hidden`＝帯の外へ1pxも描けない（auto/scrollではないのでiPhone安全・§123t）
  ・`.rfiles` は幅が本当に足りないとき**折り返す**（§125の型：重なるのではなく行が変わる）
- 検証は `scratchpad/tag17c.js`（PC/ph 各9項目。**onerror を殺して「壊れた写真が残った」最悪の
  状態を作り**、番号が出ない・提出書類の全ボタンが elementFromPoint で押せることまで見る）。
  回帰22本＋fil17×2＋fast1＋cvscroll＋scrollcnt すべて○。

### 136. 3D操作パッドのボタン4枚を本人の絵に（2026-08-18c）
- **＋／−／⟲回る／⟳回る** が届いた（緑の丸ボタン）。`icons/btn_d3_zin/zout/rl/rr.png`。
  組み込みは末尾の `nn-d3padimg`（絵が読めたら白い枠・背景・影を消して絵だけに＝§12の型。
  読めなければ従来の白ボタンのまま）。**絵が増えたら nn-d3padimg-js の MAP に1行足すだけ**。
  未着＝上から／横から／全体／画面 の4個。
- ★届いた絵はJPEG（白背景の焼き込み）。**フチからの塗りつぶしで透過化**（§81）→トリム→高さ168px。
  中の白い絵（＋や矢印）は黒い輪郭で守られるので消えない。
- 長押しで動き続ける配線（§39）はそのまま生きる（img は pointer-events:none）。検証は d3padimg.js。
- **2026-08-18d 追記**：上から（btn_d3_tup）／横から（btn_d3_tdn）も届いて6個に。
  **矢印が上へ＝上から見る（既存の ◹ と同じ向き）／下へ＝横から見る**の対応で入れた。
  未着は 全体（d3_fit）／画面（d3_theme）の2個だけ。

### 140. 下地（構造体）を5種から選べるようにした（2026-08-19a）★本人の指示
「RC／S＋ALC／SRC／W／S＋ルーフデッキ から選べて、下地の感じも本当にその構造体のように」。
`nn-kouzou-js`（末尾）。保存は **`state.kouzou`**（図面と一緒に保存）。

- **選ぶ場所は2か所**：積算・設定パネルの先頭「下地（構造体）」と、③断面のバー。どちらも連動。
- **①2Dの断面ハッチ**（`nnKouzouHatch2D`。輪郭でクリップして中に描く）
  ・RC＝45度の細線＋骨材の点／**SRC**＝それに**H形鋼の記号**を重ねる
  ・S＋ALC＝**600mmごとのパネル目地**＋気泡（ALCの特徴）
  ・**W＝上に野地板（合板12×2）＋下に垂木45×105@455（木口の×つき）**。
    ★**全体を木で塗りつぶさないこと。** 実際の木造は「板＋垂木＋空き」なので、
      塗りつぶすと構造に見えない（最初そうして作り直した）。
  ・S＋ルーフデッキ＝**躯体の下にぶら下がる台形の波**（山高50・ピッチ150・t1.2の札つき）。
    ★★**デッキだけはクリップの外に描く。** 中に描くと3Dでも下面が見えず
      （カメラは水平 phi≦1.45 までしか下がらない）構造が分からない。実際に1回そうして作り直した。
- **②3Dの材質**：`nnMat` を包んで **kind==='concrete' のときだけ**下地の材質に差し替える
  （build3D 本体は触らない＝図面の3Dにも自動で効く）。質感はその場で描く（画像ファイルを増やさない・§80）：
  `alcTex`（気泡）／`woodTex`（木目）／`deckTex`（亜鉛めっき）。RC・SRCは既存の `nnConcTex`。
- **③3Dの構造の形**（`nnKouzouMesh3D`。断面を押し出した立体に足す）
  ・デッキ＝**厚さ1.2mmの波形鋼板**を躯体の下に／W＝**垂木**を下に／ALC＝パネルの目地／RC・SRCは無し
- **④PDF**：躯体の塗りを下地のパターン（`<pattern id="kz">`）に差し替え、図面に「下地：〇〇」を明記。
- 検証は `_check/kouzou.js`（15項目：5種の描き分けを画素の指紋で比較・3Dの色と形・PDF・保存・図面3Dにも波及）。

### 139. 断面は「平面図と同じように描く」方式に作り直し（2026-08-18g）★本人の指示
版fの「H・Wを入力」方式は却下。**断面の輪郭を自分で描く**方式にした（`nn-sectdraw-js`）。

- **③断面タブの先頭に「✎ 断面を描く（部位専用図）」**。図面が無いときは自動でこのモード。
  ・座標は m（x＝よこ／y＝高さ・上が＋）。**1マスは 50/100/250/500mm を「▦」で切替**
  ・打点の作法は平面図と同じ：**1点目＝交差点／以降＝5度きざみ＋長さ（マスの1/10）きざみ＋
    既存点への たてよこ そろえ**。スマホは**指の右上に赤い照準**（+36,-52）。
  ・道具＝✎描く／🛡防水層（辺をタップで指定）／↩↪／🗑全消し／▦マス／⊡全体／奥行き
  ・データは **`state.sect`**（cell/pts/closed/wp/depth）＝図面と一緒に保存・復元される
- **⬔ 3Dで見る**＝描いた輪郭を **ExtrudeGeometry で奥行きぶん押し出す**。
  「防水層」に指定した辺には外側に 4〜26mm の帯を同じ奥行きで付ける（工法の色・砂の質感つき）。
  ★**外向きは「回り方の符号」だけに頼らない**。帯の中点が輪郭の内側に入っていたら反転する
    （点の内外判定で確かめる）。符号だけだと中に埋まって見えないことがある。
- **📐 PDFで出す**＝`nnSectDrawPDF`（nn-draw-js の中）。描いた輪郭をキリのいい縮尺（1/1〜1/100）で
  断面詳細図に。各辺の寸法・全幅/全高・GL・防水層の太線・層構成・注記・表題欄つき。
- ★★**踏んだ罠2つ（どちらも実バグ）**
  1. **「閉じる」は何よりも優先する。** 始点を狙っても 5度・長さのきざみが働いて**始点を通り越し**、
     いつまでも閉じられなくなる（§35の教訓と同じ）。始点の26px以内なら**始点そのもの**を返す。
  2. **キャンバスの「中の画素数」は使う直前に必ず合わせる**（§24と同じ罠）。入れ物の大きさが
     変わったのに合わせ直す前に座標を計算すると、その比率ぶんタップがズレる。
     実測で `cv.height/rect.height` が 1.76（dpr=2のはず）＝**12%・27pxズレて図形が閉じられなかった**。
     → `ensureSize()` を `pos()` の先頭で呼ぶ。
  3. ★**描き始めたあとは画面の大きさが変わっても倍率・位置を変えない**（`view.ever`）。
     毎回 fit() すると描いている途中で図が飛ぶ。合わせ直すのは向きを変えたときと「⊡ 全体」だけ。
- 版fの入力方式（`_nnSecFree`・仮パラペットを組む `_nnSecPoly`）は撤去。検証も `sec3d.js` →
  **`sect2.js`（17項目）** に置き換え。sec2chk の「部位を選べる」は4件のまま（先頭に描くモードがある）。

### 138. 断面図を最初からつくれる＋断面→3D変換（2026-08-18f）★本人の要望
「最初から通常の図面／断面図を選んで作成でき、断面図を3Dに変換できるか」→ できる。実装した。
想定＝**該当部位専用の図面を提出する**フロー（断面詳細図PDF＋3Dで見せる）。

- **③断面タブに「✎ 直接入力（部位専用図）」**（部位セレクトの先頭）。図面が無いときは自動でこのモード。
  種別（パラペット/壁当り）・立上りH・天端W をバーで直接入力→その場で断面詳細図。
  ★一度 free になっても、**図面ができたら部位で開き直す**（自分で直接入力を選んだときだけ維持＝freeChosen）。
- **「⬔ 3Dで見る」（sec_3d）**＝断面の値から**4m×2.5mの仮部位**（正面の1辺だけその納まり・他は立上りなし）を
  作り、**build3D をラップして差し替えて組む**（`nn-sec3d-js`・§119の型。build3D本体は触らない）。
  既存3Dの質感・継目・面取りがそのまま付く。元の図面データは組む瞬間だけ差し替えて必ず復元。
  ・仮部位は `_nnSecPoly()`（座標はマス単位＝4.0/scaleM）。
  ・**ヘッダーの④3D＝いつもの図面の3D**：setTab もラップし、⬔ボタン経由（KEEPフラグ）以外の
    タブ移動でモックアップモード解除。
  ・★**3Dを初めて開くときは three.js 読み込みが非同期で T がまだ無い**。d3ViewIso は
    T ができるまで150ms間隔でリトライ（最大6秒）。即時に呼ぶと角度が入らない（実際にNGが出た）。
- **PDF**：直接入力モードの「📐 PDFで出す」は、仮部位に**差し替えて** nnSectionPDF() を呼ぶ
  （majorHW がその値になる）。push ではなく差し替え＝既存部位と混ざらない。
- テスト更新（§68）：sec2chk「部位を選べる」3件→**4件**（直接入力が先頭に増えた）。新規＝`sec3d.js`（11項目）。
- 宿題：直接入力に**アゴ（水切り）**の入力は未対応（3D側は辺編集で対応済み・§119）。断面2D図への
  アゴ描画も未対応。要望が来たら secF に追加＋nnSecDraw へ描き足し。

### 137. 3Dパッド：ナイトモード削除・「全体」を視点2ボタンに（2026-08-18e）
本人の指示「3Dのナイトモードは上のボタンにあるから削除。全体ボタンはこの2つ（図面と同じアングル／少し変えて3D）」。

- **🌙画面（d3_theme）をパッドから削除**。夜/昼は上のツールバー（tl_night/tl_day）に一本化（§118a）。
  使われなくなった `#d3pad #d3_theme` のCSSも削除。
- **⌂全体（d3_fit）→ 視点2ボタンに置き換え**。どちらも「全体表示＋決まった角度」。
  ・**図面と同じ**（`d3ViewPlan` / `icons/btn_d3_plan.png`）＝真上から。**theta=+90度**
  ・**斜めから**（`d3ViewIso` / `icons/btn_d3_iso.png`）＝theta=90度+0.5rad・phi=0.95 の3/4アングル
  実装は `d3View(theta,phi)`：先に `build3D()` で全体表示（tx/tz/r を合わせ直す）→ そのあと角度を入れて `T.rev++`。
  ★`d3Fit` は角度を触らないので、この順番でよい。`window.d3Fit` は互換のため残してある。
- **★図面と同じ向きにする理屈**：2Dの `p.y` はそのまま3Dの `z` になる（build3D）。カメラは
  `(tx+r·sinφ·cosθ, r·cosφ, tz+r·sinφ·sinθ)` から中心を見る。真上に近いとき画面の上は
  `-(cosθ, sinθ)` 方向なので、**θ=+90度**にすると画面の上＝`-z`＝図面の上。
  ★**φ=0（真ぴったり真上）にしないこと。** three.js のカメラの上向きが定まらず絵が回る。**0.16 で止める**。
  検証は投影で実測（`Vector3.project`）：図面の +x が画面右・+y が画面下、を数値で確認（`_check/d3view.js`）。
- ★**パッドのボタンはキャンバスのタップを奪わない**（受け口は `T.renderer.domElement` にあり、
  `#d3pad` は兄弟要素なので伝わらない）。4ボタンを叩いても `sel` が変わらないことを確認済み。
- **パッドはこれで8個すべて自作アイコン**（＋／−／上から／横から／⟲／⟳／図面と同じ／斜めから）。
- テストが古くなった2件を更新（§68）：`f3chk`（3Dパッドの🌙→視点2ボタン・**③ボタンの表示は
  §118bで絵文字を消す仕様に変わっていたので「点灯が変わる」に**。これは今回の変更前から★NGだった）、
  `d3padimg`（d3_fit → パッド8個すべてが絵）。新規＝`d3view.js`。

### 135. 客先登録・数字パッド・物件詳細ヘッダー刷新ほか（2026-08-18b）
本人の指示4件＋白い枠削除。

- **①客先登録（index.html 末尾 `nn-tokui`）**：ホームのメニューボタン（旧「準備中です」）から開く。
  タブ2つ＝元請（社名/担当者/TEL/メール/**入金日/支払条件/支払サイト**/備考）／仕入業者（社名/担当者/
  TEL/FAX/メール/備考）。追加・✎編集・🗑削除。保存は `nn_tokui_v1`（バックアップLISTに追加済み・§46）。
  見本＝記録帳の主な元請5社（支払条件はRAWと同じ）＋発注のVENDORS 2社。
- **②ホームの設定・用語集**：白いタイル（背景・角丸・影）を削除。絵36px＋drop-shadowだけに。
- **③発注の数字パッド（hacchu.html 末尾 `nn-numpad9`）**：スマホだけ。数量・単価の欄を
  タップすると自前の大きなパッド（最低44pt）が開く。**pointerdown の preventDefault で
  フォーカスを止める＝iOSキーボードは出ない**（§89の知識の応用）。
  たて＝画面下いっぱい／よこ＝**左寄せ**（単価・数量・金額の列が右に見えたまま打てる）。
  「↓次へ」で確定して次の行へ（★commitすると再描画でDOMが変わるので、**位置は先に控える**）。
  ★よこ向きのセレクタは `html[data-nnphone="1"] #nnPad` で書くこと（#nnPadだけだと詳細度で負ける）。
  パソコンはパッドを出さない（キーボードの方が速い）。
- **④物件詳細ヘッダー刷新（renderDetail＋`nn-dethead2`）**：一覧カードの作法に統一。
  1段目＝物件名・ステータス塗り・記号バッジ(badgeCls)・数量・操作（編集/報告書/一式）＋状態色の境界線／
  2段目＝囲み（住所/契約日/元請/受注金額/利益予定額）／3段目＝タグはチップ1行。
  **緑の大きなタグ枠（nnDefPanel・§2026-08-11z）は廃止**（絵はチップ内に出る）。
  よこ向きでKPI＋進行状況まで最初の画面に入るようになった（タブ開始 233pt）。
  ★記号は `p.spec`（kigou というフィールドは無い）。dpanel.js/dpmo.js は削除（det1.js が後継）。
- 検証：tokui1／pad1／det1（_check/ に保存）。回帰 kdef・edit1・pwlist ほか○。

### 134. 8点指摘の一括改修（2026-08-18a）★①〜⑧のうち⑥以外を実施
Claude側からの改善提案8点に本人が指示を返し、⑥（たて4ページの文字4.4pt）以外を一括で実施した。

- **①ホーム（index.html）**：会社名を「株式会社三浦工業」に統一。バナーを3段
  （施工中14件／契約済7件／完成済50件＝**記録帳RAWの集計値**。`.bukken-stack` で縦積み・
  タップで記録帳へ）。付箋は仕様登録の実数（登録済仕様50件・マイ仕様0件）。
  ★**数字は直書き**なので、記録帳のRAWを変えたらここも直すこと（コメントあり）。
- **②発注（hacchu.html）大改修**：
  ・現場一覧は **`hacchu_sites.js`（自動生成）** から。`_check/hacchu_sync.py` が記録帳のRAWを
    読んで42件（施工中14・契約済7・見積済/調査済21）を書き出す。**★記録帳の物件を変えたら
    mapsync.py と同様にこれも流し直す**こと。sw.js ASSETS 追加済み。
  ・明細は **KO_ITEMS（工法記号→材料テンプレ）** から自動生成（lock付き）。作り込み済みの
    J051（サン太平）/J077（発寒南倉庫）は CURATED が優先。見積済は単価未確定で開けない（従来どおり）。
  ・旧 g1/g2/g3 の履歴データは `NN_GID_ALIAS` で J051/J077/J060 に読み替え（保存済み履歴が壊れない）。
  ・現場選択は記録帳の作法（白地・角0・状態色の縦棒・記号バッジ）＋検索＋ステータスチップ。
  ・**発注書PDF（`nnHacchuPDF`）**を新設：A4たて実寸 `@page 210mm 297mm`・緑の帯＋nnDocBack（§43の型）。
    小計/消費税10%/税込合計・印枠・発注No。③確認画面と履歴の両方から出せる。MediaBox検算 210×297mm。
- **③用語集**：`nn-yougo-compact` でタブ・検索・分類・かなを圧縮、説明文はスマホ非表示。
  最初の用語 よこ333pt→**214pt**（85%→54%）。
- **④カメラ**：入口メニュー `#nnEntry`（納まり資料作成／現場台帳作成／図面で積算）。
  現場台帳→ `kirokucho_demo.html?view=list`（`nn-viewparam-js` 新設）、
  図面で積算→ `zumen_sekisan.html?photo=1`（nnPhotoOpen 自動起動を新設）。
  ★**camchk.js は先に `nnEntryPick()` を呼ぶこと**（入口で main が隠れているため）。
- **⑦ライブラリ**：行を記録帳の作法に刷新（`.lr2`・白地・角0・区分色の縦棒・記号バッジ）。
  **サムネイルはPILで描いた納まり模式図 `icons/lib_th_L1〜L8.png`**（`_check/libthumb相当は
  scratchpad参照`・sw.js ASSETS追加済み）。絵が無い登録は onerror で📷枠に戻る。
  ★ライブラリ内の転用先現場（g1〜g3の旧ダミー）はスコープ外でそのまま。次回直すこと。
- **⑧図面・積算**：ツールバーの**長押し(0.45s)で名前ツールチップ**（`nn-tbname`）。
  長押し後の click は1回だけ抑止（名前を見たいだけで機能が動かないように）。
- **テストが古くなった4本を更新**（§68）：camchk（入口）、ui2chk（.lrow→.lr2・チップ帯は
  §131から2段が仕様）、r4chk（tl_theme廃止→夜/昼ボタン・サンプルのボタン名）、
  hacchk は**削除**（hacchu2.js が後継）。新規チェック：home3/tbname/yougo2/cam3/lib2/hacchu2。
  ★**r4chk の3D部分（インラインのニセTHREE）は §63以降の3D改修に未追随**＝前からの宿題。
    直すなら stub3.js に載せ替えること（zfix2等の4本と同じ backlog）。

### 133. ★実機の「謎の1」の決着＝マスのHTMLから「動く文字」を全廃（2026-08-17d）
17cの対策でも実機（iOS）で「1」が残り、マスが見切れ・提出書類が密着（本人・3回目の指摘）。

- **★「1」の正体＝物件番号**。マスには `onclick="nnShotAdd(1)"` のように**物件id（この物件は1）が
  埋め込まれて**おり、iOSでは何らかの形でこれが文字として漏れてマスの間に出ていた。
  16x・17cの対策（番号 `<i>` を消す）が外れ続けたのは、**見えていた「1」は表示用の番号ではなく
  タップ処理に書いた物件idだった**から。
- **直し方＝マスのHTMLに「動く文字」を一切書かない**：
  ・セルは `<span class="psh" data-i="0" title="…">`＋（写真があれば `<img>`）だけの**定数**
  ・タップは `#list` で**1か所で受ける**（delegation。`.pcard` の data-pid と `.psh` の data-i で判定）
  ・「他」ボタンも onclick を外して同じ受け口に
  ・写真URLは `replace(/["'<>\s]/g,'')` で掃除してから埋める
  **＝どの端末がどう解釈しても、文字が生まれようがない。**
- **★見切れ・密着の対策＝帯の幅を数字で明示**。実機では「中身なりの幅」の計算が想定より
  狭く出て、17cの overflow:hidden がマスと「他」を切り取っていた。
  `.pshgrid` を **PC 656px／スマホ 242px** の固定幅に（5枚・10枚が必ず全部見える）。
  提出書類との間は margin+padding で **32px（PC 34px）** のはっきりした余白。
- ★教訓：**「表示している文字」を消しても直らないときは、属性に書いた文字を疑うこと。**
  HTMLの文字列に動的な値（id・番号・名前）を混ぜない作りにすれば、この類は根絶できる。
- 検証は `scratchpad/strip17d.js`（PC/ph：マスのHTMLに onclick も数字も無い・5/10枚全部見える・
  「他」が見える・余白32px以上・delegation で正しい物件の写真選択が動く）。
  回帰24本＋fast1＋cvscroll＋scrollcnt すべて○。

### 141. タップの光を小さく・指をなぞる光／断面作図の操作性を大幅改修（2026-08-19b）★本人の指示
「ボタンを押した時の光る面がデカすぎて、そしてすぐ消えるのでわかりにくい。特にデカすぎるのを
直して。指で線を引く細かい操作が要るので、指でなぞったら光がリアルタイムで指をなぞる演出が必要。
そして断面図作成。操作が壊滅的。ボタンが少ない、移動ができない、本来平面図であった必要な
ボタンがない。大幅に改修せよ。」

**①タップの光を小さく（全11ページ）**
- `.tapfx` の芯 32px→**16px**・外の輪 `inset:-14px`→**-6px**・枠1.5px→2px。
  ★**見た目の大きさは「芯＋輪＋演出の拡大率」の掛け算**。以前は
  (32+14×2)×1.8＝**約108px**＝指より大きい丸が広がっていた。いまは (16+6×2)×1.45＝**約42px**。
  ★大きさを直すときは**拡大率（`scale`）も一緒に下げること**。芯だけ縮めても広がり方は変わらない。
- **消えるのが速い問題**：`.3s`→**`.55s`**、後片付けの `setTimeout(…,340)`→**600**。
  ★片付けの時間を延ばし忘れると、演出の途中で要素が消えて「点滅」して見える。必ず両方直す。

**②指をなぞると光がついてくる（全11ページ・`<script id="nn-trail-js">`）**
- 画面ぜんぶを覆う `#nnTrail`（canvas・`pointer-events:none`・z-index 9997）を1枚置き、
  指を置いている間だけ、通った跡を**0.42秒かけて薄れる琥珀色の線**＋先端の明るい点で描く。
  ・`pointer-events:none` なので**下のボタンの操作を一切邪魔しない**（検証済み）。
  ・**描くのは「キャンバスの中の画素」で行う**ので、パソコンの表示倍率（zoom・§61）に左右されない。
  ・線が消えたら描画ループを止める（指を離して0.42秒後）＝端末が熱くならない。
- ★**タップの光（点で光る）と、なぞりの光（線を引く）は別物**。前者は「押した場所」、
  後者は「いま指がどこにあるか」を見せるもの。図面の作図では後者が要る。

**③断面作図の道具を8→15個に（`nn-sectdraw-js`）**
`✎描く／⇱修正／✥移動／⊕点／🛡防水層／↩戻る／↪進む／＋／−／⊡全体／▦マス／📏寸法／
📐見本／🗑全消し／奥行き`。平面図タブにある操作をひととおり用意した。
- **⇱修正**＝頂点をつまんで動かす（**1mm単位**）。つかんだ瞬間に「戻る」用の控えを取る。
- **✥移動**＝指でなぞると図が動く（**マウスの車輪でも拡大縮小**できる）。
- **⊕点**＝辺をタップで**点を1つ足す**／頂点をタップで**その点を消す**。
  ★点を足し引きしたら、**防水層に指定した辺の番号（`wp`）もずらすこと**。
    ずらさないと、関係ない辺が防水層になる。
- **📏寸法の札をタップ → 長さを数値入力**（`nnNumAsk`＝数字キーパッド・§35-2）。mm で入れる。
- **📐見本**＝標準的なパラペットの断面（平場900・立上り300・天端250・スラブ150）を一発で入れる。
  ★**まず見本を入れて、頂点を動かして合わせる**のが、いちばん速い使い方。
- 道具を変えると、画面下の案内文もその道具の使い方に変わる。

**★この改修で見つけた実バグ2つ（どちらも「閉じられない・ズレる」の原因）**
1. **始点に戻れない**：5度きざみ・長さきざみの丸めが働いて**始点を通り越して**いた。
   → **始点の26px以内なら、きざみを無視して始点そのもの**を返す（§35と同じ決まり）。
   ★「閉じる」は他のどの補正よりも優先すること。
2. **タップが12%（27px）ズレる**：キャンバスの「中の画素数」が、入れ物の大きさが変わったあと
   合わせ直されていなかった（§24と同じ罠）。→ 座標を計算する `pos()` の先頭で
   **必ず `ensureSize()` を呼ぶ**。
   ★もう1つ：**描き始めたあとは画面の大きさが変わっても倍率・位置を変えない**（`view.ever`）。
     毎回「全体表示」に合わせ直すと、描いている途中で図が飛ぶ。

**★`e.target.closest is not a function`（全10ページで直した）**
`document` で受けるタップ音の処理が `e.target.closest(...)` を無条件に呼んでいた。
**`document` 自身が対象になったときは `closest` が無い**ので、そこで止まっていた。
→ `const tc=(e.target&&e.target.closest)?e.target:null;` で確かめてから使う。
★**document／window で受けるタップの処理では、`e.target` が要素とは限らない。**

- 検証は `_check/sect3.js`（14項目：道具15個・見本・移動・拡大・頂点つまみ・戻る・
  札の数値入力・点の追加と削除・寸法の表示切替・案内文・**光が16px**・**なぞると光がついてくる**・
  JSエラーなし）。回帰は zumen 20本＋記録帳14本＋他ページ8本とも★NGなし
  （photochk／f4chk／allpages2 の3件は**この改修の前から同じ**＝ニセTHREEと#sideの既知の宿題）。

### 142. 既にある絵を、絵の無いボタンへ転用した（2026-08-19c）★本人の指示
「3Dのボタンについては、既にアイコン用意して代用できるやつはそっちも転用して。」
**新しい絵は1枚も増やしていない。** 意味が一致するものだけを使い回した（`nn-3dicon`）。

| ボタン | 転用した絵 | 理由 |
|---|---|---|
| 3Dの ⬛ 押し出し（`d3ext`） | `btn_p_oshidashi` | 部材スタンプの「押出し」と同じ意味 |
| 断面バー ⬔ 3Dで見る（`sec_3d`） | `btn_d3_iso` | 3Dパッドの「斜めから」と同じ絵 |
| 断面バー 🌙 画面（`sec_theme`） | `btn_night` | ツールバーの夜画面と同じ |
| 断面の道具 ✎描く／⇱修正／✥移動 | `btn_draw`／`btn_sel`／`btn_pan` | 図面タブの同じ道具 |
| ↩戻る／↪進む／＋／− | `btn_undo`／`btn_redo`／`btn_d3_zin`／`btn_d3_zout` | 同上 |
| ▦マス／📏寸法／📐見本／🗑全消し | `btn_grid`／`btn_dims`／`btn_sample`／`btn_clear` | 同上 |

- **⊕点・🛡防水層・⊡全体の3つは合う絵が無いので文字のまま**。無理に別の絵を当てない。
- 仕組みは**ツールバーの `NN_TB_ICONS`＋`nnTbIcons()` に相乗り**（新しい仕掛けを作らない）。
  断面の道具バーはタブを開いたときに作られるので、`setTab` を包む＋`#secBar` を
  MutationObserver で見張って**そのつど付け直す**。
- ★**記号だけのボタン（↩ ↪ ＋ − 🗑）は、絵が付いたら文字を消すこと。**
  `nnStripEmoji` は「消すと空になる」ときは元の文字を残す作りなので、そのままだと
  絵の横に「🗑」が並んで二重に見える。`SYM` の一覧で明示的に空にしている。
- ★**`textContent=` の直書きは、絵を丸ごと消す**（§118 と同じ罠）。
  `sd_cell`（▦ 100mm）は3か所で書き換えていたので**全部 `nnBtnTx()` に直した**。
  1か所でも残すと、そのボタンだけ押した瞬間に絵が消える。
- スマホは絵だけにして道具バーを詰めた（マスの「100mm」だけは数値が要るので文字を残す）。
  実測：断面の道具バー **たて 164px（5段→3段）／よこ 102→72px**。
- 検証は `_check/icon3d.js`（PC／スマホ 各7項目：絵が付く・先頭の絵文字が消える・
  マスの数値は残る・押し出しに絵・絵を置いても押せる・3Dパッドは今までどおり8個とも絵）。
- ★**テストが古くなった1件**（§68）：`sect2.js` の「案内が出ている」。
  §141 で案内が**道具ごとの使い方**に変わったため（product は正しい）。

### 143. 標準納まり詳細図を作った（2026-08-19d）★「このレベルが描けるか試して」への回答
本人から**公共建築工事標準詳細図（塗膜防水立上り・アルミ押え金物）**と
**メーカーの納まり図（HT-SYSTEM）**の2枚が届き、「実際にこのレベルが自分で描けるか試して。
できなかったらなぜできないかを考えて、実装して」との指示。

**★まず描いてみた結果＝届かなかった。** いまの断面詳細図（`nnSectionPDF`）は
「立上りH・天端W・スラブt」の**3つの数値からその場で線を引くだけ**なので、
概略の納まりまでしか出せない。参考図との差は次の3つに集約できた。

| 参考図にあるもの | いまの図 | なぜ出せないか |
|---|---|---|
| 役物の**断面の形**（押え金物の垂れ・笠木・キャント・シール） | ただの細い長方形 | **形をデータで持っていない** |
| 寸法補助線＋**●**・**括弧付きの参考寸法**・累進寸法 | 線3本・端は●だが括弧なし | **寸法の作法が入っていない** |
| 材料ごとに**違うハッチング**（躯体／断熱／モルタル／シール／仕上げ） | 全部おなじ45度斜線 | **ハッチが1種類しかない** |

→ この3つを**データとして持たせた**のが `<script id="nn-detail-js">`（`window.nnDetailPDF`）。
入口は積算・設定パネルの **「📘 標準納まり詳細図（元請提出用）」**。型は3つ：
塗膜防水 立上り（アルミ押え金物）／アスファルト防水 パラペット（アルミ笠木）／
塩ビシート 機械的固定 立上り。立上りH・躯体厚・キャント・断熱厚を入れて出す。

- **① 役物の断面（`PARTS`）**：押え金物・笠木・キャント・シールを**点の並び**で持つ。
  出（で）・板厚・垂れを引数にしたので、寸法を変えても形が崩れない。
- **② 寸法の作法（`mkDim`）**：補助線→寸法線→両端●→線の上に数値。
  **`ref:true` で括弧付き＝参考寸法**。`H（水上300以上）`のような**記号寸法**も出せる。
- **③ ハッチ（`hatchDefs`）**：躯体＝45度／断熱＝緑クロス／モルタル・キャント＝点／
  シール＝濃いクロス／外壁仕上げ＝逆45度。**SVGの pattern なので拡大しても崩れない。**
- **端末部の拡大詳細（S=1/2）を同じ紙に添える**。1/5では押え金物・シール・防水層の
  重なりが1mm以下になって形が読めないため（実際の詳細図も拡大図を添える）。
  ★拡大図の**防水層の厚さは3倍に誇張**し、その旨を図中に明記した。

★**踏んだ落とし穴（どれも実バグ）**
1. **寸法の数値が 1/5 になっていた。** 座標の差（＝紙のmm）をそのまま数値にしていた。
   **寸法には必ず「実物の mm」を渡すこと。** いまは値を明示的に渡す形にしてある。
2. **図が紙の左上に寄って下半分が空いた。** 平場を520mmしか描いていなかったため。
   平場700mm・壁は下へ300mm伸ばして破断線で切ると、1/5でちょうど紙が埋まる。
3. **拡大図が本図や注記に重なった。** `SH.ay1` は**すでに表題欄を引いた値**なのに、
   置き場所の計算でもう一度引いていた。★`ay0/ay1` は「図が置ける範囲」であって紙の端ではない。
4. `layersOf(null)` で落ちた。工法が未選択のときは既定の層構成に落とす。
- 図枠・表題欄・線の道具は **`window.nnSheetKit`** として公開し、他の図面と**同じ部品**を使う
  （ここを分けると図面ごとに枠の見た目がずれる）。
- 検証は `_check/detail1.js`（3つの型×9項目＋数値が実寸か。A3実寸・ハッチ5種・●の数・
  括弧寸法・記号寸法H・拡大詳細・注記・JSエラーなし）。
- ★**まだ届いていないところ**（参考図②の水準）：ドレン・脱気筒・出隅入隅・貫通部の納まり、
  多層の材料呼称をブレースでまとめる書式、平面図との対応（キープラン）。
  役物の形は `PARTS` に足すだけなので、必要な納まりが決まったら順次増やせる。

### 144. 標準納まり詳細図を作り直した（2026-08-19e）★「全然ダメ」の指摘を受けて
版d（§143）を出したところ「全然ダメ。これを設計者と監理者に提出するの？」と指摘された。
**そのとおりだった。** 俯瞰すると、版dでやっていたのは
**「参考図に載っている要素を数え上げて同じ要素を並べた」だけ**で、
テストも `●が16個以上ある` `括弧付きがある` という**存在チェックしか書いていなかった**。
**読む人が読めるか**を一度も測っていない。

**★§62 と同じ失敗をくり返した。**（見本を目で見て似せる → 何度も外す → 実測して決着）
**見本は必ず画素で実測してから作ること。**

## 参考図を実測して分かった差（ここが本質）
| | 参考図 | 版d |
|---|---|---|
| 躯体の表現 | **薄グレー塗り＋まばらな二重斜線**（13mm間隔） | 細かい45度ハッチ＝図が真っ黒に見える |
| 防水層 | **太い黒帯**（一目で追える） | 細線3本＝見えない |
| 文字の大きさ | 図の高さの約3.7% | **約1%**（1/4の大きさ） |
| 引出線 | 指す先のすぐ脇から短く | **図を斜めに9本横断**（最長130mm） |
| 図の占める面積 | 紙のほぼ全面 | 紙の左半分だけ |

## 直したこと
- **見え方の決め事を `STY` に集約**（寸法4.0mm・引出3.6mm・防水層の帯・線の太さ）。ここを変えれば図全体の印象が変わる。
- **躯体＝`#e6e6e6` 塗り＋13mm間隔の二重斜線**。防水層＝**太い黒帯**（実物5mm・紙上1.1mm以上）。
- **縮尺 1/5 → 1/2**。そのために描く範囲を詰めた（平場210・スラブは80だけ見せて破断）。
  ★**寸法線と数値が入る余白（左45・上28・下10mm）を「先に」引いてから縮尺を決める**こと。
  引かずに決めると図が図枠からはみ出す（版dで実際にはみ出した）。
- **引出線は屋根側の空き（void）にまとめ、文字の高さを「指す先の高さ」に合わせて並べる**。
  並べ方は3段構え：①指す先の高さを希望値に ②上から押し下げて重なりを解く
  ③下端からはみ出したら引き上げる。②だけだと下の数個が下端で団子になる。
  → 横断の最長 **130mm → 62mm**。
- **凡例（ハッチの意味）を追加**。層構成・注記は右の帯に。
- **型の名前を「端末の納まり」にした**（版dは「塗膜防水 立上り」の題で工法AS-T1の層構成が出て、
  図と説明が食い違っていた）。工法は別に選ぶ。
- **使いやすさ**：入口の窓が**いま描いている図面から初期値を拾う**（立上りHは `nnMajorHW()`、
  工法は `SPECS` の一覧＝選ぶと層構成が自動で入る）。
  ★`SPECS` は top-level の `const` なので `window.SPECS` では読めない（§47と同じ）。
  ★`nnMajorHW()` の H は **mm**。×1000 すると 300000 になる（実際にやらかした）。
- **表題欄の図面名称は長すぎると紙からはみ出す**。「納まり詳細図（…）」と入れ子にせず型名だけ入れる。

## ★検証を「存在チェック」から「読めるか」に変えた（`_check/detail1.js`）
- 縮尺が 1/2 以上／図が紙の主役（面積31%）／**図枠からのはみ出し0**／
  **引出線が材料の中を通る長さ ≤ 70mm**／いちばん小さい文字 ≥ 2.5mm／寸法の数値 ≥ 4.0mm／
  ハッチの使い分け／●／凡例・層構成・注記／記号寸法H／寸法が実物のmmか。
- ★**横断の判定は bbox ではなく `path.isPointInFill()`（実際の形）で見る**こと。
  bbox だと図の上辺に沿って走るだけの線まで数えてしまい、問題の無い引出線を落とす。
- ★**`<defs>` の中（ハッチの型）は図ではないので数えない**。数えると全部「はみ出し8mm」と誤検知する。

### 145. 役物（ドレン・脱気筒・笠木…）が消せなかった（2026-08-19f）★致命バグ
「こういう脱気筒とかドレンとかのアイテム、全削除・削除・範囲選択・選択すべて対象にして。
今削除したくてもできない」との指摘。**置いた役物を消す手段が実質なかった。**

★**原因は3つ。どれも「役物は本体とは別の入れ物（`state.parts`）にある」ことを
  忘れて、本体側だけ直していたこと**（§73 で相乗り方式にした副作用）。

1. **全削除（`clearAll`）が `state.polys` しか消していなかった。**
   部位が0面のときは confirm も出ないので、**押しても何も起きないように見える**
   （頂いた画面がまさにその状態＝0面なのに役物だけ残っている）。
   → 役物も消す。件数を confirm に出す（「部位3件と役物5件を…」）。
2. **範囲選択（`finishMarq`）が頂点しか集めていなかった。**
   → 役物の中心が枠の中にあれば選ぶ（`psel`）。選んだ役物はオレンジで表示。
   「選択を削除」ボタンも役物だけのときに出る。**Deleteキー**でも消える。
3. **🗑削除（`smartDelete`）は「1つ選んだ役物」しか消せなかった**（§120）。
   → 範囲選択でまとめて選んだぶんも消せるようにした。

- 役物側（`nn-parts-js`）に口を4つ追加：`nnPartsCount` / `nnPartsInRect` /
  `nnPartsHilite` / `nnPartsDelete` / `nnPartsClear`。
  **本体から役物を触るときは、必ずこの口を通すこと**（`state.parts` を直接いじらない）。
- ★**「選択」ツールでの1つ選びは元から動いていた**（§120で直した）。
  今回できなかったのは「まとめて消す」手段のほう。
- 検証は `_check/partdel.js`（14項目：選択→🗑／範囲選択→削除／Deleteキー／
  部位0面でも全削除で消える／部位と役物の両方／再読み込み後も消えたまま）。
- ★**テストの落とし穴**：ツールバーは方眼の上に重なっているので、
  **画面の上のほうの座標でドラッグを始めるとボタンを押してしまう**
  （実際に「Deleteキーで消えない」と誤検知した。原因は選択自体ができていなかったこと）。
  範囲選択のテストは画面の下のほう（マス7〜9あたり）で行うこと。

### 146. 断面タブが平面図と作法がバラバラだった（2026-08-19g）★根本原因は1つ
本人の指摘4件「線の太さをなぜ平面図のものを使わないのか」「なぜ断面だけ方眼紙が小さいのか」
「部位専用図・RC（鉄筋コンクリート）の長文が無駄スペースを取っている」「操作性が悪い」。

★**原因は1つ**。§139 で断面作図を作ったとき、**平面図の作図コードを再利用せず別物として
  新しく書いた**。そのため線の太さ・寸法札・バーの作法が全部バラバラになった。
  「早く動くものを出す」を優先して既存の資産を使わなかった、こちらの判断ミス。

**実測した差**
| | 平面（図面タブ） | 断面（直す前） |
|---|---|---|
| バーの厚み（たて） | 95px | **164px**（＋69px＝作図面が1割狭い） |
| バーの厚み（よこ） | 63px | 72px |
| 防水層の線 | ―（辺は1px・§124） | **4.5px** |
| ボタン | 27個すべて絵だけ | 絵と文字が混在 |
| 中身 | アイコンのみ | 「部位」「辺」「下地」「奥行き」の**文字ラベル**＋長文のプルダウン |

**直したこと（スマホ）**
- ラベルの文字（部位／辺／下地／奥行き）を**出さない**（`hidesp`。title に残すので長押しで分かる）
- 「✎ 断面を描く（部位専用図）」→ **「✎ 自分で描く」**
- 3Dで見る／画面／PDFで出す は**絵だけ**（`font-size:0` ＋ `::first-letter` で絵文字だけ出す）
- プルダウンは `max-width:26vw`・12px、奥行きの欄は42px
- **防水層の線 4.5px → 2px**（平面の辺は1px。太線で示すのは図面の作法なので2pxで止めた）
- 結果：バーの厚み **たて 164→98px（図面95）／よこ 72→69px（図面63）**
- ★**テストが古くなった1件**（§68）：`sect2.js` の「選択肢が『断面を描く』」→「自分で描く」。

★**まだ直っていないこと（宿題）**：寸法の札が平面（文字だけ）と断面（白地＋青枠の箱・11px固定）で
  別物のまま。本筋は**平面の描画関数を断面から呼ぶ**こと。いまは見た目を寄せただけで、
  コードは2本のままなので、また分かれる。

**★2026-08-19h：断面バーが方眼紙を覆っていた（本当の原因）**
版gで文字を減らしても「方眼紙が緑の帯まで来ていない」と言われた。**測ったら原因は別だった。**
- 図面タブの `#toolbar` … `background` なし・`pointer-events:none`＝**ボタン1つずつが方眼紙の上に浮く**
- 断面タブの `#secBar` … `background:rgba(255,255,255,.93)` ＋ `border-bottom`＝**不透明な白い帯**が
  方眼紙の上端を左右いっぱいに覆っていた（キャンバス自体は同じ大きさで上端まで在った）
→ `#secBar` を図面タブと同じ作り（背景なし・`left/right/top:10px`・`pointer-events:none` ＋
  `#secBar>*{pointer-events:auto}`）にし、各部品に影を付けて浮かせた。
★**「方眼紙が小さい」＝キャンバスが小さいとは限らない。** 上に不透明なものが乗っているだけのことがある。
  実測は「キャンバスの rect」だけでなく **背景色（background）** も見ること。
- 下地のプルダウンは**バーだけ短い名前**（`optsShort`：RC／S＋ALC／SRC／W（木造）／S＋デッキ）。
  「RC（鉄筋コンクリート）」のままだと1行まるごと占めていた。正式名は title と右パネルに残る。
- ★`#secBar select` の `max-width` は **100px が上限**。それを超えると たて画面でバーが1段増える
  （実測 97px→129px）。

**★2026-08-19i：③断面を描いて ④3D を押すと真っ白だった**
「なんで2枚目を3Dにしたら何も映ってないの？」の答え＝**そういう作りにしていた**（§138）。
`setTab` の包みが「⬔ボタン経由でないタブ移動では断面3Dモードを解除する」ので、
ヘッダーの ④3D を押すと **図面（`state.polys`）の3D＝0面＝真っ白**になっていた。
- 直し方：**部位が1つも無く、断面が描いてある（閉じている）ときだけ、④3D で断面の3Dを見せる。**
  部位があるときは今までどおり図面の3D。判定は `state.polys.length===0 && state.sect.closed`。
- あわせて **④3D が空のときに理由を出す**（`#d3empty`：「まだ立体にするものがありません」＋
  ①図面へ／③断面へ のボタン）。今までは説明のない灰色の画面だった。
  `build3D` と `setTab` を包んで、組み立てのあとに出し入れを合わせる。
- ★**「押しても何も起きない」は、たいてい仕様どおりに動いている。**
  作った本人にしか分からない前提（この場合は「⬔ボタン経由のときだけ」）が入っていないか疑うこと。
- 検証は `_check/sec3dtab.js`（何も無いときの案内／断面だけで3Dが出る／部位があるときは図面の3D）。

### 147. 御見積書＋数量根拠書／データ保全（2026-08-20a）★「絶対売れるように」の第1弾
「絶対売れるように変更できるところとか追加できるところ追加して。任せる」を受けて、
売れない理由の分析（①データが消える ②数字の根拠がない ③一本道が未完）から3つを実装した。

**①御見積書（`nn-mitsu-js`・zumen_sekisan.html 末尾）＝図面→数量→見積の一本道が完成**
- 入口：積算・設定パネルの見積合計の下「📄 御見積書（元請提出用・数量根拠書つき）」。
- 窓で 宛先（**客先登録の元請から選択**・無ければ手入力）／件名／工事場所／
  諸経費%／出精値引き／有効期限日数。支払条件は選んだ元請の登録から自動。
- A4たて2枚：1枚目＝御見積書（御見積金額の枠・明細・小計・諸経費・税抜・消費税・税込合計・
  印枠・備考）／2枚目＝**数量根拠書**（部位ごとに 平場=実測面積、立上り=辺長×H を
  高さごとに集計した内訳、天端=辺長×W、シート割付の取り都合計算）。
- 数量は recalc と**同じ関数**（quantities/spec/wariAgg）から取る＝画面の積算とズレない
  （`window.nnEstimateData()` に集約）。役物は置いた数×登録単価。
- 自社情報は発注ページと同じ `nn_hacchu_myco`。見積Noは `nn_mitsu_seq` で連番。
- ★★**数量は小数1位に丸めてから単価を掛けること。** 丸める前の数量で掛けると
  「101.3㎡×¥5,800 ≠ ¥587,250」のように**表示と金額が合わない見積書**になる
  （元請の担当者が電卓で検算したら落ちる。実際になっていて検算で見つけた）。
- ★検証は**存在チェックでなく独立の検算**（`_check/mitsu1.js`）：表の全行で
  数量×単価＝金額、小計＝Σ金額、諸経費＝小計×率、税＝税抜×10%、合計、
  根拠書の掛け算（H300：40.0m×0.3m＝12.0㎡）と立上り合計の突き合わせまで見る。

**②データ保全（全11ページ `nn-persist-js`＋ホーム `nn-bknag-js`）**
- `navigator.storage.persist()` を全ページで要求（容量逼迫時に localStorage が
  自動削除されにくくなる。使えない環境では黙って何もしない）。
- ホームに**バックアップの促し**：「30日以上書き出していない＆保存データがある」ときだけ
  1行の帯（下部ナビの上・重ならない）。[書き出す]＝設定の窓へ／[あとで]＝7日出さない。
  `doExport()` が `nn_bk_last` を控え、書き出すと消える。検証は `_check/bknag1.js`（8項目）。

**★残り（売るために次にやるべきこと・本人と相談）**
1. 見積書の**単価を仕様登録・材料登録の実単価と連動**させる（いまは SPECS のデモ単価）。
2. 実際の現場3〜5件で数量を検算し「合っている記録」を作る（営業資料になる）。
3. データのクラウド保存（アカウント）＝会社導入の前提。Cloudflare 移行と合わせて設計。

### 148. 材料登録：価格改定の履歴＋価格表の一括取り込み（2026-08-20b）★実物の仕入価格表を見て
本人からメーカーの実物の仕入価格表（PDF）が「参考までに」と届いた。読み取れた本質は2つ。
①**1枚の表に価格改定の列が4本並ぶ**＝値上げが激しく「いつの単価か」が命。
②**ああいう表はメーカーから届く**＝1品ずつ手で転記するのが現場の実態。
★固有名（客先名・担当者名・顧客別の単価）は公開リポジトリに入れない。構造だけを使う。

**①価格改定の履歴（`nn-kaitei-js`・zairyo_toroku.html 末尾）**
- 単価を変えると、**前の単価が日付つきで自動で残る**（`m.hist`＝[{d,p}]・直近12回）。
  `saveDetail` を包む相乗り方式（§73）。保存は items 丸ごとJSONなので自動で永続化。
- 詳細の単価の下に「**価格改定 ¥6,850(日付) → ¥7,800(現在)　+13.9%**」の行が出る。
  値上げ交渉・見積の説明にそのまま使える。

**②価格表の一括取り込み（データ管理モーダルの先頭）**
- メーカーの価格表（Excel）から**製品名と単価の列をコピーして貼るだけ**。
  1行=1品。**数字が複数あるときは一番右（＝最新の改定列）を単価に採る**（実物の表の形）。
- 照合は 登録済み → カタログ の順（名前のゆれは 空白・全半角・大小文字を吸収）。
  貼り付け→「照合する」→ 更新/新規/変更なし/見つからない の一覧を見せてから
  「まとめて更新」。**更新分は前の単価を履歴に残す**。
- ★★**カンマ区切りの貼り付けは「桁区切りのカンマ」を先に潰すこと。**
  `8,200` をセル区切りで割ると 8 と 200 に割れて**200円**になる（検証で実際になった）。
  タブがあればタブだけで割る／無ければ `(\d),(?=\d{3})` を先に消してから割る。
- ★`CATALOG` は top-level の const。`window.CATALOG` では読めない（§47と同じ）。
- 検証は `_check/kaitei1.js`（9項目：履歴・表示・照合3態・最右の列を採る・一括更新・
  取り込みでも履歴・再読み込み後も残る）。

### 149. 3Dの「全体表示」で下が見切れる（2026-08-21a）★目安の式をやめて実測に
本人のスクショ（3Dタブ・よこ向き）で「下が見切れている。直して」。**そのとおりだった。**
写真を画素で数えて確認：**いちばん下の行に屋根の画素が1426個**あり、下へ行くほど増えていた
＝立体が画面の外まで続いている。

- **★原因は「大きさ×1.5」という目安の式**（`T.r=Math.max(横,奥行,高さ×1.6,4)*1.5`）。
  この式は **画角（50度）も画面の縦横比もツールバーの厚みも見ていない**。
  よこ向きは「横に広くて縦が短い」ので、縦が真っ先に足りなくなる。
- **直し方＝実際に画面へ投影して測る**（`<script id="nn-d3fit-js">`）。
  ・`nnD3FitR()`＝立体の外接箱の**隅8点をカメラで投影**し、収まるまで `T.r` を最大6回調整。
  ・使える範囲は **`useW=W-16`／`useH=H-（ツールバーの厚み）-10`**。
  ・**ずらしは `camera.setViewOffset(W,H,-voX,-voY,W,H)`**（カメラの写す範囲をずらす＝
    立体を画面上で平行移動できる）。`T.voX/voY` に px で持つ。
- **★大きさを合わせるだけでは足りない。** カメラが見ているのは**高さ0.4mの点**なので、
  塔屋のように背の高い建物は**絵が上へ寄る**。大きさだけ合わせると上がはみ出す
  （実測 PC で上に18px）。→ **中心のズレ（`wantY-(y0+y1)/2`）も毎回足し込む**。
- **★距離を変えたら、もう一度中心を測り直すこと。**
  ループの最後で `T.r` を変えると、その前に測った「中心のズレ」は古くなる。
  そのぶん外に出る（実測 よこの「図面と同じ」で下に23px）。
  → ループを抜けたあと、**中心のズレだけを測って直す仕上げの1回**を必ず入れる。
- **★角度を変えたら測り直すこと。** `d3View`（図面と同じ／斜めから）は
  `build3D()`→角度を入れる の順なので、**角度を入れたあとに `nnD3FitR()`** を呼ぶ。
  先に測って後から回すと、真上から見たときにはみ出す。
- 効果（実測・サンプル形状）：はみ出し **よこ 下56px・たて 左82px → 全部0**。
  絵の大きさも大きくなった（よこ 115×145 → 238×219px）。
- ★**「画面に収まっているか」はカメラの数値から逆算しないこと。**
  §117t（現場マップのポチ）と同じ教訓。**投影して測るのが唯一確実。**
- 検証は `_check/d3fit.js`（よこ／たて／PC 各10項目：上下左右のはみ出し0・ツールバーの下に来る・
  小さすぎない・視点2ボタンでもはみ出さない・**＋ボタンで寄れる**・JSエラーなし）。
  ★**＋−は §39 でタップと長押しに作り替えたので `el.click()` では動かない**。
    検証は本物のマウス（`page.click`）で押すこと。

### 150. 下絵（読み込んだ写真）が選べない・消せない（2026-08-21b）★本人の指摘
「画像を添付したら削除できない。削除できるようにして。選択もできるようにして」。

- **★原因＝消す手立てが右パネルの奥にしか無かった。**
  「✕ 下絵を削除」は右パネルの下のほう（**スクロールしないと見えない**）にあり、
  スマホでは引き出しを開けてさらに探す必要があった。方眼の上の写真そのものは
  **位置調整モードのときだけ**わくと✕が出る作りで、ふだんは触れなかった。
  ＝写真を1枚置いただけの状態では、事実上どこからも消せない。
- **直し方（末尾の `nn-uimg` / `nn-uimg-js`。本体は書き換えず包む方式）**
  ・**「選択」ツールで写真をタップすると選べる**（オレンジの点線わく＋右上に赤い✕）。
    `pickEdge` を包み、**辺に当たらなかったときだけ**写真の当たり判定を見る
    （辺の選択は今までどおり優先）。回転していても正しく判定する。
  ・選ぶと画面の左下に**小さなカード**（✥動かす／⟲⟳／透明度／🗑削除／✕）。
    スマホでも指が届くので、引き出しを開けなくても消せる。
  ・消せる口を4つに：**画面の✕／カードの🗑削除／ツールバーの🗑削除／Deleteキー**。
  ・**🗑削除は「ほかに消すものが無い」ときも下絵をたずねる**
    （写真だけ置いた状態で「削除するものがありません」と言われて詰むのを防ぐ）。
  ・**全削除は部位・役物と一緒に下絵も消す**（§145 と同じ考え方）。
    ★元の `clearAll` にもう一度たずねさせないよう、**その呼び出しの間だけ `window.confirm`
    を通す**ようにした（消す処理を2か所に書き写さないため）。
- ★**カードの高さの置き場所は毎回はかって決める**（`place()`）。下には案内文（`#hint`）と
  スマホの「📊 積算・設定」があり、**案内文は道具によって行数が変わる**ので決め打ちだと重なる。
  ★`getBoundingClientRect` は表示倍率をかけた**後**のpx、`style.bottom` はかける**前**のpx（§61）。
  **必ず `window.nnPZ` で割ること。** 1366／2000／2560px で重なり0を実測。
- 検証は `_check/uimg.js`（`node _check/uimg.js` ／ `ph`。PC・スマホ 各20項目：
  タップで選べる・カードが出る・ほかの部品と重ならない・回す/透明度・
  ✕/🗑/Deleteキー/全削除のどれでも消える・辺の選択は今までどおり）。
  ★**検証の落とし穴**：下絵の初期表示は幅40マス＝画面より大きいことがあり、
    **右上の✕が画面の外に出てクリックできない**。テストでは画面幅の45%に縮めて置くこと。

### 151. 図面・積算に入口メニューを付けた（2026-08-22a）★本人のモック準拠
「図面・積算を開くといきなり平面図作成に映る。メニューを用意したら親切だしわかりやすい」
「アイソメ図はひとまず削除（リリースしてから作る）」
「3D反映は"2つを作ったら反映できますよ"と伝えたいのに、これだと最初から3D反映が
　一つのメニューみたいになる。積算も項目に入れるとして、どういうUIなら使いやすい？」

- **★答え＝「自分でつくるもの」と「自動でできるもの」を同じ並びに置かないこと。**
  同じ大きさのタイルで横に3つ並べると、3Dも自分で作るものに見える。
  → **上下2段に分け、間に「▼ ①②をかくと、ここから下は自動でできます」を入れた。**
  ・上段（大きい・緑の実線わく）＝**① 平面図をかく／② 矩計図をかく**
  ・下段（小さい・点線わく）＝**3Dで見る／積算・見積／提出書類**
  ★点線＝「まだ使えない」、実線＝「使える」で、見ただけで前後関係が分かる。
- **状態を数字で出す**のが肝（`refresh()`）。かく前は「まだ かいていません」、
  かいたあとは **①＝✓ 屋根3面／101.3㎡**・**積算＝概算 ¥870,410**・**提出書類＝4種類 出せます**。
  ＝「かくと何が出てくるか」が結果の数字で分かる。②は①があれば
  「①から自動で出ます（手でもかけます）」と出す（矩計図は辺のH・Wから自動で描けるため）。
- **まだ使えないタイルを押したら、案内して①へ連れて行く**（死んだボタンにしない）。
  トースト「先に ① 平面図をかきます」→ 図面タブ＋描画ツール。
- **↩戻るは「メニューへ → ホームへ」の順**（仕様登録の 詳細→一覧→ホーム と同じ・§6）。
  帯にボタンを増やすとスマホで2段になるので、既にある戻るに役目を足した。
- ★**他のページから用事つきで来たときはメニューを出さない**（カメラの「図面で積算」＝`?photo=1`、
  記録帳の「作成図面」＝`?open=`）。
  ★**`location.search` はこのスクリプトより前で `history.replaceState` により消されている**ので、
    印（`window.nnSkipMenu`）を立てて見ること。search を見に行くと必ず空で判定を外す（実際に外した）。
- **施工層構成図（アイソメ）のボタンは外した**（本人の指示・ひとまず）。
  **PDFを作る処理（`nnIsoPDF`）は残してある**ので、戻すときはボタンを1行足すだけ。
- 実装は末尾の `<style id="nn-zmenu">`＋`<script id="nn-zmenu-js">`（本体は書き換えない）。
  メニューは `main` の上にかぶせる（`position:absolute; inset:0`）ので、キャンバスの大きさ計算に影響しない。
- ★**よこ向きは1画面に入れること**。①②の絵（`.ic`）は1行まるごと使うので、
  よこ向きでは出さない。実測：中身283px＜使える312px・下部ナビとの重なり0。
  ★**下段の小さいカードの説明は、よこ向きでも消さないこと**（「概算 ¥○○」＝いちばん見せたい数字がここ）。

**★★検証スクリプト89本への影響（大事）**
メニューが作図面の上にかぶさるので、**読み込み直後にキャンバスを実クリックする検証が全部落ちる**。
→ `_check/*.js` の **zumen を開く行と `reload()` の行の末尾に**
`await p.evaluate(()=>{try{nnZMenuClose();}catch(_){}});` を機械的に足した（82行＋10行）。
★**新しく検証を書くときも、zumen を開いたら1行足すこと。** 足さないと実クリックが全部メニューに当たる。
- 検証は `_check/zmenu.js`（`node _check/zmenu.js` ／ `ph` ／ `land`。各22項目）。
- ★**テストが古くなった1件**（§68）：`tbname.js` の「タップでは機能が動く」。
  消すものが何も無いと `smartDelete` は confirm を出さない（トーストだけ）ので、
  先にサンプル形状を読み込むようにした。あわせて**長押し中の件数を、ふつうのタップより前に控える**
  よう直した（後で読むと、そのタップぶんまで数えて必ずNGになっていた）。

### 152. 3Dのカメラは「使う人が動かす以外、絶対に動かさない」（2026-08-22b）★本人の指示
「何か選択、削除、設置してもカメラのアングルは必ず変更しないように。
例：パラペットを壁当たりにしたらカメラアングルがいきなり変なところに動く。おかしい」。

- **★原因＝build3D の「形の外接箱が変わったら全体表示に合わせ直す」条件（`key!==d3FitKey`）。**
  パラペット→壁当りに変えると**既定2.6mの壁が立って建物の高さが変わる**＝外接箱が変わる＝
  合わせ直しが走り、中心（tx/tz）と距離（r）が全体表示に戻っていた。
  役物を置く・消す・部位の削除・undo でも同じことが起きる。
- **直し方＝合わせ直すのは2つのときだけ**：
  ①**最初の1回**（`d3FitKey===''`＝3Dをまだ一度も合わせていない）
  ②**視点ボタン**（`d3WantFit`＝⌂系・図面と同じ・斜めから・orientationchange）
  外接箱の key 比較は廃止（`d3FitKey='done'` を立てるだけ）。
- ★**編集で建物が画面から外れても、勝手に戻さない**のが正しい仕様。
  戻したいときは視点ボタン（図面と同じ／斜めから）を押してもらう。
- ★§85⑧（2画面の描きかけプレビュー）がカメラを保存・復元しているのはそのまま互換。
  断面の「⬔ 3Dで見る」は d3ViewIso() 経由＝視点ボタン扱いなので今までどおり合う。
- 検証は `_check/camfix.js`（10項目：壁当り・立上り500・役物の置き/消し・部位削除・undo の
  すべてでカメラ7値（theta/phi/r/tx/tz/voX/voY）が1つも変わらない／最初の1回と視点ボタンは効く）。
- ★f4chk の1件★NGは**この変更の前から同じ**（ニセTHREE未追随の既知の宿題・§134）。

### 153. 3Dの上で直接かいて「押出し・引込み」／役物のリアルタイム配置（2026-08-23a）★本人のモック準拠
「3D上で描画ができるように。立上り側面や天端に対して線をひけて、線を結ぶと長方形になり、
押出しでそのまま押し出される（数字で奥行き）。引っ込みも。3Dで作成したすべての面が編集対象。
ドレン・脱気筒も3D上に配置でき、リアルタイムでどこに置くか分かるように」。

- **実装は末尾の `nn-3ddraw`／`nn-3ddraw-js`**（§119と同じ「build3D 本体には手を入れず上に載せる」方式）。
- **①3D描画**：「描画」ツールのまま3Dの面をタップ（§121の setTool ラッパーから draw を
  図面タブへ弾く対象から外した）。
  ・1点目＝レイキャストで当たった面がそのまま作図面になる（**平場・立上りの側面・天端・
    作った立体の面、どれでも**）。面の座標系は `basisFor(n)`＝壁なら u=水平・v=上向き。
  ・動かすと**赤い点線＋寸法の札**（HTML）がついてくる。2点目で長方形が確定（5cmきざみ）。
  ・カード（#nnD3Card）から **⬆押出し／⬇引込み** → nnNumAsk で奥行きmm。
- **②立体のデータは `state.d3sol`**（面の原点p・法線n・基底u,v・角a,b・奥行きd・mode）。
  state 丸ごと保存なので**保存・復元は自動**（loadState は wholesale・§73と同じ）。
  ・押出し＝面の法線方向へ箱を生やす（makeBasis(u,v,n) の行列で向きを合わせる）。
  ・**引込みは本物の穴あけ（CSG）はしない**。濃い色のくぼみ板＋内側の枠＋「引込み 250」の札で
    へこみの印として見せる。CSGライブラリ追加は今後の判断。
  ・どちらも寸法の札（CanvasTexture）を面に貼る＝何がどれだけ出て/へこんでいるか常に見える。
- **③選択・削除**：選択ツールでタップ→オレンジで光る＋カード（🗑削除/✕）。
  🗑・Deleteキー・全削除にも参加（clearAll ラッパー：部位が消えたときだけ一緒に消す）。
  統一タップ受け口（nn-3dedit-js）に `nnSolPickAt` の分岐を足した（§121の「受け口は1本」を守る）。
- **④役物スタンプのゴースト**：3Dで pointermove のたびにレイキャストし、**半透明の緑の箱＋
  赤いピン＋名前の札**が指についてくる。置く処理（nnPlaceAtGrid）と**同じ丸め**で位置を出すので、
  ゴーストの場所＝実際に置かれる場所。置き終わる/やめると消える。
- ★**タップとカメラ操作の見分けは §119 と同じ**（8px以上動いた/0.7秒以上＝視点操作）。
  カメラは §152 の決まりどおり一切動かさない（camfix で回帰確認）。
- ★**まとまり（Group）は T.scene 直下に持つ**（T.group は build3D の先頭で毎回空にされる・§119）。
  build3D を包んで毎回 `nnSolRender()` で載せ直す。
- 検証は `_check/d3draw.js`（28項目：3Dのまま描画・赤い点線と札・長方形カード・押出し400・
  **壁の面を画面走査で実際に見つけて横向きに押し出す**・引込み・選択/🗑/全削除・再読み込みで残る・
  ゴーストの追従と札・置くと消える・2D描画の回帰・カメラ不変・JSエラーなし）。
- ★宿題：引込みの本物の穴あけ（CSG）／立体の積算への計上（いまは表示のみ）／
  立体の移動・寸法の後編集。要望が来たら d3sol の形のまま足せる。

### 154. 鳩小屋のリッチ3D部品＋Blender(.glb)読み込み口（2026-08-23b）★本人の写真準拠
「複雑な鳩小屋みたいなものを作成したい。いずれBlenderでテクスチャを加工して
リアルに近付けたい。できるかどうかも含めて対応して」→ **両方できる。実装した。**

- **①鳩小屋スタンプ**（ツールバーBの3段目「◆ 鳩小屋」・NN_STAMPS の hatogoya）。
  3Dでは**コンクリ台座＋箱＋笠木（ひさし・水切りの垂れ）＋ステンレス配管（本数は幅なり・
  エルボで下りて支持ブロックへ）**の写真準拠の手続きモデルで出る（`nn-rich3d-js` の buildHatogoya）。
  選択・回転・複製・削除・ゴースト配置は既存の役物システムがそのまま効く。
- **②Blenderのモデル（.glb）を読む口**：`models/<kind>.glb` を置くと手続きモデルの代わりに
  **PBRテクスチャ付きのBlenderモデル**が出る（横幅Wに等倍で縮尺・底を屋根面に）。
  書き出し方は `models/README_glb.md`（実寸・プリンシプルBSDF・Draco圧縮OFF・3MB以内）。
  ★§63 で PBR・環境光・影・ACES は整備済みなので、Blenderで焼いた質感がそのまま出る。
- **★GLTFLoader は three r159 の examples/jsm を「モジュールなし」に変換して同梱**
  （`vendor/GLTFLoader.js`・110KB）。r148以降、examples の UMD 版は配布されないので、
  `import {...} from 'three'` → `window.THREE` の分割代入、`toTrianglesDrawMode` を同梱、
  `export` → window への代入、に機械変換した（変換スクリプトの要領はコミット参照）。
  **3Dで鳩小屋系の部品が出たときだけ** script タグで読む（nnLoadGLTF・§63と同じ考え方）。
  検証：Pythonで自作した GLB（立方体＋64px市松テクスチャ）を読み、三角形12枚・
  テクスチャ・MeshStandardMaterial まで実測（`_check/assets/testcube.glb`）。
- **作り＝parts3D（箱で描く既存処理）には手を入れない**：build3D のあとに箱を
  `visible=false` にして、同じ位置・回転にリッチモデルを **T.group へ**重ねる
  （次の組み直しで自動的に消える＝後片付けいらず）。モデルの全メッシュに partIdx を
  持たせるので、リッチモデルをタップしても部品として選べる。
- ★**sync は「1コマあとで」呼ぶこと**。nn-parts-js が DOMContentLoaded で build3D を
  もう一度包み直すため、**箱を作る parts3D が包みのいちばん外側（＝最後）**になる。
  即時に呼ぶと箱がまだ無い時点で走って空振りする（実際に空振りした）。
  あわせて箱に `_nnRichDone` の印を付けて二重配置を防ぐ。
- ★**ツールバーのBの2段目にボタンを足すと1600pxで段が落ちる**（§118cの罠をまた踏んだ）。
  落ちた段がキャンバスを覆い、**partdel の2Dクリックがボタンに当たって7件★NG**になった。
  鳩小屋ボタンは3段目（保存の行）に置いて解決。**ボタンを増やしたら tb2 を流すこと。**
- 検証は `_check/hatogoya.js`（17項目：ボタン・置ける・箱が隠れて配管/エルボが出る・
  タップで選べる・カメラ不変・回る・GLB読込（三角形/テクスチャ/PBR）・
  glbを置いたら差し替わる・消える）。
- ★宿題：鳩小屋の2D表示は箱のまま（平面図では箱で正しい）。glbの容量目安は3MB／個。
  Blenderモデルが増えたら models/ に置くだけ（HTMLは触らない）。

### 155. ゼロから役物を組んで登録できるようにした（2026-08-23c）★本人の意図はこちらだった
§154の「◆鳩小屋ボタン」に対し「そうじゃない。**現場で採寸したものから、ユーザーが任意の
役物をゼロから作れる操作性そのもの**が欲しい。簡単な操作で複雑なものが作れること」との指摘。
**プリセットを増やすのではなく、§153の3D描画を「組む→登録→使い回す」まで通した。**

- **①円柱が押し出せる**（配管・丸ダクト用）。長方形カードに「⚪ 円柱」。かいた長方形に
  内接するだ円断面（W×H）で、材質はステンレス調。shape:'cyl' を d3sol に持つ。
  ★だ円は **geometry 側に焼き込む**（`cg.rotateX(π/2); cg.scale(1,H/W,1)`）。
    mesh.scale だと子の寸法札・選択の光る箱までゆがむ。
- **②寸法の数値入力**（採寸からの入力）。長方形カード「✎ 寸法」＝よこ→たて（mm・数字パッド）。
  できた立体も選択カード「✎ 寸法」でよこ・たて・奥行きを後から直せる。
  **1点目の角（a側）は動かさず b側を動かす**＝置いた基準がずれない。
- **③立体の移動**。選択カード「✥ 移動」→移動先をタップ（同じ面の上・5cmきざみ・大きさ不変）。
  タップは統一受け口（nn-3dedit-js）の**先頭**で `nnSolMoveTo(rc)` が受ける（何よりも優先）。
- **④「📦 役物に登録」**（3Dタブ・立体があるときだけ左上に出る）。組んだ立体一式を
  confirm→名前(prompt)→単価(数字パッド)で **役物ライブラリに kind:'custom' として登録**。
  ・形は `item.custom.sols`＝d3sol の座標を「外接箱の底面中心」からの相対に直して丸ごと保存
  ・w/d/h は外接箱(mm)。登録と同時に d3sol は空にし、**同じ場所に1個置き直す**（§85⑩の流儀）
  ・置く・回す・複製・削除・積算・保存・**他の現場での再利用**は §73 の役物システムがそのまま受ける
  ・3Dの姿は `nnCustomBuild(P)`（nn-custompart-js）が組み立てる。nn-rich3d-js の sync に
    「P.custom があれば nnCustomBuild」の分岐を足した（箱を隠して重ねる仕組みに相乗り）。
    ★custom には probeGLB を走らせない（<kind>.glb は無いので404を取りに行かない）
- ★**立体の寸法札（nnSolLab）は作図の当たり判定から除く**こと。札は面から20mm浮いた
  透明板なので、除かないと「札の上にかいた」＝20mm浮いた立体ができる。
- ★ツールバーには**ボタンを1個も足していない**（§118cの段落ちを避けた）。登録ボタンは
  #three-wrap 内のフロート（#d3ext の下）。
- 検証は `_check/custompart.js`（29項目：円柱・寸法入力・移動・登録・custom の3D再現・
  タップで選べる・積算・カメラ不変・再読み込み・2D回帰）。
  回帰 d3draw/hatogoya/partschk/partdel/camfix/tb2/icon3d/d3fit/zmenu/uimg/d3chk/sect2 全部○。
  ★fix4.js/part3dchk.js は scratchpad 時代の名前で `_check/` に無い（欠落・回帰ではない）。
- ★宿題：円柱の材質・色を選ぶ／面をまたぐ移動／custom 役物の分解（編集し直し）。
  要望が来たら custom.sols の形のまま足せる。

### 156. スマホの3D編集を大幅改善（2026-08-23d）★「スマホ版の操作性が壊滅的」
指摘は「スマホ版の操作性が壊滅的に悪い。大幅に改善して」。直近で作った3D編集
（§153・§155）は**スマホだと指の真下に照準なしで打点する方式**＝2D（§28）と
現場マップ（§32）で捨てたはずの操作のままだった。ここが本丸。

- **①3Dにも「赤い照準」**（nn-3ddraw-js 内・#nnD3Aim）。指を置くと右上（+36,-52）に
  赤い照準が出て、指を動かして狙い、**離した瞬間に照準の位置で確定**する。
  対象は3つ：**描画（1点目・2点目）／役物スタンプの設置／立体の✥移動**。
  ・照準中は**カメラを凍結**：#three-wrap の capture の pointermove で
    stopPropagation（カメラのパン処理は el のバブル側なので、これで届かなくなる）。
  ・**2本目の指が来たら照準をやめてピンチに譲る**（§28と同じ）。
  ・★**down は止めない**（カメラ側の指の管理 tp に登録させる）。up も止めず、
    **`window._nnAimUsedId=pointerId`** を立てて el 側の2つの受け口
    （nn-3ddraw の hook・nn-3dedit の統一受け口）に「処理ずみ」を伝える＝
    二重打点の防止と、カメラ側の後片付け（tEnd）の両立。
  ・マウスは今までどおり（pointerType==='mouse' は照準に入らない）。
  ・打点・ゴースト・寸法札の中身は commitDrawAt/lineAt/ghostAt/placeCommitAt に
    **関数として切り出して、マウスと照準で共用**（コードを2本にしない）。
- **②2本指のまま動かすと表示位置も動く**（核の触り処理に真ん中の移動を追加）。
  §72の「2本指は拡大縮小だけ」は**傾き**をやめた話で、パンは別。描画ツール中は
  1本指が照準に取られるので、パンの手段がこれになる。pinch.js（phi/theta不変）は今も○。
- **③左下の積み重なりを解消**：
  ・立体を選んだら役物の選択を外す・逆も外す（**カードとバーが同時に出ない**）
  ・スマホでは d3edit の「何も選んでいないときの案内」を出さない（左下に常駐して
    立体カード・役物バーと重なっていた。使い方は「？」にある）
- **④カードのボタンを指サイズに**（#nnD3Card・#nnSolReg：高さ実測41px）。
- ★**検証の教訓2つ**（_check/aim3d.js・26項目）：
  1. **引きの画では「狙った世界座標」と突き合わせない**。光線が手前の別の面に
     当たるのが正しい動きなので、約束どおり「**画面上の照準の位置＝打点の投影位置**」で見る。
  2. スタンプは「**ゴーストの場所＝置かれる場所**」を平面座標で突き合わせる
     （箱の中心はレベル差で画面上ずれるので、画面投影の比較はダメ）。
  合成タッチは `new PointerEvent(type,{pointerType:'touch',...})` を el に dispatch
  （capture の受け口・カメラ凍結・二重打点まで全部この方法で再現できる）。
- 回帰：pinch/d3draw/custompart/camfix/d3fit/hatogoya/partdel/tb2(ph) すべて○。

### 157. 壁当り（壁面）の立上り防水の仕上がり（2026-08-23e）★本人の指摘
「壁面にすると、露出アスの場合、立上り防水の仕上がりでなくなる」。実測したら、
**パラペット（para）に入れた直しが、壁当り（kabe）だけ古いまま残っていた**（4つとも）。

| | パラペット | 壁当り（直す前） |
|---|---|---|
| 立上り防水の板 | 角から角まで（joint） | **辺の長さぶんの板を辺の中央に置く**＝出隅で切れる（§70の直し漏れ） |
| 質感のUV | 実寸（nnUvMetric） | **無い**＝砂の粒が辺いっぱいに伸びて「のっぺりした板」（§80の直し漏れ） |
| 継目（はみ出しアス） | 貼りかけ・縦の継目あり | **1本も無い** |
| 端末の押え金物 | ―（天端で納まる） | **壁の中に埋まっていた**（内側へ0.028。壁の厚みは0.15）（§68の直し漏れ） |

- ★**「仕上がりに見えない」の主因はUV**。砂付の粒が横に引き伸ばされると、
  材料の面ではなく「色を塗った板」に見える。texture があっても UV が無ければ意味がない。
- 直したあと：角から角まで／実寸UV／縦の継目（シート幅のピッチ・角の0.3m以内は立てない）／
  平場への貼りかけとその端のはみ出しアス／**押え金物を防水層の手前に出し、上にシーリング**。
- ★**はみ出しアスの道具（beadSlope/beadBall/beadBetween）は、パラペットの中から
  辺のループの外へ出した**（中身は変えていない）。壁当りからも同じ継目を使うため。
  ★同じ「立上り」なのに作りが2本に分かれていたのが、直し漏れの根本原因。
- ★ウレタン塗膜（`sp.type!=='sheet'`）では継目を出さない（実物に継目が無い）。
- 検証は `_check/kabe1.js`（19項目：UVの倍率が「長さ×4」とぴったり・板が両隣の防水層と
  ぴったり突き合う・縦の継目と貼りかけ・押え金物が壁より手前かつ防水層より手前・
  シーリングが上・塗膜では継目0・積算の端末金物・カメラ不変）。
  見た目の前後くらべは `_check/kabeshot.js`（`node _check/kabeshot.js _before.html /tmp/x.png`）。
- ★検証の落とし穴3つ：①`p.evaluate(FN)` は関数を渡すだけで呼ばれない（``(${FN})()`` にする）
  ②工法は **`state.specCode`**（部位ごとではない）③継目の有無を「総数の比較」で見ない
  （辺をパラペット→壁当りに変えると、その辺のパラペットぶんの継目が消えるので必ず減る）。
  **壁の面の上（z≈th+6mm・y>0.05）にある継目を数える**こと。
- 回帰：lapchk/facegap/beadaudit/d3chk/kouzou/d3draw/custompart/camfix/partdel/hatogoya/
  aim3d/sect2/warichk すべて○。zfix2(2件)・f4chk(1件) は**この変更の前から同じ**
  （ニセTHREE未追随の既知の宿題・§134。`_before.html` と比べて同数を確認）。

### 158. 3Dで屋根の面をタップ→青く選択→高さを直感変更／右パネルすっきり（2026-08-23f）★PDF指示
本人からPDF（5枚）。「①右枠が無駄な情報が多すぎ・すっきりさせて ②平面図で図面を1つ
作ったら自然と項目が追加されるように ③屋根の面を選択したら ④青く面が選択されて
⑤高さを直感で変更できるようにして」。狙いは**「直感で説明されなくてもいきなり高度な
建物や防水層が作れる」＝ファン化**。実装は末尾の `nn-psel`／`nn-psel-js`。

- **①右パネルすっきり（空のときの情報を削る）**
  ・部位パネルの空文＝「①図面で屋根を1つかくと、ここに自動で行が増えます」（②の答えを文言にした。
    行の自動追加・自動命名（屋根①…）・面数表示は元からある）
  ・下絵パネル＝**画像を読み込むまでは「🖼 画像を読み込む」だけ**（#upanel.noimg で他を隠す。
    読み込むと全部戻る）
  ・選択中の辺の説明＝2行→1行
- **③④3Dで平場をタップ＝その部位を選ぶ**
  ・build3D の平場の防水層（mem）に `userData.polyIdx` を持たせ、統一受け口（§121）の
    辺→役物→立体の**後ろ**に分岐を足した（選択ツールのときだけ・手前に別の物があれば選ばない）
  ・選ぶと**青い面（0x3fb6e8・opacity .55）**を平場にかぶせる（psg＝T.scene直下・§119の型）。
    右の一覧の該当行も緑（.on）＋scrollIntoView。相互排他（辺・役物・立体⇔面）を4か所に配線
  ・空タップ・Esc・タブ移動で解除。カメラは動かさない（§152）
- **⑤高さ（GL+）を直感変更**
  ・選ぶと下に**カード**（#nnPolyCard：名前・GL+入力・▲+0.5/▼−0.5・面積・✕）
  ・**打ちながら3Dへ即反映**：`nnLvLive(i,v,commit)`＝280msまとめて lv→sync→save→draw→
    （3D/2画面なら）build3D→青い面も追従。一覧の行の GL+ 欄も oninput で同じ処理
  ・★**打っている最中は renderPolyList しない**（入力欄が作り直されてフォーカスが飛ぶ）。
    反対側の欄（行⇔カード）は activeElement を確かめて value だけそろえる
- ★受け口の当たり判定：**手前の1件目が polyIdx を持たないときは選ばない**（奥の屋根を
  拾うと、壁をタップしたのに裏の屋根が選ばれて混乱する）。pick箱・寸法札・透明は飛ばす。
- 検証は `_check/psel1.js`（PC/ph 各23項目：空文・下絵1ボタン・自動追加と命名・タップ選択・
  青い面の高さと色・行の連動・カード・打ちながら反映・▲0.5・行の欄からも反映・空タップ解除・
  辺タップで相互排他・カメラ不変）。回帰21本すべて○。
- ★テストの落とし穴：`deco`（辺の当たり判定のまとまり）は nn-3dedit の**閉包の中**＝
  evaluate から見えない。T.scene から `userData.pick` の箱を探して投影すること。

### 159. 右ウィンドウ撤去＋躯体の立ち上げ基準（2026-08-23g）★§158の直後に強い指摘2件
「なぜウィンドウが不要と伝えたのに残っている？右にでかでかあったら邪魔」
「選択した面だけ高さを変えるつもりが、建築物そのものの高さが変わってる。おかしい」。
★§158では「右枠の中身を削る」と読んだが、**指示は最初から「右ウィンドウそのものの撤去」**
だった（モックp2＝右列なし・キャンバス右上に小さな表）。

- **①右ウィンドウ撤去（nn-rtbl／nn-rtbl-js）**
  ・**PCでも #side は右からの引き出し**（スマホ§23の作りを流用）。ふだんは作図面が画面
    いっぱい。「📊 積算・設定」（PCは main に置く＝全タブで押せる）で開閉。
  ・かわりに**キャンバス右上に浮かぶ屋根の表**（モックp2そのまま）：
    屋根｜下地｜高さGL+｜面積｜仕様 ＋「＋新しい屋根を追加」。①図面と④3Dで表示。
    行タップ＝選択（3Dなら青い面も）・GL+はその場で打ちながら反映・✕削除・名前も編集可。
    受け口は delegation 1か所。作り直しは renderPolyList を包んで追従（入力中は作り直さない）。
  ・下地・仕様の列は**現場ごとの設定（state.kouzou／specCode）の表示**（行ごとには持たない）。
  ・★`main` は `overflow:hidden`（transformで画面外に隠した引き出しが横スクロールを生むため）。
  ・★★**閉じた引き出しの中の行へ `scrollIntoView` しないこと**。main が横に引きずられ、
    「開いていないのに引き出しが見えている」状態になった（実際になった）。
    引き出しが open のときだけスクロールする＋保険で interval が main.scrollLeft を 0 に戻す。
- **②躯体は「下にある部位の屋根」から立ち上げる**（build3D の depth）
  ・これまで躯体は**常に地面(0)から lv まで**の柱だった。そのため1面の GL+ を下げると、
    その上に載っていた塔屋が「地面からの12mの塔」として露出し、**建物が分解したように
    見えた**（＝「建築物そのものの高さが変わってる」の正体。データは1面しか動いていない）。
  ・自分の足元（polyCenter）が別の部位の中にあれば、その部位の lv から立ち上げる。
    メイン屋根を 9→3 にしても塔屋は「屋根の上の箱」のまま（絶対高さは保つ）。
  ・躯体に `name='nnBody'`＋`userData.bodyIdx` を付けた（検証用。polyIdx は使わない＝
    壁タップで面選択にならないように）。
- 検証は `_check/rtbl1.js`（26項目：引き出しの開閉・横スクロール0・表の列・自動で行が増える・
  GL+/選択/削除/追加・3Dでも表・ツールバーと重ならない・塔屋の躯体の底=メイン屋根・
  下げても塔にならない・他の面は1mmも動かない）。回帰17本すべて○。

### 160. GL+は「青い面」だけ動かす／下地・仕様を屋根ごとに／引き出しのパネル削除（2026-08-23h）
本人の指摘3点（強い口調）。①「青い面だけ高さ調整しろ。なんでパラペットまで持ち上がってんだよ。
色ついているのが対象に決まってるじゃん」②「屋根の名称は変更できるのに、下地はRC造限定なの？
おかしいでしょ。防水仕様も選べるようにして」③「③ができたら4枚目の項目（引き出しの
下地パネル・部位パネル）いらんでしょ」。

**①GL+＝青い防水面だけが動く（`poly.bodyLv` を新設）**
- ★§159で「躯体を下の部位から立ち上げる」に直しても足りなかった。**GL+を変えると
  躯体・パラペットごと動く仕様そのものが誤り**。「色のついた面＝選択対象」が本人の意図。
- **`poly.lv`＝防水面（青い面・GL+で動く）／`poly.bodyLv`＝躯体・パラペット（GL+では不動）**。
  build3D の forEach 冒頭で `lvDeck=+poly.lv||0; lv=bodyLv` に分離し、
  **lvDeck を使うのは3つだけ**：防水面（mem）の高さ・平場の継目（BEAD_Y）・maxY。
  それ以外（躯体・壁・パラペット・立上り防水・面取り・継目・baseLv比較）は全部 lv（bodyLv）のまま
  ＝既存コードは無変更で「躯体基準」になる。**この分離のしかたが変更を最小にする肝**。
- 既定：旧データは `bodyLv=lv`（loadStateで正規化）／新規（closePoly）は両方0。
- **「⬆ 躯体も合わせる」ボタン**（`nnLvFollow`・#nnPolyCard内）＝押したときだけ
  `bodyLv=lv` にして nnSyncSharedEdges→build3D。**nnLvLive からは nnSyncSharedEdges を削除**
  （GL+で境界の辺種別が勝手に変わらない）。nnSyncSharedEdges 自体も bodyLv で比べるよう変更。
- カメラは §152 のとおり全操作で不動（検証で7値一致を確認）。

**②下地・防水仕様が屋根ごとに選べる**
- **`poly.kouzou` / `poly.spec`** を新設（無ければ全体の設定に従う）。loadStateで検証つき正規化。
- `specOfPoly(p)`（spec() の下・window公開）。**build3D と draw() の forEach 内で
  `const sp=specOfPoly(poly)` とシャドーするだけ**で、中の全処理（色・継目・割付縞）が屋根ごとになる。
- 躯体の質感は **`window._nnKzHint`**：build3D が部位ごとにヒントを立て、nn-kouzou-js の
  nnMat ラッパが `_nnKzHint||state.kouzou` を見る。forEach を抜けたら null に戻す。
- **積算（recalc）と見積データ（nnEstimateData）は仕様ごとのバケット集計**に。
  ★全部同じ仕様なら今までとまったく同じ表（テスト互換）。混在時だけ「平場（AS-T1）」のように
  コードを付けて行を分け、注記「材料概算・割付は全体の設定基準」を出す。
  ★数量根拠書の polys にも spec コードを追加。mitsu1 の独立検算は通ったまま。
- **浮かぶ表の下地・仕様セルをプルダウンに**（.rkz/.rsp・delegationのchangeで保存→recalc→build3D）。
  ★行タップの除外は `INPUT` → `INPUT|SELECT|OPTION`、作り直し抑止も SELECT を含める。
- ★割付図・断面・PDFの一部は代表仕様（state.specCode）のまま（注記で明示）。スマホは表が無いので
  従来どおり全体設定のみ（引き出しの #specsel・断面バーの下地）。

**③引き出しの「部位（屋根・面）」「下地（構造体）」パネルを削除（PCのみ）**
- display:none で温存（#polylist 等は各所が参照。消すと壊れる）。
  「選択部位を回転」の行だけ「選択中の辺」パネルへ appendChild で移設。
- ★スマホは浮かぶ表が無いので、引き出しのパネルを残す（PHONE分岐）。

**★3Dタブで浮かぶ表が操作パッドを覆っていた（今回発見の潜在バグ）**
- 表は right:10px 固定＝**右端の #d3pad（＋−⟲⟳の列）の真上**に出ていて、＋が押せなかった
  （§159から存在。d3fit の page.click タイムアウトで発覚）。
- place() で **3Dタブのときだけ right＝パッドの幅＋18px** に逃がす。実測で覆い0。
- ★右端に固定する浮き部品を増やしたら、**同じ右端の部品と実測で突き合わせること**。

**★テストが古くなった4件（§68。product は正しい）**
- `kabe1`：テストが部位に `spec:'AS-T1'` を入れていた。当時は無意味なデータだったが、
  **今日から「屋根ごとの仕様」として本当に効く**ようになり、state.specCode を X-2 に変えても
  その部位だけ AS-T1 のままで継目が消えず★NG。→ p.spec をやめ state.specCode に。
  ★**「使っていないはずのフィールド」をテストデータに入れると、後日その名前が本物になって襲ってくる。**
- `rtbl1`：下地・仕様が文字→プルダウンに（td.c のテキスト → select の value を見る）。
- `zdir`：ボタン名「サンプル形状」→「サンプル」（§118の改名にテスト未追随・前からの宿題）。
- `stub3.js`：ニセTHREEの obj に **userData:{} が無く**、§159 の `body.userData.bodyIdx=` で
  facegap/beadaudit が丸ごと落ちていた（前からの宿題）。userData を足して復旧。
  ★ただし **facegap は「面の数0」のまま素通り**（変更前ファイルでも同じ＝ニセTHREE未追随）。
  zfix2(2)・f4chk(1)・photochk(1)・zdir の継目1件も同じファミリー（§134の宿題のまま）。
- 検証は `_check/bluelv1.js`（PC26／スマホ20項目：青い面だけ動く・躯体不動・カメラ7値不動・
  ⬆で躯体が動き境界も同期・仕様/下地の3D反映・仕様別集計・表のプルダウン・パネル削除・
  再読み込みで残る）。回帰：rtbl1/psel1/camfix/adjchk/kouzou/d3fit/d3draw/custompart/kabe1/
  partdel/uimg/sect2/zmenu/d3chk/mitsu1/sec3dtab/aim3d/hatogoya/lapchk/warichk/ang5/align1/
  cross/cross2/sec2chk/partschk/split1/icon3d/tb2/sect3/d3view/tbname/beadaudit すべて○。

### 161. 屋根の表をドラッグで移動・拡大縮小／躯体（立体）の高さも調整（2026-08-23i）★本人の画像指示
指示は2つ。①（表を赤枠で囲んで）「拡大縮小と移動をマウスのドラッグでできるようにして」
②「確かにこの面の上下は動くが**その対象面は立体で高さ調整できるようにして**」
（＝青い面と対になる**躯体そのもの**の高さも変えたい。添付は屋根①が箱状に立ち上がった絵）。

**①表を自由に動かせる・大きさを変えられる（`nn-rtbl`）**
- 見出し行が**つまみ**（`✥ 屋根の表（◯面）`）＝ドラッグで移動。**右下の斜線**＝ドラッグで拡大縮小
  （`transform:scale()`・0.6〜2.2倍・`transform-origin:top left`）。**↺ 位置**で元の場所と大きさに戻る。
- 置き場所は `localStorage('nn_zumen_rtblpos')`＝`{x,y,s,fold}`。**次に開いても同じ場所・同じ大きさ**。
- ★**動かしたら place() の自動配置をやめる**（`pos.x!=null` なら何もしない）。やめないと0.7秒ごとに戻される。
- ★座標は**表示倍率で割る**（§61）。`getBoundingClientRect` は倍率後・`style.left` は倍率前。
- ★`setPointerCapture` は try で囲む（§19）。

**★①で判明した実害＝表が3Dの屋根を覆っていて、その下がタップできない**
- 検証（psel1）が落ちて発覚。クリック位置(1084,245)が表(976〜1536×174〜319)の中に入り、
  **屋根の名前欄（.rnm）に当たっていた**。版gから存在し、今回の幅拡大で悪化。
- → **たたむ（∧）ボタン**を新設（`window.nnRoofFold(on)` でも呼べる）。たたむと見出し1行になり、
  3Dの右上が触れるようになる。幅も 560→510px に抑えた。
- ★**画面の上に浮かせる部品を増やしたら、その下にある操作を実際にタップして確かめること。**
  「見えている＝触れる」ではない。

**②躯体（立体）の高さ（`poly.bodyLv`）を直接編集できるようにした**
- 表の列を **面GL+(m)／躯体GL+(m)** の2つに分けた（`.rlv` / `.rbl`）。3Dのカードにも
  「躯体GL+」の欄と ▲▼ を追加（`nnBlLive` / `nnBlStep`）。
- **面GL+＝青い防水面だけ／躯体GL+＝建物の立体だけ**。互いにつられない（§160の分離をそのまま使う）。
- ★境界の辺の種別（壁当り／パラペット）は**躯体の高さ**で決まるので、`nnBlLive` は
  **確定時（commit）だけ** `nnSyncSharedEdges()` を呼ぶ。打っている最中は呼ばない。
- カメラは §152 のとおり不動（検証で7値一致）。
- 項目が増えたのでカードは `flex-wrap:wrap`＋`max-width:min(92%,860px)` で折り返す。

**★テストの揺れを1件つぶした（product は正常）**
- rtbl1「押すと右から引き出しが開く」＝`transition .22s` ＋初期化直後の重さで、
  400ms/1000ms の**時間待ちでは間に合わないことがある**（変更前のファイルでも再現）。
  → `waitForFunction` で「開き切ったか」を条件で待つ形に変更。
  ★**アニメーションの完了を時間で待たない。条件で待つこと。**
- psel1 は「3Dをタップする前に `nnRoofFold(true)` で表をたたむ」を追加（実機と同じ回避方法）。
- 検証は `_check/rtbl2.js`（28項目：つまみで移動・大きさ・保存・再読み込み・↺・たたむ／
  躯体GL+で立体だけ立ち上がる・青い面は不動・カメラ不動・表とカードの両方から変えられる）。
  回帰33本すべて○。

### 162. 屋根の表をモック準拠に作り直し（2026-08-23j）★本人のモック画像つき
指摘は3つ＋速度。①「拡大縮小の解釈が違う。**境界線にマウスを置いたら ↔ になり
拡大縮小できるように**」（＝窓と同じ作法）②「**たたむとか、位置といらない。閉じるを右上に**」
③表の中身をモックの形に（下が既存）④「更新スピードが遅い。もっと早く更新して」。

**①拡大縮小＝窓の縁をドラッグ（scale方式は廃止）**
- 境界線（左・右・下・角）にマウスを置くと **↔／↕／⤡ カーソル**になり、ドラッグで
  幅・高さが変わる（`edgeOf()`＝縁7px以内の判定。文字は拡大しない＝窓のリサイズと同じ）。
- 保存は `{x,y,w,h,closed}`（旧 `s`/`fold` は読み込み時に捨てる）。
- ★**手で広げたときは `maxWidth/maxHeight` を `none` に**。既定の上限（min(640px,54vw)）が
  残っていると、pos.w を保存しても見た目が頭打ちになる（実測で発覚：774px指定→640pxのまま）。
- 移動＝上の帯（.rhd）か**緑の見出し行（th）**をつかんでドラッグ。行タップの選択とは衝突しない
  （thは行選択に使われないので、つかむ場所として安全）。

**②右上に ✕（閉じる）／「たたむ」「↺位置」は廃止**
- ✕で表ごと消え、同じ場所に小さな **「▦ 屋根の表」** ボタンが出る（押すと元の場所・大きさで復帰）。
- `window.nnRoofFold(on)` は「閉じる/開く」の口として温存（psel1 が3Dタップ前に使う）。

**③列＝屋根|下地|高さ|平場|仕様|立上り㎡|仕様|総面積（モックのとおり）**
- **仕様の列が2つ**：平場（`p.spec`・既存）と**立上り・天端（`p.specT`・新設）**。
  例：平場=X-1・立上り=X-2 のような使い分けができる（モック3行目）。
- `specTOfPoly(p)`（無ければ平場と同じ）。loadStateで検証つき正規化。
- **3D**：build3D の ringsOf ループ先頭で `const sp=specTOfPoly(poly)` とシャドー
  ＝立上り・天端の防水の色・質感・継目が立上り仕様に。平場（mem・平場の継目）は平場仕様のまま。
- **積算（recalc）・見積データ**：平場は spec、立上り/天端/アゴは specT でバケット集計。
  全部同じなら今までとまったく同じ表。混在時は「立上り（X-2）」のようにコードを付ける。
- 平場㎡・立上り㎡（＝立上り+天端）・総面積は quantities() から毎回計算して表に出す。
- 躯体GL+ の列は表から外した（3Dで屋根を選んだときのカードに残っている・§161）。

**④更新スピード：デバウンス 280ms → 90ms**（nnLvLive／nnBlLive）。
  高さを打ってから3Dが動くまで 実測92ms。

- ★テストの罠：`[...tds].map(x=>parseFloat(x))` は**要素そのもの**を渡して全部NaNになる。
  `x.textContent` を忘れない。
- 検証は `_check/rtbl2.js`（23項目：列・✕→▦復帰・縁カーソル・左/下の縁ドラッグ・見出し行で移動・
  保存と再読み込み・specTの保存と積算/見積の分離・92ms・躯体GL+はカードで生存）。
  rtbl1はヘッダー期待値を更新。回帰33本すべて○。

### 163. 3Dで壁の面をクリック→赤くして押出し・引込み／屋根の表に立上り・天端（2026-08-23k）★モック準拠
本人のモック3枚＋追加指示。①選択ツールで**壁の面をクリック→面ぜんぶが赤くなり赤い点線の枠**
②その面を**押し出す・引っ込める**（モックの赤い矢印）③屋根名が枠から見切れる・枠が狭い
④「更新が遅い」。追加で⑤表に立上り高さ・天端幅⑥積算・設定から「選択中の辺」「防水仕様の選定」
「下絵」を削除⑦下絵はツールバーへ。

**★①でいちばん大事な発見＝壁の面には当たり判定が無かった**
- 当たり判定の箱は **立上り（300mm）の帯だけ**（`ph=Math.max(G.hh,0.3)`）。塔屋の壁の
  真ん中をクリックしても何も選べない状態だった（モック1枚目「クリックすると次の画像のように」）。
- → **躯体の壁の全高＋立上り**（`base`〜`bodyLv+hh`）を覆う箱にした。足元の高さは
  build3D と同じ理屈の `nnBaseLvOf(poly)`（下にある部位の屋根から立ち上げる）。
- ハイライトも同じ範囲・**赤（0xff4136・opacity .42）＋赤い点線の枠**（EdgesGeometry＋
  LineDashedMaterial・`computeLineDistances()` を忘れない・`raycast=function(){}` で当たり判定から外す）。

**★当たり判定を広げたら、役物・立体が選べなくなった（受け口の順序）**
- 統一受け口（§121）は「辺 → 役物 → 立体 → 面」の順。壁の判定が大きくなったぶん、
  笠木や押し出した箱をタップしても**後ろの壁が先に取られる**（d3draw・custompart が各6件★NG）。
- → **「役物 → 立体 → 辺 → 面」に入れ替えた**。役物・立体は壁の手前に出ているので、これで両立する。
- ★**当たり判定を大きくしたら、その手前にある物の選択が壊れていないか必ず回帰で見ること。**

**②面の押出し・引込み（`nnEdgeOffset(m)`）**
- 選んだ辺を**法線方向へ平行移動**（隣の辺は頂点を共有しているので自動で伸び縮み）。
  外向きは「辺の中点を少し法線側へ寄せた点が多角形の中か」で判定（中抜きは逆）。
- 面積が0.5㎡未満になるときは元に戻して「これ以上は引っ込められません」。
- カードに **➡押出し／⬅引込み（各0.5m）／…mm（数値入力）**。カメラは動かない（§152）。

**③屋根名の枠と文字（★16px!important の罠）**
- `.rnm` を `width:100%; min-width:150px` に広げた。長い名前は**入るところまで文字を縮める**
  （`fitNames()`＝scrollWidth>clientWidth の間 0.5pxずつ）。
- ★**縮まらなかった原因は §50 の `input{font-size:16px !important}`（iOSの自動拡大よけ）**。
  表の中は `#nnRoofTbl .rnm{font-size:12px !important}` で上書きし、JSも
  **`el.style.setProperty('font-size', fs+'px','important')`** で書く。ふつうの `style.fontSize=` は負ける。

**④更新の速さ**：デバウンス 90ms → **requestAnimationFrame（約16ms）**。
  build3D 自体は実測 **44〜52ms**（サンプル3面）なので、打った直後の次のコマで反映される。

**⑤〜⑦引き出し（積算・設定）をさらに整理**
- 表に **立上り(mm)・天端(mm)** の列を追加。値はその屋根の**いちばん多い値**（`majorHWof`）を出し、
  変えると **その屋根の全辺に入る**（`nnRoofHW`）。立上り0にすると「立上りなし」になる。
- 「選択中の辺」「防水仕様の選定」「下絵」の3パネルを display:none（DOMは温存。
  `renderEdgeEdit`・`defH/defW`・`setSpec` が参照するので消してはいけない）。
- **下絵はツールバーの「🖼 下絵」**（`nnUimgPanel`）＝#upanel を `#canvaswrap` へ移して
  浮かぶ小窓にする。もう一度押すと引き出しへ戻して閉じる。
- 検証は `_check/face1.js`（23項目）。rtbl1/rtbl2 は列の期待値を更新。回帰34本すべて○。

### 164. 平場も他の面と同じ扱いに／選択モードの解除／端部の面（2026-08-23u）★本人の指摘3件
「1_選択モードが解除できない。この面選択とドラッグで自由に移動する機能は、選択ボタンを
押した状態のみ発動させて。2_新しく出てくる面。これは早く対応して。3平場だけなぜか
赤くならない。というかいきなり建物全体の高さが変わった。平場は平場のみがドラッグで
他の面同様に移動できるようにするって言ったよね。天端面は違うだろ。」

- **①面の選択・ドラッグは「選択」ツールのときだけ**（`nnFaceDrag` の pointerdown と
  統一の受け口の両方に `tool!=='sel'&&tool!=='select'` の門を追加）。
  解除は **同じ面をもう一度タップ／何もない所をタップ／Escキー** の3つ。
- **②端部の面（endA/endB）**＝壁の左右の小口。当たり判定とハイライトを付け、
  ドラッグでその辺を辺の向きに伸ばす・縮める。1つの辺につき out/in/top/endA/endB の5面。
- **③平場（屋根の面）も他の面と同じに**：タップで**赤く**なり、上下ドラッグで
  **`poly.lv`（平場）だけ**が動く。躯体・パラペット（`bodyLv`）は1mmも動かない。

★**この日いちばんハマった「平場が赤くならない」の真因（3段構え）**
1. **`renderEdgeEdit()` が選択を消していた。** §120 で入れた安全弁
   「選んでいた辺が無くなっていたら選択を外す」が、平場（`e:-1`＝辺ではない）を
   **毎回「無効な選択」と判定して `sel=null` にしていた**。
   → `sel.f==='deck'` のときは辺の有無で消さない分岐を先に置く。
   ★**「辺」を前提にした安全弁は、辺でないものを選べるようにした瞬間に牙をむく。**
2. **同じ要素に付いた2本の受け口。** `nnFaceDrag` の pointerup と統一の受け口は
   **どちらも `T.renderer.domElement`** に付いている。**同じ要素どうしでは
   `stopPropagation()` では止まらない**（capture/bubble に関係なく登録順に全部走る）。
   → `stopImmediatePropagation()` ＋ 既存の印 **`window._nnAimUsedId=e.pointerId`**
   （統一の受け口が元から見ている「処理ずみ」の合図・§156）を立てる。
3. **選んだあとの赤い面が、次のドラッグの邪魔をしていた。** 赤いハイライトは
   `deco` の中にあるので、2回目に掴むと「面のハイライトに当たった」扱いになり、
   辺の編集（`faceInfo()`）へ流れて `ring.pts[-1].x` で落ちていた。
   → ヒットの先頭が `userData.face==='deck'` なら**そのまま平場のドラッグ**に入れる。
- ★1回目のタップで解除しないよう、pointerdown の時点で **「押す前から選ばれていたか」
  （`st.was`）を控える**。控えないと「選んだ瞬間に解除」になる（実際になった）。
- ★検証で数える場所に注意：**飾り（ハイライト・当たり判定）は `deco`＝`T.scene` 直下**で、
  `T.group` ではない（§119）。`T.group.traverse` で数えると必ず0件になる。
- 検証は `_check/deck1.js`（8項目：赤くなる・平場だけ上がる・躯体不動・
  もう一度押すと外れる・選び直せる・選択ツール以外では無反応・端部の面8枚・JSエラーなし）。

### 165. 道具のトグル解除／対のボタン／図面の天端面／躯体を立体（㎥）に（2026-08-23v）★本人の指示
指示は4つ。①機能ボタンは選択中からもう1回押したら解除 ②タテ／ヨコ割付・夜／昼画面のような
対になるボタンは稼働中のものだけ表示 ③図面に天端面を表記（モック2枚目のとおり）
④また平場だけが浮いた。「躯体を面ではなく㎥（立体）として成形して」。

- **①`setTool` の先頭で「同じ道具をもう一度＝選択に戻す」**（描画・中抜き・範囲選択・移動・
  段差ライン。「選択」自体は解除しない）。1行で全ボタンに効く。
- **②`nnPairBtns()`**：ヨコ／タテ割付、夜／昼画面は**いま動いている方だけ**を出す。
  押すともう片方へ切り替わる（`nnWariToggle` / `nnThemeToggle`）。
  ★出すのは「動いている方」。逆にすると、いまどちらなのか分からない（1回間違えた）。
- **③図面（2D）に天端の帯**：パラペットの辺の外側へ **天端幅W** ぶん出した帯を塗り、
  外側に線を引く。角がつながるよう辺の両端もWだけ伸ばす。
  ★寸法どおり（1マス=1mなら250mm＝1/4マス）。太く見せるために誇張しないこと。
- **★④「平場が浮く」の真因＝躯体を「面」として作っていたこと。**
  躯体の高さは `bodyLv` までで、平場（`lv`）を上げても躯体の天板はそのまま＝
  **平場だけが空中に浮き、四方の側面が無かった**（写真3枚目）。
  → **躯体の天板を `max(bodyLv, lv)` にする**。平場を上げた分だけ立体の高さが増え、
  **四方に側面（壁）が必ず出る**。パラペット・天端は `bodyLv` のままなので動かない。
  ★`lv` と `bodyLv` を分けた（§160）だけでは足りず、**躯体の押し出し量にも
    平場の高さを効かせる**必要があった。
- 検証は `_check/tb3.js`（13項目：道具のトグル・対のボタンの出し分けと切替・
  天端の帯・平場を2m上げると躯体が2mの立体になる・天端は不動・JSエラーなし）。

### 166. 選択ボタンの解除／天端の「謎のライン」／平場と天端の独立（2026-08-23w）★本人の指摘3件
「①選択中に選択ボタンをクリックしても押されたまま。本来解除されるはず ②なぜ外壁側の
パラペットに謎のラインが入っているのか。毎回気になっていた ③平場を上げると天端が消える。
平場と天端がまた同じ判定になっている。独立させて」。

- **①「選択」自体も解除できるようにした**。§165 では「選択」を解除の受け皿にしたので、
  選択ボタンだけは押しっぱなしに見えていた。いまは **`tool='none'`（道具なし）** を作り、
  もう一度押すと点灯が消える。もう一度押せば選び直せる。
- **★②「謎のライン」＝天端の上を横切る継目**（§72 で入れたもの）。
  外壁側の縁の**30mm手前まで**伸びていたので、外から見ると
  パラペットの上に等間隔の短い線が並び、外壁に模様が入っているように見えていた。
  → **天端を横切る継目は出さない**。立上りの縦の継目と面取りの斜めの継目だけで、
    重ねてあることは十分わかる。
- **★③「平場を上げると天端が消える」の真因＝§165 の直し方が乱暴だった**。
  躯体の天板を `max(bodyLv, lv)` にしたので、平場を2m上げると**躯体が2mの塊になり、
  高さ0.3mのパラペットを丸ごと飲み込んで**いた（＝天端が消えた）。
  → **躯体は今までどおり `bodyLv` まで**。そのうえで
    **平場が躯体より高いときだけ「床スラブ」を別の立体として立ち上げる**（`nnDeckBody`）。
  ★**床スラブは壁の厚みぶん内側に細める**（`nnInsetOutline`）。細めないと、
    せっかく残したパラペットをまた飲み込む。実測：20mの辺で 0.25〜19.75＝天端が外周に残る。
  ＝天端は元の高さのまま外周に残り、その内側で平場だけが上がって四方に側面が出る。
- ★**「A と B は独立」と言われたら、片方の立体がもう片方を覆っていないかまで見ること。**
  数値の上では独立していても、飲み込んでいれば利用者には「消えた」と見える。
- 検証は `_check/deck2.js`（11項目：選択ボタンの点灯と解除・天端の外壁側に継目0本・
  立上りの継目は残る・天端の位置不変・躯体の高さ不変・床スラブ2m・平場2m・
  床スラブが天端の内側・JSエラーなし）。

### 167. 天端の「謎のライン」の真因と、キノコ状の平場（2026-08-23x）★§166の直しが2つとも不十分だった
指摘は2つ。「①パラペットの謎のラインが直ってない ②天端のはみ出しアスが消えている。
平場を持ち上げるとキノコ状になっている。平場と天端がまだ独立されていない」。

- **★①「謎のライン」の正体は1つではなかった（3つの重なり）**
  1. 天端を横切る継目（§166で削除）
  2. **面取りを渡る斜めの継目＋玉が、天端の面から6mm突き出ていた**。
     すれすれの角度で外から見ると、突き出た頭が等間隔の線に見える。
     → **太さを半分**（`nnBeadGeom(L, BEAD_R*0.5)`・玉も0.55倍）にして面に沿わせた。
  3. **質感タイル（256px＝25cm）の継ぎ目**。粒をキャンバスに描くとき端で切れるので、
     タイルの境目が薄い線になり、**25cmごとの縦線**が天端・壁に並んでいた。
     → **粒を±Sにも重ねて描く（巻き付け）**＝タイルがつながる（`dot()` ヘルパー）。
  ★**「等間隔の線」は間隔を測って犯人を当てること。** 0.9m間隔＝継目／0.25m間隔＝質感タイル。
- **②天端のはみ出しアスは復活**（§166で消したのは行き過ぎ。「消えている」と指摘された）。
  低くつぶした形（round なし）＋外側の縁から60mm離す＝実物らしさと「外から線に見えない」の両立。
- **★③キノコの真因＝床スラブを「壁の厚み（250mm）」ぶん内側に細めたこと**（§166）。
  防水面は建物いっぱいなので、**250mmの庇のように四方へはみ出して**キノコに見えた。
  → 細めは **12mmだけ**（`nnInsetOutline` に「extra が負＝固定量で細める」モードを追加）。
  12mmは壁（250mm）の中に完全に隠れるので、見た目は建物幅いっぱい＝庇ゼロ。
  天端は今までどおり外周に残る（壁が床スラブより厚いため）。
- ★**「内側に細める」は目的を考えて量を決めること。** 目的は「壁と面がぴったり重なって
  ちらつくのを避ける」だけなので、数mmで足りる。壁の厚みぶん細めるのはやり過ぎだった。
- 検証は `_check/deck2.js`（14項目）。すれすれの外からの実写（/tmp/graze.png）で線が出ないこと、
  GL+0.1／GL+3 の実写でキノコにならないことを目視確認。

### 168. パラペットは「平場に載る」＝上げても必ず見える（2026-08-23y）★ここで決着
「平場を上にあげると今度は天端が見えなくなった」。§166・§167 は
**「天端は絶対高さのまま」**と読んで作ったが、それだと平場を上げるほど
天端が床スラブの下に沈み、いずれ見えなくなる（当然）。

- **★正しい理解＝パラペットは「屋根のふち」なので、必ず平場に載る。**
  ・`bodyLv` … 建物（躯体）の高さ。**平場を上げても変わらない**＝建物は高くならない
  ・`lv`（＝`lvDeck`）… 平場の高さ。**壁・パラペット・立上り防水・継目はここから立ち上げる**
  実装は build3D の部位ループ冒頭で `const bodyLv=…; const lv=lvDeck;` に変えるだけ。
  以降のコード（辺のループ全部）は無変更で「平場基準」になる。
- **床スラブ（riser）は細めない**（§167 の12mmもやめた）。パラペットがその上に載るので、
  細める理由が無くなった。**外壁に段の線が出ない**（12mmの段が横一直線の線に見えていた）。
- ★**「A と B を独立させて」は「絶対高さを固定する」とは限らない。**
  部品の**親子関係**（パラペットの親は屋根、屋根の親は建物）を先に決めること。
  独立させるべきは「建物の高さ」と「平場の高さ」で、パラペットは平場の子。
- 検証は `_check/deck2.js`（12項目：天端が平場と一緒に上がる・躯体の高さは不変・
  床スラブ2m・建物と同じ幅・天端の継目は低い・選択の解除）。

### 169. 平場と天端は完全に独立／Shift＋クリックの複数選択（2026-08-23z）★本人の代替案
「①外壁パラペット面を選ぶと中途半端な部分しか選択できない ②平場も天端も同時に高さが変わる。
代替案：**SHIFTキーを押している間に他の部位も選択すると2つ選べる**ようにして。
そしたら否が応でも天端と平場は独立せざるを得ないはず」。★この案がいちばん筋が良かった。

- **①壁の当たり判定・ハイライトが「躯体（bodyLv）まで」しか無かった。**
  平場を上げると床スラブのぶん壁が高くなるのに、判定は元の高さのままなので
  **下の一部しか選べない＝中途半端**に見えていた。
  → `Math.max(bodyLv, lv)` で「見えている壁ぜんぶ」を覆う（当たり判定とハイライトの両方）。
- **②パラペットは `bodyLv` 基準に戻した＝平場（lv）とは完全に独立**（§168 を撤回）。
  §166〜§168 は「独立」と「常に見える」を両立させようとして迷走した。
  **本人の代替案どおり、独立を優先し、まとめて動かしたいときは複数選択で行う**のが正解。
- **★Shift＋クリックで面を追加選択**（`nnSelMulti`／`nnSelAdd`／`nnIsSel`）。
  ・選んだ面はすべて赤くなる。もう一度 Shift＋クリックで選択から外す
  ・**まとめてドラッグ**＝平場と天端（壁）を同じ量だけ上下（`st.withBody`）
  ・ふつうのクリックで選び直し（複数選択は解除）
  ★受け口は3か所（統一の受け口・nnFaceDrag の壁／平場）。**すべてに Shift の分岐が要る**。
    1か所でも漏れると「Shiftしても追加されない面」ができる。
- 検証は `_check/multisel.js`（8項目：独立・壁の判定2.3m・Shiftで2面・両方赤い・
  まとめてドラッグで同じ量・ふつうのクリックで解除）。
- ★`_check/face1.js` の4件（壁のクリックで選ばれる 等）は**この変更の前から同じ**
  （前の版のファイルで再現を確認）。テスト側の当て方の問題で、別件の宿題。

### 170. 建物・平場・パラペットを「実物どおりの親子」にした（2026-08-24a）★平場と天端問題の決着
「外壁は良くなったがまだ謎のラインが入ってる。平場・天端問題はだめ。
**立上りと天端の設定が数字入力だけど、そこが原因なんじゃないの？**」
★最後の一文がすべての答えだった。

- **★迷走の原因＝「高さの独立」を "絶対高さを別々に持つこと" だと考えたこと。**
  §166〜§169 で
  ・天端を絶対高さで固定 → 平場を上げると**埋まる**
  ・天端を平場に載せる → 「同時に変わる」と怒られる
  ・完全に独立 → 平場を下げると**浮く**／上げると**埋まる**
  を行ったり来たりした。**どの組み合わせでも必ず破綻する。**
- **正しい形（実物どおり）**
  ・躯体（建物）の天端 ＝ **平場（屋根の面）**。1つの立体で、天板が平場。
  ・パラペットは**その上に立つ**。だから浮きも埋まりもしない。
  ・`bodyLv` は geometry では使わない（`lvDeck` に一本化）。床スラブ（riser）も廃止。
- **独立は「高さ」ではなく「操作」で取る**
  ・平場の高さ … 平場をドラッグ（建物ごと上下）
  ・**立上りH … 天端の面を上下にドラッグ**（平場は1mmも動かない）
  ・**天端W … 立上りの内側の面をドラッグ**
  ★これが本人の言う「数字入力だけなのが原因」の答え。面をドラッグで決められる。
- **★「謎のライン」の最後の1つ＝天端の継目が太かった**（直径12mm）。
  天端は上から見る面なので、太いと等間隔の線に見える。**0.42倍（直径5mm）**にした。
  `beadBetween` に `thin` 引数を足しただけ（他の継目の太さは変えない）。
- 検証は `_check/deck3.js`（9項目：躯体1つ・平場を2m上げると躯体が2mの立体・
  防水面がぴったり載る・パラペットも一緒・天端の面ドラッグでHが増える・平場は不動・
  天端の継目6mm以下）。deck2／multisel の期待値も更新。

### 171. 天端の高さは固定・平場は躯体ごと上下する（2026-08-24b）★平場と天端問題の本当の答え
「本来**天端の高さは変わらない**はずで、**真ん中の平場が躯体一緒に持ち上がる**のが正しい動き。
なのにこれは天端も平場も躯体ごと持ち上がっている」。

- **★これが正解だった。** §166〜§170 では
  「天端＝絶対高さ固定」と「パラペットは平場に載る」を**両立できない**と思い込んでいたが、
  **両立する**。パラペットの底を平場に載せたまま、**立上りH を平場が上がった分だけ縮める**だけ。
  ・パラペットの底 ＝ 平場（載っているので浮かない・埋まらない）
  ・パラペットの上端 ＝ 平場 ＋ H ＝ **一定**（H が縮むから）
  ・躯体の天板 ＝ 平場（一緒に上がる）
  実物どおり：かさ上げすると立上りが浅くなる。**現場でよくある話そのもの。**
- 実装は平場ドラッグの中で1つ足すだけ（`nnGrabH` で全辺のHを控え、`ed.h = h0 - dh*1000`）。
  リング（外周＋穴）ぜんぶが対象（`nnRingsAll`）。0mm 未満にはしない。
- **天端のはみ出しアスは「見える太さ（0.85倍）」に戻し、置き場所を天端の内側半分だけにした**。
  §167 で細くしすぎて「消えている」と言われ、太いままだと「等間隔の線」に見える。
  **太さではなく“どこに置くか”で解決する**のが答えだった（外側の縁まで渡さない）。
- **★GL+0m より下は作れないようにした**（地下防水は対象外・本人の指示）。
  ドラッグ・表の入力・カード・読み込み時の正規化の4か所すべてで 0 未満を 0 に丸める。
- 検証は `_check/deck4.js`（9項目：天端の上端が不変・平場と躯体が一緒に上がる・Hが縮む・
  はみ出しアスがある・GL+0mで止まる（入力とドラッグの両方））。

### 172. 天端のはみ出しアスは端末まで／平場の選択に天端を含めない（2026-08-24c）
「天端のはみ出しアスが端末まで伸びてないからおかしい。あとそもそも平場を選択した時点で
天端の面積まで範囲対象になっている、そこがおかしい」。

- **①天端のはみ出しアスを端（端末）まで通した**。§171 では「線に見える」対策として
  天端の内側半分だけに置いたが、**納まりとしては端末まで行くのが正しい**。
  いまは面取り（CH）から反対側の面取りまで＝天端幅ぜんぶを渡る。
  ★「線に見える」問題は §167 の質感タイルの継ぎ目と、面取りを渡る継目の太さで
    すでに解決済み。**継目を短くする必要は無かった。**
- **★②平場を選ぶと天端まで赤くなっていた真因＝ハイライトが `depthTest:false`**。
  赤い面は「手前に必ず描く」設定なので、**輪郭ぜんぶで塗るとパラペットの上まで
  赤くかぶさる**（実際には平場の板は天端より下にある）。
  → **立上りの内側の線まで細めてから塗る**（`nnInsetOutline(..., 0.006)`）。
  実測：30×10m・天端幅250mm で赤い範囲は 0.256〜29.744＝**天端は含まない**。
  ★数量（平場300㎡）は輪郭ベースのままで正しい。**見せ方だけを直した。**
- 検証は `_check/deck5.js`（6項目：継目の長さ＝天端幅−面取り×2・赤い範囲が内側の線まで）。

### 173. 天端面と平場面を切り分けた＝「浴槽の床が上がる」形（2026-08-24d）★本人の見立てが正解
「選択時は天端を含めなくなったけど、持ち上げたら天端面の範囲まで上に持ち上げている。
おそらく**天端面と平場面を明確に切り分ける必要がある**んじゃない？今はたぶん何かが同じになってる」。

- **★「同じになっていた」もの＝形（輪郭）と、躯体の天板。**
  ・防水面（mem）も床スラブも**建物の輪郭ぜんぶ**で作っていた＝天端の下まで一枚の板
  ・躯体の天板が `lvDeck`（平場）だった＝平場を上げると**建物ごと**上がる
  この2つのせいで、平場を上げると天端の範囲まで一緒に持ち上がっていた。
- **正しい形＝「浴槽（バスタブ）」**
  ・**躯体の天板は `bodyLv` で固定**（平場につられて上がらない）
  ・**平場（床）は立上りの内側の形**（`nnInsetOutline`）で作る＝天端の面積を含まない
  ・かさ上げ（平場＞躯体）のぶんは、**内側だけの床スラブ**（`nnDeckBody`）で立ち上げる
  ・パラペットは床に載り、上端は固定（§171 の H 自動縮小はそのまま）
  → 壁はそのまま・**中の床だけが上がる**。天端は1mmも動かない。
- ★**「Aを動かしたらBまで動く」ときは、AとBが同じ形／同じ変数を共有していないかを疑う。**
  今回は「輪郭」と「躯体の天板」の2つが共有物だった。
- 数量（平場300㎡）は輪郭ベースのままで変えていない。**見た目の作りだけを分けた。**
- 検証は `_check/deck6.js`（9項目：躯体の天板が動かない・躯体は輪郭のまま・
  床スラブは内側だけ・平場が上がる・天端の上端が不変）。

### 174. 平場の高さ変更を1か所に集約した（2026-08-24e）★§173 は撤回
「なんかおかしくなったよ。平場と立上り入隅のラインの境界線がないんじゃない？」
→ §173（躯体の天板を固定し、内側だけの床スラブで立ち上げる）は**見た目が破綻したので撤回**し、
§172（2026-08-24c）の形に戻したうえで、**本当の原因1つだけ**を直した。

- **★本当の原因＝「平場の高さを変える入口が2つあって、片方だけ天端を守っていた」。**
  ・平場のドラッグ … §171 で「上げた分だけ立上りHを縮める」を入れた（天端は動かない）
  ・**屋根の表／カードの GL+ 入力（`nnLvLive`）… 何もしていなかった**
  → 表から高さを入れると、天端まで一緒に持ち上がる。本人が見ていたのはこちら。
- **`window.nnSetDeckLv(poly, want)` に集約**（本体側・前方に `nnRingsAll` の保険つき）。
  ・天端（パラペットの上端）の高さは変えない＝上げた分だけ立上りHを縮める
  ・**立上りは 50mm を下回らない**＝天端が消えない。これ以上は平場を上げられない
  ・GL+0m より下げない（地下防水は対象外）
  ドラッグも表の入力もこの1本を通す。**入口が増えたら必ずここを通すこと。**
- ★ドラッグ中は**控え（`st.h0s`）から毎回Hを作り直してから**呼ぶ。
  そうしないと「縮める」処理がコマごとに積み重なって一瞬で0になる。
- ★**修正の入口が複数ある処理は、必ず1つの関数にまとめること。**
  §171〜§173 の迷走は「ドラッグだけ直して、入力欄を直していなかった」ことが原因。
- 検証は `_check/deck7.js`（12項目：表の入力でも天端の上端が不変・立上りが縮む・
  躯体は平場と一緒に上がる・50mmで止まる・戻すと立上りも戻る・GL+0m・平場の選択に天端を含まない）。

### 175. 「今度は持ち上がらなくなった」＝上げ幅を止めていたのが原因（2026-08-24f）
§174 で `nnSetDeckLv` に **「立上りが50mmになるまでしか上げられない」上限**を入れたのが誤り。
既定の立上りは **300mm** なので、**平場は0.25mまでしか上がらない**＝実質「持ち上がらない」。

- → **上げ幅の制限は撤廃**。立上りHが50mmまで縮んだら、そこから先は
  **H=50mmのままパラペットごと一緒に上がる**（かさ上げしきった状態）。
  これで「天端は動かない」も「いくらでも上げられる」も両立する。
- ★**安全のための上限は、ふだんの値で試してから入れること。**
  今回は立上り1000mmで検証していたので気づかなかった（既定は300mm）。
  検証データは**既定値でも必ず1本流す**。
- 検証は `_check/deck7.js`（13項目。**立上り300mm（既定）で平場を2m上げられる**を追加）。

### 176. 天端がクリックできなかった真因＝平場のつかみが横取りしていた（2026-08-24g）
「できないよ。しかも天端も選択できないし」。**実際のマウスで再現して原因を数値で特定した。**

- **★真因**：`nnFaceDrag` の pointerdown は
  ①ハイライト（`userData.face`）に当たったか → ②当たっていなければ**平場をつかむ**
  という順序だった。**当たり判定の箱（`userData.pick`）は見ていなかった。**
  そのため、天端の箱にちゃんと当たっていても「ハイライトが無い＝何も無い」と判断し、
  奥にある平場をつかんでいた（＝天端を選べない・平場が勝手に上がる）。
  ★ヘッドレスで `intersectObjects` の中身を出したら、1件目が
    `pick:{f:'top'}` なのに sel が `deck` になっていた。**これが動かぬ証拠。**
- **直し方（2つ）**
  ・当たり判定の箱に当たっていたら**平場をつかまない**（`faceAhead`）
  ・**面を選んでいる状態でその面をつかんだら、必ず「面のドラッグ」にする**
    （ハイライトを外しても、選択中の面の操作が続く）
- **ドラッグの効きを細かく**（1pxあたり 1/3・5cmきざみ）。以前は60pxで3.8m動いていた。
- 立上りの下限は 50→**150mm**（細すぎると天端が消えたように見えるため）。
- ★**「できない」と言われたら、まず本物のマウスで再現して
  レイキャストの中身を出すこと。** 推測で直すと何度でも外す（§166〜§175の反省）。
- 検証は `_check/facedrag.js`（8項目：天端が選べる・天端のドラッグでHが増える・
  そのとき平場は動かない・平場が選べる・平場がドラッグで上がる・立上り150mm・表の入力）。

### 177. ★フリーズ・落ちる・形が黒くなる の原因（2026-08-24h）★計測で特定
「図面・積算の操作中にフリーズしたり、落ちたり、形状が黒になったりする」。
**推測せず、three.js の `renderer.info` で計測して特定した。**

## 計測でわかったこと（30×16mの1面・立上り300）
| | 前 | 後 |
|---|---|---|
| GPUに載っている形の数 | **863 →組み直すたびに増え続ける** | **68（増えない）** |
| 組み直し1回 | **54ms** | **4ms** |
| JSメモリ | 67MB | 12〜15MB |
| 3Dの部品数 | 842個（うち**継目が808個**） | 同じ（見た目は不変） |

- **★①「黒くなる・落ちる」の原因＝GPUメモリの解放漏れ。**
  `build3D` の後片付けが `while(g.children.length) g.remove(...)` だけで、
  **`dispose()` を一度も呼んでいなかった**。three.js は remove しても
  GPUのバッファを解放しないので、組み直すたびに数百個ずつ溜まる。
  ドラッグ中は毎秒60回組み直すので、数秒でメモリ切れ →
  **WebGLのコンテキストが失われて全部が黒くなる／タブごと落ちる。**
  → `nnFreeGroup()` を新設し、外した部品の geometry / material を必ず解放する。
  ★**使い回している物（`userData.nnShared`）と質感（texture）は解放しないこと。**
- **★②重さの主因＝継目（はみ出しアス）の形を毎回800個つくり直していた。**
  `nnBeadGeom` は円柱の全頂点に4つのサイン波でゆらぎを与える処理。1本あたり数百頂点。
  → **長さ1cm・太さ0.1mm に丸めて使い回す**（`_nnBeadCache`）。玉も2種類だけなので使い回す。
  材質（`nnMat`）も（種類・色・両面）で使い回す。
- **★★ここで一度こわした（重要）**：使い回す形を、呼び出し側が
  `gm.rotateZ()` / `gm.scale()` で**その場で作り変えていた**。
  共有している元の形が壊れ、2回目からは向きも太さも狂う（検証で即座に出た）。
  → **向き・つぶしまで含めて覚える `nnBeadGeomT(len,r,向き,つぶし)`** に統一。
  ★**使い回す物は、呼び出し側で絶対に加工しないこと。**
- **③ドラッグ中の組み直しは「1コマに1回」にまとめた**（`nnBuild3DSoon`＝
  requestAnimationFrame）。以前は指を動かすたびに呼んでいた。
- ★検証スクリプトのニセTHREE（`stub3.js`）に **geometry/material の `userData`** を足した
  （無いと `nnShared` の代入で落ちる）。これで beadaudit・f3chk・facegap が復活した。
- 検証は `_check/perf3d.js`（9項目：30回組み直しても形が増えない・1回15ms以内・
  JSメモリ・使い回しても形が狂わない）。

### 178. ★ドラッグ中のフリーズの本当の原因＝シェーダーの作り直し（2026-08-24i）
§177 で組み直しを 54ms→4ms にしたのに、**実際のドラッグはまだ遅かった**。
CPUプロファイラを取ったら、**時間の69%が `getShaderInfoLog` / `getProgramInfoLog`**
＝WebGLのシェーダーを作り直していた。40コマのドラッグに **35.7秒**。

- **★真因＝飾り（`deco`＝当たり判定・赤いハイライト）の後片付けで材質を捨てていた。**
  three.js は材質を `dispose()` すると、その材質のために組み立てたシェーダー（プログラム）も
  参照が0になった時点で解放する。次のコマで同じ材質を作ると**また一からコンパイル**。
  `deco` は組み直しのたびに作り直すので、**毎コマ2本ずつコンパイル**していた。
  → ①後片付けで**材質は捨てない**（形だけ解放）
    ②飾りの材質を **`nnDecoMat('inv'/'red')` の2つに固定して使い回す**
  ★**「材質を捨てる＝シェーダーを捨てる」。材質は使い回して捨てないのが正解。**
    解放が要るのは形（geometry）だけ。
- **★あわせて継目の作りを軽くした**（§177の続き）。計測で
  「継目を隠すと描画が **97ms→1ms**・三角形 **106,120→440**」＝重さの99%が継目だった。
  1本あたり768三角形（まわり8分割×長さ48分割）→ **まわり4・長さ最大12 で96三角形**に。
  玉も 8×6 → 5×4。**見た目は前後の実写で見比べて違いなし。**
- ★ドラッグ中は影の作り直しをしない（`window.nnDragBusy`）。止まって0.26秒後に一度だけ作り直す。

## 最終的な数字（30×16mの1面）
| | 直す前 | 直した後 |
|---|---|---|
| 組み直し1回 | 54ms | **4ms** |
| 描画1回 | 97ms | **3ms** |
| 三角形 | 106,120 | 27,632 |
| GPUに載る形 | 863（増え続ける） | **68（増えない）** |
| ドラッグ60コマの処理 | 数十秒 | **3ms** |
| 組み直し1回のシェーダー作り直し | 2本 | **0本** |

- ★**「重い」と言われたら CPUプロファイラを取ること。**
  推測で「描画が多いから」と決めつけると、今回のようにまったく別の原因を見逃す。
  `Profiler.start/stop`（CDP）と `renderer.info` と `gl.compileShader` の呼び出し数、この3つで足りる。
- 検証は `_check/perf3d.js`（11項目：形が増えない・4ms・シェーダー0本・ドラッグ60コマ300ms以内）。

### 179. 大きな図面のための「まとめ描き」（2026-08-24j）
屋根20面の図面で計測：**部品4,480個・描画の呼び出し4,881回**。
呼び出し1回ごとにCPUの手間がかかるので、これが大きな現場での重さになる。

- **★同じ形の継目を1回でまとめて描く**（`nnInstanceBeads`＝three.js の InstancedMesh）。
  継目の形は §177 で「長さ・太さごとに使い回し」にしたので、**種類は数十しかない**。
  20面の例では **4,880本の継目が12種類**にまとまる。
  ・部品 4,480 → **692**／呼び出し 4,881 → **1,093**
  ・**CPUの手間（8×8の画面で計測＝塗る処理を除いた純粋な呼び出しの手間）
    18.1ms → 4.9ms**
  ・三角形の数は変わらない＝**見た目は同じ**
- ★**継目が1200本未満の図面ではまとめない**（`NN_INST_MIN`）。
  ふだんの1〜2面ではそのまま。まとめると1本ずつの部品が消えるので、
  検証スクリプト（継目を1本ずつ数えるもの）もこれまでどおり動く。
- **★★GPUが無い端末（ソフト描画）では、まとめ描きはかえって遅い。**
  検証環境（SwiftShader）で実測すると 描画 24ms → 168ms と悪化した。
  → `nnIsSoftGL()` で描画の種類を見て、ソフト描画なら**まとめない**。
  ★**計測環境がソフト描画のときは、GPU前提の最適化を「遅くなった」と誤判定する。**
    塗る処理を除いた「呼び出しの手間だけ」を、画面を8×8にして測ると本当のところが分かる。
  ★検証用に `window.nnForceInstance=true` で強制できる。
- 検証は `_check/inst3d.js`（8項目）と `_check/stress3d.js`
  （6周回のドラッグ・高さ入力・タブ往復でメモリが増えないこと）。

### 180. 道具の「もう一度押したら解除」が、プログラムからの呼び出しでも働いていた（2026-08-24k）
検証 `uimg`（下絵を選ぶ）が3件落ちたままだったので追ったら、**実バグ**だった。

- **★原因**：§166 の「同じ道具をもう一度押したら解除」を `setTool()` の入口に置いたため、
  **プログラムの中から「選択モードにしておく」つもりで `setTool('sel')` を呼ぶと、
  すでに選択中だったときにモードが切れて `tool='none'` になっていた。**
  下絵をタップしても選べない（選択ツールではないので受け付けない）のはこれが原因。
- → **解除するのは「ツールバーのボタンを押したとき」だけ**にした（`setTool(t, byUser)`）。
  HTMLの onclick だけ `setTool('sel',1)` のように印を付ける。内部の呼び出しは今までどおり
  「そのモードにする」だけ。
- ★**トグル（押すたびに入切）を関数の入口に置くと、内部から状態を整える呼び出しまで
  裏返ってしまう。** トグルは「利用者の操作」に限ること。
- 検証：`uimg` が全部○に復活。`tb3` は「実際にボタンを押す」形に直し、
  「プログラムから選択モードにしてもモードは切れない」を追加。

### 181. 全ページ・全ボタンの総当たり点検（2026-08-24m）
「他の機能でも不要なプログラムは削除し、軽くせよ」を受けて、まず**壊れている所を機械的に探した**。
`_check/allbuttons.js`＝**11ページ × PC/スマホ で、押せるボタンを全部押してJSエラーを拾う**
（全削除・印刷・保存など、押すと困るものは名前で除外）。

- **見つかったのは現場マップの3件だけ**。いずれも**地図がまだ読めていないとき**に
  ボタンを押すと `google is not defined` / `null の setMapTypeId` で止まっていた。
  ★JSエラーで止まると**そのあとの操作が全部効かなくなる**＝利用者には「固まった」と見える。
  電波が弱い現場ではふつうに起こる。
  → `nnMapReady()` で見張り、まだなら**案内を出して何もしない**（`toggleTrace`／`setViewMode`）。
- 直したあと、**全11ページ・全ボタンでJSエラー0件**。
- ★このスクリプトは**ボタンを増やしたら流す**こと。壊れた導線を数分で洗い出せる。

### 182. 保存の「無言の失敗」を無くした（2026-08-24n）
- 端末の空き容量が足りないと `localStorage.setItem` は例外を投げる。
  受け止めていないと**そこで処理が止まる**ので、
  「押しても何も起きない」「消したのに戻る」という形で表に出る（原因が分かりにくい типの不具合）。
- 点検したところ、現場マップに**9か所**（屋根の輪郭・ピンの位置・写真の削除・工程のチェック・
  現場の削除・屋根高さ・ピンの高さ）、図面・積算に**2か所**、受け止めていない保存があった。
  ※写真の追加だけは元から受け止めていた。
- → 現場マップに **`nnSet(key,value)`**（受け止めて知らせる・成功したかを返す）を新設して
  すべて通した。図面・積算の2か所も try で囲んだ。
- ★**保存は必ず受け止めること。** 無言で失敗すると、利用者には
  「アプリが壊れた」としか見えない。
- 検証は `_check/save1.js`（7項目：例外が外に出ない・戻り値・お知らせ・ふだんは保存できる・
  **ソースに無防備な保存が残っていない**）。
- ★現場記録帳の緑バーの位置合わせ（`syncListSb`）も、0.7秒ごとの総当たりから
  「中身や大きさが変わったときだけ」に変えた（ResizeObserver）。この処理は
  `scrollHeight` と `getBoundingClientRect` を読むので、そのたびに画面の配置計算が走る。

### 183. ★「形状が黒になる」の正体＝WebGLの土台の取り上げ（2026-08-24p）
本人の指摘「操作中にフリーズしたり、落ちたり、**形状が黒になったり**する」の、黒の部分。

- **★原因**：スマホ（とくにiPhone）は、ほかのアプリに切り替えたり記憶が足りなくなると、
  3Dを描く土台（**WebGLのコンテキスト**）を取り上げる。これまで受け口が無かったため、
  1. **ブラウザが土台を返してくれない**
     （`webglcontextlost` のときに **`preventDefault()` を呼んで初めて**返す約束になる。
      呼ばないと二度と戻らない＝**永久に黒いまま**）
  2. 返ってきても**空の光（環境光・PMREM）は作り直されない**。材質は空の光を見て色を決めるので、
     光が無いと建物が**真っ黒**に描かれる。
- **実測**（`WEBGL_lose_context` でわざと取り上げて再現）
  | | 暗い画素の割合 |
  |---|---|
  | 正常 | 0.1% |
  | 取り上げ→復帰（直す前） | **73.2%** |
  | 直したあと | 0.1% |
- **直し方**（末尾の `nn-ctxlost-js`。`init3D` 本体は触らず包む＝§119の型）
  ・`webglcontextlost` … `preventDefault()` ＋「休みます」の案内 ＋ 描くのを止める
  ・`webglcontextrestored` … **空の光を作り直す**（`nnMakeEnv`）→影を計算し直す→`build3D()`→描き直す
- ★**この作りは「3Dを使うページ」なら必ず要る。** 受け口が無いと、一度取り上げられた端末は
  アプリを開き直すまで黒いままになる。
- 検証は `_check/ctxlost.js`（直す前のファイルで★NG5件・直したあと全部○）。
  ★**キャンバスの絵を `drawImage` で読むと真っ黒になる**（`preserveDrawingBuffer` が false で、
    描き終わった直後に中身が捨てられるため）。**同じ場で描き直して `readPixels`** で読むこと。

### 184. ★平場を上げて下げると立上りが 300mm→3150mm に化けていた（2026-08-24p）
- **原因**：`nnSetDeckLv` が高さを ①150mmで止める ②10mm単位に丸める の2つを毎回していたので、
  **止めた分と丸めた分が失われる**。3m上げると 300→150 になり、下げると 150+3000＝**3150mm**。
  ＝平場を上げ下げしただけで、3mを超えるパラペットができていた。
- **直し方**：`ed._hv` に「止める前・丸める前の本当の高さ」を控える。
  ★**ほかの場所（辺の編集・天端の面ドラッグ・屋根の表）で高さを変えたときは控えを使わない**
  （`_hv.h` と `ed.h` が一致するときだけ信用する）。こうすれば**ほかの処理を1つも直さずに済む**。
- 検証は `_check/deck7.js` の⑤（3m上げて戻す・40回上げ下げ）。

### 185. ★「躯体の高さ（bodyLv）」は3Dに一切効かない幽霊の項目だった（2026-08-24p）
- 3Dの組み立て（build3D）は **`poly.lv`（平場の高さ）しか見ていない**（§174で決着）。
  なのに `poly.bodyLv` という別の項目が残っていて、
  ・カードの「躯体GL+」「⬆ 躯体も合わせる」＝**押しても3Dは1mmも動かない飾り**
  ・Shiftで平場と天端をまとめて動かす機能（§169）＝**bodyLvに書いていたので効いていなかった**
  ・**保存して開き直すと `bodyLv=lv` が入り**、そのあと平場を下げると
    「壁の足元」と「境界の辺の種別」を**古い高さで**決めてしまい、**塔屋が宙に浮いた**
    （実測：メイン屋根 9m→5m にしても塔屋の足元は 9m のまま）
- **直し方**：高さの持ち主を `lv` ひとつにまとめ、`bodyLv` は読み込み時に捨てる。
  Shiftのまとめ移動は「立上りHを変えずに lv を動かす」＝本当に天端も一緒に上がるようにした。
- **消したもの**：`nnBlLive` / `nnBlStep` / `nnLvFollow`、カードの「躯体GL+」欄と2つのボタン、
  表の `.rbl` の受け口。
- ★**教訓：同じものの高さを2つの変数で持たない。** どちらかが必ず古くなり、
  「見た目は正しいのに当たり判定や自動判定だけ狂う」という見つけにくい不具合になる。

### 186. 継目の形の置き場（キャッシュ）が増え続けて落ちる（2026-08-24p）
- 継目の形は「長さ」ごとに覚えて使い回している。ところが**指で高さを動かすと辺の長さが少しずつ
  変わる**ので、**1秒のドラッグで25個**の割合で新しい形が増えていた。
- ★**上限（900個）に達したあとが最悪**だった。覚えないのに「使い回す印（nnShared）」だけ付けていたため、
  **作った形が二度と解放されず**GPUの記憶を食いつぶしていた（＝重くなる・落ちる・§183の黒につながる）。
- **直し方**：①覚えられたときだけ印を付ける ②いっぱいになったら**長く使っていないものから捨てる**
  （`nnBeadStore`。`dispose` はGPU側を返すだけで形のデータは残るので、消し間違えても絵は壊れない）。
- 検証は `_check/beadcache.js`（60秒使い続けても置き場が増えない・絵が1画素も変わらない）。
  ★§184を直したことで、そもそも新しい長さが生まれにくくなり、実測では**222個で完全に頭打ち**になった。

### 187. 検査そのものの手入れ（2026-08-24q）★★これをしないと本物の異常が埋もれる
- **①ニセTHREEの検査4本が、ずっと空回りしていた**（zfix2／f4chk／zdir／photochk）。
  「まとまり（Group）」のうち**いちばん最後のもの**を見ていたが、あとから
  当たり判定用のまとまりが増えたため、最後は空っぽ＝何を調べても0件＝毎回★NG。
  → **子の数がいちばん多いまとまりを返す小道具 `__zMain()`** を `stub3.js` に足し、8本を差し替えた。
- **②`_land.html`（よこ向き再現のコピー）を「誰かが先に作ってくれている」前提の検査が22本**あり、
  単独で走らせると404で丸ごと落ちていた（`showView is not defined`）。
  → **`_check/mkland.js`** を作り、22本の先頭で自分で用意するようにした。
  ★**新しくよこ向きの検査を書くときも、先頭で `require('./mkland')()` を呼ぶこと。**
- **③空振りの判定を1つ見つけた**：`face1` の「カードに押出しがある」は
  **ページのどこかに同じ文字があれば必ず通る**書き方だった（廃止されたカードでも○になっていた）。
  ★**`document.body.innerHTML` に文字が含まれるか、で判定しないこと。**
- **④判定のゆとりが細かすぎた**：`f4chk` は 0.1mm で見ていたため、
  メートル単位の計算の丸め（1mm）で毎回★NG。2mmに広げた（元の不具合は100mmなので十分見つかる）。
- **⑤仕様が変わったのに古いままだった検査**：deck1／deck2／multisel／psel1／bluelv1。
  §164〜§176 の行き来（平場と天端の独立）で決着した形に合わせ直した。
- ★**「毎回★NGが出る検査」を放っておかないこと。** 慣れて見なくなり、本物の異常を見逃す。

### 188. 全ページの点検（2026-08-24q）
- **①押しても何も起きないボタンの総当たり**（`/tmp/deadbtn.js` の考え方）：
  ボタンを押して「画面が変わったか・保存が変わったか・移動したか」を見る。
  §185の「躯体GL+」はこれで見つかる種類の不具合。
- **②存在しない処理を呼ぶボタン**（静的に検出）：全11ページで**1件だけ**あった。
  ホームの版表示 `<div id="nnver" onclick="nnPerfOpen()">` ＝ その処理はどこにも無く、
  **タップのたびにJSエラー**。計測表を開く仕組みは別にちゃんと入っていたので、書き残しの削除だけで解決。
- **③idの重複**：`<style>` と `<script>` で同じ名前を使っている箇所があるが、
  **コードから探していない**ので実害なし。`dz`（写真/資料/図面タブ）と `hVal` は
  同時にDOMへ出ない作りなので問題なし。
- **④公開先に混ざっていた一時ファイル**：`__L_hacchu.html`（検証で作る発注ページのコピー・128KB）が
  リポジトリに入っていた。外して `.gitignore` に登録。
  ★**検証で作るファイルは必ず `.gitignore` に入れること。**

### 189. よこ向きスマホで画面ごと横にずれる（2026-08-24s）
- 図面・積算のよこ向きで、作図中に指を横へ払うと**画面全体が右へ361pxずれ**、
  画面の外に隠してある「積算・設定」の引き出しが顔を出していた。
- **原因**：引き出しは `transform` で画面の外へ出して隠しているが、隠した先も
  「ページの中身」として数えられるので横スクロールが生まれる。
  `body` には `overflow-x:hidden` が入っていたが、**実際に動いていたのは `html` のほう**。
- 直し方：`html{overflow-x:hidden}`（現場マップで同じ原因を直したのと同じ話・§2「謎の空白」）。
- ★**「はみ出しているか（scrollWidth）」ではなく「実際に横へ動かせるか」で見ること。**
  画面の外に隠したパネルは幅としては数えられるが、動かせなければ利用者に影響しない。
  `allpages2` の判定もこの形に直した。検査は `_check/noxscroll.js`（全11ページ×たて/よこ）。

### 190. 既存防水の長い名前が枠から見切れていた（2026-08-24t）
- 一覧カードの「既存防水」に、いちばん長い「露出アスファルト防水」が入らず 14〜22px はみ出していた。
- ★**2026-08-13h で一度直したのに、写真の幅を 258→250→200→172px と小さくするたびに
  枠も狭くなり、その都度この見切れが戻っていた。** 確かめるはずの検査（card4chk）が
  **写真の帯の名前（`.phhint`→`.phcap`）が変わったのに古い名前を見ていて丸ごと落ちており**、
  何も見ていなかったため誰も気づけなかった。
- 直し方＝物件名と同じ「入る大きさまで文字を小さくする」（`fitOne` に一般化）。枠の幅は変えない。
- ★**教訓：検査が「落ちている」のは「通っている」より危険。** 落ちた検査は静かなので、
  その範囲がまるごと無防備になる。`_check` は毎回「動いたか」も見ること。

### 191. 現場マップに案内（トースト）が無く、押しても本当に何も起きなかった（2026-08-24u）
- 「地図をまだ読み込めていません」という説明は用意してあったのに、
  **このページだけ `toast()` が無く**、`console.warn` に落ちていた＝誰にも見えない。
  電波の弱い現場で 面積計測・回転・傾き を押しても、無反応で理由も分からない状態だった。
- 直し方：他ページと同じ見た目の案内を用意し、回転・傾きのボタンにも同じ門番を付けた。
- ★**押しても何も起きないボタンの総当たり点検 `_check/deadbtn.js` を新設**した。
  「画面が変わったか・保存が変わったか・移動したか」を見る。§185の「躯体GL+」もこれで見つかる。
  ★指の操作に近づけるため **pointerdown → pointerup → click の順に流す**こと。
    `click` だけだと、絞り込みチップのように pointerdown で反応する部品を誤判定する。

### 192. 端末の保存がいっぱいのとき、黙って中身が消えていた（2026-08-24w）
- `localStorage.setItem` は容量を超えると例外を投げる。受け止めていないとそこで処理が止まり、
  利用者には**「保存されたつもり」で中身だけが消える**（いちばん怖い失敗）。
- §182 で現場マップと図面は直したが、**ライブラリ・発注・材料登録・仕様登録・用語集・カメラの
  6ページが残っていた**。とくにライブラリは写真を保存するので、いちばん起きやすい。
- 全ページ共通の受け止め方（`nnSet`）に統一。検査は `_check/quota.js`（保存を必ず失敗させて、
  例外で止まらず案内が出るかを全ページで見る）。
- あわせて **`_check/save2.js`** を新設：屋根2面＋穴＋辺ごとの設定＋アゴ＋役物＋断面＋下地・仕様を
  かいて保存 → 開き直して**16項目すべて一致**することを確認（データが欠けないことの担保）。

### 193. 図面・積算だけプルダウンが異様に大きかった（§50の直し漏れ・2026-08-24w）
- スマホのとき「**すべてのプルダウンを16pxにする**」指定が、このページだけ残っていた
  （`nn-zumen-phone` の `html[data-nnphone="1"] select{font-size:16px !important}`）。
  下地13px・仕様13.5px・断面のバー11.5px といった指定を全部打ち消していた。
- §50（2026-08-06m）で**別の指定からは外したのに、こちらが残っていた**。
  プルダウンはピッカーが出るだけで画面は拡大しないので、16pxにする必要はない。
- ★同じ趣旨の指定が**1つのページに2か所ある**ことがある。直すときは
  `grep -n 'font-size:16px !important' *.html` で全部見ること。
- 平面図の設定欄（`#nnPlanBox`）だけは、となりの入力欄と大きさをそろえるため意図して16pxのまま。
  検査 `selchk` はそこを数えて毎回★NGだったので、除外して本物の取りこぼしが見えるようにした。

### 194. 速さの実測（2026-08-24・スマホ相当の4倍負荷）
| | 直す前 | 直した後 |
|---|---|---|
| 現場記録帳：ページが表示されるまで | 848ms | **532ms** |
| 現場記録帳：一覧を開くときの位置合わせ | 342ms | **12ms** |
| 図面：3Dの組み直し | 54ms | 4ms |
| 図面：1コマ描く | 97ms | 3ms |
| 図面：継目の置き場（60秒使い続けて） | 増え続ける | **222個で頭打ち** |
- **緑のスクロールバーの位置合わせ**は、一覧の高さを測るために
  100枚のカード（約9,400の部品）の置き場所を毎回計算し直していた。起動時に6回・
  一覧を開くときに3回。**同じコマに何度頼まれても1回にまとめる**だけで上の数字になった。
- ★**「measure（測る）」系の処理は、呼ぶたびに全体の計算が走る。** 何度も呼ばれる所では必ずまとめること。
- 2Dの作図は屋根20面でも `draw` 25ms・`recalc` 23ms で余裕。PDFの組み立ても 5〜43ms。
  断面タブを開くのは269msかかるが、開いたときの1回だけ（描き直しは4ms・見張りも0回）。

### 195. 検査そのものの整理（2026-08-24v）
- 検査スクリプトが **302本** まで増えていたが、そのうち **105本は合否（○／★NG）を出さない
  一回きりの道具**だった（画面を撮るだけ・数値を出すだけ・当時の調べもの用）。
  本物の検査に混ざっていて「どれを流せば安心なのか」が分からなくなっていたので
  **`_check/_tools/` に移した**（消してはいない）。
  ・`_check/*.js` 200本＝合否を出す検査　・`_check/_tools/` 104本＝一回きりの道具
- ★**新しく書くときは必ず ○／★NG を出す形にすること。** 出さないものは道具であって検査ではない。
- 直した検査：rtbl2（廃止した躯体GL+を呼んでいた）／r4chk（中のニセTHREEが古い→stub3へ）／
  card4chk（写真の帯の名前）／pwchk（見出しの絵7→9）／qswap（絵のURLの版名を決め打ち）／
  housort（物件データを入れ替えるとずれる数値を決め打ち）／selchk（意図した16pxを数えていた）。

### 196. 3Dの「まとめ描き」を切っていた条件が誤りだった（2026-08-24y）★測り方の教訓
- §179（2026-08-23j）で「GPUが無い端末（ソフト描画）ではまとめ描きがかえって遅い」と判断して
  切っていたが、**前の計測には1回目のシェーダー（絵の描き方）の作成時間が混ざっていた**だけだった。
- ★**温めてから測る**（1回描いてから、5回描いた中央値を採る）と、結果が逆転した。
  屋根20面・同じ絵（三角形の数は同一）での実測：
  | | まとめ描きなし | まとめ描きあり |
  |---|---|---|
  | 部品の数 | 4,480 | **692** |
  | 描画の呼び出し | 4,881 | **1,093** |
  | 1コマの時間 | 30.4ms | **8.8ms** |
- → 端末の種類では切らないことにした（「継目1200本以上」の条件はそのまま）。
  逃げ道として `window.nnInstanceOff=true` で止められる。
- ★★**速さを比べるときは必ず温めてから測ること。** 1回目には
  シェーダーの作成・テクスチャの転送・初回のレイアウトなど、
  2回目以降には出ない支度が入る。それを含めて比べると、正しい判断ができない。

### 197. 全200本の検査を1本ずつ流し切った（2026-08-24y）
- 整理後の `_check/*.js` 200本を**順番に1本ずつ**流した（同時に流すとCPUの取り合いで
  時間切れ・誤判定が出るため）。引っかかったのは10本だけで、内訳は
  ・古い場所・古い名前を見ていた（kbn1／newic／tbicon／wxchk）→ 直した
  ・もう走れない・後継がある（secchk／photo2／gridchk／fps／fps2）→ `_tools/` へ
  ・時間の取り合いによる誤判定（fil17／hatogoya）→ 単独で流すと○
- ★**集計のしかたの注意**：`grep "★NG"` は要約行の「★NG0」も拾ってしまう。
  **`★NG` のあとが数字でない行**を数えること（`grep -cE '★NG[[:space:]]+[^0-9]'`）。
- ★**検査を1本ずつ流すと200本で数時間かかる。** 背景で流しておき、
  その間に別の調べものを進めるのが効率がよい。

### 198. この日に新しく作った検査（2026-08-24）
| 名前 | 何を守るか |
|---|---|
| `ctxlost` | 3Dの土台を取り上げられて戻ったとき、形が真っ黒にならない |
| `beadcache` | 60秒使い続けても継目の置き場が増え続けない（重くなる・落ちる） |
| `qty1` | 数量と見積の金額が、図形から手で計算した値と合う（紙の数字も） |
| `save2` | 図面を保存して開き直しても1つも欠けない（16項目） |
| `save3` | 記録帳の不具合タグ・内訳・工程／材料の単価と価格改定履歴／発注履歴が残る |
| `quota` | 端末の保存がいっぱいのとき、黙って消えずに知らせる（全ページ） |
| `sync1` | 記録帳・マップ・発注の物件データが食い違っていない（100件） |
| `docback2` | 書類9種すべてに「戻る」と「印刷」がある（PWAでは戻れなくなるため） |
| `noxscroll` | よこ向きで画面ごと横にずらせない（隠した引き出しが顔を出す） |
| `deadbtn` | 押しても画面も保存も変わらないボタンを総当たりで探す |
| `inputs` | 入力欄に変な値を入れても NaN や undefined が出ない |
| `mkland` / `mkbefore` | よこ向き用のコピー・変更前のファイルを、検査が自分で用意する小道具 |

### 199. 端末の保存が「使えない・壊れている」ときの守り（2026-08-24z）
§182・§192 では**書き込み側**（容量オーバー）だけ守っていた。**読み出し側が無防備だった。**

- **①保存がまったく使えない端末**（プライベートブラウズ／「サイトのデータをブロック」設定）では
  `localStorage.getItem` そのものが例外を投げる。守っていないと**ページを開いた瞬間に処理が止まる**。
  実測：**図面・積算と現場マップが SecurityError で開かなかった**（他9ページは無事）。
  ・現場マップ … APIキーの読み出し（`getKey`）と一覧の幅（`osamari_panel_w`）＝どちらも開いた瞬間に走る
  ・図面・積算 … 寸法表示の設定（`nn_zumen_dims`）
  → 読み出しも try で囲んだ。**全11ページが、保存が使えなくても普段どおり開く**（部品の数まで一致）。
- **②保存が壊れている**（形が違う）ときも落ちる。中身をそのまま使っていたので、
  `null` や数字が入っていると**そのあとの処理で例外**になり、しかも壊れた保存は消えないので
  **二度と開けない**（いちばん怖い失敗）。実測で見つかったもの：
  ・ライブラリ … `userItems is not iterable`（`nn_lib_items` が null／数字）
  ・用語集 … `Cannot convert undefined or null to object`（`nn_yougo_stars`）
  ・現場マップ・発注・客先登録・写真・保存図面・図面本体・表の位置 … 同じ形の危うさ
  → **「使える形」でなければ使わない**（`Array.isArray` / `typeof==='object'` を確かめる）。
- ★**教訓：保存は「書けないとき」だけでなく「読んだ中身の形が違うとき」も守ること。**
  書けない失敗はその場で気づくが、読んだ形が違う失敗は**アプリが壊れたように見える**。
- 検査は `_check/nostore.js`（保存が丸ごと使えない状態で全11ページ・普段の部品数と見くらべる）と
  `_check/junkdata.js`（**8通りの壊れ方**＝こわれた文字／途中で切れた／null／数字／真偽／
  ただの文字列／一覧のはずが箱／箱のはずが一覧 × 全11ページ）。
  ★でたらめなキーだと Google の地図は当然読み込めないので、その知らせは数えない。

### 200. 一覧の絞り込みで件数が狂う（2026-08-24z）★遅い端末でだけ出る
現場一覧は「まず14件→残りは次のコマ」の2段構え（§95）。

- **★`clearTimeout` では後段を止められない。** `setTimeout(()=>requestAnimationFrame(続き),0)` なので、
  **setTimeout がもう発火して「次のコマ待ち」に入っていると、clearTimeout は何もしない**。
  そこへ次の組み立てが走ると、**古い続きが新しい一覧に足される**。
  実測：11件のはずが**97件**／100件のはずが**186件**（14＋86＋86）＝条件に合わない物件が並ぶ。
- 直し方：組み立てに**通し番号**（`_nnListSeq`）を持たせ、番号が変わっていたら続きを捨てる（2行）。
- ★**速い機械では十数ミリ秒しかない窓**なので、ふつうに操作しても再現しない。
  実機（スマホ）や、他の検査と同時に走らせて機械が混み合ったときに出る。
  検査（`_check/relist.js`）は **`requestAnimationFrame` をわざと足止めして**必ず再現させる。
- ★同じ形の作り（前半だけ先に出して、残りを次のコマで足す）を書くときは、
  **必ず通し番号で「自分がまだ最新か」を確かめること。**

### 201. おかしな形・とんでもない数字で画面が固まる（2026-08-24z）★フリーズの本命
本人の「操作中にフリーズしたり落ちたりする」の、**残っていた原因**。

- **①点が0個の形で3Dが落ちる**（`Cannot read properties of undefined (reading 'x')`）。
  「戻る」や点の削除で 0個・1個・2個の形は実際にできる。落ちると**3Dが動かなくなる**。
  → 点が3つ未満の輪は使わない（`ringsOf` と build3D の入口の2か所）。
- **②寸法に 999999 と打つと画面が固まる。** 座標が 1e9 になると
  ・割付の帯のくり返しが**何億回**
  ・1辺の縦の継目が**何億本**
  ・そして**絵を描く処理（点線）の中で止まる**＝**JS側でくり返しを止めても直らない**
  → 3段構えで守った。
  1. **座標の見張り `nnSanePolys`**＝数字でない点は捨て、**±8000マス**に丸める。
     通す場所は **`draw()` の先頭・`commit()`・`saveState()`・`loadState()`**。
     ★**`draw()` の先頭でも通すのが肝**（絵を描く処理の中で固まるので、JS側の上限だけでは止まらない）。
     ★**入れ物（配列）は作り替えないこと。** 作り替えると、つかんでいる最中の頂点の参照が
       古い配列を見たままになり、指の操作が効かなくなる（`splice` で中身だけ直す）。
  2. **くり返しの上限**：割付の帯 8000／シート 8000枚／1辺の縦の継目 **400本**（`NN_SEAM_MAX`）。
     ふつうの屋根（30m×20m）は22帯66枚・1辺222本なので、当たるのは打ち間違いのときだけ。
  3. **入力の上限**：辺の長さ 1000m／写真の実寸 0.1〜1000m／面を動かす ±50m／
     押し出し 200m／断面の辺 100m／立体のよこ・たて 100m。
     （立上りH 3000mm・天端W 2000mm・壁厚 1000mm・引込み 5000mm は元から上限つきだった）
- **効果（1e9 の座標を入れて実測）**
  | | 直す前 | 直した後 |
  |---|---|---|
  | 図面を描き直す | **固まる（戻らない）** | 8ms |
  | 積算（recalc） | 固まる | 684ms |
  | 3Dを組み立てる | 固まる | 366ms |
  | 見積のデータ | 固まる | 367ms |
- 検査は `_check/degen.js`（点が0/1/2個・面積0・自分と交差・座標が文字／抜けている／
  とんでもなく大きい の9通り × 「落ちない」「5秒以内」）。
- ★**「上限」はふだんの値で試してから入れること。** §175 でも同じ失敗をした
  （立上り50mmの上限を入れて、既定300mmでは平場が上がらなくなった）。

### 202. この回に調べて「問題なし」と分かったこと（2026-08-24z）
探し続けた結果、直す必要が無いと確かめられたもの。**次に同じ所を疑わないため**に残す。

- **見張り（イベントの受け口）・ノード・JSメモリの漏れは無い。**
  図面・積算でタブを50往復（描き直し・積算・高さ入力つき）／現場記録帳で画面を40往復しても、
  受け口 0個・ノード 0個・メモリ ±0.1MB で**まったく増えない**。
- **頂点の多い図面**：2Dは300頂点でも 描き直し4.5ms・積算5.9ms（辺が短くなると寸法の札を出さない
  作りなので、かえって軽くなる）。3Dは300頂点で組み立て169ms・1コマ20ms。
  ★3Dの「形の数」は**辺の数に比例する**（壁・天端・防水層を辺ごとに1つずつ作る留め継ぎ方式のため）。
    300頂点で2400個。ふつうの現場（20面・160辺）では1300個なので問題にならない。
- **継目の形の置き場（キャッシュ）の上限は 420 のままでよい。**
  1200に増やして測ったが**まったく速くならなかった**（300頂点で 79ms → 96ms）。
  重さの正体は継目ではなく、辺ごとの壁・帯の形だった。
- **404 になっているファイルは全部「まだ絵が届いていないアイコン」**（`def_*` 21枚・`kou_*` 7枚・
  `btn_rotl` など9枚・`hou_*` 4枚・`icon_zumen`）。`onerror` で文字に戻るので不具合ではない。
  ページごとの404の数：記録帳26・現場マップ13・図面4・他0。
- **`sw.js` の保存一覧は123件すべて実在**。しかも `c.add(...).catch(()=>{})` なので、
  1つ足りなくても入れ替えは失敗しない（作りが正しい）。
- **貯まり続けるものの上限**：続きの控え（undo）80手・材料の価格改定12回・計測表7回で頭打ち。
  発注の履歴だけ上限が無いが、保存できないときは知らせる作りなので黙って消えることはない。
- **よこ向きで画面ごと横にずらせない**（§189 の直しは全11ページで効いている）。

### 203. 0.8秒ごとの見直しを「変わったときだけ」にした（2026-08-24ab）
全11ページに、画面の高さを測り直す処理が **0.8秒ごとに動き続けて**いた
（`setInterval(fill, 800)`。下部ナビの自動開閉や画面の切替に追従するためのもの）。

- ★この処理は **画面ぜんぶの置き場所を計算し直させる**（`getBoundingClientRect`）。
  ふだんは安いが、**何かを動かした直後は高くつく**。現場記録帳は部品が3万個あるので、
  一覧をスクロールしている間の仕事のうち **19%** がこれだった（計測で判明）。
- 直し方：**窓の大きさ・下部ナビの出入り・表示中の画面・表示倍率**をつないだ短い文字を作り、
  前回と同じなら**測らずに戻る**。どれも安く読める値なので、置き場所の計算は起きない。
  中身の高さが変わったとき（一覧を組み立て直したとき）は、呼ぶ側が `fill(true)` で必ず測らせる。
  ★その `fill(true)` は**画面を出したあと**（次のコマ）に回すこと。
    組み立ての途中で測ると、そのぶん表示が遅れる（実測 140ms → 436ms → 128ms）。
- あわせて、一覧カードの**文字合わせ**（長い物件名・既存防水を枠に収める処理）も直した。
  ・1つずつ「測る→直す」をくり返していたのを、**まとめて測る→まとめて直す**に
  ・一度そろえた文字は、窓の大きさが変わるまで**測り直さない**
  ・「同じ枠・同じ言葉」の答えを覚えておく（既存防水・区分・構造は決まった言葉しか入らない）
  ・指で勢いよくスクロールしている間は走らせず、**手が止まって0.18秒後にまとめて1回**
- ★**正直な結果**：指でなぞるくらいの速さでスクロールしたときの
  「いちばん長い止まり」は **260ms → 273ms** でほぼ変わらなかった。
  1画面ぶんずつ飛ばすような送り方では効くが、ふつうの操作では**もともと文字合わせが主犯ではない**。
  主犯は「新しく見えたカードを組み立てること」そのもので、これは避けられない仕事。
  ただし 0.8秒ごとの見直しを止めたぶんは、**どのページでも常に効く**（電池にもよい）。
- ★**教訓：直す前に profiler で「どこに時間が行っているか」を見ること。**
  今回も、見た目で怪しい所（文字合わせ）より、地味な定期処理のほうが大きかった。

### 204. 3D図面ビューアが圏外で開けなかった（2026-08-24ac）★§63の積み残し
現場マップの「面を書き出して見る3D図面ビューア」（`exportFace('html')` が作るページ）だけ、
three.js を**外の置き場（cdnjs）から読んで**いた。§63 で本体は同梱に切り替えたが、
ここは「別物なので未対応」のまま残っていた。

- **電波が弱い現場では「3Dの部品を読み込めませんでした」で何も見られない。**
  実測（外に出られない環境で再現）：**絵を描く命令0回・エラー画面が出る**。
- 直し方：**まずこのサイトの中の `vendor/three.min.js` を読む**（絶対の道すじにして、
  書き出したファイルを別の場所で開いても届くようにする）。読めなかったときだけ外の置き場へ。
  ★同梱は r159、ビューアは r128 向けに書かれていたが、使っている機能
  （Box3・BufferGeometry・GridHelper・ShapeUtils・MeshLambertMaterial ほか）は
  すべて r159 にもあるので、そのまま動いた（画面の写真で色23種・屋根が描けているのを確認）。
- ★**あわせて外の置き場への通信が1つ減った**（誰がいつ開いたかが外に出ない・§45の方針にも合う）。
- 検査は `_check/vwthree.js`（**直す前のファイルでは★NG6件**・いまは全部○）。
  ★**描いているかを見張るときは、three.js の部品ではなく
    いちばん下の「絵を描け」の命令（`drawElements`／`drawArrays`）を数えること。**
    three.js の描画は部品の中に直接ぶら下がっているので、型（prototype）に細工しても効かない。

### 205. 現場マップの割付のくり返しにも上限を付けた（2026-08-24ac）
なぞった屋根が思わぬ大きさになったとき（点が1つ遠くに飛んだ等）、
シーム・ジョイントの線を何万本も作って画面が固まる形になっていた。
行の数 2000・線の数 20000 で頭を打たせた。
ふつうの屋上（50m四方）は50行ほどなので、この上限に当たることはない。

### 206. 元請に出す書類に「NaN」が刷られる形が残っていた（2026-08-24ad）
書類9種（平面図・断面詳細図・割付図・施工層構成図・標準納まり詳細図・御見積書・
工程表・物件報告書・発注書）の**中身の文字**を機械的に調べたところ、
**御見積書の有効期限に `NaN/aN/aN まで`** が出る形が残っていた。

- 原因：`plusDays(n)` が「日数が数字でないとき」を守っていなかった。
  画面から出すときは入力欄の値が入るので出ないが、
  **`window.nnMitsuPDF` は外から呼べる**ので、日数を渡さないと必ずこうなる。
- 直し方：数字でなければ30日として扱う（1日を下回らない）。
  実測：`{}`／`null`／`"あ"`／`-5`／`30` のどれを渡しても、ちゃんとした日付になった。
- ★**紙に出るものは「あり得ない値が出ない」ことを機械で確かめること。**
  画面はおかしければ気づけるが、紙は元請に渡ってから気づく。
- 検査は `_check/docnan.js`（9種の中身から NaN・undefined・Infinity・[object Object]・
  null・マイナスの金額 を探す）。
  ★発注書は「現場を選ぶ→数量を入れる→確認画面」まで進めないと作れない。
    途中で呼ぶと `draft` が無くて落ちる（検査を書くときの注意）。

### 207. 圏外でも開けるようにするかは、本人の判断待ち（2026-08-24ad 調査）
「電波が届かないところで、いちど開いたページをもう一度開けるか」を全11ページで調べた。

- **いまの設定（`NN_USE_SW=false`）では、11ページとも開けない。**
- **`NN_USE_SW=true` にしたコピーで試すと、11ページとも圏外で開けた**
  （取れないのは版の確認 `ver.txt` だけ＝これは想定どおりで、JSエラーも出ない）。
  ★確かめ方は `_check/_tools/swoffline.js` に残した（手順もその中に書いてある）。
- ★**切り替えは本人の了解を取ってから**（引き継ぎメモ「最初の1回だけ遅い件」の決まり）。
  保存分を先に返す方式になるので、**「直したのに変わってない」の起き方が変わる**。
  版名4点セットの更新は必須のまま。

### 208. 写真の読み込みを軽くした（2026-08-24ae）★落ちる原因の予防
現場写真を入れるところ（現場記録帳のカード・カメラのページ）は、
**「ファイル全体を文字（base64）にしてから画像にする」**やり方だった。

- ★いまのスマホの写真は1枚10MBを超える。実測：**12MBの写真 → 16MBの文字**（約1.33倍）。
  この文字と、画像に直したぶんが同時に記憶に乗るので、**古い端末では落ちる**。
- 直し方：**`createImageBitmap`（ブラウザが直に読む道）**を先に試す。文字をまったく作らない。
  使えない端末では今までの道に落ちる（そのときも、カメラは文字ではなく
  一時のURL＝`createObjectURL` を使うようにした）。
- **カメラのページは、大きすぎる写真を横4000pxまでに落とす**。
  画面に出すのは1200px前後なので見え方は変わらず、記憶は大幅に減る。
- 出てくる写真（記録帳＝横640px・JPEG）は**今までとまったく同じ**（検査で寸法・形式・重さを確認）。
- **図面・積算の2か所も同じにした（2026-08-24af）**
  ・**下絵（航空写真）**…長辺1600pxに縮めてから保存する作りは今までどおり。
    その**前**にファイル全体を文字にしていたのをやめた。貼り付け（Ctrl+V）も同じ道を通す。
  ・**写真から起こす**…写真そのものは保存しない画面なので、文字を作る必要がまったく無かった。
    大きすぎる写真は横4000pxまでに落とす。
  ★`uLoadImage(データURL)` は残してある（ほかから呼ばれても動くように）。
    中身は `uLoadSource(元, 幅, 高さ)` に分けた。
- 検査は `_check/photoimg.js`
  （4000×3000の写真を入れて、**2つの読み方の両方**で 横640px・比率・JPEG・200KB以下 を確かめ、
   16枚続けて入れても記憶が増えないことも見る。**下絵も同じく両方の道**で確かめる）。


### 209. 画面から開けない機能・押しても何も起きない仕掛けを一掃（2026-08-24ag）
「どこからも呼ばれていない処理」を全ページで機械的に洗い出したところ、
**単なる書き残しだけでなく「作ったのに使えない機能」**が見つかった。

- **★材料登録・仕様登録の「データ管理」が、画面のどこからも開けなかった。**
  中身は CSVで書き出す／バックアップ・復元／**価格表の一括取り込み（§148）**／全データ削除。
  作りは全部そろっているのに、開くボタンだけが無かった。
  → ヘッダーに **「⚙ データ」** を足した（検査 `_check/datamodal.js`）。
- **★現場マップの「90度ライン」は、そもそも一度も働いていなかった。**
  切替の処理（`toggleOrtho`）も表示の付け替えも用意されていたが、
  **肝心の「直角に折る」処理（`orthoSnap`）がどこからも呼ばれていない**＝機能は死んでいた。
  はじめは切替ボタンを足したが、**押しても何も起きないボタンになる**と分かったので取りやめ、
  死んでいた処理ごと削除した。
  ★**「ボタンが無い」ときは、まず“中身は生きているのか”を確かめること。**
    中身が死んでいるのにボタンだけ足すと、いちばん悪い形（押しても何も起きない）になる。
- **削除した書き残し**（どこからも呼ばれていないことを、HTML・検査・引き継ぎメモまで含めて確認）
  ・現場マップ … `runOneClick`（3点で屋根面を検出の古い方。いまの実装は別にある）／`dragTargetPts`
  ・図面・積算 … `nnPlanVals`／`nnSolCount`／`d3Deselect`／`perimeterM`／`edgeInwardNormal`
  ・現場記録帳 … `goPropertyExtras`／`inRange`
  ・4ページ … `mkBtn`（開発中に使っていた「表示切替」の浮きボタン。いまは作られない）
  ・国交省仕様 … `setView`（カードと一覧の切替は §6 で廃止済み）
  ・ライブラリ … `exSearch`（検索例のタグは §50 で削除済み）
- **残した（消してはいけないもの）**
  ・`gm_authFailure` … Google の地図が呼ぶ　・`nnPhotoTest◯◯` … 検査が使う
  ・`nnRoofFold`／`d3Fit`／`nnAdjPending`／`uLoadImage` … 検査・互換のため
  ・**`nnEdgeThick`／`nnEdgeThickAsk`／`nnEdgeThickSet`（壁の厚み）… 中身は生きているが入口が無い。**
    データ（`e.wall`）は保存も3Dへの反映も動いているので、**ボタンを足せばすぐ使える**。
    どこに置くかは見た目の判断なので、本人に相談してからにする。
- ★**探し方**：`function ◯◯(` と `window.◯◯=function` を集め、
  そのページ・検査・メモ全部での出現数から定義の数を引く。0以下なら誰も呼んでいない。
  ★ただし **`$` のような1文字の名前・IIFE（その場で動く関数）・
    HTMLの `onclick` から呼ばれるもの**は誤って挙がるので、1つずつ中身を見て確かめること。

### 210. 保存が壊れているとき「開ける」だけでなく「操作もできる」ようにした（2026-08-24ag）
これまでの見張り（§192・junkdata）は **「壊れた保存でもページが開くか」までしか見ていなかった**。
そのため、**開いたあとに操作したときだけ止まる**不具合が残っていた。実際に4件見つかった。

- **★工程表（施工中のタブ）が二度と開かなくなる。**
  保存が「行の並び（配列）」でない形だと、開いた瞬間に `rows.forEach is not a function` で止まる。
  一度こうなると、消す手立ても無いので**施工中のタブが永久に開かない**。
  → 読み出すときに「並びであること」「1行ずつが箱であること」を確かめる（`nnSchedRows`）。
- **★新規物件がまったく作れなくなる。** 下書きの `faces` が並びでない形だと、
  登録の窓を開いた瞬間に `forEach is not a function` で止まっていた。
- **★図面が真っさらな別物になる。** 図面の保存を**そのまま** state に入れていたので、
  保存が別の形だと**縮尺・工法などの基本の設定ごと消え**、そのあと `toFixed` のところで止まった。
  → 既定の上にかぶせる（`Object.assign({}, NN_STATE0, o)`）。足りない項目は既定のまま。
  縮尺・工法・流し方向・重ねしろは、値そのものも確かめて、変なら既定に戻す。
- **★役物の一覧が出せなくなる。** 保存に null が混じっていると `name` を読んで止まっていた。

★**ここが今回いちばんの学び**：これまで直してきたのは
  **「並びであること（Array.isArray）」までで、「1件ずつが箱であること」を見ていなかった。**
  `[null, 1]` のような中身は全部すり抜けて、あとで中身を読んだところで止まる。
  → 同じ形の見張りを **材料登録・仕様登録・ライブラリ・カメラ・発注・客先登録・現場マップ**
    の7か所にも入れた（1件ずつ `r&&typeof r==='object'&&!Array.isArray(r)` で通す）。

- **検査を新しく2本**：`_check/junkop.js`（壊れた保存で**ふだんの操作**ができるか。8ページ×7通り）
  と `_check/gantt1.js`（工程表の保存が壊れていても施工中のタブが開けるか）。
  ★**「開けるか」だけの検査では足りない。** 開いたあとの操作まで動かすこと。

### 211. 測り方でつくってしまった「見かけの固まり」（2026-08-24ag）
「3D→①図面のタブ切替に4秒かかる」と出たので調べたが、**測る仕掛けが原因だった**。
長いコマを数えるために `requestAnimationFrame` の見張りを入れていたが、
これを **CPU4倍の重さの設定と一緒に使うと、見張り自体が長いコマを作る**。
見張りを外して測り直すと、どのタブ切替も **20ms前後**だった（3Dを開く前も後も同じ）。
★**測る仕掛けを入れたまま「遅い」と判断しないこと。** 必ず素の状態でも測って突き合わせる。

- あわせて確かめた（どれも問題なし）
  ・何も操作しない6秒間のCPU＝**98%は休み**（0.7秒ごとの見張りは合計2%未満）
  ・現場記録帳の画面を10周切り替え＝要素・監視・記憶とも**まったく増えない**
  ・図面20面で3D・タブ往復・高さ変更を8周＝**記憶 7.9MB・形693個で完全に横ばい**
  ・工程表の日数は**170日で頭打ち**、書類9種は**おかしな形でも全部出せる**

### 212. 打ち込んだ文字を画面と紙にそのまま入れていた（2026-08-24ah）★全ページ
「A&B工務店」のような**ふつうの社名**や、メモに書いた `<至急>` で
**カードや吹き出しの後ろが丸ごと消える**状態だった。全ページに残っていた。

- **見つけ方**：値を「絵を読み込めなかったら印を立てる札」に置き換え、
  画面に出したあとで印が立ったかを見る（`<img src=x onerror=…>`）。立てば逃がしそこね。
  ★**文字を目で追っても見つからない。** どこか1か所でも生で入れていれば印が立つので、
    この方法なら**取りこぼしが無い**。
- **直したところ**
  ・現場記録帳 … 一覧カード・物件詳細・ダッシュボード（工法別の表・円グラフの吹き出し）・
    工程表・絞り込みメニュー・屋根の内訳。**あわせて75か所**。
    共通の逃がし `nnE()` を用意し、`window.nnE` にも入れた
    （★`const` は**その `<script>` の塊の中だけ**なので、他の塊から使えない）。
  ・**物件報告書（元請に出す紙）** … 打ち込んだ文字をそのまま刷っていた。逃がしを追加。
  ・図面・積算 … 屋根の名前（辺の編集パネル・全辺へのボタン・屋根の表の入力欄）
  ・現場マップ … 現場名・住所・工法（吹き出し・一覧・地図の見出し・トレースの案内）
- **元から正しかったところ**（確認済み）：ライブラリ・用語集・客先登録・発注・カメラ・
  材料登録・仕様登録・図面の書類6種（平面図・断面詳細図・割付図・施工層構成図・
  標準納まり詳細図・御見積書）・工程表PDF・発注書PDF。
- ★**紙は元請に渡ってから気づく**ので、書類は画面より大事。検査を2本作った：
  `_check/escdoc.js`（書類9種）と `_check/esc1.js`（画面13か所）。

### 213. 調べて「問題なし」と確かめたこと（2026-08-24ah）
- **何も操作しない6秒間のCPU＝98%は休み**。0.7秒ごとの見張り（14本）は合計2%未満で、
  止める値打ちは無い。
- **タブの切り替えは20ms前後**。3Dを開く前も後も同じ。
  ★「3D→①図面に4秒かかる」と出たのは**測る仕掛けのせい**だった。
    長いコマを数える `requestAnimationFrame` の見張りを、CPU4倍の設定と一緒に使うと、
    **見張り自体が長いコマを作る**。素の状態で測り直すこと（§211）。
- **長く使っても増えない**（実測）
  ・現場記録帳：画面を10周切り替え → 要素・監視・記憶とも横ばい
  ・図面：20面で3D・タブ往復・高さ変更を8周 → 記憶7.9MB・形693個で横ばい
  ・図面：四角を100個かいて全削除、を5周 → 記憶3.8MB・監視385・履歴80で横ばい
- **書類9種は、おかしな形（点2個・面積0・8の字・巨大・極小）でも全部出せる**。
- **工程表の日数は170日で頭打ち**、`while(true)` は1つだけでその場で終わることを確認。
- **sw.js の保存一覧123件はすべて実在**。`sw.js` と `ver.txt` は意図的に入れていない
  （この2つが更新の合図なので、保存すると新しい版に入れ替わらなくなる）。
- **公開されるファイルの合計は20MB**（うち three.js 652KB・引き継ぎメモ571KB）。
  大きな画面写真はすべて `.gitignore` で公開先に入っていない。
- **画面のボタンから、存在しない処理を呼んでいるものは0件**（全11ページを機械で走査）。

### 214. 数値の欄に とんでもない数字を打つと画面が固まる（2026-08-24ai）★フリーズの本命その2
図面・積算の数値の欄をひとつずつ調べたところ、**「エンドラップ」に 999999999 と打つと
画面がまるごと固まった**（実測。20秒待っても戻らない）。

- **★原因は `splitPieces`（シートを巻長ごとに切り分ける処理）。**
  重ねしろ（endLap）が巻長より大きいと、切り出しの開始位置が
  **マイナス100万m** から始まり、0.5mずつ進むので **200万回**くり返す。
  2026-08-24aa で「割付の帯8000・シート8000枚」の上限を入れたが、
  **その上限は外側のくり返しにしか付いておらず、この内側の while には無かった。**
- **直し方（2段構え）**
  ①**重ねしろは巻長の90%を超えない**ようにする（重ねが巻長を超えるのは実物ではあり得ない）。
    くり返しにも上限（4000枚）を置く。
  ②入力を受けるところでも丸める（サイドラップ・エンドラップとも 0〜5000mm）。
- **あわせて上限を入れたもの**（打ち間違いで数量が桁違いになるのを防ぐ）
  ・立上り 30m まで／天端 10m まで／水切りアゴ 3m まで／壁の厚み 3m まで
    （実物はこの1/10以下。辺の編集・初期値の欄・保存の読み込み の3か所すべてに入れた）
  ・GL+ は 0〜1000m（世界一高いビルでも830m）。`Infinity` を入れると
    **保存できない値（null）になって図面が壊れて**いたのも、これで直る。
  ・数量の実測：直す前は 立上り **4e298㎡**（見積が意味をなさない）→ いまは 339㎡。
- ★**「上限を付けた」と書いてあっても、内側のくり返しには付いていないことがある。**
  くり返しが入れ子になっているときは、**いちばん内側にも上限を置くこと。**
- 検査 `_check/bignum.js` を新設（直す前は★NG6件・いまは全部○）。

### 215. 保存がいっぱいのときの知らせを、次にすることが分かる形にした（2026-08-24aj）
図面の「💾 保存」が失敗したとき、`保存できませんでした（容量がいっぱいです）` としか
出ておらず、**次に何をすればよいか分からない**（消し方も、なぜ溢れたのかも分からない）。

- ★**実測**：下絵（航空写真）を入れた図面は **1件で約0.5MB**。端末が持てるのは合わせて5MBほどなので、
  **10件でいっぱいになる**（保存できた件数を数えて確認）。図面だけなら60KB／件なので数十件入る。
- → 知らせを「いま◯件 保存されています。『📂 開く』から古い図面を消すか、
  ホームの『設定』からデータを書き出してください。（航空写真を下絵にした図面は1件で0.5MBほど使います）」に変えた。

### 216. 端末（iPhone）の版が古いときの見え方（2026-08-24aj・調べただけ）
新しめの機能を使っている箇所を機械で洗い出した。**壊れるものは無い**。
| 使っている機能 | 使える iOS | 使えないと |
|---|---|---|
| `content-visibility`（一覧を後回しに組み立てる・§95） | **18以上** | 100件を一度に組み立てる＝開くのが遅くなるだけ |
| `showPicker`（1タップでプルダウンを開く） | 16以上 | もう1回タップで開く（try で囲ってある） |
| `createImageBitmap`（写真を軽く読む） | 15以上 | 今までの読み方に落ちる（分岐あり） |
| `aspect-ratio`／`ResizeObserver`／`IntersectionObserver` | 13〜15以上 | 実質すべてのiPhoneで使える |
★どれも「使えなければ前のやり方に戻る」形なので、古い端末でも**動かなくなることはない**。

### 217. さらに調べて「問題なし」と確かめたこと（2026-08-24aj）
- **数値の欄に とんでもない数字（999999999／1e300／−999999）を打っても、
  他の10ページは固まらない**（画面にある数値の欄すべてに一気に打ち込んで確認）。
- **入れ子のくり返しを全ページで洗い出した**。上限が無いものは
  ・現場マップの塗りつぶし（面積の上限あり）・カメラの目盛り（間隔は固定値）
  ・DOMの付け替え（子が減るので必ず終わる）だけで、**データ次第で暴走するものは残っていない**。
- **カメラ・国交省仕様のページも、くり返し操作で記憶・監視が増えない**
  （カメラ 0.8→0.9MB／国交省 1.0MB・監視108 で横ばい）。
- **一覧の1行ごとにスクロールする箱を作っていないか**を全11ページで確認（§123の落ちる原因）。
  いちばん多い現場記録帳でも31個（うち19個は絞り込みメニュー＝固定の数）。**1行ごとのものは0**。
- **写真から起こす（射影変換）**は、四隅が一直線・同じ点・ねじれ・とんでもない座標のどれでも
  「断る（null）」に落ちる守りが入っている。
- **スマホ（393×852・CPU4倍）で一通り操作した実測**：ホーム2.2秒／記録帳5.6秒／検索0.9秒／
  詳細0.9秒／ダッシュ1.5秒／工程表3.7秒／図面4.9秒／積算0.9秒／3D 4.1秒／見積書0.03秒。
  **JSエラーは1件も出ない。**

### 218. 図面・積算：戻る・進むの誤タップと、選択中が見えない問題（2026-08-25a）★本人の指摘
「戻ると進むのボタンが小さくて誤タップしやすい。選択中のアイコンが視認しずらい」。実測して直した。

- **★誤タップの正体は「当たり判定の重なり」**。戻る・進むは 29×29px（Apple の推奨44ptの2/3）で、
  すき間3pxなのに当たり判定を左右5pxずつ広げていた（§35）＝**重なった帯では後ろのDOM（進む）が勝つ**。
  つまり**戻るの右端を押すと進むが動く**。ボタンが小さいだけでなく、押し違いが構造的に起きていた。
  → ①絵を 27→36px（ボタン実測 40×40px）②左右に margin 3px ③当たり判定の左右の広げを 5→2px。
  **掃引テスト**（3pxおきに elementFromPoint）で undo→すき間→redo の順に並び、逆転ゼロを確認。
  ★**隣り合うボタンの ::after を左右に広げるときは、すき間より狭くすること。**
    広げた量×2 ＞ すき間 だと必ず「押したのに隣が動く」になる。
- **★選択中が見えない正体は「20%の薄緑＋2pxの内側リング」**。方眼紙の上では地と同化していた。
  → **このアプリの決まり「選択中＝黄色」**（下部ナビの `_on`・一覧カードの選択と同じ）にそろえ、
  ゲームUI式の上下2段の黄色（#ffe873→#ffd23e・46/47%でスパッと）＋濃い緑の外リング2.5px。
  断面の道具バー（.sdb）の絵ボタンも同じ黄色に統一した。
- **★断面バーは「最初の道具（✎描く）に印がそもそも付いていなかった**（タップして初めて付く）。
  `setTool(tool)` を初期化の最後に1回呼ぶだけ。**開いた直後に選択が見えない画面は、これを疑うこと。**
- パソコンは触っていない（文字つきボタン・濃緑の塗りで元から見える）。よこ向き2段・たて3段・はみ出し0は維持。
- 検査は tb2（PC/ph）・sect3・sect2・tbname・partdel・icon3d の回帰＋掃引テストで確認。

### 219. 作図の操作性①〜④：長方形・⊕点・辺ドラッグ・複製・全体表示（2026-08-26a）★本人の依頼「提示した機能を対応してみて」
32項目の改善案（別途提示）のうち、優先と伝えた4つを一括で実装した。すべて `zumen_sekisan.html`。

- **①▭ 長方形ツール（`tool='box'`）**：角を2回タップするだけで長方形の屋根が1つできる。
  ・1つ目の角も2つ目の角も「方眼の交差点＋既存の角への吸着（vertexMagnet）」＝隣の屋根とぴったり繋がる。
  ・プレビューは点線の枠＋よこ・たての寸法。**作ったあとも長方形ツールのまま**（続けて何個でもかける）。
  ・幅か奥行きが0になる2点目は断る（角は生きたまま）。スマホは赤い照準（draw/holeと同じ道）。
  ・戻る／右クリック／Esc／🗑削除＝1つ目の角の取り消し。
- **②⊕ 点ツール（`tool='addpt'`）＋ 選択中の辺のドラッグ**
  ・辺をタップ＝点を足す（**辺の上から外さず**、始点からの距離だけ10cmきざみ）。
    **挿入は pts と edges を同じ番号に同じ数だけ**（nnEdgeSplitRange と同じ決まり）。
  ・頂点をタップ＝点を消す（3点未満になる削除は断る）。頂点をつまむ＝動かす（0.1m単位）。
    **つまみは rdrag（範囲選択の頂点移動）に相乗り**：`rdrag.tapDel` を持たせ、
    離したとき動いていなければ削除に化ける（`nnRdragEnd`）。マウスとタッチの後始末を1本化した。
  ・「選択」で選んだ辺は、**もう一度つかむと法線方向へドラッグで動く**（`edrag`）。
    両端の頂点を同時に動かすので、隣の辺は自動で伸び縮み（3Dの面ドラッグ§163と同じ考え方）。
    元の位置は灰色の点線で残し、動かした量（◯.◯m）を出す。
- **③⧉ 複製（`nnPolyDup`）**：辺の設定・中抜き・仕様・下地・高さごと深い写しにして、
  元の幅＋1マスだけ右に置く。入口は屋根の表の各行（.dd）と、引き出しの部位リスト。
- **④⊡ 全体表示（`nnFitView`）**：部位・描きかけ・役物・長方形の1点目まで含めた外接箱を、
  **ツールバーの下の範囲**に収める（ツールバーは方眼に浮いているので bottom を実測して避ける）。
  何も無いときは最初の表示（cellPx 34／スマホ22）に戻る。**3Dタブで押したら d3ViewIso（斜めから全体）**。
- ★**同じタップを受ける道が増えたときの決まり（今回も踏んだ）**：
  ・スマホの「押した瞬間から動く道具」は `DRAG_TOOLS` に足す（addpt と、選択中の辺の掴み直し）。
  ・照準で打つ道具は pointerdown の draw/hole の並びと `aimCommit` の両方に分岐を足す。
  ・3Dタブで押したときの案内は `NAMES`（nn-3dtools の setTool ラッパ）に1行足す。
- ★検査の教訓：**辺0に点を足すと新しい点は pts[1]**（pts[2]ではない）。テストが隣の角を
  叩いて「動かせない」と誤判定した（product は正しかった）。
- 検査は `_check/uxtool1.js`（PC16項目／スマホ14項目）。tb2 のA1行目の並びは新仕様に更新。
  回帰：tb2(PC/ph)・ang5・cross・cross2・align1・adjchk・partdel・zdir・zmenu(PC/ph)・uimg(PC/ph)・
  sect2・sect3・tbname・icon3d・split1・partschk・d3draw・custompart・aim3d・camfix・deck7・save2・qty1 全部○。
- 宿題（32項目の残り・次の優先）：スナップの見える化の拡充／下絵まわりの操作短縮／
  部位のミラー（左右反転）／複製した部位をその場でドラッグ配置。

### 220. 黒い帯・タップ音・タップ直後のラグ（2026-08-26b）★本人の指摘3件「完全に治るまで治し続けろ」
実機のスクショ（たて画面・ホーム画面から起動）で3件。全11ページに同じ直しを入れた。

- **★①「たて画面の下が黒い帯」＝§203（変わったときだけ測る）の合図に穴があった。**
  ホーム画面から起動したiPhoneは innerHeight（と visualViewport.height）が
  **実際より小さい値のまま変わらない**（§5-2）。本当の下端（目印 #nnBtmProbe）は
  起動の少し後に伸びるが **resize は来ない**。§203の合図は申告値しか見ていないので
  「変わっていない」ことになり、**一度も測り直さない＝--nnvh が小さいまま＝黒い帯**。
  §5-2 の直しは「0.8秒ごとに無条件で測る」ことに支えられていて、§203 がその支えを外していた。
  → **合図に目印（standaloneのときだけ probeBottom）を追加**＋起動直後だけ 0.3/1.2/3秒に測り直す。
  ★**教訓：「変わったときだけ測る」最適化を入れるときは、“測らないと変化に気づけない値”が
    合図に入っているかを必ず確かめること。** 申告値が嘘をつく前提の直し（§5-2）は特に危ない。
  ★現場マップだけ body{height:var(--nnvh)} ではなく **fitApp() が --nnvh を読む**作り。
    検査の測り先はページの実態に合わせる（body を見ても直っていないように見える）。
- **★②「タップ音がちゃんと作動しないことがある」＝2つの穴。**
  1. 止まった装置を「起きてから鳴らす」作りだったので、起きるのが遅い回は画面が変わって鳴らずじまい。
     → **先に「鳴る予約」を入れる**（止まった装置への予約は、動き出した瞬間に鳴る）。
  2. 電話・他アプリの割り込みで **resume しても永久に起きない装置**になることがある（iOS）。
     → 1秒たっても動かなければ、**次のタップ（利用者の操作の中）で装置を close して作り直す**
     （作り直しは操作の中なら必ず動く＝§89 と同じ決まり）。
- **★③「ボタンを押してからのラグ」の一因＝なぞりの光（§141）がタップでも動いていた。**
  押した瞬間に画面いっぱいのキャンバス（2倍＝4倍の画素）を作って毎コマ描き直し、
  離して0.42秒後まで続けていた。**最初のタップではキャンバスの割り当てで大きく止まる**
  （実測・CPU4倍：指を置いた600msの間 修正前=1866msのコマ落ち1回／修正後=16.7ms間隔で38コマ。
  この環境はソフト描画なので数字は誇張だが、方向は実機も同じ）。
  → **タップ（動かさない指）では何も描かない**（7px以上なぞったときだけ）・**等倍で描く**・
  **shadowBlur（1本ごとのぼかし計算）をやめて「太く薄い線＋細く明るい線」の2度がき**に。
  なぞりの光の見え方・追従は今までどおり。
- **残るラグの本命＝初回のページ読み込み**（1ページ130〜740KBを4Gで取る）。
  直すには NN_USE_SW=true（端末に保存して即表示）が要るが、**切り替えは本人の了解を
  取ってからの決まり**（§207）。了解が出たら版名4点セットと一緒に切り替える。
- 検査を3本新設：`_check/fillband.js`（目印だけが後から伸びる状況を作り、--nnvh が追従するか。
  **修正前のファイルは700のまま＝検査が本物なのを確認済み**）／`_check/sndfix.js`（ニセの音の装置で
  ①起きる ②予約が先に入る・二重に鳴らない ③死んだ装置の作り直し）／`_check/trail1.js`
  （タップでは描かない・なぞると描く・等倍・離すと止まる・ナビの移動が壊れていない）。
  回帰：uxtool1(PC/ph)・tb2 ph・aim3d・sect3・zmenu ph・fast1・allpages2・noxscroll すべて○。

### 221. 黒い帯の本命・天端の帯の角・よこ画面3Dの下切れ（2026-08-27a）★本人の指摘3件
実機スクショ4枚。「①ホームの下部にまだ謎のクリアランス ②③図面の4隅の外側が飛び出している
③スマホよこ画面で3Dの下だけ見えない。何度も指摘している」。

**★①黒い帯の本命＝JSから見える高さが「全部」嘘をつく端末がある**
- 版aと版b（§220の修正後）のスクショを画素で比べたら**帯が1画素も変わっていなかった**
  ＝§220 の「目印（fixedの下端）を合図に足す」でも直らない端末がある。
  申告(innerHeight)・visualViewport・**目印までぜんぶ**実際より約60pt小さい値のまま。
- ★**ホーム画面起動＝全画面なので、screen（画面そのもの）の高さだけが本当の高さ。**
  pageH() に候補として追加（全11ページ・`scratchpad`の fix27a.py 方式）。
  ・iPhoneの screen は向きに関係なく「たて向きの値」＝よこ向きは短い方を使う
  ・**縮小ページ（width=980）の保護**：CSSpxの h（約2100）に対し screen(852) は
    `sc > h+1` に入らないので誤作動しない（fillband③で検証）
- **manifest.json の background_color を #3a3d3c → #153f25（ナビの緑）に**。
  文書の外に塗られる色・起動画面の色がこれ。帯の色の正体は「文書の背景色が
  文書の終わりの先まで塗られたもの」（ホーム=濃灰・マップ=薄緑と、ページごとに色が違うのが証拠）。
  ★**iOSはmanifestをホーム画面登録の時点で焼き込む**＝色を変えたらアイコンを入れ直してもらう。
- **ホームの版表示タップ（計測表）に高さの実測を追加**：申告/vv/目印/screen/--nnvh/起動形態。
  もし帯が残ったら、このスクショ1枚で「どの値が嘘か」が確定できる。
- ★教訓：**「直したのに変わってない」は、まず修正前後のスクショを画素で比べること。**
  1画素も変わっていなければ、修正が効く前提（合図・値の出どころ）ごと疑う。

**★②天端の帯（2Dの図面）の四隅の飛び出し＝両端をWぶん伸ばす作りだった**
- 帯は辺ごとに描くので、角をつなぐために両端を天端幅Wぶん伸ばしていた。
  そのため**角の四角が2枚の帯で二重塗り（濃い）＋図形の外へブロック状に飛び出し**ていた。
- → **外側の線どうしの交点で突き合わせる（留め継ぎ）**。壁の3D（§35-3）と同じ考え方。
  隣の辺に帯が無い・平行・交点が異常に遠い（スパイク）ときは角で直角に切る。
- ★帯は屋根の**内側**に描かれる（`ringNormal` は内向きの法線を返す）。
  検証（`_check/tenba1.js`）は**キャンバスの不透明度（α）を画素で読む**：
  帯1枚＝約153・二重塗り＝約173・外への飛び出し＝約51。修正前ファイルで★NGが出るのを確認済み。

**★③よこ画面3Dの下切れ＝犯人は2人いた**
1. **描画ループの見張りが「幅しか」見ていなかった**（`el.width!==Math.floor(w*pr)` のみ）。
   よこ向きは「幅そのまま・高さだけ後から縮む」（下部ナビの出入り・高さの測り直し）が起きるので、
   キャンバスが古い高さのまま残り、**絵の下が画面の外に切れていた**。高さも見張るように。
   ★**大きさの見張りを書くときは、幅と高さの両方を必ず見ること。**
2. **全体表示の実測（nnD3FitR）が「画面がまだ正しい大きさになる前」に走ると、
   そのまま二度と直らなかった**（§152 が編集での合わせ直しを禁じているため）。
   → **nnD3FitHeal**：合わせたときの画面の大きさとカメラ（`T.fitW/fitH/fitCam`）を控え、
   **画面の大きさが2%以上変わって、かつカメラを誰も触っていないときだけ**合わせ直す。
   使う人がカメラを動かしたあとは絶対に動かさない（§152 はそのまま）。
   向きの回転（orientationchange）での合わせ直しも今までどおり（§149）。
- ★検証の教訓：**Playwrightの setViewportSize で向きが変わると orientationchange が本当に発火する**。
  「カメラを触ったあとは動かさない」の検査を回転で行うと、§149 の正当な合わせ直しを
  不具合と誤判定する（実際にした）。**向きが同じまま高さだけ変える**こと（`_check/d3heal.js`）。
- 検証：fillband（3場面に増補）・tenba1・d3heal（各・修正前ファイルで★NG確認済み）。
  回帰：d3fit・camfix・d3view・uxtool1(PC/ph)・ang5・align1・sect3・aim3d・zdir・split1・
  warichk・sec2chk・qty1・save2・allpages2・noxscroll・fast1 すべて○。

### 222. 元請別ダッシュボードと「結果」タグ（2026-08-27b）★営業の切り口から逆算して作った
本人のロープレの振り返りから決まった売り文句「**社長の脳内を整理した状態で打ち合わせに臨める**」を
支えるための機能。①元請別の成績 ②手戻りした現場の履歴、の2つを実装した。

- **「元請別 成績」パネル**（ダッシュボードの最後・工法別の下）
  ・列＝元請／比率バー／実績数／受注額／利益額／平均利益率／**予実差**／**手戻り**
  ・**予実差 ＝ 完成した現場の「実績の粗利率」−「見積のときの粗利率」（pt）**。
    マイナス＝**見込みより残らなかった元請**。赤で出す。完成した現場が無ければ「—」。
  ・**手戻り ＝ 手戻り／手直し／クレームのタグが付いた現場の数**（「増額あり」は数えない）。
  ・並び替え・比率バーの対象・列の編集は**工法別パネルと同じ仕組みに相乗り**（§60）。
    `COLS.moto` ／ `dashSort.moto` ／ `MOTO_GET` ／ `motoSortReset` を足しただけ。
- **不具合タグに「結果（次の現場に活かす）」グループを追加**（`res`・紫）
  ・**手戻り／手直し／クレーム／増額あり** の4つ。DEFSは25→**29種**になった。
  ・★`DEFS` を増やしたら `_check/kdef.js` の「◯グループ・◯行」も直すこと（§71と同じ。今回も引っかかった）。

**★★この日いちばんの発見：ダッシュボードが「読み込みの速さ次第で」間違った数字を出していた**
- 手戻りの数が **0件のまま**になる。原因は
  **ブラウザが長いページを読んでいる途中でも1回画面を描く**こと。
  その隙にダッシュボードが組み立てられると、**ページの終わりにある不具合タグのスクリプトが
  まだ動いていない**ので、タグが1つも無い状態で集計されていた。
  `requestAnimationFrame` で起動を遅らせてあっても、**rAF はページを読み終わる前に発火し得る**。
- ★**外した直し方2つ（記録として残す）**
  1. タグを入れたあとに `renderDash()` をもう1回呼ぶ → **+820ms**（CPU4倍）。丸ごと2回組み立てるので当然。
  2. 起動を `DOMContentLoaded` まで待つ → **2.3秒 → 6.3秒**。
     いまの作りは「読み込みの途中で先に画面を出す」ことで速く見せているので、待たせると台無しになる。
- **正しい直し方＝元データを、先に読まれる場所へ移す。**
  タグの見本（`NN_DEF_SAMPLE`）と保存の読み出し（`nnDefStore`）を**本文側の起動直前**に移し、
  そこで `p.defects` を入れておく。下の `nn-defect-js` はそれを使う。
  実測 2347ms → 2542ms（新しいパネル1枚ぶんの+195msだけ）。
- ★**教訓：ページの終わりのスクリプトで props に足した項目を、本文側の集計から使わないこと。**
  使いたいなら**元データを本文側へ移す**。「後から1回呼び直す」は必ず遅くなる。
  同じ形の危うさは他にもある（faces_sample.js は先に読んでいるので無事）。
- 検証は `_check/moto1.js`（PC/スマホ 各15項目。**画面の数字を、検査側で別に足した値と全社つき合わせる**
  ＝集計を作り直しても壊れたら気づける）。回帰24本すべて★NGなし。

### 223. 取り合い（仕様の違う屋根どうしの境界）を3D・図面・積算に出した（2026-08-27c）★売り文句③
本人のロープレの振り返り「**塗膜防水と露出防水の取り合い／露出アスと塗膜アスの取り合いを
3Dで見せながら打ち合わせして、1回で通す**」を支えるための機能。

- **自動で見つける**（`nnToriaiList`）。条件は3つ：
  ①隣り合う辺が半分以上重なっている ②**平場の高さ（GL+）が同じ** ③**平場の防水仕様が違う**
  ★高さが違う境界は、パラペット／壁当りで縁が切れる＝取り合いではない（`nnSyncSharedEdges` が受け持つ）。
- **納まりは3つから選ぶ**（辺の属性 `e.tor`／両側の辺に同じ値を書く）
  | 種類 | 3Dの姿 | 単価（デモ） |
  |---|---|---|
  | 見切り＋シール | アルミ見切り材30×20＋上にシーリング | 3,800円/m |
  | 重ね | 片方の防水層を150mm乗り上げ＋端部シール（**⇅で乗せる側を入替**） | 2,600円/m |
  | 立上りで縁切り | 高さ100mm・厚み60mmの立上り＋天端キャップ | 5,200円/m |
- **図面(2D)**＝紫の破線＋札「取合 X-2／AS-T1／見切り＋シール」。
  ★札は法線方向に**24px**出す（13pxだと辺の寸法の札と重なる）。
- **積算**＝「取り合い処理（◯◯）◯m」の行が自動で増える。
- **一覧の小窓**（ツールバー「⇹ 取り合い」）＝取り合い箇所を並べ、その場で納まりを選ぶ。
  行をタップすると図面と3Dでオレンジに光る。
  ★置き場所は**左**（右は「屋根の表」と3Dのボタン列がある）。
    高さは**ツールバーを実測して、その下**に置く（重なると押せなくなる・§150と同じ）。
- 作りは §119／§153 と同じ「**build3D と draw は書き換えず、上から包んで飾りを足す**」方式
  （`nn-toriai` / `nn-toriai-js`）。3Dの飾りは `T.scene` 直下の `nnToriai` グループ。

**★ハマったところ**
1. **保存して開き直すと納まりが既定に戻る。** `loadState` の `normE` は
   **知っている項目しか写さない**作りなので、新しい項目 `tor` が毎回捨てられていた。
   ★**辺に項目を足したら `normE` にも1行足すこと。** 足さないと保存されない。
2. 検査で使う仕様コードは **AS-T1／AS-J3／X-2／S-M2／AS-N** の5つだけ。
   存在しないコード（X-1 など）を入れると既定に落ちるので、
   「2つの屋根の仕様を変えたのに取り合いが0件」になる（最初これで悩んだ）。
- 検証は `_check/toriai.js`（PC/スマホ 各19項目：同じ仕様は取り合いにしない・高さ違いも除く・
  長さが図面と合う・積算の金額＝数量×単価・3種とも3Dに出て位置が正しい・乗せる側の入替・
  小窓が屋根の表とツールバーに重ならない・**開き直しても残る**）。
  回帰20本すべて★NGなし（`degen` の1件は**この変更の前から同じ**＝1e9の座標で5.3秒かかる既知の宿題）。
- ★**まだ届いていないところ**：取り合い部の**断面詳細図PDF**（§143 の `PARTS` に形を足せば出せる）。
  架台まわりの納まりも未着（役物の側で持つほうが筋がよい）。

### 224. アルミ笠木（パラペットの天端に自動で乗る）（2026-08-27d）★本人の断面図より
本人から田島ルーフィング「ライナーコービングS」の断面図が届き、それに合わせて作った。
ツールバーの **「⌂ アルミ笠木」** から、屋根ごとに「笠木あり／なし」を切り替えるだけ。

- **①天端幅から品番を自動で選ぶ**（`nnCopingPick(mm)`／寸法表 `NN_COPING_TBL` は13品番）。
  例：天端250mm →「ライナーコービングS300」（幅300・板厚2.0・L=4.0m）。
  ★**適用範囲は1mmも重ならないようにすること**（80〜105／106〜120／…／416〜460）。
    重なっていると、境目の値（240 等）で**どちらの品番になるか運任せ**になる。最初これで検査が落ちた。
  表の範囲外（80mm未満・460mm超）は**選ばず、画面ではっきり知らせて積算にも計上しない**。
- **②3Dは断面どおり**（`nnKasagiSync`）。パラペットの天端（`lv+hh`）に乗り、
  **外壁側が72mm・屋上側が60mm＝屋上へ向かって下がる**（本人の確認「笠木は逆です。外壁側が高く、
  内側が低いようにして」）。雨は屋上のドレンへ流れる。
  ・角は**留め継ぎ**（外側どうし・内側どうしのずらした線の交点。壁と同じ考え方・§35-3）。
  ・笠木の幅は品番の幅なので、天端250mmに幅300の笠木なら**外へ25mm・内へ25mm出る**（実測で確認）。
- **③積算**に「アルミ笠木 ライナーコービングS300（L=4.0m×15本）60m」と
  「同 コーナー（495×495）4個」が自動で増える（`nnKasagiRows`）。
  **本数＝長さ÷1本の長さ を切り上げ**。コーナー＝笠木のある辺どうしが出会う角の数。
- **④図面(2D)** は笠木のある辺に金属色の線を重ねる（範囲外の辺は赤）。
- **⑤天端幅が辺ごとに違うと品番も分かれる**（小窓に品番ごとの長さと本数を並べ、積算も分ける）。
  1つ目の辺の品番だけ出すと、実際と違う品番を発注してしまう。
- ★**画面の積算（`recalc`）に足したら、御見積書（`nnEstimateData`）にも足すこと。**
  片方だけだと「画面の金額」と「元請に出す紙の金額」が食い違う。
  §223 の取り合いも紙に出ていなかったので、ここで一緒に足した。
- 作りは §119／§223 と同じ「**build3D と draw は書き換えず、上から包んで飾りを足す**」方式。
  飾りは `T.scene` 直下の `nnKasagi` グループ（`T.group` は組み直しのたびに空になる）。

★**踏んだところ**
1. **`normE` に `kasagi` を足し忘れると保存されない**（§223 とまったく同じ罠。
   `normE` は知っている項目しか写さないので、開き直すと消える）。**辺に項目を足したら必ずここも直す。**
2. **小窓の場所が「取り合い」と同じ**なので、開くときはお互いを閉じる。
   笠木側だけ書いても足りない（**`nnTorPanel` も包む**）。
3. **★ボタンが1つ増えて、スマホのツールバーが3段→4段になった**（方眼紙が32px狭くなる）。
   → **絵がまだ無い「記号＋文字」のボタンは記号だけにする**
   （▭長方形→▭／⊡全体→⊡／📂開く→📂／🖼下絵→🖼／⇹取り合い→⇹／⌂アルミ笠木→⌂／
   ⊕点→⊕／◆鳩小屋→◆）。実測 138px → **106px（3段）＝ボタンを足す前と同じ**。
   ★**短くする前の名前は `data-nm` に控え、長押しの名前（§134⑧）はそこを先に見る**ようにした。
     控えないと、絵が届いたときに `.tbtx` が記号だけになり、**長押ししても「▭」としか出なくなる**。
   ★**ボタンを増やしたら必ず `_check/tb2.js`（PCとスマホ）を流すこと**（§118cから何度も踏んでいる）。
4. 部材スタンプに元から**「◆ 笠木」**があるので、こちらは**「⌂ アルミ笠木」**と名乗る
   （同じ名前のボタンが2つあると必ず混乱する）。
- 検証は `_check/kasagi.js`（PC／スマホ 各41項目：品番の選定と境目・範囲外・4辺に付く・
  外周60mと合う・**外壁側372mm／屋上側360mm**・外へ25mm・積算の本数とコーナー数・
  開き直しても残る・外すと消える・小窓が屋根の表とツールバーに重ならない・
  天端幅が混ざると品番が分かれる・御見積書にも出る）。
  `_check/tbname.js` にも「記号だけに短くしたボタンの長押しで元の名前が出る」を足した。
  回帰53本すべて★NGなし。

### 225. コンクリートの質感を「本物の写真」にした（2026-08-27e）★本人が送ってくれたテクスチャ
本人が ambientCG（無料・商用可のCC0）から Concrete034 を取ってきて送ってくれた。
これまで躯体・外壁は「その場で粒を描く」手描きの質感（§80）だったが、
写真にすると型枠の跡・打ち継ぎ・骨材が出て、一気に本物らしくなる。

- **置き場所は `textures/concrete_color.jpg`（色・85KB）と `concrete_normal.jpg`（凸凹・136KB）**。
  差し替えるだけで質感が変わる。**無くても壊れない**（今までの手描きの質感に落ちる）。
- **読むのは3Dタブを開いたときだけ**（`nnPhotoTexLoad`）。ページを開く速さは今までどおり。
  読めなかったとき（圏外・置いていない）は `nnPhotoTexState()===3` で、今までの質感のまま。
- 作りは §119／§224 と同じ「**build3D と nnMat は書き換えず、上から包む**」方式（`nn-phototex-js`）。
  すでに作られた材質は `_nnMatCache` をたどって差し替え、あとから作られるものは `nnMat` の包みで当てる。
- **凸凹は法線マップ（normalMap）に任せ、今までの bumpMap は外す**。
  両方あると凹凸が二重になって、のっぺりした見え方になる。

★**踏んだところ・覚えておくこと**
1. **もらったファイルの縦横比を必ず見ること。** 今回は 1024×512（2:1）だった。
   質感は「1タイル＝実物の何m×何m」に貼るので、**写真の縦横比と貼る四角の縦横比を
   そろえないと、粒や気泡が縦につぶれて見える**。いまは **1タイル＝よこ2m×たて1m**。
   ★ここは `texture.repeat` で調整する。幾何のUV（`NN_TEX_PER_M`）は
     砂付ルーフィングと共用なので触らないこと（触ると防水面の粒まで変わる）。
2. **色は sRGB・凸凹は sRGBでない**（`colorSpace` を分ける）。同じにすると凹凸が浅く出る。
3. **法線マップは GL と DX の2種類がある。ブラウザ（three.js）は GL のほう。**
   DX は緑の向きが逆なので、貼ると光の当たり方が上下逆になる。
4. **つなぎ目（タイルとして繰り返せるか）は数字で確かめられる**：
   「左端と右端の画素の差」が「となりどうしの画素の差」と同じくらいなら継ぎ目が出ない。
   今回は 3.7 対 3.4（色）・6.5 対 5.2（凸凹）で合格。
5. **本人から届いた3枚のうち2枚はまったく同じファイル**（1画素も違わなかった）。
   GL と DX のつもりで送ってくれたが、同じものが2回入っていた。
   **もらったファイルは、まず中身を数字で見比べること。**
   Roughness（つやの具合）は届かなかったが、**無くても実用上は困らない**
   （`nnMat` の roughness 0.95 が効いているため）。あれば汚れ具合の差が出てさらに良い。
- 検証は `_check/tex1.js`（14項目：3Dタブまで読まない・読んだら貼られる・
  1タイル2m×1m・写真と貼る四角の縦横比が一致・sRGBの扱い・
  読めないときは今までの質感のまま・写真ありなしで絵が変わる）。
- ★**次にやるなら**：砂付ルーフィング・塩ビ・ウレタンの面。ここは
  **本人の現場写真**（真上から・影の入っていない1枚）がいちばん効く。
  ファイルを増やすので、同じく「3Dタブを開いたときだけ読む」を守ること。

**★2026-08-27f：本人が zip をそのまま送ってくれたので、元の画質＋つやの具合を足した**
- **元のファイルは 1024×512（2:1）だった**＝§225の「1タイル2m×1m」の判断はそのままでよかった。
  ★**もらった素材は zip のまま受け取るのがいちばん確実。**
    チャットで1枚ずつ送ると、送信アプリが**もう一度圧縮する**（届いた凸凹は元と平均2.96の差があった）。
- **つやの具合（Roughness）を追加**（`concrete_rough.jpg`）。
  ★**`roughnessMap` は `material.roughness` に掛け算される**ので、
    当てるときは **`roughness=1.0` に戻す**こと。0.95のままだと写真の意図より一段つやが出る。
  この写真の中身は 0.48〜0.75（平均0.52）。
- **`?v=NN_VER` を URL に付けた**（§66）。同じ名前で中身を差し替えるので、
  付けないとブラウザが古い絵を使い続ける。**版名4点セットを上げれば入れ替わる。**
  ★検査で通信を止めるときは、glob の `*.jpg` では `?v=…` に当たらない。**正規表現で止めること。**
- 大きさ：色98KB／凸凹187KB／つや59KB＝**合計345KB**（3Dタブを開いたときだけ）。
- 出どころと差し替え方は **`textures/README.md`** に書いた（ambientCG・CC0＝商用可・表示不要）。
  ★**zip の残り（.blend／.mtlx／Displacement／NormalDX／見本のpng）はリポジトリに入れていない。**
    ブラウザでは使わないうえ、公開リポジトリが重くなるだけ。

### 226. パラペットの高さを辺ごとに変えると、角が「斜めに切られて」見える（2026-08-27g）★本人の指摘
「長方形の建物を作って笠木も入れて、一辺の天端の高さをあげたら、なんで斜めにカットされてるの？
断面が斜めカットはおかしいよ？」

- **★原因は「留め継ぎ（45度）」そのもの。**
  隣り合う壁の角は `joint()` で留め継ぎにしてある（§35-3）。留め継ぎの合わせ目は
  **45度の垂直な面**で、高さがそろっているうちは両方の壁に挟まれて**見えない**。
  ところが片方の辺だけ高くすると、**高さの差のぶんだけ45度の面がむき出しになる**。
  これが「斜めにカットされた断面」の正体。
- **直し方＝高さが違う角は留め継ぎにしない**（`jointH`）。実物どおり
  **高いほうが角を通しで作り、低いほうがその内側に突き当たる**（突き付け）。
  ・高いほう … 相手の壁の厚みぶん、角の先まで伸ばす
  ・低いほう … 相手の内側の面で止める
  高さが同じ角は**今までどおり留め継ぎ**（見えないので直す必要がない）。
  同じ直しを **アルミ笠木（`endPt`）** にも入れた（笠木も45度に切れていた）。
- **数量・見積は1円も変わらない**（`quantities` は多角形から計算しており、3Dの作りとは別）。

★**この不具合を「無い」と誤診しかけた（検査の作り方の教訓）**
最初、斜めの面を「**上下の傾き（法線のY成分）**」で探したが、**0件**と出た。
**留め継ぎの45度の面は垂直なので、法線のYは0**。上下の傾きだけ見ても永久に見つからない。
→ **壁の向き（x軸・z軸）のどちらにも平行でない「縦の面」**として探すのが正解。
★**「斜め」には2種類ある**：①上下に傾いた面（法線Yで分かる）②平面で斜めに切った面（法線Yは0）。
  探すときは必ず両方を見ること。
★あわせて「低いほうの天端より上に出ているか」で絞ること。そうしないと、
  高さがそろった正常な角の留め継ぎまで拾ってしまう。

- 検証は `_check/corner1.js`（長方形・別の辺・逆回り・L字の4通りで
  「むき出しの斜めカットが0件」＋高さがそろった角はすき間なし＋数量が変わらない）。
  **`node _check/corner1.js before` で直す前と比べられる**（直す前は4通りとも★NG）。
- ★**残っている宿題**：**壁当り（kabe）と隣り合う角**はまだ留め継ぎのまま。
  壁当りの高さは「立上り＋外壁（既定2.6m）」で、`ed.hh`（立上りだけ）とは別に持っているため、
  同じ直しをそのまま当てられない。指摘が来たら、実効の高さを `eds` に持たせてから直すこと。

### 227. コンクリートの写真が「テクスチャと違う」（2026-08-27g）★本人の指摘
「なんかコンクリートの感じ、テクスチャと違くない？？」

- **★原因は「色に掛け算される」こと。** three.js は `material.color` に `map`（写真）を掛ける。
  パラペットの色は 0xb9bcb4（0.72くらい）、写真の平均も0.72なので、
  **0.72×0.72＝0.52 まで沈み、濃淡の幅も半分**になる。
  そのうえ明るさの補正（ACES・露出0.82）が乗るので、**写真よりのっぺりして見える**。
- **直し方＝写真を貼るときだけ、色みは残したまま「いちばん明るい成分が1.0」になるようにそろえる。**
  （白1色にすると、躯体・パラペット・外壁のわずかな色の違いが消えるため）
  1つの材質につき1回だけ（`m.userData._nnPhotoCol`）。
- ★**写真の質感を貼るときは、材質の色を必ず見直すこと。**
  手描きの質感（§80）は「ほぼ白（1.0中心）」で作ってあったので問題が出なかったが、
  写真は実物の明るさ（0.72）なので、色を1.0側にそろえないと必ず沈む。

### 228. 下部の帯（記録帳・マップ・仕様材料）と、角の飛び出し（2026-08-27h）★本人の指摘2件

**①「下部の謎の空間」が縮小ページと fixedナビのページに残っていた（§221の直しの穴2つ）**
§221 で入れた「screen の高さで直す」候補は、実は**2種類のページで効いていなかった**。
実機スクショの実測：帯は 176実画素＝**約59pt**（＝§221 と同じ「嘘の申告」のぶん）。

1. **縮小ページ（width=980：記録帳・仕様登録・材料登録・国交省仕様）**
   screen の高さは pt（852）で、縮小ページの中は CSSpx（約2100）。**単位が違うまま比べていた**ので、
   「保護」の条件（`sc > h+1`）に絶対に入らず、候補が一度も採用されていなかった。
   → **「画面の1pt＝CSSの何px」（innerWidth ÷ 画面の幅）を掛けて、CSSpx に直してから比べる**。
   等倍ページでは倍率1＝今までと同じなので、他のページの動きは変わらない。
2. **ナビが position:fixed のページ（国交省仕様・現場マップ）**
   fixed は「**申告された**画面の下端」に貼り付く。申告が嘘だと**CSSでは直せない**
   （--nnvh をいくら正しくしても、fixed の置き場所は変わらない）。
   → fill() で本当の高さ（pb）と申告の差を測り、**差のぶんだけ `style.bottom` を負にして
   下へ貼り直す**。▲（#navShowTab）も同じ差だけ負の margin で下げる。
   ふつうの端末では差が0なので、何も変わらない。
- ★**「同じ直しを入れた」つもりでも、単位（pt/CSSpx）と置き場所の決まり（fixed）が違う
  ページでは効いていないことがある。** 直したら、ページの種類ごとに1枚ずつ確かめること。
- 検証は `_check/fillband.js`（20項目に増補。④縮小ページ＋嘘の申告で 2124px になる・
  ナビが852ptに届く／⑤fixedナビが852ptに貼り直される）。

**②高さの違う角の「外壁側への飛び出し」（§226の直しの穴）**
§226 の突き付け（butt joint）は「高いほうを相手の厚みぶん伸ばす」を**全部の角**でやっていた。
- **★出隅（凸の角）では、2枚の壁の帯はもともと角で重なっている。** 伸ばす必要が無く、
  伸ばすと**建物の外の空中に 250mm 突き出る**（実機写真の赤い破片の正体。実測 0.276m）。
- 正しい決まり：**出隅＝高いほうは角でまっすぐ切る・低いほうだけ相手の厚みで止める／
  入隅＝高いほうだけ相手の厚みぶん通してすき間をふさぐ・低いほうは切らない**。
  どちらの角も、調整するのは**2辺のうち片方だけ**。
- 出隅か入隅かは §77 と同じ判定（外積の符号と回り方＝ringArea）。壁（jointH）と
  アルミ笠木（endPt）の両方に同じ分岐を入れた。
- ★検証の教訓：§226 の「斜めカット検出」は**軸に平行な面は素通し**なので、この飛び出しを
  見つけられなかった。→ corner1.js に**「輪郭の外へ飛び出していない」**（全頂点を多角形と
  突き合わせ・笠木の外出25mmだけ許す）を追加。直す前のファイルで★NG4件＝検査が本物。

### 229. 3Dのピンチ（二本指ズーム）が「いじっていると崩れる」を根治（2026-08-28a）★本人の指摘
「二本指を広げるとズーム・狭めると反ズーム。最初できているはずなのに、3Dいじってるとおかしくなる。
操作が途中で崩れないように徹底して」。**原因は3つ重なっていた。**

1. **★面を選ぶと、二本指ズームが永久にできなくなっていた（主犯）。**
   「選択」ツールで屋根や壁の面に触れると面ドラッグ（nnFaceDrag）が始まるが、
   **2本目の指が来たとき st を作り直すだけ**で、ピンチに譲らなかった。
   最初（描画ツールのまま）はピンチが効き、編集を始める（＝選択を使う）と崩れる
   ＝「最初できているのに、いじってるとおかしくなる」の正体。
   → **2本目の指が来たら面ドラッグを取り消して（値も戻して）素通しする**＝2本ともカメラに
   届いてふつうのピンチになる（§28・§156 の照準と同じ決まり）。
   面ドラッグ自体にも **指の番号（pid）** を持たせ、始めた指以外の move/up には反応しない。
2. **★指の記帳（tp）が漏れる。** up が届かなかった指（電話の割り込み・システムジェスチャ等）が
   記帳に残ると、**以後「1本指なのにピンチ扱い」になり、なぞるだけでズームが動き続ける**
   （検証で再現：ずれ34%・二度と直らない）。
   → 記帳を **capture に分離**（機能側が stopPropagation してもかならず記帳される）＋
   **pointercancel / lostpointercapture / blur / visibilitychange** でも消す＋
   **次に指を置いたとき6秒より古い記帳は掃除**する。
3. **★3本目の指（手のひらの触れ）や指の組替えでズームが飛ぶ。**
   基準（tg）が「2本になった瞬間」しか取り直されず、3本→2本の組替えで別の指の組と
   古い基準を比べていた。
   → **指の組が変わるたびに基準を取り直し、最初の move で「いまの実寸」から測り直す（fresh）**。
   基準の2本以外の指はズーム計算に入れない。
- あわせて：**面ドラッグ中はカメラを動かさない**（同じ要素どうしの stopPropagation は
  先に登録された処理を止められないので、`nnFaceBusy()` の印で見る）。
- ★**教訓：同じタップ・同じ指を複数の受け口で扱うときは、
  「指の番号」「2本目が来たらどちらに譲るか」「記帳の後始末」を最初に決めること。**
  1つでも欠けると「最初は動くが、使い込むと崩れる」形になる（§121 の受け口統一と同じ系統）。
- 検証は `_check/pinch2.js`（12項目：面を選んだままのピンチ・平場が動かない・3本目の指・
  組替え・ゴーストの掃除・面ドラッグの生存とカメラ不動。**`before` で直す前と比較＝
  直す前はゴーストで★NG2件**）。従来の pinch.js（phi/theta 不変）もそのまま○。

### 230. 外壁面のハイライトが途中で終わる／現況（既存の仕上がり）を選べるようにした（2026-08-28b）★本人の指摘と質問

**①「赤で示されてる面が、途中で終わってる」（外壁側面を選んだとき）**
- ★原因＝**当たり判定と、赤く塗る範囲がずれていた**。
  ・押せる範囲（`f:'out'` の箱）… 躯体の壁＋立上り＝**笠木の下まで**（§163でそうした）
  ・赤く塗る範囲 … **躯体の壁だけ**（§2026-08-23m）
  そのため笠木のすぐ下に「押せるのに赤くならない帯」が残っていた（実測：立上り400mmぶん）。
- 実物の外壁は屋根のところで切れておらず、パラペットの外側の面はその続きなので、
  **赤も地面から笠木の下までひとつながり**にした（`hlH = 壁の高さ + 立上り`）。
- ★§2026-08-23m の「パラペットを含めない」は、当時ハイライトが**箱**で、内側・天端まで
  染まっていたことへの手当て。**§2026-08-23p で「外側の面だけ（板1枚）」にした時点で
  役目を終えていた**のに、高さの制限だけが残って今回の不具合になった。
  ★**対策を重ねたときは、前の対策がまだ要るかを必ず見直すこと。**
- 検証は `_check/facehl.js`（赤い面の上端が笠木の下まで届く・押せる範囲と一致する）。

**②現況（仕上がりの見た目）を屋根ごとに選べるようにした**
本人の質問「新築施工前の躯体コンクリートや、改修前の劣化した露出アスファルト防水や、
保護コンクリート仕上げにすることもできる？」→ できる。**屋根の表に「現況」の列**を足した。

| 現況 | 3Dの見え方 |
|---|---|
| 新規防水（既定） | いままでどおり、仕様どおりの仕上がり |
| 躯体コンクリート（新築・防水前） | 防水層をコンクリートに。**継目（はみ出しアス）は出さない** |
| 劣化した露出アス（改修前） | 色あせ・汚れのまだら。**継目は残る**（既存の重ね） |
| 保護コンクリート仕上げ | コンクリート＋**3mごとの伸縮目地**。継目は隠れる |

- 作り＝`nn-genkyo-js`（末尾）。**build3D 本体は書き換えない**（§119の型）：
  ①部位ごとのヒント `window._nnGkHint` を build3D が立てる（**1行だけ**足した。§160の `_nnKzHint` と同じ手）
  ②`nnMat` をいちばん外側で包み、防水層（sheet/vinyl/coat）の材質だけ差し替える
  ③伸縮目地は飾り（`T.scene` 直下の `nnGenkyo` グループ）として後から載せる
- ★**数量・見積は1円も変わらない**（見た目だけの切り替え）。検査で金額の一致まで見ている。
- ★質感は**その場で描く**（画像ファイルを増やさない・§80）。
  ・劣化アスのまだらは **端に近いものを反対側にも描く（巻き付け）** ＝タイルの継ぎ目が出ない（§225）
  ・**1タイル＝実寸2m**（`repeat = 1/(NN_TEX_PER_M×2)`）。既定の0.25mのままだと
    細かい格子に見えて「ピンクのタイル」になった（最初そうなった）
- ★伸縮目地は「溝」なので、**`nnMat('concrete')` を使わないこと**。
  写真の質感（§225）が乗って明るくなり、溝に見えない。濃い色の板を自前で作る。
- ★目地の線は「屋根の中に入っている区間」を歩いて拾う（`pointInPoly`）。
  **中抜き（穴）の中には入れない**・輪郭の外へ出さない。くり返しの上限つき（§214の型）。
- ★継目を出さない現況では、透明にした部品を **`visible=false`** にする。
  透明のまま残すと絵には出ないが、描く手間（draw call）はかかる（§178の教訓）。

**★この改修で見つかった別の不具合（検証で発覚）**
`nn-rtbl-js`（屋根の表）は**末尾で `nnRoofTbl(true)` を1回呼んでいる**。
その1回は、後ろにある `nn-genkyo-js` が `NN_GENKYO` を作る**前**に走るので、
**図面を保存したまま開き直すと、現況のプルダウンが「新規防水」だけ**になっていた。
→ `nn-genkyo-js` の最後で、もう一度だけ表を組み立て直す。
★**後ろのブロックが用意する一覧を、前のブロックが使うときは、
  「後ろが読み終わったら作り直す」を入れること。**（読み込みの順で中身が欠ける）

### 231. 隣り合う屋根の境界に「勝手に壁面が発生する」／現況を3択に（2026-08-28c）★本人の指摘

**①隣り合う屋根の境界に、同じ壁が2枚できていた**
本人の実際の図面（屋根① L字・GL+4.0・W造・54.7㎡／屋根② GL+0・RC造・101.1㎡・隣り合わせ）で再現できた。
- 境界の辺は `nnSyncSharedEdges` が自動で「低いほう＝壁当り」にする（これは正しい）。
- ところが壁当りの3D（`e.k==='kabe'` の枝）が、**隣の部位の高さまでの外壁をここでも立てていた**
  （`findSharedLevel` で隣の lv を拾って `wallUp=shared-lv-hh`）。
  その壁は**隣の部位の躯体そのもの**なので、**背中合わせに同じ壁がもう1枚**できていた。
  ・図面に何もしていないのに壁面が勝手に現れる
  ・その外壁（クリーム色 `0xd8d0c2`）が隣の躯体（W造＝木）を**覆い隠す**ので、下地の見た目まで変わる
  実測：高さ4mの立体が **4件**（屋根①の躯体1＋余計な外壁3）。屋根②の側から見た1枚目は
  `ExtrudeGeometry #fff6e5`（外壁）で、木の躯体が隠れていた。
- 直し方＝**辺を共有する相手がいるなら外壁を立てない**（`if(shared!=null) wallUp=0;`）。
  既定の2.6mは「**図面にない**隣の建物の外壁」に立て替えるためのものなので、
  相手がいないときだけ使う（この動きは今までどおり）。
  直したあと：高さ4mの立体は **1件（屋根①の躯体）だけ**、見える壁は `nnBody #c9a97a`（木）。
- ★**教訓：同じ壁を2か所から作らないこと。** 部位の躯体は自分で壁を持っているので、
  隣の部位がその壁をもう一度作ると必ず二重になる。
- 検証は `_check/adjwall.js`（本人の図面そのままの形。`before` で直す前は★NG2件）。

**②現況を「躯体／既存防水／改修後防水」の3択に変更（§230②の作り直し）**
本人の指示「「躯体」「既存防水」「改修後防水」のどれかを選べるようにし、
選択したものが表現される仕様に変更してほしい」。改修の打ち合わせで見せたい3つの状態にそろえた。

| 現況 | 3Dの見え方 |
|---|---|
| 改修後防水（既定） | いままでどおり、仕様どおりの新しい仕上がり |
| 躯体（防水前の下地） | 防水層を**下地の材質**に。継目（はみ出しアス）は出さない |
| 既存防水（改修前） | 色あせ・汚れのまだら。**継目は残る**（既存の重ね） |

- ★**「躯体」は `nnMat('concrete')` を通す**ので、下地（§140）に合わせて
  **RC造＝コンクリート／W造＝木／S＋デッキ＝亜鉛めっき** と自動で変わる。
  （包みの順は 下地 → 写真の質感 → 現況。いちばん外側から内側を呼ぶので、両方が効く）
- **§230② の「保護コンクリート仕上げ」は、この3択に整理したので外した**（伸縮目地の処理ごと削除）。
  必要になったら git の履歴から戻せる（版 2026-08-28b）。
- 版 2026-08-28b の保存値は読み替える（`oldas`・`hogo` → `exist`）。知らない値は捨てて既定に戻す。
- ★**数量・見積は1円も変わらない**（見た目だけの切り替え）。検査で金額の一致まで見ている。
- ★検証の落とし穴：**下地の違いは「色」ではなく「質感（map）」で出る**ので、
  材質の色を見比べても差が出ない（写真の質感を当てるとき色は白に寄せている・§227）。
  **実際に描かれた絵（画素）で見比べること。**
- 検証は `_check/genkyo.js`（15項目に作り直し）。

### 232. 「生まれた面を、全部 出したり引っ込めたりしたい」への対応（2026-08-28d）★包括的な依頼
本人の依頼（そのまま）「3Dで作る際、かならず面が発生するでしょう。どの部位とかに限らず。
出したり引っ込めたりしたらそこにまた必ず面が生まれる。その発生した面を全てを、出したり、
引っ込めたり、その面の一部の区画を出したり、引っ込めたりしたい。世の中の建築物の大半を
納まりナビで作れるようにしたい。屋上だけでOK。だから孤（弧）を描けるようにもしたいし、
階段とかも作れるようにしたい。階段はつくるとしたらblenderで作るけど」。
**4つに分けて実装した（A〜D）。**

#### A 2D図面に「弧（アール）」（`window.nnEdgeArc` / ツールバー「◜ 弧」）
- 辺を選んで「◜ 弧」→ **ふくらみ（mm）** を入れると、その辺が **16本の折れ線の弧**になる。
  ＋と−で外・内が入れ替わる。**弦と矢から半径を出す**（`R=(弦²/4+矢²)/(2×矢)`）。
- 向きは **`ringNormal`（内向き）を裏返して外向き**にする。ここを間違えると、
  ＋を入れたのに内側へへこむ（実際にへこんだ）。
- ★**弧の16本には同じ印（`e.arc`）を付ける。** これが無いと
  ・**寸法の札が16枚並んで図面が読めない**（いまは弧ぜんぶで**まん中の1枚だけ**。
    文字は「弧 20.5m（R26.0m）」＝弧の長さと半径）
  ・保存すると弧かどうかが分からなくなる
- ★★**辺に項目を足したら `ek()` と `normE()` の両方に足すこと**（§223 でも同じ罠）。
  `ek()` は「知っている項目だけ」を返すので、足さないと**その場（描画）で消える**。
  `normE()` に足さないと**開き直したとき消える**。今回どちらも踏んだ。
- 検算：弦20m・矢2m → 弓形の面積 26.87㎡（理論値）に対し、折れ線で **+26.77㎡**。

#### B 面の上に「自由な形（多角形＋弧）」をかいて押出し・引込み（`nn-3ddraw-js`）
- 3Dの面をタップ → 長方形カードの **「⬡ 形を変える」**（または最初から自由な形）→
  タップのたびに点が増える → 始点をタップで閉じる → **⬆押出し／⬇引込み**。
  **「◜ 直前の辺を弧に」** で、その辺だけ弧にできる（A と同じ計算を面の座標系で）。
- データは `state.d3sol` に `{shape:'poly', pts:[[u,v],…]}` として持つ（外接の四角も
  `a`/`b` に入れる＝大きさの表示・移動に使う）。**保存・復元は state ごとなので自動。**
- 3Dは **`ExtrudeGeometry`（平面の形＋奥行き）**。面の座標系（u,v,n）をそのまま
  向きに使う（`makeBasis(u,v,n)`）ので、**屋根・立上り・天端・作った立体、どの面でも同じ**。
- ★**札は「形の重心」に置く**。外接の四角の真ん中だと、L字のときへこみ（何も無い所）に出る。

#### ★★B で見つかった実バグ＝押し出したものが屋根の**下**にもぐっていた
- 防水層の平場は**両面の板**で、作りによっては**法線が下向き**になっている。
  `faceHit` はその向きをそのまま「面の表」にしていたので、押し出すと下へ生えていた。
- → **「見えている側」を表にする**（`if(n.dot(rc.ray.direction)>0) n.negate();`）。
  ★**両面の板を扱うときは、法線をそのまま信じないこと。** カメラから見た向きで決める。

#### C 作った立体の面をドラッグで出し入れ（`window.nnSolDragTry`）
- 「選択」ツールで、立体の**表の面**をつかんで動かすと奥行きが変わる（5mmきざみ）。
  横の面は選ぶだけ（動かさない）。**カメラは動かない**（§152）。
- 入口は **`nnFaceDrag` の pointerdown に1行足しただけ**
  （`if(window.nnSolDragTry && nnSolDragTry(e))return;`）。§121 の「受け口は1本」を守る。
- ★**カメラに「いまつかんでいる」と伝える**のは `window.nnFaceBusy` の**包み直し**。
  同じ要素に付いた先に登録された処理は `stopPropagation` では止まらない（§229）。
- ★**2本目の指が来たらつかみをやめてピンチに譲る**（§229 と同じ決まり）。
- **できた面の上に、さらに長方形・自由な形をかいて押し出せる**（積み重ね。検証済み）。

#### D 階段（Blenderモデルの受け皿）
- ツールバー **「◆ 階段」**（`NN_STAMPS.kaidan`・900×2400×1500）。
  3Dでは **段板・側桁・手すり**の姿になる（蹴上げ190mm＝1500mmで8段）。
- **`models/kaidan.glb` を置けば、Blenderで作ったモデルが優先される**
  （§154 の `probeGLB`／`nnLoadGLTF`／`RICH` の仕組みにそのまま乗せた。HTMLは触らない）。
  書き出し方は `models/README_glb.md`。

★**検査**：`_check/arcpoly.js`（A〜D の28項目。**直す前のファイルでは★NG20件**＝本物）。
★**テストの手入れ**：`_check/pinch2.js` が時々落ちていた原因は**テスト側**だった。
  キャンバスをつかんだまま使っていたが、**キャンバスは作り直されることがある**
  （大きさの合わせ直し・§183 の土台の取り上げ）ので、イベントが宙に浮いて
  「ピンチが効かない」と誤判定していた。**毎回いまのキャンバスを探す**ようにして解消。

### 233. 不具合巡回で見つけて直したもの（2026-08-28e）★本人の指示「ずっとチェックし続けて」
検査を1本ずつ全部（234本）流しつつ、検査が見ていない所を手で触って回った記録。

#### ★★いちばん大きな発見：工程表の日付が100件中36件でNaNになっていた
- `ensureGantt` が **「2026-09 着工予定」のような“文字の予定”をそのまま `new Date` に渡していた**。
  Invalid Date になり、その物件の工程が**ぜんぶ `NaN-NaN-NaN`**。
  ・物件詳細の「工程」タブに棒が1本も出ず、日付の列に `null` と出る
  ・**「工程表PDF」に NaN が刷られる**（1物件で92個・4物件で276個）。元請に出す紙。
  ・見つけ方：**全100物件で工程を作って、日付として読めるかを機械で確かめた**（1200本中432本が異常）。
- 直し方＝**日付として読めるものだけを日付にする**（`nnPickDate`）。
  年と月が書いてあれば**その月の1日**として読む。
  開始＝着工日→契約日→予定→今日／終わり＝完成日→予定→開始+45日、の順に決める。
- ★あわせて**「終わりが開始より前」の逆さまの棒**も直した（31件）。
  元請工程の「付帯塗装工事」は *防水の開始+10日 〜 防水の終わり−6日* なので、
  **防水が16日より短い現場だと必ず逆転する**。`R()` の中で 開始≦終わり に丸める。
- ★**教訓：日付は「読めなかったとき」を必ず決めておくこと。**
  文字で予定を持つ欄（「◯月 着工予定」）は現場の台帳ではふつうにある。
- 検査 `_check/gantt2.js`（直す前は★NG4件）。

#### 弧（§232）をかいた図面の書類が読めなくなっていた
- **平面図PDFが数字だらけ**になっていた。弧は16本の折れ線なので
  ①辺ごとの寸法が16個並ぶ ②通り芯が16本立って丸囲みが重なる。
- 直し方：弧ぜんぶで **「R◯◯m」を屋根の内側に1つだけ**（外側は寸法チェーンが走っていて必ず重なる）。
  通り芯は**弧の途中の頂点からは立てない**（始点と終点だけ残す）。
- ★**弧のまとまりを求める処理は共通の道具 `nnArcGroups` に集約**した。
  画面の図面（draw）とPDF（nn-draw-js）で別々に書くと必ず食い違う（§146の教訓）。
- ★**弧にすると、その辺のアルミ笠木・壁の厚み・アゴ・取り合いが消えていた。**
  `ek()` は知っている項目しか返さないので、`ek()` で作り直すと知らない項目が落ちる。
  **元の辺の中身をそのまま写す**（`Object.assign({}, src, {arc:aid})`）のが正解。

#### 役物（脱気筒・ドレン等）が1個ごとにボタンを押し直す必要があった
- 脱気筒は「100㎡に1箇所」、ドレンも複数あるのがふつう。**続けて置ける**ようにした。
  やめるのは **Esc／同じボタンをもう一度／道具を変えたとき**。
- ★§180の教訓どおり、**トグル（もう一度押したらやめる）は「ボタンを押したとき」だけ**。
  `nnStamp(key, byUser)` にし、ツールバーの8個だけ `1` を渡す。
  プログラムから続けて呼ばれたときに裏返ると、置けなくなる。
- ★**`setTool` は byUser で分けずに必ず置くモードを終わらせる**。
  終わらせないと「選ぶつもりのタップで役物がもう1つ置かれる」。

#### スマホのツールバーが3段→4段になっていた
- 弧と階段を足したぶん。**作図面が32px狭くなっていた**（実測 138px→100pxに戻した）。
  絵を25pxに・すき間を2pxに。**絵がまだ無いボタンは20〜24pxと細くて押しづらかった**ので
  絵のあるボタンと同じ27px幅にそろえた。
- ★**ボタンを増やしたら必ず `_check/tb2.js` を流し、たて画面の段数を実測すること**（§118cから何度も踏んでいる）。
- 階段の記号は **▤**（鳩小屋の ◆ と同じだと、記号だけのときに見分けがつかない）。

#### 落ちる経路の手当て
- 材料登録 `saveDetail`：一覧の表示を切り替えると入力欄が消えるのに、そのまま読んでいた。
- 現場記録帳 `nnDefClose`／ホームの客先登録 `render`：開いていないときに呼ばれると落ちていた。
- 御見積書 `nnMitsuPDF`：入力なしで呼ぶと落ちていた（ふつうの値で埋める）。

#### 調べて「問題なし」と確かめたこと（次に同じ所を疑わないため）
- ダッシュボードの押せるもの239個／物件詳細 100物件×8タブ（800画面）／
  ライブラリ・用語集・国交省仕様・仕様登録・材料登録の一覧と中のボタン：**JSエラー0**。
- **絞り込み12種すべて**、件数が「実際に出ている件数」と一致。組み合わせ・リセットも正しい。
  帯（金額・数量）の境目も抜けなし、並びも小→大。
- **物件報告書を全100件**作って NaN・空・短すぎ が0件。工程表PDFも全100件で0件。
- **でたらめな屋根の形を20通り×書類6種（120枚）／3Dを20通り**：落ちない・NaN無し・
  GPUに載る形が増え続けない（`_check/fuzzdoc.js`／`fuzz3d.js` に残した）。
- 保存キーの一覧（§46のバックアップ）に**利用者のデータの漏れは無い**
  （一覧に無いのは計測表・版の記録・バックアップの促し用の4つだけ）。
- 全11ページ × PC／たて／よこ の見た目：**横はみ出し0**。
- 発注の通し（現場を選ぶ→数量→確認→発注書PDF→履歴）：金額が正しく、NaNなし。

#### 検査そのものの手入れ
- `pwchk`（見出しの数）・`det1`（タグの数）を**決め打ちにせず、実際のデータと突き合わせる**形に。
  §222で元請別パネルが増えて10個になり、毎回★NGが出ていた。
- `pinch2` は他の検査と同時に走ると1回目の指の後片付けが間に合わず、ときどき落ちていた。
  待ち時間を足し、落ちたときに指の記帳（`window.__tpDump`）を出すようにした。
- `d3draw` は「続けて置ける」仕様に合わせて更新。

#### ★2026-08-28f 3Dの「寸法の札の絵」がGPUに溜まり続けていた（巡回の続き）
- 立体（押出し・引込み・自由な形）の**寸法の札は、作り直すたびに新しい絵**を作っていた。
  面をドラッグして奥行きを変えると**毎コマ1枚ずつ GPU に溜まる**（実測：160コマで166枚・
  作っては消すのを6周で 171→196 と増え続ける）。1枚64KBなので2秒のドラッグで約8MB。
  **§183の「形が黒くなる・落ちる」とまったく同じ仕組み**（§186の継目の置き場と同じ話）。
- 直し方は2つ。
  ①**同じ文字の絵は使い回す**（`txLabel` に置き場。24枚で頭打ち・長く使っていないものから捨てる）
  ②**後片付けで絵も捨てる**。`nnSolRender` の後片付けは形と材質しか捨てていなかった。
    使い回している絵（`_nnShared`）と使い回しの材質は捨てない。
- 実測：160コマ動かしても**30枚で頭打ち**、作っては消すのを6周しても増えない。
- ★**教訓：`new THREE.CanvasTexture` を作る所は、必ず「使い回し」か「後片付け」のどちらかを付ける。**
  three.js は材質を捨てても、その中の絵（map）までは捨てない。
- ★**見つけ方**：`renderer.info.memory.textures` を、使い倒しながら何周も見る。
  形（geometries）・記憶（JSHeap）・見張り（listeners）は増えていなくても、
  **絵だけが増えていることがある**（今回まさにそれ）。検査 `_check/texleak.js`。

#### ★2026-08-28f（続き）立体を描き直すたびにシェーダーを作り直していた
- 上の絵の漏れを直すときに気づいた。**`nnSolRender` の後片付けが、使い回しの材質まで捨てていた。**
  ・このページの決まりは **`userData.nnShared`**（§177）なのに、3Dでかいた立体まわりだけ
    `_nnShared` という**別の印**を使っていた → 使い回しの材質（コンクリ・アルミ）が毎回捨てられる。
  ・材質を捨てると**シェーダー（絵の描き方）も作り直し**になる（§178でいちど直した「固まる」原因）。
  ・**実測：面をドラッグする40コマで、シェーダーの作り直し240回。**
- 直し方は2つ。
  ①見分けを1本にする（`nnShared(o)`＝`_nnShared` でも `userData.nnShared` でも真）。
   後片付けも `nnFree(o)` に集約（形と絵は捨てる／使い回しは捨てない）。
  ②**立体まわりの材質も使い回す**（`solMat(key,make)`：本体・ふち・引込みの枠・選択の橙）。
   **札の材質も文字ごとに使い回す**（`labMat`。絵だけ使い回しても材質を作り直せば同じこと）。
- **実測：シェーダーの作り直し 240回 → 0回**、絵は30枚で頭打ち。
- ★★**教訓：印（フラグ）の名前は1つに決めること。** 同じ意味の印が2つあると、
  片方しか見ていない場所で「使い回しのつもりの物が捨てられる」。
  ★**測り方**：`gl.compileShader` の呼び出し回数を数える。ソフト描画の環境でも
  この数字は環境に左右されないので、当てになる（§63）。

### 234. 国交省仕様のナビが右で切れる／戻るが役物を面倒みていなかった（2026-08-28h）★巡回で発見
巡回（全11ページ × 320/375/393/430px の横はみ出し掃引）と、機能どうしのかみ合わせ調べで見つけた2件。

**①国交省仕様（kokkosho）だけ、下部ナビの「国交省仕様」が画面の右で切れていた**
- 実測：ページの幅が **980 → 1015px** に広がり、1段目の5個が入りきらず、
  いちばん右（選択中で1.25倍になる）が **38px はみ出して**いた。実機では右端が欠けて見える。
- **★原因は、仕様の詳細パネル（`.detail`）を `right:-100%` で画面の外に隠していること。**
  隠した先も「ページの中身」として数えられるので横スクロールが生まれ、そのぶん
  **ページの幅が広がる**。下部ナビは `position:fixed`（left:0/right:0）なので、
  広がった幅（1015px）に合わせて自分も広がり、5個が入らなくなっていた。
  ＝図面・積算で直したのとまったく同じ話（2026-08-24r・§189）。
- 直し方：末尾に `<style id="nn-noxscroll">` を足して **`html` と `body` の両方**に `overflow-x:hidden`。
  ★**`html` だけでは効かない。** `.detail` は `position:fixed` なので、
    ルートの overflow では切り取られない。**body 側にも入れて初めて 980px に戻る**（実測）。
- ★**検査がこれを見つけられなかった理由**：`noxscroll.js` は
  「`window.scrollTo(9999,0)` で実際にずらせるか」だけを見ていた。
  この不具合は**ずらせない（scrollXは0のまま）のに幅だけ広がる**形なので素通りしていた。
  → **`documentElement.scrollWidth − clientWidth`（ページの幅そのもの）** も見るようにした。
  直す前のファイルで★NGが出ることを確認済み。

**②役物を置いたあと「↩戻る」を押すと、役物は残ったまま、ひとつ前にかいた屋根が消えていた**
- ★原因：戻る・進むの記録（`hist`）が **屋根の形（`state.polys`）だけ**を覚えていた。
  役物（`state.parts`）と3Dで作った立体（`state.d3sol`）は記録の対象外なので、
  ・役物を置いても記録が増えない（`commit()` は中身が同じだと何もしない）
  ・そこで↩を押すと、**ひとつ前の「屋根の変更」まで巻き戻る**
  実測：屋根2つ＋役物1個 → ↩1回 → **屋根1つ・役物1個**（消してほしくないものが消える）。
- 直し方：記録を `nnHistSnap()`＝`{屋根, 役物, 立体}` の3つにまとめた（3か所の書き換えだけ）。
  `saveState()` は元から `commit()` を呼んでいるので、**役物を置く・消す処理には一切手を入れていない**。
  ★**断面（`state.sect`）は入れない。** 断面タブが自前の戻る・進むを持っているので、
    一緒に覚えると取り合いになる。
- 直したあと：↩1回＝立体だけ戻る／↩2回＝役物だけ戻る／↩3回＝屋根が戻る、と1手ずつ戻る。
  ↪進むも同じ順で復活する。
- 検査は `_check/undo2.js`（直す前のファイルでは★NG4件＝本物）。

**あわせて作った検査**
- `_check/arc2.js`（弧が数量・保存・笠木・3D・中抜きとかみ合うか。
  弦10m・矢2m の弓形13.8㎡が理論どおり足されることまで見る）
- `_check/sol2.js`（押出し・引込み・自由な形の立体と階段スタンプが、保存・復元・3D表示まで通るか）
- ★`_check/bluelv1.js` の「木造の色」は**テストが古くなっていた**（product は正しい）。
  2026-08-27e から写真の質感を貼るようになり、貼る材質は
  「いちばん明るい成分が1.0」に色をそろえている（§227）ので、木の `0xc9a97a` は `0xffd79c` で出る。

### 235. 段差ラインで笠木が消える／全削除の戻るが2回必要／見積番号が引っ越しで消える（2026-08-28i）★巡回で発見
どれも「静かに消える」たぐいで、使っている本人が気づきにくいもの。

**①アルミ笠木を付けた屋根を「⛰ 段差ライン」で2つに分けると、笠木が全部消えていた**
- 実測：分ける前 4本 → 分けたあと **0本**（アゴ・壁の厚み・取り合いは残っていた）。
- ★原因：分割は辺を **`ek()` で作り直す**が、`ek()` は「知っている項目だけ」を返す関数で、
  **`kasagi`（笠木の有無）だけが入っていなかった**。
  `normE()`（保存の読み書き）には入れてあったので、保存・復元では消えない。
  ＝**分けたときだけ静かに消える**という、いちばん見つけにくい形だった。
- 直し方：`ek()` に `kasagi` を1行足すだけ。
- ★**辺に項目を足したら `ek()` と `normE()` の両方に足すこと。**
  `ek()` に足し忘れる＝その場（分割・図面の描画）で消える／
  `normE()` に足し忘れる＝開き直したときに消える。§223・§224 でも同じ罠を踏んでいる。
- 検査は `_check/split5.js`（直す前のファイルでは★NG1件）。

**②「全削除」のあと ↩戻る を1回押しても何も戻らなかった（2回押す必要があった）**
- ★原因：全削除は「屋根と役物を消す」「3Dで作った立体を消す」で**保存が2回**走るので、
  戻る・進むの記録が **2つに割れて**いた。1回目の↩は「立体だけ戻す」ので、
  画面上は**何も起きていないように見える**。
- 直し方：`nnHistMark()`／`nnHistCollapse(h0)` を新設し、
  **ひとつの操作で増えた記録を1つにまとめる**。全削除の2つの包み（下絵つき・立体つき）で使う。
- ★**ひとつの操作で `saveState()` が2回以上走る処理を書いたら、必ず記録をまとめること。**
  まとめないと「↩を押しても何も起きない」に見える。

**③御見積書の番号（`nn_mitsu_seq`）が、書き出し／読み込みの対象に入っていなかった**
- 引っ越し・機種変更のあと見積番号が1に戻り、**同じ番号の見積書が2枚**できてしまう。
- ホームの「設定」の一覧（index.html の `LIST`）に1行足した（§46の決まり）。
- ★**新しい保存キーを増やしたら `LIST` にも足すこと。** 足し忘れを機械で見つけられるよう、
  **`_check/keylist.js`（ブラウザを使わない静的な検査）** を新設した。
  全ページから `'nn_…'`／`'osamari…'` の文字を集めて `LIST` と突き合わせる。

**④検査の手入れ**
- `_check/degen.js` … ほかの検査と同時に流すと機械が混み合い、
  本当は固まっていないのに時間切れに見えることがあった（実測 2.3秒の処理が7秒に）。
  **5秒を超えたときだけ、もう1回だけ測り直す**ようにした。
- `_check/noxscroll.js` … 「ページの幅そのものが広がっていないか」も見るようにした（§234）。

### 236. 屋根が多いと図面の操作が重かった（2026-08-28j）★計測で原因を特定
屋根20面の図面で「↩戻る」や高さの変更をすると、そのたびに 0.2秒ちかく止まっていた。

- **★まず計測した**（§178の決まり。推測で直さない）。CPUの中身を見ると
  **83〜91%が `getBoundingClientRect`（画面の置き場所を測る処理）** だった。
  ・`fitNames`（屋根の名前を枠に入る大きさまで小さくする）… 入力欄1つにつき
    「font-size を書く → はみ出しを測る」を最大14回くり返していた。20面なら最大280回。
  ・`place`（表をツールバーの下・操作パッドの左に置く）… 「書く→測る」を混ぜていた。
    しかも**0.7秒ごとにも呼ばれる**。
  ★**書いたあとに測ると、そのたびに画面ぜんぶの置き場所を計算し直す**（§203と同じ型）。
- **直し方は3つ**
  1. `fitNames` … 「まとめて測る → まとめて書く」を最大3周に。
     はみ出し具合の比から必要な大きさを一発で見当づけるので、ふつうは1〜2周で終わる。
  2. `fitNames` … **名前と表の幅が前と同じなら、測らずに前の大きさをそのまま当てる**。
     ↩戻る・高さの変更では名前が変わらないので、測ること自体が要らない。
     ★**合図に画面の寸法を読まないこと。** 作り直した直後に少しでも読むと、そこで約90msかかる。
       表の幅は「手で広げた値（pos.w）」と窓の幅で足りる。
  3. `place` … 先にまとめて測って、あとからまとめて書く。値が同じときは書かない。
- **効果（屋根20面・1コマに1回ずつ作り直す実測）**
  | | 直す前 | 直したあと |
  |---|---|---|
  | 屋根の表の作り直し（中央値） | **177.5ms** | **6.9ms** |
  | いちばん速い回 | 58.6ms | 5.6ms |
  ★見た目・動きは1つも変えていない（名前の縮小・置き場所・列は同じ。回帰22本とも○）。
- 検査は `_check/rtblperf.js`（直す前のファイルでは★NG）。

**あわせて作った検査**
- `_check/deeptabs.js`／`_check/deepzu.js`／`_check/deepsel.js`
  … 「最初の画面」ではなく**何かを開いたあとの画面**でボタンを片っぱしから押し、
  JSエラーが出ないかを見る（記録帳8タブ×3物件で336個、図面4タブで280個、他7ページ）。
  ★押すたびに「いま画面にあるボタン」を取り直すこと。まとめて集めてから押すと、
    途中の描き直しで古いボタンを押してしまい、product は正しいのに★NGが出る（実際に出た）。
- `_check/total1.js` … 画面の積算の合計と、御見積書（紙）の合計が一致するか。
  役物のぶんは `nnEstimateData()` の `parts` に別で入っているので、
  **`rows` だけを足すと役物のぶんが抜ける**（検算するときの注意）。
- `_check/framever.js` … 2026-08-11r で「枠まるごとの絵」をやめて四隅だけにしたのに、
  検査が古い作りのまま**ずっと★NGを出し続けていた**。いまの四隅の絵を見るように書き直した。
  ★**「毎回★NGが出る検査」を放っておかないこと**（§187と同じ反省）。

### 237. 仕様登録もよこ向きでページの幅が広がっていた（2026-08-28k）
- よこ向きで、ページの幅が **980 → 1014px** に広がることがあった（6回中2回）。
  メーカーの帯（`#makerbar`）は中身が1287pxあり、ふだんは帯の中で横に送るのではみ出さないが、
  読み込みの途中で一瞬だけ帯が広がると、**幅980の決め打ちのページでは
  「見えている幅」がそのぶん広がったまま戻らない**（国交省仕様と同じ話・§234）。
- 直し方は同じ：`html{overflow-x:hidden}`（body は元から hidden）。
- ★**同じ作りの `zairyo_toroku` は、いまのところ起きない**（メーカーの帯が短いため）。
  起きたら同じ1行を足すこと。
- ★**この不具合は「6回に2回」しか出ない**。1回試して出ないことを「無い」と決めないこと。
  幅980の決め打ちのページ4枚を、よこ向きで6回ずつ測って見つけた。

**検査の手入れ**
- `_check/pfimg.js` … まだ絵が届いていない置き場（`hpic_moto.png` など。`onerror` で消える作り）の
  404 を数えて★NGにしていた。§202のとおり数えないようにした。

### 238. 長い工事名が「紙の外」まで刷られていた（2026-08-29a）★元請に出す紙
図面の5種（平面図・断面詳細図・割付図・施工層構成図・標準納まり詳細図）の**表題欄**は、
入力した工事名・場所・元請・作成者・図面番号を**そのまま刷って**いた。

- **★実測**：工事名を長め（60字）にすると、右端の「工事名称」の欄から飛び出し、
  **紙の外まで 106mm** 伸びていた。A3の紙が420mmなので、印刷すると右端で切れる。
  ・欄の幅は 67mm・文字3mm ＝ **22字までしか入らない**。
    「サン太平レジデンス 屋上防水改修工事」は18字で入るが、
    「◯◯マンション大規模修繕工事に伴う屋上防水改修工事」は25字＝**入らない**。
    ふつうにある長さで起きる。
- **直し方＝`A.fit(文字, 文字サイズ, 幅)` を図面側にも入れた**（工程表PDF（§44）には
  元からあった。図面側だけ無かった）。入らない分は「…」で切る。
  日本語は1文字＝文字サイズ、英数・半角カナは0.55倍として見積もる。
  ★**切る幅は「欄の幅」から出すこと。** 紙の幅で切ると、欄からははみ出したまま
    となりの欄の文字に重なる。
- 直したのは表題欄の5か所（作成者・元請・縮尺/年月日/図面番号の値・工事名称/場所/図面名称の値）と
  図面名の箱。`titleStrip` は **`window.nnSheetKit` として5種すべてが共有**しているので、
  1か所直せば5種とも直る。
- ★**確かめ方**：SVGを実DOMに描いて **`getComputedTextLength()` で実幅を測る**。
  文字数から見当をつけるのではなく、実際に描いた幅で見ること。
  検査は `_check/longtext.js`（**直す前のファイルでは★NG5件**＝本物）。
  `node _check/longtext.js <ファイル名>` で変更前と比べられる。
- 御見積書・物件報告書（どちらもHTMLで折り返す）・工程表PDF（元から A.fit）は問題なし。

### 239. 壊れた保存の「1件の中の項目」で、ライブラリと仕様登録が使えなくなっていた（2026-08-29b）
§199・§210 では「並びであること」「1件ずつが箱であること」まで直したが、
**その1件の中の項目（部位・工程・写真・注意）の形は見ていなかった**。

- **★ライブラリ**：部位（`bui`）が文字／欠けている／null だと、一覧の組み立てで
  `.slice().map()` が止まり、**一覧が丸ごと0件**になる（**見本8件まで消える**）。
  写真・工程が変な形だと、その項目の詳細を開いたところで止まる。
- **★仕様登録**：工程（`steps`）が文字／欠けている／null が混じっていると、
  **その仕様を開いた瞬間に止まる**。画面から直すこともできない。
- どちらも**古い版のバックアップを読み込んだとき**などに起こり得る
  （§46 の書き出し／読み込みは、値を解釈せず生の文字列のまま入れる作りなので、
  形の違う古いデータもそのまま入る）。
- **直し方＝読み込むときに「使える形」に直す**（`nnLibFix`／`fixSp`。捨てずに拾う）。
  ・並びのはずの項目 … 文字なら1件の並びに、null・欠けは空の並びに
  ・工程 … 箱でないものを外す（仕様は空になったら空の1行を入れる）
  ・名前・工法・区分 … 文字にそろえる
- ★**教訓：壊れ方の検査は3段階で見ること。**
  ①ページが開けるか（§199）②一覧が出るか ③**開いて操作できるか**（§210）。
  §210 で②③まで広げたが、**見ていたのは「1件まるごとの形」だけ**だった。
  今回は**その中の項目**。同じ形の危うさは「並びのはずの項目」があるところ全部にある。
- 材料登録（`hist`）と客先登録は元から守られていた（確認済み）。
- 検査は `_check/junkfield.js`（**直す前のファイルでは★NG9件**＝本物）。
  `node _check/junkfield.js before` で変更前と比べられる。

### 240. 屋根・部位の内訳が壊れていると、現場一覧が丸ごと出なくなっていた（2026-08-29c）
§239 と同じ形の見落とし。今度は **`faces_sample.js`**（現場記録帳と現場マップの**両方**が読む）。

- `nnFaceList` は保存された値を**そのまま返して**いた。中身が文字だと、
  呼び出し側の `.forEach`／`.map` で止まる。止まる場所が
  **`render()`（現場一覧の組み立て）の中**なので、**一覧が丸ごと出なくなる**。
  実測：`showView('list')` が `a.forEach is not a function` で止まり、カード0件。
- 直し方＝**並びであること・1件ずつが箱であることまで確かめてから返す**（`okRows`）。
  ★**「わざと空にした（＝見本を消した）」印は残すこと。**
    空の並びは今までどおり「内訳なし」として扱い、見本には戻さない。
    使えない形（文字・数字・箱）のときだけ見本に戻す。
- 検査は `_check/facesjunk.js`（**直す前のファイルでは★NG3件**＝本物）。
  **壊れ方だけでなく、ふつうの動き3通り**（見本／正しい保存／わざと空にした）も
  一緒に見るので、直しで意味が変わったらすぐ分かる。
  `node _check/facesjunk.js before` で変更前と比べられる。
- ★**同じ形の危うさは「保存から読んだ並びを、そのまま返す関数」全部にある。**
  返す前に形を確かめること。

### 241. 現場マップ：屋根の輪郭が壊れていると面積が出せなくなる（2026-08-29d）
§239・§240 と同じ形。`loadRoofs` は「面の一覧かどうか」までは確かめていたが、
**面の中身（点の並び）は見ていなかった**。

- 面が文字・数字・null だと、あとで `.map`／`.forEach` するところで止まり、
  **その現場の面積が出せなくなる**。点が文字・null・2つだけ、でも同じ。
- 直し方＝読み込むときに「使える形の面」だけを残す（`nnRoofFaceOK`／`nnRoofPtOK`）。
  ・面＝3点以上の並び ・点＝`[lat,lng,alt?]` か `{lat,lng,alt}` で、緯度経度が数字
  ★**点の書き方は2通りある**（並びと箱）。片方だけ通すと、昔のデータが読めなくなる。
  ★**昔の書き方（面が1つだけ）との見分け**は `v[0][0]` が並びかどうかで付ける。
    面が3つ以上ある今の書き方は「点の並び」に見えるので、ここを間違えると
    面が3つの図面だけ読めなくなる（検査に「面3つ」を必ず入れること）。
- 良い面と悪い面が混ざっているときは、**良い面だけ残す**（全部捨てない）。
- 検査は `_check/roofjunk.js`（**直す前のファイルでは★NG7件**＝本物）。
  昔の書き方・今の書き方（面2つ／面3つ）・点が箱の形、も一緒に見る。
  `node _check/roofjunk.js before` で変更前と比べられる。

### 242. ★発注履歴：現場を1件消しただけで、履歴の画面が二度と開けなくなっていた（2026-08-29e）
これまでの §239〜§241 は「壊れた保存」の話だったが、**これはふつうに使っていて起こる**。

- 発注履歴は、現場と業者を **「番号」だけ**で覚えている（`gid`／`vid`。名前は持っていない）。
- 現場の一覧（`hacchu_sites.js`）は **§105 のとおり現場記録帳から作り直す**。
  だから **物件を消す・名前を変えると、その番号が一覧から消える**。
- すると `gById(gid)` が見つからず、`g.st` を読んだところで止まる。
  **過去の発注が1件あるだけで、発注履歴の画面が丸ごと開けなくなる。**
  実測：発注書を見る・発注書PDF・↺再発注 も全部止まる。業者を消したときも同じ。
  明細（`lines`）が並びでないとき（古い保存・書き出しの読み込み）も同じ。
- 直し方（`hacchu.html`）
  ・`gSafe(id)`／`vSafe(id)` … 見つからないときは
    **「（一覧にない現場：J051）」のような名前を出して先へ進む**（止めない）
  ・`linesOf(d)` … 明細は並びでなければ空として扱う
  ・`startDraft` … 現場が無いときは**理由を伝えて止める**
    （半端な発注書を作らせない。「↺ 同じ内容で再発注」が黙って無反応になるのを防ぐ）
- ★**教訓：番号（id）で覚えているものは、その番号があとから消えることがある。**
  ・見つからないときに止まるのではなく、**分かる形で出して先へ進む**こと
  ・とくに「別の画面のデータから作り直す一覧」（発注の現場・現場マップの現場）を
    参照している番号は、消える前提で書くこと
- 検査は `_check/histjunk.js`（**直す前のファイルでは★NG5件**＝本物）。
  `node _check/histjunk.js before` で変更前と比べられる。

### 243. ★★保存図面を開くと、いま描いている図面が消えることがあった（2026-08-29f）
現場記録帳の「作成図面」タブ →「開く →」（`zumen_sekisan.html?open=保存ID`）の流れ。
**いちばん怖い形の不具合**（利用者の作りかけが黙って消える）。

- **①中身を確かめずに上書きしていた。**
  `localStorage.setItem('nn_zumen_v1', it.data)` を**先に**やってから読み直していたので、
  保存の中身が壊れている（`data` が無い・途中で切れている・屋根の並びが無い）と、
  **いま開いている図面がその壊れた中身で上書きされ、まっさらになって**いた。
  → **使える形か先に確かめる**。読み取れないときは
    「いま開いている図面はそのままにしてあります」と伝えて、何も書かない。
- **②「入口メニューを出さない」印が、読み直しで消えていた。**
  印（`window.nnSkipMenu`）もURLの `?open=` も、そのあとの `location.reload()` で無くなる。
  ＝§151 の意図に反して、**保存図面を開いてもその上に入口メニューがかぶさって**いた。
  → 読み直しても残る置き場（`sessionStorage`）に移す。1回使ったら消す。
  ★**reload をまたいで伝えたいことは、変数でもURLでもなく sessionStorage に置くこと。**
- **③番号が見つからないときは、URLの `?open=` を消してから戻る。**
  消さないと「用事つきで来た」と見なされて入口メニューが出ず、
  まっさらな作図面だけが残って、次に何をすればよいか分からなくなる。
- あわせて現場記録帳の `nnZuSaves`（作成図面の一覧）も直した：
  1件ずつが箱であることを確かめる（`null` が混じると一覧が出せなかった・§239）／
  名前・日付を**そのまま入れない**（§212）／番号は `encodeURIComponent` でURLに入れる。
- ★**教訓：上書きは「新しい中身が使えると分かってから」。**
  先に書いてから確かめる作りは、失敗したときに元に戻せない。
- **同じ不具合が「📂 開く」（図面ページの中の一覧）にもあった**（2026-08-29g で追加修正）。
  ・`loadSaves` を1か所で整える（1件ずつが箱・名前と日付は文字）＝
    `null` が混じると一覧が組み立てられなかったのも直る
  ・`nnSaveDataOK(raw)` を新設し、**「📂 開く」と `?open=` の両方が同じ道具**で確かめる
  ・一覧の名前・日付を逃がす（§212）。`esc` は別のかたまりの中にあって
    ここからは見えないので、その場用の `nnSvEsc` を置いた
    ★**同じ名前の関数があっても、別の `(function(){…})()` の中なら使えない。**
      「あるはずなのに ReferenceError」はこれを疑うこと。
- 検査は `_check/zopen.js`（**直す前のファイルでは★NG6件**＝本物。
  「📂 開く」の5項目も入れてある）。
  `node _check/zopen.js before` で変更前と比べられる。

### 244. A4横だと、工法名と引出線の文字が紙から出ていた（2026-08-29h）
§238 は表題欄の話だったが、**A3では収まっていて A4横（幅297mm）だけ飛び出す**ものが残っていた。

- 断面詳細図の**引出線の文字**（「防水層 立上り（改質アスファルトシート防水 トーチ工法（密着））」）
  … 紙から **+2.3mm**
- 割付図の**施工条件の欄**（工法・シート規格）と**凡例の工法名** … 紙から **+1.4mm**
- どれも `A.fit(文字, 文字サイズ, 紙の右端までの幅)` で収めた。
- ★**紙の大きさを選べるものは、大きいほうだけで確かめないこと。**
  A3で収まるのは当たり前で、**きつい方（A4）で見て初めて分かる**。
  検査 `_check/longtext.js` は A3・A4 の**両方**を回すようにした。
- ※標準納まり詳細図は用紙の設定を見ずいつもA3（そういう作り）。

### 245. 3Dで部材が置けない／カメラが真横まで倒れない／タブ名（2026-08-29i）★本人の指示
指示は4件。①3Dで鳩小屋などを配置できない（平面図からしか置けない）
②真横から見たいのにアングルがちょっと上からになる＝断面の形を確かめられない
③「図面」→「平面図」、「断面」→「断面図」に ④②割付のタブは削除。

- **★①の原因＝面のドラッグ（`nnFaceDrag`）が指を横取りしていた。**
  ゴーストは出て「タップで設置」とも出るのに、離しても置かれない。
  実測すると **`pointerup` が統一の受け口に届いていなかった**（スパイで確認）。
  面のドラッグの `end()` は、離すときに `stopImmediatePropagation()` を呼ぶ。
  ★**同じ要素に付いた受け口どうしは `stopPropagation` では止まらない**（§164）ので、
    後から登録された「置く処理」まで巻き添えで止まっていた。
  → **`nnFaceDrag` の pointerdown で、スタンプを持っている間（`nnPlacingId()`）は
    そもそも始めない**。3D描画（`nnD3DrawOn`）と同じ形の門番を足しただけ。
    「押し出し」中（`_nnExtOn`）も同じ理由で譲るようにした。
  ★**教訓：同じ要素に複数の受け口があるときは「止める」ではなく「先に始めない」で譲ること。**
- **②カメラを倒せる限界を 1.45rad（約83度）→ 真横（90度・`NN_PHI_MAX=Math.PI/2`）に。**
  これでパラペットの形（立上り・天端・アゴ・笠木）が断面図と同じ見え方になる。
  ★真上（phi=0）は上向きが定まらず絵が回るので、下限 0.15 はそのまま。
- **③④タブは ①平面図／②断面図／③3D の3つに。**
  ②割付のタブは `display:none` で**残す**（`setTab` が点灯を切り替えるので、
  消すと落ちる）。割付は平面図のツールバーの「ヨコ割付／タテ割付」から開く。
  画面に出る文言の「①図面」「③断面」「④3D」も直した（コメントは経緯なので触らない）。
- 検査は `_check/d3ui.js`（**直す前のファイルでは★NG7件**＝本物）。
  回帰19本（d3fit／d3view／camfix／pinch2／aim3d／zmenu／sec3dtab／tb2／deck1／deck2／
  deck7／facedrag／multisel／partdel／custompart／d3draw／hatogoya／psel1／bluelv1）すべて★NGなし。

**★「天端の両端の書き込み」について（2026-08-29i 調査）**
本人の指摘。断面（x=6m・1辺だけ立上り1500mm）を数値で出したところ、
天端まわりにある部品は**全部 `#14120f`（ほぼ黒）＝はみ出しアス**だった。
| 位置 | 中身 |
|---|---|
| z 0.02〜0.23・y 1.512 | 天端を**横切る**はみ出しアス（天端幅ぜんぶ・§172） |
| z 0.23・y 1.512 | 玉（`nnBeadBall`・継ぎ目を隠す丸・§74） |
| z 0.227〜0.258 | 面取りを渡るはみ出しアス（`nnChamBead`・§79） |
| z 0.256・y 1.478 | 玉 |
＝「天端の両端の書き込み」は、この**黒い玉とはみ出しアスの端**。
★ここは §166→§167→§171→§172 で「出す／出さない」を4回行き来している場所なので、
  **勝手に決めずに本人の指示を待つ**こと。田島ルーフィングの納まり
  （https://cad.tajima.jp/?cat=3）は**この環境から見られない**（egress blocked）ので、
  標準にしたい納まり図は画像でもらうこと。

### 246. ★天端の両端の「三角の空洞」＝面取りの端がふさがっていなかった（2026-08-29j→kで決着）
本人が赤い点線で囲って指摘。1辺だけパラペットを高くすると、その**天端の両端に
白い三角の穴**があく。

- **★原因＝天端の面取り（§76・§78）は「斜面の板1枚」で作っていて、
  壁の端（小口）がふさがっていなかった。**
  壁は ①本体（高さ hh−CH）②天端寄りの帯（CH）③④その間の斜面 の4つで組む。
  ①②は押し出しなので端に蓋が付くが、**③④の斜面には蓋が無い**。
  断面で見ると、端の面には
  `(0,hh−CH)-(CH,hh)-(CH,hh−CH)` と `(th−CH,hh)-(th,hh−CH)-(th−CH,hh−CH)` の
  **三角形が2つ、面の無いまま残る**。
  高さのそろった角では隣の壁がふさぐので見えないが、**1辺だけ高くすると
  隣が低くて塞がらず、両端に三角の穴があいて見えた**。
- **版jの直し方＝面取りを0にする、は誤りだった（版kで撤回）。**
  本人の指摘「この線はなんですか？天端の内側の面取りが消えている。
  **内側の面取りは必須です。間違えないでください**」。
  フラットにすると、天端防水層の端（th）と立上り防水層（th+6mm）の6mmの段が
  上から**線**に見える（面取りがあれば斜面の防水層が覆っていた）。
- **★正しい直し方（版k・2026-08-29k）＝面取りは残して、端の小口を三角の板でふさぐ。**
  `capEnd(other,PO,PK,PIK,PI)`：**隣の壁が低い・隣に壁が無い端だけ**、
  外の三角（O〜K）と内の三角（IK〜I）を `slope()` で1枚ずつ置く。
  ・★**高さのそろった角には置かないこと。** 隣の壁の面と重なってちらつく（Zファイティング）。
    条件は jointH と同じ「隣の hh がこの辺の hh−2mm 以上なら隠れる」。
  ・★`slope` の第4点は第3点と同じでよい（2枚目の三角は面積0＝描かれない。
    three.js の normalize は `divideScalar(length()||1)` なので長さ0でも NaN にならない）。
  ・天端防水層の幅は面取りの内側（CH〜th−CH＝**210mm**）のまま。
- ★**確かめ方＝光線を撃つ**（`_check/tenba.js`・書き直した）。
  面取りの帯の高さ（hh−1cm）・面取りの奥行き（z=0.0145／0.2355）で、壁の中から
  両端の外へ4本撃つ。ふさがっていれば当たり、穴なら素通り。あわせて
  「面取りの斜面が在る」「そろった角に余計なふさぎ板が無い」「防水層の幅0.21m」も見る。
  **版i（面取り・穴あり）＝★NG4／版j（フラット）＝★NG2／版k＝全部○**＝検査が本物。
- ★**教訓：穴をふさぐのに、穴のまわりの形ごと消さないこと。**
  形（面取り）は本人が §76・§78 で明示的に頼んだもの。消せば穴は消えるが、
  頼まれた形も消える。ふさぐ板を足すのが正しい。
- `beadaudit` は CH=0.02 の期待値に戻した（ゆとり6mm＝継目の形は長さ1cmきざみで
  使い回している（§177）ため。これは版jで直した部分をそのまま残した）。

**★田島ルーフィングの断面図から読み取れたこと（本人提供・2026-08-29）**
`cad.tajima.jp` は**この環境から見られない**（egress blocked）ので、画像でもらうこと。
もらった「露出アスファルト防水（断熱仕様）」の断面から、躯体まわりだけを拾うと：
- 天端の上面はほぼフラットに見えるが、**この製品では内側の面取りは必須**（本人の指示。
  版jで「図面がフラットだから」と面取りを消して怒られた。図面の読み取りより本人の指示が上）
- 立上りは **水上から150以上**
- 天端の上に **ライナーコービングS250**（アルミ笠木）＋**強力ガムシール**
- 天端まわりの寸法の連なり：`20｜20｜≧180｜10｜20`（合計250＝コービングS250の幅）
- 防水層の端部は **ベーパス** で押える
- 平場の **水勾配 1/50〜1/20**
★断熱材（ギルフォーム）・防湿層・ギルキャントは**入れない**（本人の指示。躯体と防水層だけ）。
★まだ入れていないもの：立上り「150以上」の目安表示／平場の水勾配／ベーパス（端部押え）。

### 247. 入口メニュー「平面図作成／矩計図作成」＋アゴあり・アゴなし（2026-08-29m）★本人のモック準拠
指示は2つ。①「平面図をかく」→**「平面図作成」**、「矩計図をかく」→**「矩計図作成」**
②平面図作成・矩計図作成の中に**アゴあり／アゴなし**を付ける（モック＝各カードの中に
白地・濃紺わく・上に文字・下に断面のかたちの絵、の小さなボタン2つ）。

- **選んだほうが「これからかく辺の既定」になる**（`localStorage('nn_zumen_agodef')`・既定はアゴなし）。
  押すとカードと同じ画面へそのまま進む（1タップで「アゴありで作成」）。
  選択中は緑のわくで見分けられ、メニューへ戻っても残る。
- **平面図側**：新しい辺は必ず **`nnNewEdge(defH,defW)`** を通す（closePoly／中抜き／
  ▭長方形／写真から起こす の4か所）。アゴありなら `ago:1, agoD:100` が付く。
  ★**新しい辺を作る場所を増やしたら、必ず nnNewEdge を通すこと**（直書きすると既定が効かない）。
- **既にかいた図面があるときに切り替えたら、confirm で「その辺にも反映するか」を聞く**
  （§85② nnDefApply と同じ作法。聞かないと画面と既定が食い違う）。
- **矩計図側**：代表のアゴは**辺の多数決 `majorAgo()`**（window.nnMajorAgo。
  出はいちばん多い値）。画面の断面（nnSecDraw）と紙（nnSectionPDF）の**両方が同じ関数**を
  使う＝画面と紙が食い違わない（§224の教訓）。
  ・アゴあり＝**コンクリート打増しの庇（出100×厚50）＋先端のアルミの垂れ＋壁ぎわのシール**。
    **笠木は出さない**（田島の納まり図でも、コンクリートアゴとアルミ笠木は別の納まり
    ＝もらったPDF 2枚（asr-2-1＝コンクリートアゴ／asr-1-1＝アルミ笠木）がまさにその対）
  ・辺を選んでいるときはその辺の ago／agoD
- ★**カードは `<button>` なので、中のボタンは `<span role="button">`**。
  button の入れ子はHTMLとして不正で、ブラウザが構造を壊す（innerHTML で外に吐き出される）。
- ★**localStorage のキーを増やしたら**：index の `LIST`（§46）に追加。
  `nn_zumen_skipmenu`（sessionStorage の1回きりの印・§243）は keylist の除外に登録した。
- ★よこ向きスマホはアゴの行が増えたぶん下部ナビに8pxかぶった → 絵を出さず文字だけ＋
  余白をさらに詰めて解消（**メニューに行を増やしたら `node _check/zmenu.js land` を流すこと**）。
- 検証は `_check/agodef.js`（21項目：改名・ボタン4つ・既定の保存・新しい辺に付く・
  多数決・画面の注記・紙にアゴ／笠木の出し分け・confirm・戻すと外れる）。
  回帰：zmenu(PC/ph/land)・sec2chk・secnum・sec3dtab・zopen・uxtool1(PC/ph)・
  longtext・save2・qty1・keylist すべて★NGなし。

### 248. 3Dの描画＝「自由な形を点でかく→閉じたら即3D＋平面図に反映」（2026-08-29n）★本人のモック4枚
指摘（そのまま）「3D図面時にもこの描画を使えるようにしているが、なぜか四角形限定で、
なぞの画面が出てきて操作が直感的ではない。自由にユーザーが決めた形を平面上に記載し、
それが3Dになるようにしたい。もちろん平面図にも反映される形で！」

- **これまで**：3Dの描画は「2点タップ→長方形→カード（押出し/円柱/…/形を変える）」。
  四角形が先で、自由な形はカードの奥に隠れていた。
- **いま**：**1点目から自由な形**（平面図の描画と同じ。タップで点が増え、始点タップで閉じる）。
  ・**屋根の平場にかいて閉じたら、カードを出さずそのまま部位（屋根の区画）になる**。
    高さ＝その屋根＋300mm・辺は立上りなし（基礎・かさ上げの形）。
    **部位なので平面図・積算・屋根の表・↩戻る・保存に全部そのまま入る**（＝「平面図にも反映」）。
    高さは面のドラッグ／屋根の表で、立上りは辺タップで後から変えられる。
  ・**壁・天端・立体の面**は従来どおりカード（押出し・引込み・⚪円柱・◜弧）。
    積み重ね（§232C）・役物づくり（§155）はこちらの流儀のまま。
    円柱は「かいた形の外接四角に内接するだ円」で作る（長方形かきの廃止に伴い）。
  ・長方形カード（showRectCard）と ✎寸法（nnSolRectDim）は削除（§209）。
    採寸の数値入力は、できた立体の選択カードの「✎ 寸法」が引き続き使える。
- **★平場かどうかは「当たった物」で決めない**。割付の継目・質感の上をタップすると
  polyIdx が無くて平場と見なされない（検証で実際になった）。
  **上向きの面で、真下0.2m以内にその点を含む屋根がある＝平場**、で判定する
  （天端は lv より0.3m前後高いので当たらない／3Dでかいた立体の上（sol）は除く）。
- **★この改修の検証で、製品の潜在バグを2つ見つけて直した**
  1. **✥移動の最中に屋根の面をタップすると、面のドラッグに食われて移動できない**。
     nnFaceDrag の門番に `nnSolMoveBusy()` を追加（§245の「先に始めない」で譲る）。
  2. **役物・立体の手前があっても、奥の平場をつかんでいた**。
     役物をタップしたのに屋根の面ドラッグに食われて選べない
     （統一の受け口の 役物→立体→…の順序（§163）と食い違い）。
     平場つかみのループの先頭で partIdx／solIdx が手前なら break。
- ★**テストの大改修**（4本＝d3draw・aim3d・custompart・arcpoly）
  ・**平場を狙う検査は座標の決め打ちをやめ、「画面から平場の画素を走査して探す」**
    （サンプルは多層 lv6/9/12。決め打ちは壁・天端に当たる。斜めの視点では
     パラペットが手前に立ち、縁に近い平場は天端に隠れる）。
  ・custompart は「平らな屋根＋真上からの視点（d3ViewPlan）＋種の土台」で組み直した。
    役物づくりは「土台の立体の面の上にかく」流儀に（平場は部位になるため）。
  ・arcpoly の quantities は **`quantities(poly, s)` と第2引数（縮尺）が要る**。
    無しで呼ぶと 0 が返り「面積0㎡」と誤判定した。
  ・カメラが遠い位置に残ったままだと立体の面が手前の物にふさがれ、ドラッグが空振りする。
    **立体を狙う前に d3ViewIso() でカメラをそろえる**こと。
- 新しい検査 `_check/deckdraw.js`（17項目：点が増える・閉じたら部位・カード出ない・
  高さ＋300・L字の面積10.5㎡・3Dの天板と四方の壁を光線で実測・平面図/積算/屋根の表・
  ↩↪・開き直しても残る・カメラ不動）。
- ★宿題：平場の自由な形に弧を入れると、部位に弧の印（e.arc）が付かず寸法の札が
  細かく並ぶ（§233の型）。指摘が来たら nnArcGroups の印を deckCommit で振る。
- ★psel1 ph の1件（空タップで青い面が残る）は**この変更の前から同じ**（stashで確認済み）。


### 249. 下地を切り替えても天端だけ前の下地のまま／天端の面取りは内側だけ（2026-08-29p）★本人の指摘2件
指摘は2つ。「①3D作成時、W造にしたあと、RC造にしたら、なぜか天端だけW造になり
他がRC造の状態になった ②天端の外壁側まで面が取れている。本来は内側だけ面を取るはずです」。

**★①下地を切り替えても、防水層の差し替え材質だけ古い下地のまま残っていた**
- 現況を「躯体（防水前の下地）」にすると、防水層（平場・天端・立上り）は
  **下地の材質に差し替えて**見せている（§231）。その差し替え材質は
  `nn-genkyo-js` の `memMat` が**覚えて使い回す**（毎回作ると重いため）。
- ★**その覚え方（キー）が「現況｜種類｜両面」だけで、下地が入っていなかった。**
  そのため W造のときに作った木の材質が、RC造に切り替えても**そのまま使い回され**、
  天端など差し替えの効いている面だけ木のまま残っていた。
  躯体の壁（`nnMat('concrete')` を直に通る側）は下地ごとに作り直されるので正しく変わる
  ＝**「天端だけW造・他はRC造」**という見え方になる。
- 直し方＝**キーに下地を入れる**（`gk|kind|dbl|kouzou`）。屋根ごとの下地（`_nnKzHint`）も
  そのまま効く。あわせて、覚えている間に捨てられないよう `nnShared` の印を付けた
  （W造などの材質は `nnMat` の使い回しを通らず毎回新しく作られるため・§177）。
- ★**教訓：「覚えて使い回す」ものは、中身を決めている値を全部キーに入れること。**
  1つでも抜けると、その値を変えたときだけ古いものが出続ける（しかも一部だけ変わるので
  原因が分かりにくい）。
- 検査は `_check/genkyokz.js`（**直す前のファイルでは★NG3件**＝本物）。
  天端・平場の上から光線を落とし、当たった材質の色の暖かさ（b÷r。木 0.33／RC 0.86）で
  下地を見分ける。W→RC→W の往復と、屋根ごとの下地でも見る。
  `node _check/genkyokz.js before` で変更前と比べられる。

**★②天端の面取りは「内側だけ」に（外側の面取りは廃止）**
- 天端には外側（外壁側）と内側（屋上側）の2か所に面取りを入れていたが、
  **実物は内側だけ**（本人の指摘）。
- 直したのは3つ：外の斜面（O→K）を廃止／天端の帯と天端の防水層を
  **外の面（O）から内側の面取り（th−CH）まで**に広げる／端の小口をふさぐ三角も
  内側だけにする（外側は帯が押し出しなので端が自動でふさがる）。
  ＝天端の防水層の幅 0.21m → **0.23m**、外壁側の角は直角のまま。
- ★**内側の面取りは今までどおり必須**（§246k・本人の指示）。触っていない。
- 検査は `_check/tenba.js`（外側の斜面が0枚・防水層が外の面まで届く・
  内側の面取りは在る、を追加。**直す前のファイルでは★NG2件**）。

**★アゴあり／アゴなしの絵を差し替えられるようにした**（本人が作った画像を置くだけ）
- `icons/ago_on.png`（アゴあり）／`icons/ago_off.png`（アゴなし）。
  規格は他のアイコンと同じ＝**背景透過PNG・透明な余白を切り落とす・高さ168px・
  幅は絵なり（正方形にしない）**。置くだけで自動で絵に変わり、
  無い／読めないときは今までの線画（SVG）に戻る。`sw.js` の ASSETS にも追加済み。
- ★**同じ名前で中身を差し替えたときのために、URLに版名を付けてある**（§66）。
  差し替えたら版名4点セットを上げれば入れ替わる。

### 250. 4画面すべてに同じ緑のライン／タブ名の変更／アゴの絵（2026-08-30a）★本人の指示
指示は3つ。①ダッシュボード・施工中物件（全体）（自社）にも、現場一覧の上にひかれている
ラインを引いて分かりやすくする（赤枠で囲った分）②「施工中物件（全体スケジュール）」→
**「施工中物件（すべて）」**、「施工中物件（自社スケジュール）」→**「施工中物件（自社のみ）」**
③アゴあり／アゴなしの絵が届いたので反映。

- **★①「現場一覧の上のライン」の正体＝`.toolbar` の `border-top:2px solid var(--green-deep)`。**
  検索・絞り込みの帯は**現場一覧のときだけ出す**作り（`showView` が display で出し分け）なので、
  帯といっしょに線も消え、他の3画面にはタブと中身の境目が無かった。
  → 末尾の `<style id="nn-topline">` で **`#dashboard, #schedview` にも同じ線**を引いた。
  ★線は「帯」ではなく**中身の箱の上**に付ける。どちらも display で出し分けているので、
    その画面のときだけ出る＝JSは1行も足していない。
  ★色は予備値つきで書くこと（`var(--green-deep,#1c6b3c)`）。
- **②タブ名**は `#viewtabs` の2か所だけ。`showView('zentai'/'jisha')` の呼び名は変えていない
  （中身・保存・検査に影響しない）。
- **③アゴの絵**（§249 の受け皿にそのまま入る）：`icons/ago_on.png`（アゴあり・220×168）／
  `icons/ago_off.png`（アゴなし・234×168）。
  ★**届いた絵はすでに背景が透過だった**（webp・alpha あり）。§81 の「フチからの塗りつぶし」を
    かける前に、**必ず `alpha` の最小値と四隅を見ること**。透過済みの絵に塗りつぶしをかけても
    何も起きないが、逆に「透過されていない」と思い込んで白を消しにいくと絵の中の白まで消える。
  ★入っているのは `.agoIc img{height:27px}`＝表示の約6倍。絵が読めたら `hasimg` が付いて
    線画（SVG）は隠れ、読めなければ線画に戻る（この仕掛けは §249 のまま）。
- ★**検査の教訓（今回踏んだ）**：`#schedview` はビュー切替のたびに**登場演出**
  （`nnRise`＝14px下からせり上がる）が走るので、**時間で待つと演出の途中を測ってしまう**
  （実測：1.1秒後は14px離れている・2.6秒後は0px）。§161 と同じで
  **`transform` が `none`／`matrix(1,0,0,1,0,0)` になるのを条件で待つこと。**
  これは前の版でも同じ動き（`_before_k.html` で確認済み）＝不具合ではない。
- 検証は `_check/topline.js`（タブ名2つ＋4画面すべてで線が2px以上・タブのすぐ下に出ている。
  **直す前のファイルでは★NG6件**）と `_check/agodef.js`（絵が4つとも読める・hasimg が付く・
  そのとき線画を出さない、の3項目を追加）。
  回帰は記録帳 card6（PC/ph/land）・card7・fil17（PC/ph）・selcolor・pwlist・fast1（PC/land）・
  d2chk・moto1・kdef・det1・gantt1・gantt2・kai3chk・namefit・scrollcnt・cvscroll、
  図面 zmenu（PC/ph/land）とも★NGなし。

### 251. 入口メニューに「区分・躯体・既存防水・新規防水」／よこ向きのアゴの絵（2026-08-30b）★本人の指示
指示は4つ。①よこ画面のときアゴありなしの納まり（絵）が無くなるので作って ②平面図・矩計図に
**躯体**の項目も選べるように（3D図面内の構造体をそのまま転用してよい）③**新築系・改修系の2パターン**
（新築＝躯体＋新規防水／改修＝躯体＋既存防水＋新規防水）④**新規防水は仕様を選べるように**。

- **①よこ向きの絵**：§247 のよこ向きの詰めで `.agoB svg{display:none}`＋`.agoIc{display:none}` と
  **絵を消していた**のが原因。文字の右に小さく（高さ17px）並べるようにした。
- **②躯体**＝`NN_KOUZOU`（RC／S＋ALC／SRC／W／S＋デッキ）をそのまま。
  ★プルダウンに **`nnKzSel` クラスを付けるだけで、積算・設定パネルや断面バーの下地と自動で連動する**
  （`nnKouzouSet` が `.nnKzSel` を全部そろえる作りになっている）。新しい入れ物を作らないこと。
- **③区分**＝`state.kubun`（`shinchiku`／`kaishu`・既定は改修）。
  ・新築のときは「既存防水」の行を出さない（`.zmCard[data-kb="shinchiku"] .kiOnly{display:none}`）
  ・**屋根の表の「現況」からも「既存防水」を出さない**（新築に既存は無い）。
    いま選ばれている値だけは、消えて分からなくならないよう残す。
  ・新築に切り替えたとき、現況が「既存防水」のままの屋根は「改修後防水」に戻して知らせる。
- **④新規防水**＝`SPECS`（`setSpec`）。**既存防水**＝新設の `state.kizon`＋`NN_KIZON`（10種）。
  3Dの現況「既存防水」の**色・質感**がこれで変わる（露出アス＝色あせた砂／押えコン＝コンクリート／
  塩ビ・ゴム・ウレタン・FRP＝平らな面／金属＝金属）。
  ★§249 と同じく、**材質を覚えるキーに `state.kizon` を入れること**。入れないと種類を変えても
    古い材質が使い回されて見た目が変わらない。

★**ハマったところ（次に同じ形を作るとき必ず踏む）**
1. **`<button>` の中に `<select>` は置けない**（§247 の「button の入れ子」と同じ）。
   置くとブラウザが構造を壊す／押しても開かない。カードを **`<div role="button" tabindex="0">`** に変え、
   キーボードの Enter・スペースでも押せるようにした。
2. **カードの真ん中が設定で埋まると「押すと進む」場所が分からなくなる**。
   状態の札と同じ行に **「▶ はじめる」** を置いた（同じ行なので背は増えない）。
   プルダウンの行（`.zmRow`）を押しても画面は移動しない。区分のチップも「選ぶだけ」
   （押した瞬間に移動すると、そのあと防水を選べない）。アゴのチップは今までどおり「選んで進む」。
3. **よこ向きは高さが足りない**（実測：足しただけでは 90px はみ出す）。効いた順に
   ・**区分のチップをタイトルの行の右端へ**（高さゼロで置ける・−20px）
   ・**設定を2列のグリッドに**（−20px）
   ・絵とタイトルを1行に（`.zmTtl`。たてで −42px）
   ★**グリッドの升目は既定で「中身より小さくならない」**（`min-width:auto`）。
     長い工法名で **横に145pxはみ出した**。`.zmRow{min-width:0}` が要る。
- 検証は `_check/zsetup.js`（PC／たて／よこ 各31項目：出ている・新築で既存防水を隠す・
  両方のカードがそろう・積算パネルと連動・開き直しても残る・**3Dで既存防水の色が種類ごとに変わる**・
  よこ向きでも絵が出る）。回帰35本すべて★NGなし。
- ★**まだ届いていないところ**：既存防水は「3Dの見た目」だけ。断面詳細図・平面図PDFの注記や
  数量への反映は未着（撤去・下地処理の数量が要るときに足す）。
