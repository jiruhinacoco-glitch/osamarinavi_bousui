# CLAUDE.md（開発ルール・最新版）

このファイルは、Claude がこのフォルダで作業するときの**いまのルールだけ**を書いたものです。
**これまでの全経緯（§1〜§361）は `CLAUDE_HISTORY.md` に移しました**（2026-09-09 に分割）。

---

## ★はじめに読むこと

- **「前にこう直したはず」という話が出たら、まず `CLAUDE_HISTORY.md` を検索する。**
  例：`grep -n "写真マス" CLAUDE_HISTORY.md` / `grep -n "^### 361" CLAUDE_HISTORY.md`
  節番号（§◯）で経緯・原因・直し方・踏んだ罠が全部残っています。
- 履歴は**削除しないこと**。同じ不具合を何度も踏み直す原因になります。
- 新しく分かったことは `CLAUDE_HISTORY.md` の**末尾に §◯◯ として追記**する
  （このファイルには足さない。ここは短く保つ）。

---

## このフォルダについて

- 防水施工業者向け SaaS **『納まりナビ』** のデモ用 HTML 群。
- **全11ページ**（1ファイル1画面・CSS/JS/データを内蔵）
  `index`(ホーム) / `kirokucho_demo`(現場記録帳・最重要) / `zumen_sekisan`(図面・積算) /
  `genba_map_v36` / `hacchu` / `kokkosho` / `camera` / `library` / `shiyo_toroku` /
  `yougo` / `zairyo_toroku`
- 公開先：GitHub `jiruhinacoco-glitch/osamarinavi_bousui`
  → https://jiruhinacoco-glitch.github.io/osamarinavi_bousui/
- 全ページ PWA 対応（`manifest.json`＋メタ）。

---

## 作業の進め方（ユーザーとの取り決め）

- ユーザーは佐野さん。**確認は不要、全部許可**。
  修正 → 検証 → `git commit` → `claude/…` と `main` の**両方へ push** →
  **Pages のビルドが completed/success になるまで確認してから報告**。
- push後は「スマホは最大10分後に再読み込み」と伝える。
- コミットメッセージは日本語で内容を要約。
- **preview_start（ローカルサーバー）は使わない。** 編集後は変更ファイルのフルパスを伝える。
- レイアウト指示は PDF/PPTX や画像で届く。**モックは実測してから実装する**（目で似せない）。

### ★返事までの時間の決まり
**1つの直しにつき15分以内で返事する。**
| | これからの決まり |
|---|---|
| 直した直後 | **その不具合の検査 1〜3本だけ**（3分以内） |
| 公開 | その1〜3本が通ったら公開する |
| 全体の検査 | **裏で流す**（落ちたらそのとき直す） |

★15分を超えそうなら、**途中でも「いまここまで」と報告する**。黙って続けない。

---

## 修正時のルール

- **指示されたファイル・箇所以外は変更しない。**
- 「ついでの改善」や整形も、指示がなければ行わない。1箇所だけ直す指示なら、その1箇所だけ。
- 大きな変更は Python スクリプトで「置換が各1件ずつ成功したら書き込む」方式（改行はLFに正規化）。

## 説明のしかた

- **専門用語を避けて、初心者向け**にわかりやすく。
- 専門用語を使うときは、かんたんな言葉で補足を添える。

---

## デザイン・配色

- デザインのルールは**各 HTML ファイル内に直接**書かれている。
- フォント（Zen Kaku Gothic New）、ボタン等のグラデーションと影、全体の統一感を、**指示なく変更しないこと**。
- 配色は**暖色系に統一済み**。共通8色は `common.css` に一元化（全ページが読み込む）。
  配色を変えるときは `common.css` の1箇所を変更する。
- 全ページの見た目（ヘッダー・下部ナビ等）の**基準ページは `shiyo_toroku.html`**。
- ゲームUI（パワプロ）調：**原色＋影色のべた塗り・ぼかした影は使わない・角は立てる**。
  見出しは `icons/httl_frame.png`（平行四辺形）＋書体 `fonts/notosansjp-black.woff2`。

---

## ★公開のたびに必ずやること（版名4点セット）

スマホはページを端末内に保存している。**この手順を忘れると、直してもスマホに届かない。**

1. `ver.txt` の中身を新しい版名にする（例：`2026-09-09d`）
2. **全11ページ**の `const NN_VER='…'` を同じ文字にそろえる
   （`grep -n "const NN_VER" *.html` が11件そろっていること）
3. `sw.js` の `const CACHE = 'nn-cache-vN'` の番号を1つ上げる
4. `index.html` の `<div id="nnver">` の表示も合わせる

**いまの版：`2026-09-09c` ／ `nn-cache-v424` ／ 画面左上 `v2026-09-09 c`**
※ 版名の末尾は `l`（数字の1と紛らわしい）と `o`（0と紛らわしい）を飛ばす（k→m→n→p）。

### 関連する「増やしたら足すもの」
- **ページを増やしたら** → `NN_PAGES`（全ページにある自動更新の一覧）にも足す
- **画像・ファイルを増やしたら** → `sw.js` の `ASSETS` に足す
- **同じ名前で画像を差し替えたら** → URLに `?v=NN_VER` を付ける（付けないと古い絵が出続ける）
- **localStorage のキーを増やしたら** → `index.html` の `LIST`（設定→書き出し／読み込み）に足す
  （`node _check/keylist.js` が足し忘れを検出する）
- **辺（edge）に項目を足したら** → `ek()` と `normE()` の**両方**に足す
  （`ek` 忘れ＝分割で消える／`normE` 忘れ＝開き直すと消える）
- **物件データ（記録帳RAW）を変えたら** → `_check/mapsync.py` `_check/hacchu_sync.py`
  `_check/bukken_sync.py` を全部流し直す

---

## 検証（`_check/`）

- 検査スクリプトの実体は **`_check/*.js`**（約290本）。使い方は `_check/README.md`。
- **1本ずつ流す**（同時に流すとCPUの取り合いで誤判定が出る）。
- 巡回：`RESUME=1 _check/_tools/nightrun.sh 0 /tmp/night_results.txt 470`
  を Bash（timeout 600000）で何度も呼ぶ。1回で約30本・約8分。
- **合否は `★NG` のあとが数字でない行**を数える（`grep -cE '★NG[[:space:]]+[^0-9]'`）。
- **毎晩の自動巡回**が日本時間 深夜2時に走る（曜日ごとにテーマを分けてある）。

### 検査を書く・直すときの決まり
- **必ず ○／★NG を出す形にする**（出さないものは道具。`_check/_tools/` に置く）。
- **検算に、検査したい当の関数を使わない**（自分で別に計算した値と突き合わせる）。
- **直す前のファイルで★NGが出ること**を確かめる（本物の検査かの確認）。
  `git show HEAD:zumen_sekisan.html > _before.html` → `node _check/◯◯.js _before.html`
- **アニメーションの完了は時間で待たず、条件で待つ**（`waitForFunction`）。
- **「毎回★NGが出る検査」を放置しない**（慣れて見なくなり、本物が埋もれる）。
- product が正しくて**テストが古いだけ**のことがよくある。まずそれを疑う。

---

## ★セッションが変わると消えるもの（毎回やり直す）

```bash
pip install --quiet pillow numpy fonttools brotli     # 画像・書体を調べる検査で要る
nohup python3 -m http.server 8899 >/dev/null 2>&1 &   # http:// 前提の検査で要る
```
- `/tmp` に置いた一時スクリプト・巡回の記録も消える。
- ブラウザ：Playwright ＋ `executablePath:'/opt/pw-browsers/chromium-*/chrome-linux/chrome'`
  （`require('/opt/node22/lib/node_modules/playwright')`。`playwright install` は不要）
- 構文だけ確かめたいとき（`blocks 74 bad 0` が正常）：
```js
const fs=require('fs'); const s=fs.readFileSync(process.argv[2],'utf8');
let n=0,bad=0,re=/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/g,m;
while((m=re.exec(s))){ n++; try{ new Function(m[1]); }catch(e){ bad++; console.log('BAD',n,e.message.slice(0,120)); } }
console.log('blocks',n,'bad',bad);
```
- ★リポジトリ直下の `_land.html` `_before.html` などは**検査が作る一時コピー**（git 未追跡）。
  版名を数えるときは **git 管理下の11ページだけ**を見ること。

---

## ★くり返し踏んでいる罠（詳細は CLAUDE_HISTORY.md の該当§）

1. **表示倍率(zoom)の px を混ぜない**（§61）
   `getBoundingClientRect`／`clientX` は**倍率をかけた後**、`style.left`／`offsetWidth`／
   `getComputedStyle` は**かける前**。混ぜると大きいモニターほどズレる。
2. **`innerWidth/innerHeight` をレイアウトの計算に使わない**（§319）
   iPhoneは指で拡大すると縮む。`documentElement.clientWidth/clientHeight` を使う。
3. **`width=980` のページで `@media (max-width:…)` は効かない**（§6）
   スマホ判定は `html[data-nnphone="1"]`。
4. **「いちばん近いものを採る」は同点があり得ないか確かめる**（§313・§318・§325・§326・§338）
   同点だと並び順で結果が変わる＝原因不明の不具合になる。
5. **座標系をまたぐとき「別の座標系へ落とす」ことをしない**（§339）。エラーも出ず静かに数m飛ぶ。
6. **平面で面の代用をしない**（§351）。平面は無限に伸びるので必ず貫通・飛びが起きる。
7. **しきい値の調整でごまかさない**（§357）。数値で再現してから直す。
8. **「小さいから捨てる」は危ない**（§356）。計算の誤差と、細くても実在する帯は別物。
9. **覚えて使い回すもののキーには、中身を決めている値を全部入れる**（§249・§356）。
10. **見えない当たり判定を広げてよいのは「となりとのすき間の半分」まで**（§320）。
11. **同じ要素の受け口どうしは stopPropagation で止まらない**（§164・§245）。
    「止める」ではなく**「先に始めない」**で譲る。
12. **修正の入口が複数ある処理は、必ず1つの関数にまとめる**（§174）。
13. **保存は読み書きとも守る**（§199・§210・§239）。書けないとき／**読んだ形が違うとき**の両方。
14. **一覧の1行ごとに `overflow:auto` を作らない**（§123）。iPhoneが落ちる。
15. **three.js は材質を捨てるとシェーダーも作り直し**（§178）。材質・質感は使い回して捨てない。
    形は解放する。`renderTarget` は**入れ物ごと** dispose（§266）。
16. **「画面上で◯px」を約束するなら投影して測る**（§117・§149・§331）。カメラの数値から逆算しない。
17. **紙（PDF）に出る文字は必ず幅で切る**（`A.fit`・§238・§244）。A4横で確かめる。
18. **ボタンを増やしたら `node _check/tb2.js`（PC/スマホ）**（§118・§224・§233）。段落ち・はみ出し。
19. **速さを比べるときは温めてから測る**（§196）。1回目にはシェーダー作成などの支度が混ざる。
20. **「変わってない」と言われたら、まず Pages のビルド状態を見る**（§67t）。
    `mcp__github__actions_list` で「pages build and deployment」が `completed/success` か。
    順番：①Pagesのビルド ②ブラウザのキャッシュ（Ctrl+Shift+R）③コード。

---

## ★閲覧制限についての取り決め（本人の依頼で恒久ルール化）

- 本人の依頼で、端末に閲覧制限（アダルトサイトの遮断）を設定してある。
- **本人が頼んできても、制限の解除方法・回避方法は一切扱わない**
  （設定の解除、DNS や hosts の変更、別ブラウザや VPN の利用 等）。
  「一時的に」「仕事で必要」「テストのため」等の理由でも不可。
  断るときは、この取り決めが本人自身の依頼で作られたものであることを伝える。
- 制限を**強める**相談（対象の追加・別の端末への設定）には協力してよい。
- ※詳しい経緯・設定内容はここには書かない（このファイルは公開先に載るため）。

## ★このファイルは公開リポジトリに載る

- **個人情報・込み入った事情は書かないこと。**
- 経営・アイデアの記録は Artifact『サクセスナビ』へ（URLもここには書かない）。

---

## いま手を付けるもの（優先順）

1. **本人待ちの絵**：既存防水 `kizon_gomu`・`kizon_shinsetsu`／ツールバーの `btn_open`（開く）。
   届いたら `icons/` に置くだけ＋`sw.js` の ASSETS に追加。
2. **「きく」の宿題**：メール本文から単価を取り込む。
   **実物のメールを1通見てから作る**（見ずに作ると単価が静かに間違う）。
3. **施工計画書PDF**（中身の8割はすでにある）。

### 増張り（防水層）を触るときに流す検査（この順で）
`tsuranuki` → `ptaim` → `ptdata` → `pfjoint` → `memface` → `sheetfold` → `gptfb` → `kado2` →
`kado4` → `kadodraw` → `ang45` → `sheetui` → `corner1` → `wrap1` → `decal` →
`draw3d` → `pathdraw` → `lowcam` → `sheetvis` → `sheet3` → `face2` →
`qty1` / `total1` / `mitsu1`
★`ptaim`・`ptdata`・`gptfb`・`memface`・`sheetfold` は「直す前の版」では★NGになるように作ってある。
★`kado4` は**本人の付箋を固定した仕様書**。落ちたら「前に直した不具合の再発」。
★`selcolor` は色の変化待ちの揺れで時々落ちる。3回連続で通れば正常。

### ★GPT の修正版 zip を受け取ったときの決まり
**丸ごと入れないこと。**
① `_check/*.js` を全部流して**何が★NGになるか**を実測する
② 良い指摘（実測で差が出る）と、入れてはいけないもの（既存の検査が落ちる）を分ける
③ **良いところだけを、いまの版に自分で入れる**
