.claude/
desktop.ini
icons/_original/

# 検証スクリプトが書き出す使い捨てのスクリーンショット（リポジトリに入れない）
/*.png
/*.jpg

# 検証で作る使い捨てのコピー（よこ向きスマホ再現・変更前との比べ用）
/_land.html
/_land_test.html
/_before*.html
/_b.html
/__L_*.html

# 検証が書き出すスクリーンショットの置き場（毎回作り直されるのでリポジトリに入れない）
/out/

# 検査で撮る画面写真（結果の絵。走らせれば作り直せるので置いておかない）
_check/*.png
_check/_tools/*.png
_check/out/
/_vwtest.html

# 作業まとめ（本人に見せるためのエクセル等）。公開サイトに載せる必要はないので入れない。
# ★公開したくなったら、この2行を消せば載る。
/_work/
_landz.html
